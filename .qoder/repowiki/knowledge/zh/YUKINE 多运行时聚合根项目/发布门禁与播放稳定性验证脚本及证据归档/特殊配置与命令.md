运行前需确保 `android-sdk/platform-tools/adb.exe` 存在或 adb 在 PATH 中；`verify-release.ps1 -ReleaseSmoke` 可通过 `-CreateSmokeKeystore` 自动生成 `app/build/tmp/echo-smoke-release-local.jks`，或直接设置 `ECHO_RELEASE_STORE_FILE` / `ECHO_RELEASE_STORE_PASSWORD` / `ECHO_RELEASE_KEY_ALIAS` / `ECHO_RELEASE_KEY_PASSWORD` 四个环境变量传入正式签名值。