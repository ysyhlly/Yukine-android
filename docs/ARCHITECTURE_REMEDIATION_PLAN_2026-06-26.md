# Yukine 架构整治计划（复核版）

日期：2026-06-26  
适用范围：`app/src/main/java/app/yukine` 当前工作树  
用途：作为后续架构整治、MVVM 迁移、依赖注入补齐、目录重组和测试护栏的执行参考

---

## 1. 文档定位

这份文档不是替代现有迁移文档，而是对外部审查结论做一次当前工作树复核后，产出的执行计划表。

与现有文档的关系：

- `docs/MVVM_MIGRATION_HANDOFF.md`
  - 仍是主迁移手册，记录长期目标、边界规则和阶段性完成情况。
- `docs/MVVM_MIGRATION_PROGRESS_2026-06-25.md`
  - 记录最近一轮迁移结果和验证情况。
- `docs/STREAMING_VIEWMODEL_SPLIT_PLAN.md`
  - 只负责 streaming 方向的专项拆分。
- 本文档
  - 负责把“审查问题 -> 当前事实 -> 可执行计划 -> 验收方式”整理成一份总表。

---

## 2. 复核结论

下列结论基于 2026-06-26 对当前仓库的本地复核，不直接照抄旧审查。

### 2.1 已复核的事实

| 项目 | 复核结果 | 备注 |
|---|---:|---|
| `app/src/main/java/app/yukine` ??????? | 288 | ??????????? |
| `app/src/main/java/app/yukine` ?????? | 160 | ????????? |
| `*Controller` ??? | 56 | ???? 58??????? |
| `*Bindings` 文件数 | 3 | 说明桥接层仍然很多 |
| `*ViewModel` 文件数 | 18 | ViewModel 已明显扩张 |
| `app/src/test/java/app/yukine` ????? | 137 | ???? 165 ?????????? |
| `MainActivity.java` ?? | 3663 | ????????? |
| `EchoPlaybackService.java` 行数 | 3893 | 播放边界体量很大 |
| `StreamingViewModel.kt` 行数 | 1932 | 仍是超大 ViewModel |
| `SettingsViewModel.kt` 行数 | 771 | 已迁出不少逻辑，但仍偏大 |
| Hilt Module 文件数 | 2 | `DashboardDataModule.kt`、`StreamingDataModule.kt` |
| `@HiltViewModel` 数量 | 5 | Hilt 已接入，但覆盖不均匀 |

### 2.2 旧审查中需要修正的点

1. “依赖注入配置缺失”不完全准确。
   当前仓库已经有 Hilt、2 个 Module、5 个 `@HiltViewModel`。问题不是“没有 DI”，而是“DI 覆盖不完整，MainActivity 仍大量手动装配”。

2. “MainActivity 只有字段注入、ViewModel 手动管理”需要更精确。
   当前 `MainActivity` 里既有 5 个 `@Inject` 字段，也有大量 `new XxxController(...)`、`new XxxUseCase(...)`、`new XxxBindings(...)`，同时通过 `new ViewModelProvider(this).get(...)` 手动获取 11+ 个 ViewModel。核心问题是宿主装配过重，不是单一技术点。

3. “Repository 没有响应式流”需要收窄表述。
   当前很多 ViewModel 已经用 `StateFlow` 持有 UI 状态，但 `MusicLibraryRepository.java` 仍主要暴露同步 `List` / imperative load-save API，响应式数据源没有真正下沉到数据层。

4. “立即按功能域拆包”方向正确，但不应作为第一步。
   纯目录搬迁会制造大量 import churn。如果 owner 边界还没收稳，先搬文件只会增加 review 成本。更合理顺序是：先收边界和宿主职责，再按稳定 owner 移包。

5. 当前工作树处于迁移中，不是静态旧项目。
   仓库已有大量未提交和未跟踪改动，且 `docs/MVVM_MIGRATION_HANDOFF.md` 已明确把外部审查作为优先级输入，而不是无条件事实源。

---

## 3. 当前核心问题

| 编号 | 问题 | 当前证据 | 直接风险 |
|---|---|---|---|
| A1 | 根目录文件爆炸 | 根目录 210 个文件 | 文件查找困难，owner 不清晰 |
| A2 | 宿主过重 | `MainActivity.java` 2299 行，手动装配大量对象 | 新功能继续堆进 Activity |
| A3 | ????? | 56 ? Controller + 3 ? Bindings | ???????????? |
| A4 | DI 覆盖不完整 | 只有 2 个 Module，`MainActivity` 仍手动 `new` 大量依赖 | 替换实现困难，依赖图分散 |
| A5 | 数据层同步接口为主 | `MusicLibraryRepository.java` 主要暴露 `List`/同步 load/save | UI 更新依赖手动刷新和桥接 |
| A6 | 超大状态 owner | `StreamingViewModel.kt` 1932 行，`SettingsViewModel.kt` 771 行 | feature 边界继续模糊 |
| A7 | 迁移过程中的脏工作树 | 当前有大量 `M/D/??` 改动 | 不适合做无边界的大搬迁 |

---

## 4. 执行原则

1. 先做边界收敛，再做目录整理。
2. 先降低 `MainActivity` 装配密度，再补 DI。
3. 先把数据 owner 稳住，再推进数据层响应式改造。
4. 每个阶段都必须带最小验证：至少一条快速检查和一条信心检查。
5. 不在当前脏工作树里做“纯重命名 + 纯搬家”的大提交。
6. 当前方向（2026-06-28）改为“先收口、再判断是否继续迁移”。暂停默认新增 `Manager` / `Coordinator` / `Controller` / `Bindings` / `Gateway`，优先删除纯转发层、补契约测试、恢复可审查基线；只有当一个切片能净减少文件数、方法数、状态源、依赖或调用链时，才继续推进 P1/P2。
7. 新审查修正（2026-06-28）：仅删除薄包装不能算完成迁移；必须优先降低 `MainActivity` 手动装配/匿名回调、`EchoPlaybackService` 播放策略残留、`EchoDatabaseHelper` migration 风险，以及并发模型碎片化。
8. 删除旧 Controller/Test 前必须说明行为覆盖迁到了哪里；字符串契约测试只能防回流，不能替代行为测试、依赖方向检查或 smoke 证据。
9. 当前收敛事实（2026-06-29）：`DownloadsCoordinator`、`FileIOCoordinator`、`NetworkCoordinator`、`LibraryCoordinator`、`NavigationCoordinator`、`SettingsCoordinator`、`StreamingCoordinator`、`PlaybackCoordinator`、`NowPlayingQueueCoordinator` 已删除；`:app` 根包文件数为 144，`MainActivityBase.java` 为 3613 行，剩余 coordinator 只有 `NetworkRenderCoordinator`。播放启动现在直接进入 `PlaybackStartController`，service bind/release 直接进入 `PlaybackServiceConnectionController`，app visible 直接调用 `EchoPlaybackService.setAppVisible(...)`；queue intents 继续由 `mountNavHostShell()` 中实际生效的 `queueViewModel.bindIntentListener(...)` 路径和 `QueueActionController` 处理；`PlaybackStateUpdateController` 已改为无状态 `object`，不再由宿主持有/构造；`PlaybackServiceHostController` 保留 service connect/disconnect 策略 owner，但不再作为 Activity 字段保存；`NetworkMenuRenderController`、`NetworkTrackListRenderController`、`NetworkSourcesRenderController` 和 `StreamingSearchRenderController` 保留为 `NetworkRenderCoordinator` 消费的 render owner，但不再作为 Activity 字段保存；streaming auth intent 直接进入 `StreamingAuthCallbackController`。
10. 2026-06-29 continuation fact: `PlaybackActionController.Listener`, `QueueActionController.Listener`, `QueueRenderController.Listener`, `HomeDashboardRenderController.Listener`, `NowPlayingStateController.Listener`, `RecommendationActionCallbacks`, `StreamingPlaybackController.Listener`, `PlaybackStartController.Listener`, `PlaybackStateEventController.Listener`, `PlaybackServiceHostController.Host`, `TrackListRenderController.Listener`, `HeartbeatRecommendationController.Listener`, `DocumentPickerController.Listener`, `BackgroundImagePickerController.Listener`, and `MainPermissionController.Listener` policy have moved out of Java anonymous blocks into `MainPlaybackActionListener`, `MainQueueActionListener`, `MainQueueRenderListener`, `MainHomeDashboardRenderListener`, `MainNowPlayingStateListener`, `MainRecommendationActionCallbacks`, `MainStreamingPlaybackListener`, `MainPlaybackStartListener`, `MainPlaybackStateEventListener`, `MainPlaybackServiceHost`, `MainTrackListRenderListener`, `MainHeartbeatRecommendationListener`, `MainDocumentPickerListener`, `MainBackgroundImagePickerListener`, and `MainPermissionListener`; shell/home listeners are provided by `ShellModule`, playback listeners/host owners are provided by `PlaybackUiModule`, track-list render listener is provided by `LibraryModule`, recommendation and heartbeat recommendation listeners are provided by `StreamingModule`, platform picker and permission listeners are provided by `PlatformModule`, and all are covered by focused behavior tests plus `MainActivityArchitectureContractTest`. Pending playback state now lives in `MainPlaybackStartListener` instead of `MainActivityBase`, playback state event policy now lives in `MainPlaybackStateEventListener`, playback service host attach/clear/render policy now lives in `MainPlaybackServiceHost`, track-list action/chrome policy now lives in `MainTrackListRenderListener`, queue render action policy now lives in `MainQueueRenderListener`, home dashboard action/shuffle policy now lives in `MainHomeDashboardRenderListener`, now-playing state/floating-lyrics policy now lives in `MainNowPlayingStateListener`, and recommendation action callback policy now lives in `MainRecommendationActionCallbacks`. Current recheck after this slice: `MainActivityBase.java` 3187 lines, `EchoPlaybackService.java` 2469 lines, `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, 56 main-source Java files, 44 main-source `*Controller` files, 127 dirty entries.
11. 2026-06-29 continuation fact: `StreamingPlaylistDialogController.Listener` callbacks moved out of the Java base anonymous block into `MainStreamingPlaylistDialogListener`, provided by `StreamingModule` and covered by `MainStreamingPlaylistDialogListenerTest` plus `MainActivityArchitectureContractTest`. The Java base now injects `MainStreamingPlaylistDialogListenerFactory` and only wires existing status/controller lambdas into it. Current recheck after this slice: `MainActivityBase.java` 3179 lines, `EchoPlaybackService.java` 2469 lines, `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, 56 main-source Java files, 44 main-source `*Controller` files, 127 dirty entries.
12. 2026-06-29 continuation fact: `StreamingPlaylistController.Listener` policy moved out of the Java base anonymous block into `MainStreamingPlaylistListener`, provided by `StreamingModule` and covered by `MainStreamingPlaylistListenerTest`, `StreamingPlaylistControllerTest`, and `MainActivityArchitectureContractTest`. The Java base now injects `MainStreamingPlaylistListenerFactory` and only supplies existing route, library, status, navigation, and dialog lambdas to the Kotlin owner. Current recheck after this slice: `MainActivityBase.java` 3131 lines, `EchoPlaybackService.java` 2469 lines, `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, 56 main-source Java files, 44 main-source `*Controller` files, 131 dirty entries.
13. 2026-06-29 continuation fact: `StreamingPlaylistImportDialogController.Listener` callbacks moved out of the Java base anonymous block into `MainStreamingPlaylistImportDialogListener`, provided by `StreamingModule` and covered by `MainStreamingPlaylistImportDialogListenerTest`, `StreamingPlaylistControllerTest`, and `MainActivityArchitectureContractTest`. The Java base now injects `MainStreamingPlaylistImportDialogListenerFactory` and only supplies selected-provider, Luoxue dialog, and playlist-link import lambdas to the Kotlin owner. Current recheck after this slice: `MainActivityBase.java` 3121 lines, `EchoPlaybackService.java` 2469 lines, `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, 56 main-source Java files, 44 main-source `*Controller` files, 133 dirty entries.
14. 2026-06-29 continuation fact: `StreamingManualCookieController.Listener` callbacks moved out of the Java base anonymous block into `MainStreamingManualCookieListener`, provided by `StreamingModule` and covered by `MainStreamingManualCookieListenerTest`, `DefaultStreamingSearchActionHandlerTest`, and `MainActivityArchitectureContractTest`. The Java base now injects `MainStreamingManualCookieListenerFactory` and only supplies selected-provider, manual-cookie dialog, login-success, and status lambdas to the Kotlin owner. Current recheck after this slice: `MainActivityBase.java` 3107 lines, `EchoPlaybackService.java` 2469 lines, `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, 56 main-source Java files, 44 main-source `*Controller` files, 135 dirty entries.
15. 2026-06-29 P0 baseline freeze recheck: current `git status --short` reports four modified entries: `M docs/ARCHITECTURE_REMEDIATION_PLAN_2026-06-26.md`, `M docs/ARCHITECTURE_STABILIZATION_PIVOT_2026-06-27.md`, `M docs/MVVM_MIGRATION_HANDOFF.md`, and `M gradle.properties`. There are no current deleted test files under `app/src/test` or `app/src/androidTest`, so the deleted-test replacement map is empty for this checkpoint. Current counts are `MainActivityBase.java` 2968 lines, `EchoPlaybackService.java` 2469 lines, `feature:data` `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0, root-package `*Controller*` 44, root-package files 170, and root-package Java files 15. `ShellViewModel` / `ShellState` / `ShellAction` source files are absent; remaining mentions are historical correction notes or contract-test guards. Serial verification passed with default Gradle settings: `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain` and `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Do not default to `--no-daemon` or `--max-workers=1`; use them only after a reproducible daemon, KSP, or lock issue.
16. 2026-06-29 P1 `initializeStoresAndDataGateways()` playlist action gateway slice: `LibraryModule` now provides the stable `LibraryPlaylistActionGateway` through `MainLibraryPlaylistActionGateway`, backed by `MusicLibraryPlaylistActionOperations(repository, syncStore)`. `MainActivityBase` now binds the injected gateway directly and no longer owns the anonymous playlist action gateway block, the local `MusicLibraryPlaylistActionOperations`, seven playlist action use case constructors, or the `StreamingPlaylistSyncStore` field. `LibraryPlaylistActionContracts.kt` now holds the playlist action contract types outside the large `LibraryViewModel.kt`, while `LibraryViewModel` remains the UI state/action owner. Current recheck after this slice: `MainActivityBase.java` 2927 lines, `EchoPlaybackService.java` 2469 lines, `feature:data` `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0, root-package `*Controller*` 44, root-package files 172, root-package Java files 15, and dirty entries 11 before this documentation update. Field count is not yet a P1 exit win because the Activity swapped the sync-store field for an injected gateway, but host data-layer coupling and manual construction both decreased. Guarded by `MainLibraryPlaylistActionGatewayTest` and `MainActivityArchitectureContractTest`; focused tests passed with `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainLibraryPlaylistActionGatewayTest --tests app.yukine.MainActivityArchitectureContractTest --rerun-tasks --console=plain` after a default cached run exposed stale transformed class output, and compile passed with `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`.
17. 2026-06-29 P1 library collection/import gateway slice: `LibraryModule` now provides `LibraryCollectionGateway` through `MainLibraryCollectionGateway`, `LibraryImportGateway` through `MainLibraryImportGateway`, and `LibraryDocumentGateway` through `ContentResolverLibraryDocumentGateway`, reusing the existing collection/import/document owners instead of adding root bindings or controllers. `MainActivityBase` now binds the injected gateways directly and no longer owns the anonymous collection/import gateway blocks, host-owned `MusicLibraryCollectionOperations`, repeated import use case constructors, Activity-local `MusicLibraryImportOperations`, `ContentResolverLibraryDocumentGateway(getContentResolver(), ...)`, or `toLibraryLoadResultUi(...)`. Current recheck after the document gateway provider slice: `MainActivityBase.java` 2872 lines, `EchoPlaybackService.java` 2469 lines, `feature:data` `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0, root-package `*Controller*` 44, root-package files 172, and root-package Java files 15. Guarded by `LibraryCollectionUseCasesTest`, `LibraryImportUseCasesTest`, `ContentResolverLibraryDocumentGatewayTest`, and `MainActivityArchitectureContractTest`; focused tests and compile passed with default daemon/workers.
18. 2026-06-29 P1 settings preference provider slice: `SettingsModule` now provides `LoadSettingsPreferencesUseCase` and `ApplySettingsPreferenceUseCase`; `MainActivityBase` only calls the injected use cases for settings load and persistence binding. The host no longer constructs `MusicLibrarySettingsPreferenceLoadOperations`, `LoadSettingsPreferencesUseCase`, `MusicLibrarySettingsPreferenceOperations`, or `ApplySettingsPreferenceUseCase`. Current recheck after this slice: `MainActivityBase.java` 2866 lines, `EchoPlaybackService.java` 2469 lines, `feature:data` `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0, root-package `*Controller*` 44, root-package files 173, and root-package Java files 15. Root files increase by one for the settings Hilt module; Activity manual construction and call-chain length decrease. Guarded by `LoadSettingsPreferencesUseCaseTest`, `ApplySettingsPreferenceUseCaseTest`, and `MainActivityArchitectureContractTest`; focused tests and compile passed with default daemon/workers.
19. 2026-06-29 P1 library store factory slice: `MainLibraryStore` now exposes `MainLibraryStoreFactory`, and `LibraryModule` provides both `LibrarySearchUseCase` and that factory. `MainActivityBase` now calls `libraryStoreFactory.create(viewModel)` and no longer constructs `MainLibraryStore`, `LibrarySearchUseCase`, or `MusicLibrarySearchOperations` directly. Current recheck after this slice: `MainActivityBase.java` 2864 lines, `EchoPlaybackService.java` 2469 lines, `feature:data` `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0, root-package `*Controller*` 44, root-package files 173, and root-package Java files 15. Guarded by `LibrarySearchUseCaseTest`, `NetworkLibraryStoreDirectAccessTest`, `PlayHistoryActionControllerTest`, and `MainActivityArchitectureContractTest`; focused tests and compile passed with default daemon/workers after rerunning the same focused command with a longer tool timeout.
20. 2026-06-29 P1 streaming search action handler factory slice: `DefaultStreamingSearchActionHandler` now exposes `MainStreamingSearchActionHandlerFactory`, provided by `StreamingModule`. `MainActivityBase` now calls `streamingSearchActionHandlerFactory.create(streamingViewModel, streamingActionGateway)` and no longer constructs `DefaultStreamingSearchActionHandler` directly in production. Current recheck after this slice: `MainActivityBase.java` 2865 lines, `EchoPlaybackService.java` 2469 lines, `feature:data` `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0, root-package `*Controller*` 44, root-package files 173, and root-package Java files 15. The manual-construction count decreases, while line count and field count are not claimed as wins because the injected factory replaces local construction. Guarded by `DefaultStreamingSearchActionHandlerTest` and `MainActivityArchitectureContractTest`; focused tests and compile passed with default daemon/workers.
21. 2026-06-29 P1 track share operations provider slice: `ShellModule` now provides `TrackShareOperations` through `TrackShareManagerOperations(trackShareManager, nativeMusicShareManager)`. `MainActivityBase` now injects `TrackShareOperations` and no longer holds `TrackShareManager` / `NativeMusicShareManager` fields or constructs `TrackShareManagerOperations` locally. Current recheck after this slice: `MainActivityBase.java` 2862 lines, `EchoPlaybackService.java` 2469 lines, `feature:data` `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0, root-package `*Controller*` 44, root-package files 173, and root-package Java files 15. Activity field count and manual-construction count decrease without adding root files, bindings, or controllers. Guarded by `TrackShareLauncherTest` and `MainActivityArchitectureContractTest`; focused tests and compile passed with default daemon/workers.
22. 2026-06-29 P1 settings store provider slice: `SettingsModule` now provides the Activity-scoped `MainSettingsStore`. `MainActivityBase` injects `settingsStore` and no longer constructs `new MainSettingsStore()` in `initializeStoresAndDataGateways()`. Current recheck after this slice: `MainActivityBase.java` 2861 lines, `EchoPlaybackService.java` 2469 lines, `feature:data` `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0, root-package `*Controller*` 44, root-package files 173, and root-package Java files 15. Activity manual-construction count decreases without adding root files, bindings, or controllers. Guarded by `LoadSettingsPreferencesUseCaseTest`, `ApplySettingsPreferenceUseCaseTest`, and `MainActivityArchitectureContractTest`; focused tests and compile passed with default daemon/workers.
23. 2026-06-29 P1 lyrics settings use-case provider slice: `SettingsModule` now provides `LoadLyricsSettingsUseCase` through `MusicLibraryLyricsSettingsOperations(repository)`. `MainActivityBase` injects `loadLyricsSettingsUseCase` and no longer constructs `LoadLyricsSettingsUseCase` or `MusicLibraryLyricsSettingsOperations` locally while configuring `LyricsViewModel`. Current recheck after this slice: `MainActivityBase.java` 2858 lines, `EchoPlaybackService.java` 2469 lines, `feature:data` `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0, root-package `*Controller*` 44, root-package files 173, and root-package Java files 15. Activity manual-construction count decreases without adding root files, bindings, or controllers. Guarded by `LoadLyricsSettingsUseCaseTest` and `MainActivityArchitectureContractTest`; focused tests and compile passed with default daemon/workers.
24. 2026-06-29 P1 library gateway policy owner slice: `LibraryModule` now provides `MainLibraryGatewayFactory`, and `MainLibraryGateway` owns the former `LibraryGateway` Java anonymous block policy for track-list play, localized status keys, favorite refresh, playlist add, library routing, search, import, and scan. `MainRouteController` implements the narrow `LibraryRouteActions` boundary, so Activity does not replace the old anonymous block with another route adapter. Current recheck after this slice: `MainActivityBase.java` 2789 lines, `EchoPlaybackService.java` 2469 lines, `feature:data` `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0, root-package `*Controller*` 44, root-package files 174, and root-package Java files 15. Root files increase by one for behavior coverage, while Activity anonymous policy and host routing logic decrease. Guarded by `MainLibraryGatewayTest`, `LibraryViewModelTest`, `MainRouteControllerTest`, and `MainActivityArchitectureContractTest`; focused tests and compile passed with default daemon/workers.
25. 2026-06-29 P1 streaming action gateway policy owner slice: `StreamingModule` now provides `MainStreamingActionGatewayFactory`, and `MainStreamingActionGateway` owns the former Java anonymous `MainActivityStreamingActionGateway` policy for quality/language lookup, auth launch delegation, resolved-track playback, login-success playlist handling, and manual cookie import ordering. `MainActivityBase` now wires host/platform lambdas into the factory and no longer contains `new MainActivityStreamingActionGateway()`. Current recheck after this slice: `MainActivityBase.java` 2769 lines, `EchoPlaybackService.java` 2469 lines, `feature:data` `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0, root-package `*Controller*` 44, root-package files 175, and root-package Java files 15. Root files increase by one for behavior coverage, while Activity anonymous streaming policy decreases. Guarded by `MainStreamingActionGatewayTest`, `DefaultStreamingSearchActionHandlerTest`, and `MainActivityArchitectureContractTest`; focused tests and compile passed with default daemon/workers.

---

## 5. 全部计划表

| ID | 优先级 | 工作流 | 目标 | 主要文件/边界 | 前置依赖 | 交付物 | 快速检查 | 信心检查 |
|---|---|---|---|---|---|---|---|---|
| P0-1 | P0 | 基线冻结 | 明确当前编译、测试、脏工作树状态 | `README.md`、`docs/*`、Gradle、`git status` | 无 | 一份基线记录和切片策略 | `git status --short` 已记录 | `compileDebugKotlin + compileDebugJavaWithJavac` |
| P0-2 | P0 | 文档对齐 | 明确本文档与 `MVVM_MIGRATION_HANDOFF.md` 的职责边界 | `docs/MVVM_MIGRATION_HANDOFF.md`、本文档 | P0-1 | 文档引用关系稳定 | 文档链接可追踪 | 后续阶段更新有统一入口 |
| P0-3 | P0 | 行为覆盖盘点 | 统计已删除测试和每条迁移切片的替代行为测试 | `app/src/test/java/app/yukine`、`MainActivityArchitectureContractTest.java` | P0-1 | 删除测试 -> 替代测试映射 | `git status --short` 中 D 测试已归类 | 受影响 focused tests 可单独运行 |
| P1-1 | P1 | 宿主装配收缩 | 把 `MainActivity.onCreate` 手动装配和匿名 listener 策略继续移出宿主 | `MainActivity.java`、`di/*`、feature assembly owner | P0-1, P0-3 | Activity 只保留 lifecycle/root/service/launcher delegation | `new Xxx`、匿名 listener、字段数下降 | 定向编译 + 行为测试 + 契约测试 |
| P1-2 | P1 | 平台 owner 补齐 | picker、dialog、backup、share、permission 全部有明确 owner | `*PickerController`、`*DialogController`、`SettingsEffectBindings.kt` | P1-1 | 平台边界清单和 owner 清单 | Activity 不再新增平台分支 | 定向单测覆盖平台 owner |
| P1-3 | P1 | 桥接层清理 | 删除只转发一层的 Controller/Bindings，且不得把策略回流到 Activity | `*Controller.kt`、`*Bindings.kt`、各 `ViewModel` | P1-1, P1-2 | 事件路径缩短，回调接口收窄 | Activity 装配/匿名策略不增加 | 相关行为单测和契约测试通过 |
| P1-4 | P1 | `Settings` 收口 | 完成设置页 owner 收敛，避免回流到宿主 | `SettingsViewModel.kt`、`SettingsPageStateBuilder.kt`、`SettingsRuntimeApplier.kt` | P1-2, P1-3 | 设置链路只经 `ViewModel -> effect/runtime/persistence` | 不新增 settings gateway 回流 | `SettingsViewModelTest` + 契约测试 |
| P1-5 | P1 | `Streaming` 收口 | 继续拆 `StreamingViewModel` 的搜索、认证、歌单、推荐、播放编排 | `StreamingViewModel.kt`、`StreamingRecommendationViewModel.kt`、`StreamingPlaylistController.kt` | P1-3 | 按 feature 拆小 owner | 行数和职责下降 | `Streaming*Test` + 编译 |
| P1-6 | P1 | 播放服务瘦身 | 把队列计算、缓存调度、URI 判断等非服务职责下沉到已有 manager | `playback/EchoPlaybackService.java`、`playback/manager/*` | P1-1 | Service 只保留 Media3/lifecycle/session 边界 | Service 行数/策略方法下降 | 播放相关单测 + smoke build |
| P1-7 | P1 | DI 补齐 | 把稳定 use case/controller/executor 的手动 `new` 收进 Hilt，减少宿主装配 | `di/*`、`MainActivity.java`、相关 owner | P1-1, P1-3 | DI 图更完整，装配减少 | `MainActivity` 的 `new` 数量下降 | 编译 + 关键注入路径测试 |
| P1-8 | P1 | 数据层护栏 | 先为 `EchoDatabaseHelper` 建 migration/事务测试，再拆 `MusicLibraryRepository` 或评估 Room | `data/MusicLibraryRepository.java`、`EchoDatabaseHelper.java`、数据库测试 | P1-7 | migration 基线和数据 owner 边界 | schema 变化有测试覆盖 | migration/事务测试 + 编译 |
| P1-9 | P1 | 并发模型收敛 | 盘点并减少裸 Thread、独立 ExecutorService、自定义 scheduler | `LyricsRepository`、`PlaybackPrecacheManager`、`PlaybackTaskScheduler`、协程调用点 | P1-6 | 每条后台任务有 owner/shutdown/生命周期说明 | 新代码不新增裸线程 | 相关单测 + 编译 |
| P2-1 | P2 | 响应式数据源 | 把关键读取路径从同步 `List` 提升到 `Flow`/可观察接口 | `data/*Repository*`、ViewModel | P1-8 | 关键列表页不再靠手动刷新驱动 | 新读路径有 observe API | 定向 ViewModel 测试 |
| P2-2 | P2 | 包结构重组 | 在边界稳定后，把 root 目录文件迁回 feature 域 | `app/src/main/java/app/yukine/*` | P1-3, P1-5, P1-8 | 根目录显著瘦身 | root 文件数下降 | 全量编译 + 搜索路径复核 |
| P2-3 | P2 | 命名与文案规范 | 统一后缀、文案入口、事件命名 | `AppLanguage.java`、`*Controller`、`*Bindings`、`*ViewModel` | P2-2 | 一致的命名规范和文案入口 | 新代码符合规则 | 定向测试 + grep 复核 |
| P2-4 | P2 | Kotlin 风格收口 | 不可变集合、`buildList`、减少 Java 风格样板 | Kotlin 源文件 | P1-8 | 代码风格一致 | review 中无新增冗余集合 | 编译 |
| P2-5 | P2 | 质量门禁 | 加强 Detekt/ktlint/契约测试/回归脚本 | Gradle、测试目录、`docs/RELEASE_EXPERIENCE_CHECKLIST.md` | 全部前置 | 明确门禁脚本和失败标准 | 本地可单独执行 | 定向测试 + `:app:check` |

---

## 6. 推荐执行顺序

### 第一批：必须先做

1. `P0-1` 基线冻结
2. `P0-3` 行为覆盖盘点
3. `P1-1` 宿主装配收缩
4. `P1-6` 播放服务瘦身
5. `P1-8` 数据层护栏

原因：

- 这是后续一切拆分的前提：先确保当前大 diff 可审查、行为测试不丢、三个 god-class 根因有明确下降路径。
- 如果不先收宿主、服务和数据库护栏，继续删桥接层只会把复杂度换位置。
- `P1-6` follows the playback target shape in `docs/ARCHITECTURE_STABILIZATION_PIVOT_2026-06-27.md`: `EchoPlaybackService` keeps only Android/Media3 system boundaries, while queue/cache/resolve/lyrics/notification policy moves into small real owners without adding a universal `PlaybackServiceFacade`.

### 第二批：收稳定边界

1. `P1-4` Settings
2. `P1-5` Streaming
3. `P1-2` 平台 owner 补齐
4. `P1-3` 桥接层清理
5. `P1-7` DI 补齐
6. `P1-9` 并发模型收敛

原因：

- 这几条是当前最明显的高耦合热点。
- 它们稳定后，数据层和目录重组才不会搬到一半又改 owner。

### 第三批：做结构性收尾

1. `P1-8` 数据层拆分或 Room 迁移评估
2. `P2-1` 响应式数据源
3. `P2-2` 包结构重组
4. `P2-3` 到 `P2-5` 规范与门禁

原因：

- 这些工作更适合在 owner 已稳定后进行。
- 其中“包结构重组”应视为收尾，不建议作为开场动作。

---

## 7. 每阶段统一验收标准

每个阶段至少满足以下四项：

1. 变更目标明确
   - 能用一句话说清“哪个 owner 变薄了，哪个 owner 接手了”。
2. 事件路径缩短
   - 至少减少一层无策略转发。
3. 测试有覆盖
   - 至少补一条与本阶段改动直接相关的单测或契约测试。
4. 编译可恢复
   - `:app:compileDebugKotlin :app:compileDebugJavaWithJavac` 通过。

对高风险阶段再增加：

- `:app:testDebugUnitTest`
- 相关设备/运行时 smoke 验证

---

## 8. 当前最合适的下一步

当前最适合立刻开始的是：

1. 先把当前已迁出的桥接层和 shell 统一收口，避免再开新 owner。
2. 以单个 Gradle 任务串行验证当前切片，先拿到稳定编译结果，再决定是否继续下一刀。
3. 只保留能够证明净减复杂度的迁移动作，并同步更新 `MainActivityArchitectureContractTest.java`。

不建议立刻开始：

- 大规模包移动
- 单纯重命名
- 在当前脏工作树上做机械性全仓整理
- 并行推进新的迁移切片

---

## 9. 复核备注

- 本计划明确采用“复核优先”原则：外部审查结论只作为优先级输入，不直接当作当前事实。
- 当前仓库已处于持续迁移中，很多问题是“仍未完成”，不是“完全没有做”。
- 后续如果根目录文件数、`MainActivity` 行数、`StreamingViewModel` 行数明显变化，本文档应同步更新对应数字。


## 2026-06-27 DIRECTION PIVOT: stabilization before more extraction

- New controlling doc: `docs/ARCHITECTURE_STABILIZATION_PIVOT_2026-06-27.md`.
- This pivot supersedes the previous default of continuing fast owner/manager extraction when the two conflict.
- Freeze broad architecture expansion first: do not add new `Manager`, `Coordinator`, `Controller`, `Bindings`, or `Gateway` layers by default.
- Stabilize the dirty migration surface, record current counts, and prefer reviewable commits/checkpoints before more migration.
- Reduce existing over-abstraction: merge/delete forwarding-only owners, shrink oversized provider/listener interfaces, and shorten UI -> service/data call chains.
- 2026-06-28 update: `PlaybackSessionGateway` was deleted as a forwarding-only service-internal wrapper; `EchoPlaybackService` now calls `PlaybackSessionManager` directly.
- 2026-06-28 update: `SettingsPageRenderController` was deleted after render ownership moved to `SettingsViewModel`; remaining label helpers now live in `SettingsLabelFormatter`.
- 2026-06-28 update: `PlaybackQueueManager.QueueProvider.currentTrack()` was removed; the manager derives it from `queue()` and `currentIndex()` to shrink the large provider interface.
- 2026-06-28 update: `DownloadQualitySelectedCallback` was removed in favor of a direct Kotlin function callback.
- 2026-06-28 update: `DownloadManagerProvider` was removed in favor of a direct `() -> TrackDownloadRequestQueue?` dependency.
- 2026-06-28 update: `NetworkMenuContentSink` was removed; `NetworkMenuEventController` now updates `NetworkMenuViewModel` directly, shortening network menu chrome publication by one forwarding interface.
- 2026-06-28 update: `CollectionsActionsSink` and the dead `publishCollectionsActions(...)` listener override were removed; collections actions now publish directly through `CollectionsViewModel.updateActions(...)`.
- 2026-06-28 update: unused migration callback types were removed from Collections, Library, NowPlaying, and Queue contracts where direct owner calls already exist.
- 2026-06-28 update: additional unused action contracts were removed from queue, queue render, network track-list, collections render, and settings controls paths after exact reference checks.
- 2026-06-28 update: recommendation self-interfaces were removed; `StreamingRecommendationViewModel` now owns direct action methods while `HeartbeatRecommendationPlayer` remains as the controller-facing boundary.
- 2026-06-28 update: `PlaylistExportController` was deleted; pending playlist export state now lives in `DocumentPickerController`, which emits export URI plus playlist id/name directly to the existing document listener.
- 2026-06-28 update: `LibraryPlaylistExportCallback` was removed after the only production export caller used an empty callback; the Java export entry now has no callback parameter.
- 2026-06-28 update: root-package `*Bindings*` zero count is now protected by `MainActivityArchitectureContractTest.rootPackageHasNoMigrationBindingsFiles`, adding a directory-level guard beyond per-file string checks.
- 2026-06-28 update: `LibraryPlayHistoryClearedCallback` and `LibraryViewModel.clearPlayHistoryJava(...)` were removed after play-history clearing settled on the direct `PlayHistoryActionController -> clearPlayHistory { ... }` path.
- 2026-06-28 update: the duplicate collection favorite write path was removed (`saveLibraryFavorite*`, `LibraryCollectionGateway.setFavorite`, and `SetLibraryFavoriteUseCase`); favorite toggles now stay on `LibraryFavoriteWriter -> ToggleFavoriteUseCase`.
- 2026-06-28 update: `PlaybackServiceProvider` and `PlaybackServiceStarter` were removed from `NowPlayingPlaybackGatewayAdapter`; the adapter now takes direct function dependencies while keeping `NowPlayingPlaybackGateway` as the service boundary.
- 2026-06-28 update: `BackupStatusSink` was removed from `BackupRestoreLauncher`; backup status-key publishing now uses a direct `(String) -> Unit` dependency.
- 2026-06-28 update: `DownloadStatusSink` was removed from `DownloadRequestController`; download feedback now uses a direct `(String) -> Unit` dependency.
- 2026-06-28 update: `TrackShareLanguageProvider` and `TrackShareStyleProvider` were removed from `TrackShareLauncher`; share language/style lookup now uses direct `() -> String` dependencies.
- 2026-06-28 update: `PlayHistoryLanguageModeProvider` and `PlayHistoryStatusSink` were removed from `PlayHistoryActionController`; play-history status publishing now uses direct function dependencies while keeping `PlayHistoryStateStore`.
- 2026-06-28 update: `BackgroundLanguageModeProvider` and `BackgroundTransformProvider` were removed from `BackgroundImagePickerController`; preview language/transform lookup now uses direct function dependencies.
- 2026-06-28 update: `StatusLanguageModeProvider` and `MessageLanguageModeProvider` were removed from status-message localization; `StatusMessageController` and `MessageTextResolver` now share a direct `Supplier<String>` language dependency.
- 2026-06-28 audit correction: wrapper deletion is no longer the default next step unless it also reduces `MainActivity` assembly/anonymous listener policy, preserves or replaces behavior tests, and does not defer `EchoPlaybackService` / `EchoDatabaseHelper` root risks.
- 2026-06-29 update: streaming search render listener policy moved out of the Java base anonymous `StreamingSearchRenderController.Listener` block into `MainStreamingSearchRenderListener`, provided by `StreamingModule` and covered by `MainStreamingSearchRenderListenerTest`.
- 2026-06-29 recheck after that slice: dirty entries 137, main Java files 56, controller files 44, `MainActivityBase.java` 3024 lines, `EchoPlaybackService.java` 2469 lines, `feature:data` `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines.
- 2026-06-29 direction correction: stop treating anonymous listener extraction as an unbounded migration loop. Execute two lanes in batches: finish at most one large listener slice, then pivot to Hilt construction migration for stable `initializeStoresAndDataGateways()` dependencies such as `LibraryPlaylistActionGateway`, `StreamingLocalPlaylistOperations`, `LoadSettingsPreferencesUseCase`, `ImportStreamingPlaylistUseCase`, `SyncStreamingPlaylistUseCase`, `EnsureStreamingLoginPlaylistUseCase`, and `GetStreamingPlaylistLinkUseCase`.
- 2026-06-29 acceptance correction: a new owner must delete Java anonymous business policy, reduce Activity manual construction, shorten a call chain, split a large interface, or add real behavior coverage. Delegation-only listener tests are insufficient as a long-term proof; every 2-3 listener slices must be balanced by a streaming/playback/data behavior test or a harder Hilt/interface reduction slice.
- 2026-06-29 checkpoint requirement: before the next broad slice, record dirty status, compile/test status, current class/file counts, and recommend a commit or rollback checkpoint because the migration surface is already large.
- 2026-06-29 Hilt slice: `StreamingModule` now provides `StreamingPlaylistSyncStore`, `ImportStreamingPlaylistUseCase`, `SyncStreamingPlaylistUseCase`, `EnsureStreamingLoginPlaylistUseCase`, `GetStreamingPlaylistLinkUseCase`, and `StreamingLocalPlaylistOperations` through `MainStreamingLocalPlaylistOperations`. `MainActivityBase` only binds the injected operations into `StreamingViewModel`; the old Java anonymous operations block and four manual use case constructors were removed. Recheck after this slice: dirty entries 5 from clean baseline, main Java files 56, controller files 44, `MainActivityBase.java` 2965 lines, `EchoPlaybackService.java` 2469 lines, `feature:data` `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines.
- 2026-06-29 review response: the provisional `ShellViewModel` / `ShellState` / `ShellAction` line was removed because it was not consumed by the real navigation path (`NavigationViewModel`, `MainRouteController`, and `EchoNavHostState`) and would create a parallel shell state model. Reintroduce typed shell state only when it becomes the single runtime source. The download directory picker unavailable message now uses `AppLanguage` key `download.directory.picker.unavailable` instead of hard-coded Chinese text.
- 2026-06-29 P0 freeze checkpoint: current `git status --short` reports four modified entries: `M docs/ARCHITECTURE_REMEDIATION_PLAN_2026-06-26.md`, `M docs/ARCHITECTURE_STABILIZATION_PIVOT_2026-06-27.md`, `M docs/MVVM_MIGRATION_HANDOFF.md`, and `M gradle.properties`; no test deletions are present, so there is no deleted-test replacement mapping to record. Current recheck counts are `MainActivityBase.java` 2968 lines, `EchoPlaybackService.java` 2469 lines, `feature:data` `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0, root-package `*Controller*` 44, root-package files 170, and root-package Java files 15. Compile and `MainActivityArchitectureContractTest` both passed serially with default Gradle settings, without `--no-daemon` or `--max-workers=1`.
- 2026-06-29 P1 Hilt construction slice: playlist action assembly moved from `MainActivityBase.initializeStoresAndDataGateways()` into `LibraryModule` + `MainLibraryPlaylistActionGateway`. The Java base now binds an injected `LibraryPlaylistActionGateway`, and the old anonymous gateway, local playlist operations, seven use case constructors, and `StreamingPlaylistSyncStore` field are gone from Activity. Recheck counts are `MainActivityBase.java` 2927 lines, root-package `*Bindings*` 0, root-package `*Controller*` 44, and root-package files 172. Focused gateway and architecture tests plus compile passed with default daemon/workers; `--rerun-tasks` was needed once to avoid a stale cached transform output, not as a daemon/KSP fallback.
- 2026-06-29 P1 Hilt construction slice: collection/import gateway assembly moved from Activity anonymous blocks into `LibraryModule` plus `MainLibraryCollectionGateway` and `MainLibraryImportGateway`. `MainActivityBase.java` is now 2873 lines, root-package files remain 172, root-package `*Bindings*` remain 0, and root-package `*Controller*` remains 44. Focused collection/import gateway tests, architecture contract, and compile passed with default daemon/workers.
- 2026-06-29 P1 Hilt construction slice: document gateway assembly moved from Activity-local `MusicLibraryImportOperations` plus `ContentResolverLibraryDocumentGateway(getContentResolver(), ...)` into `LibraryModule.provideLibraryDocumentGateway(...)`. `MainActivityBase` now injects and binds `LibraryDocumentGateway`; `MainActivityBase.java` is now 2872 lines, root-package files remain 172, root-package `*Bindings*` remain 0, and root-package `*Controller*` remains 44. Focused document gateway and architecture tests plus compile passed with default daemon/workers.
- 2026-06-29 P1 Hilt construction slice: settings preference load/apply use cases moved from Activity-local construction into `SettingsModule`. `MainActivityBase` now injects `LoadSettingsPreferencesUseCase` and `ApplySettingsPreferenceUseCase`; `MainActivityBase.java` is now 2866 lines, root-package files are now 173, root-package `*Bindings*` remain 0, and root-package `*Controller*` remain 44. Focused settings use-case tests, architecture contract, and compile passed with default daemon/workers.
- 2026-06-29 P1 Hilt construction slice: library store/search assembly now uses `MainLibraryStoreFactory` and a `LibraryModule`-provided `LibrarySearchUseCase`. `MainActivityBase.java` is now 2864 lines, root-package files remain 173, root-package `*Bindings*` remain 0, and root-package `*Controller*` remain 44. Focused library search/store tests, architecture contract, and compile passed with default daemon/workers.
- 2026-06-29 P1 Hilt construction slice: streaming search action-handler assembly now uses `MainStreamingSearchActionHandlerFactory`, provided by `StreamingModule`. `MainActivityBase.java` is now 2865 lines, root-package files remain 173, root-package `*Bindings*` remain 0, and root-package `*Controller*` remain 44. Focused action-handler behavior tests, architecture contract, and compile passed with default daemon/workers.
- 2026-06-29 P1 Hilt construction slice: track share operations assembly moved from Activity-local `TrackShareManagerOperations(trackShareManager, nativeMusicShareManager)` into `ShellModule.provideTrackShareOperations(...)`. `MainActivityBase.java` is now 2862 lines, root-package files remain 173, root-package `*Bindings*` remain 0, and root-package `*Controller*` remain 44. Focused share-launcher behavior tests, architecture contract, and compile passed with default daemon/workers.
- 2026-06-29 P1 Hilt construction slice: settings store assembly moved from Activity-local `new MainSettingsStore()` into `SettingsModule.provideMainSettingsStore()`. `MainActivityBase.java` is now 2861 lines, root-package files remain 173, root-package `*Bindings*` remain 0, and root-package `*Controller*` remain 44. Focused settings use-case tests, architecture contract, and compile passed with default daemon/workers.
- 2026-06-29 P1 Hilt construction slice: lyrics settings use-case assembly moved from Activity-local `LoadLyricsSettingsUseCase(MusicLibraryLyricsSettingsOperations(repository))` into `SettingsModule.provideLoadLyricsSettingsUseCase(...)`. `MainActivityBase.java` is now 2858 lines, root-package files remain 173, root-package `*Bindings*` remain 0, and root-package `*Controller*` remain 44. Focused lyrics settings use-case tests, architecture contract, and compile passed with default daemon/workers.
- 2026-06-29 P1 Hilt construction slice: lyrics loader assembly moved from Activity-local `LoadTrackLyricsUseCaseLyricsLoader(LoadTrackLyricsUseCase(LyricsRepositoryLoadOperations()))` into `SettingsModule.provideLyricsLoader()`. `MainActivityBase.java` is now 2857 lines, root-package files remain 173, root-package `*Bindings*` remain 0, and root-package `*Controller*` remain 44. Focused `LoadTrackLyricsUseCaseTest`, `LyricsViewModelTest`, architecture contract, and compile passed with default daemon/workers.
- 2026-06-29 P1 Hilt construction slice: network action use-case assembly moved from Activity-local `NetworkActionUseCases(...)`, `MusicLibraryWebDavSourceOperations(repository)`, and `MusicLibraryNetworkLibraryOperations(repository)` into `LibraryModule.provideNetworkActionUseCases(...)`. `MainActivityBase.java` is now 2840 lines, root-package files remain 173, root-package `*Bindings*` remain 0, and root-package `*Controller*` remain 44. Focused `NetworkActionsViewModelTest`, `NetworkLibraryUseCasesTest`, `WebDavSourceUseCasesTest`, architecture contract, and compile passed with default daemon/workers.
- 2026-06-29 P1 policy-owner slice: library gateway host policy moved from the Java anonymous `LibraryGateway` block into `MainLibraryGateway`, created by `LibraryModule.provideMainLibraryGatewayFactory()`. `MainRouteController` now implements `LibraryRouteActions`, Activity only supplies existing host/platform lambdas, and `MainActivityBase.java` is now 2789 lines; root-package files are now 174, root-package `*Bindings*` remain 0, and root-package `*Controller*` remain 44. Focused `MainLibraryGatewayTest`, `LibraryViewModelTest`, `MainRouteControllerTest`, architecture contract, and compile passed with default daemon/workers.
- 2026-06-29 P1 policy-owner slice: streaming action gateway host policy moved from the Java anonymous `MainActivityStreamingActionGateway` block into `MainStreamingActionGateway`, created by `StreamingModule.provideMainStreamingActionGatewayFactory()`. Activity only supplies existing host/platform lambdas, and `MainActivityBase.java` is now 2769 lines; root-package files are now 175, root-package `*Bindings*` remain 0, and root-package `*Controller*` remain 44. Focused `MainStreamingActionGatewayTest`, `DefaultStreamingSearchActionHandlerTest`, architecture contract, and compile passed with default daemon/workers.
- 2026-06-29 P1 policy-owner slice: now-playing playback gateway service-start construction moved from `MainActivityBase.initializeNowPlayingGateways()` into `PlaybackUiModule.provideMainNowPlayingPlaybackGatewayFactory(...)` plus `NowPlayingPlaybackServiceStarter`. Activity only binds `nowPlayingPlaybackGatewayFactory.create(() -> playbackService)` and no longer constructs `NowPlayingPlaybackGatewayAdapter` or the playback service start `Intent`; `MainActivityBase.java` is now 2762 lines, root-package files remain 175, root-package `*Bindings*` remain 0, and root-package `*Controller*` remain 44. Focused `NowPlayingPlaybackGatewayAdapterTest`, `MainNowPlayingGatewayTest`, `NowPlayingViewModelTest`, architecture contract, and compile passed with default daemon/workers.
- 2026-06-29 P1 Hilt construction slice: play-history action controller creation moved from `MainActivityBase.initializeStoresAndDataGateways()` into `LibraryModule.provideMainPlayHistoryActionControllerFactory()`. Activity still keeps the real confirmation-dialog path but no longer calls `new PlayHistoryActionController(...)`; `MainActivityBase.java` is now 2760 lines, root-package files remain 175, root-package `*Bindings*` remain 0, and root-package `*Controller*` remain 44. Focused `PlayHistoryActionControllerTest`, architecture contract, and compile passed with default daemon/workers.
- 2026-06-29 P1 policy-owner slice: the long `NetworkActionsViewModel.Listener` anonymous block moved from `MainActivityBase.initializeNetworkOwners()` into `MainNetworkActionsListener`, created by `LibraryModule.provideMainNetworkActionsListenerFactory()`. Activity now only binds the injected listener factory with existing callbacks for library replacement, network navigation, collections reload, and status publication; `MainActivityBase.java` is now 2697 lines, root-package files remain 175, root-package `*Bindings*` remain 0, and root-package `*Controller*` remain 44. Focused `MainNetworkActionsListenerTest`, `NetworkActionsViewModelTest`, architecture contract, and compile passed with default daemon/workers.
- 2026-06-29 P1 settings runtime controls slice: settings runtime playback, lyrics, and floating-lyrics controls moved out of `MainActivityBase` anonymous implementations into `MainSettingsRuntimeApplierFactory` plus concrete controls beside `SettingsRuntimeApplier`, provided by `SettingsModule`. Activity now only supplies existing runtime providers to `settingsRuntimeApplierFactory.create(...)`; `MainActivityBase.java` is now 2632 lines, root-package files remain 175, root-package `*Bindings*` remain 0, and root-package `*Controller*` remains 44. Focused `SettingsRuntimeApplierTest`, `SettingsViewModelTest`, architecture contract, and compile passed with default daemon/workers.
- 2026-06-29 P1 collections render listener slice: the anonymous `CollectionsRenderController.Listener` block moved out of `MainActivityBase.initializeRenderOwners()` into `MainCollectionsRenderListener`, created by `LibraryModule.provideMainCollectionsRenderListenerFactory()`. Activity now only supplies existing host/platform lambdas and selected-playlist state sources; `MainActivityBase.java` is now 2562 lines, root-package files are now 176, root-package `*Bindings*` remain 0, and root-package `*Controller*` remains 44. Focused `MainCollectionsRenderListenerTest`, architecture contract, and compile passed with default daemon/workers.
- 2026-06-29 P1 library-groups render listener slice: the anonymous `LibraryGroupsRenderController.Listener` block and local `new ArtistInfoRepository()` moved out of `MainActivityBase.initializeRenderOwners()` into `MainLibraryGroupsRenderListener`, `LibraryModule.provideMainLibraryGroupsRenderListenerFactory()`, and `LibraryModule.provideArtistInfoRepository()`. Activity now only supplies existing host callbacks to the factory; `MainActivityBase.java` is now 2517 lines, root-package files are now 177, root-package `*Bindings*` remain 0, and root-package `*Controller*` remains 44. Focused `MainLibraryGroupsRenderListenerTest`, architecture contract, and compile passed with default daemon/workers.
- 2026-07-03 playback owner/interface audit: after queue snapshot reuse plus media-source/precache boundary coverage, `EchoPlaybackService.java` is 1318 lines, has 55 top-level `Playback...` field declarations, 24 top-level `Playback*Owner` field declarations, 5 short-lived queue-owner factory methods, 0 Service `fromPlaybackQueueManager(...)` calls, 0 Service direct `queueStateSnapshot(...)` calls, and 43 `Playback*Owner.java` files. Production playback code still has no `PlaybackQueueManager.QueueProvider` or `PlaybackMediaSourceResolutionOwner` hits; next queue/read work should inspect existing `MainActivityBase -> playbackService.queueSnapshot()` call sites without adding a broad facade.
- 2026-07-03 queue/read slice: `MainActivityBase` now has only one direct `playbackService.queueSnapshot()` occurrence, inside the existing `playbackQueueSnapshot()` helper. Heartbeat seed binding, streaming playback listener setup, queue ViewModel binding, queue rendering, and current streaming queue resolution now reuse that helper; `MainActivityArchitectureContractTest` guards the single direct read.
- 2026-07-03 queue/interface test slice: `PlaybackQueueManagerTest` now covers `QueueStateSnapshot` derived booleans from `currentTrack`, `currentIndex`, and `queueSize`, while `MainActivityArchitectureContractTest` guards `currentIndex()` and `setCurrentIndex(...)` as private manager implementation details rather than public queue API.
- 2026-07-03 queue/read audit after NOTE 99-100: owner count is still a constraint, not the goal. The remaining four Service `playbackQueueStateOwner::currentTrack` dependencies feed distinct runtime gain, position persistence, notification artwork, and Wi-Fi lock owners; do not collapse them into a global Service supplier unless it removes a real constructor/interface dependency. Keep measuring Service field count, owner fields, and initialization wiring alongside line count.
- 2026-07-03 Wi-Fi lock queue-state slice: `PlaybackWifiLockManager` now reads current-track state from `PlaybackQueueManager.queueStateSnapshot()` inside `feature:playback`, and `EchoPlaybackService` passes `playbackQueueManager` instead of `playbackQueueStateOwner::currentTrack`. This removes one Service current-track supplier chain and lowers remaining Service `playbackQueueStateOwner::currentTrack` dependencies from 4 to 3 without adding an owner or expanding URI/cache resolution.
- 2026-07-03 position persistence queue-state slice: `PlaybackPositionManager.stateProviderFromPlaybackState(...)` now reads current-track state from `PlaybackQueueManager.queueStateSnapshot()` inside `feature:playback`, and `EchoPlaybackService` passes `playbackQueueManager` instead of `playbackQueueStateOwner::currentTrack`. This removes another Service current-track supplier chain and lowers remaining Service `playbackQueueStateOwner::currentTrack` dependencies from 3 to 2 without adding an owner or moving position persistence into Service.
- 2026-07-03 owner/interface audit after the two queue-state slices: `EchoPlaybackService.java` remains 1413 lines, strict `private Playback*` field declarations remain 53, strict `private Playback*Owner` field declarations remain 22, Service `fromPlaybackQueueManager(...)` and direct `queueStateSnapshot()` calls remain 0, and `Playback*Owner.java` files remain 43. The remaining two Service current-track suppliers feed runtime replay-gain state and notification artwork; do not replace them with a global Service supplier unless a real constructor/interface dependency is removed.
- 2026-07-03 notification artwork queue-state slice: `PlaybackNotificationArtworkManager` now reads current-track state from `PlaybackQueueManager.queueStateSnapshot()` inside `feature:playback`, and `EchoPlaybackService` passes `playbackQueueManager` instead of `playbackQueueStateOwner::currentTrack`. This removes the last Service current-track supplier chain without adding an owner, facade, or Service-level supplier field; architecture contracts now keep Service `playbackQueueStateOwner::currentTrack` occurrences at 0.
- 2026-07-03 precache interface slice: `PlaybackPrecacheManager.StateProvider` dropped `upcomingTracksForPrecache(int)`, and the precache manager now reads upcoming tracks directly from `PlaybackQueueManager`. This narrows the precache state interface by one method while keeping cache policy in `PlaybackPrecacheManager` and queue authority in `PlaybackQueueManager`.
- 2026-07-03 precache interface slice: `PlaybackPrecacheManager.StateProvider` also dropped `currentTrack()`, and `PlaybackPrecacheStateOwner` no longer holds `PlaybackQueueStateOwner`. Current-track reads for precache now go directly through the existing `PlaybackQueueManager` held by `PlaybackPrecacheManager`, narrowing the interface without adding another owner or moving cache policy out of precache.
- 2026-07-03 queue/read interface slice: `PlaybackQueueStateOwner.upcomingTracksForPrecache(int)` was removed as a migration residue after precache stopped consuming it. Upcoming precache reads remain on `PlaybackQueueManager` only, keeping the Java-facing state owner narrower instead of moving another call site.
- 2026-07-03 queue/read interface slice: `PlaybackQueueStateOwner.currentTrack()` was removed as another derived helper. Lyrics and notification state owners now read current track from `queueStateSnapshot()` directly, keeping current-track authority in `PlaybackQueueManager` without adding a Service-level supplier or broad snapshot facade.
- 2026-07-03 resolver/cache boundary test slice: `PlaybackPrecacheManagerTest` now verifies that current-track precache uses `StateProvider.currentPlayerMediaItem()` as the input to the provider-backed media-item matcher before skipping leading cache work. This locks the URI/MediaItem matching boundary without adding a resolution facade.
- 2026-07-03 owner/interface audit checkpoint: `QueueProvider` and resolution facade hits remain 0, `PlaybackQueueStateOwner` is narrowed to `queueStateSnapshot()` plus `queueSnapshot()`, Service current-track supplier and direct queue snapshot hits remain 0, and owner count remains the risk signal (`43` app `Playback*Owner.java` files, `24` final-inclusive Service owner fields). Do not continue by adding owners; continue only with a focused behavior test, a real interface/field reduction, or a read-only checkpoint.
- 2026-07-03 Service wiring slice: `PlaybackLyricsStateOwner` is now local to `EchoPlaybackService.onCreate()` and no longer a Service field. This removes one `Playback*Owner` field while preserving the existing lyrics manager/state-provider boundary and avoiding P4 runtime behavior changes.
- 2026-07-03 MediaSession wiring slice: `PlaybackSessionCommandOwner` is now local to `EchoPlaybackService.onCreate()` and captured by the `PlaybackSessionManager` player factory instead of being a Service field. This removes one more owner field and a lazy Service branch while preserving the same `PlaybackSessionPlayer` delegate boundary.
- 2026-07-03 owner/interface audit after the two Service wiring slices: `EchoPlaybackService.java` is 1404 lines, final-inclusive Service `Playback*Owner` fields are 22, production `fromPlaybackQueueManager(...)` / `QueueProvider` / Service direct `queueStateSnapshot(...)` / Service current-track supplier / resolution facade hits remain 0, and resolver/cache ownership remains with `PlaybackMediaSourceProvider`, `PlaybackMediaCacheOperations`, and `PlaybackPrecacheManager`. Focused feature/app tests plus app Kotlin/Java compile passed.
- 2026-07-03 resolver/cache boundary test slice: `PlaybackPrecacheManagerTest` now covers the non-matching current-player `MediaItem` path, proving precache falls back to cache state/data-source work instead of treating the player as filling the leading range. This adds behavior coverage without adding a resolution facade or moving cache policy out of `PlaybackPrecacheManager`.
- 2026-07-04 current-track wiring correction batch: `PlaybackPlayHistoryRecorder.recordIfPlaybackStartedAction(...)` now takes `Supplier<PlaybackQueueManager>` and reads queue state inside the owner, and `PlaybackPositionManager.stateProviderFromPlaybackState(...)` now takes `Supplier<PlaybackQueueManager?>` instead of a by-value manager because the position manager is created before `playbackQueueManager` is assigned in `EchoPlaybackService`. Current-track authority remains in `PlaybackQueueManager.QueueStateSnapshot`; `EchoPlaybackService.currentTrackFromQueueStateSnapshot()` is removed; local metrics after the batch are `EchoPlaybackService.java` `1373` lines, `0` helper references, and `0` direct Service `playbackQueueManager.queueStateSnapshot()` calls. Focused `PlaybackPlayHistoryRecorderTest`, `PlaybackPositionManagerTest`, architecture contract, and app Kotlin/Java compile passed.
- 2026-07-04 queue API narrowing slice: `PlaybackQueueManager.persistQueueState()` is removed from the public queue-manager API. `PlaybackShutdownLifecycleResourcesOwner.persistQueueState(...)` now maps `PlaybackQueueManager.queueSnapshot()` plus `queueStateSnapshot().currentIndex` into `PlaybackQueueStore.save(...)`, keeping queue source-of-truth in the manager while moving the trivial shutdown persistence mapping to the shutdown lifecycle owner that already owns resume-flag persistence. Focused `PlaybackQueueManagerTest`, `PlaybackShutdownLifecycleResourcesOwnerTest`, architecture contract, and app Kotlin/Java compile passed.
- 2026-07-04 queue completion interface slice: `PlaybackQueueCompletionOwner.prepareStopAfterAutomaticAdvance(int)` is removed as an external owner helper. `stopAfterAutomaticAdvance(int)` now performs the existing queue-manager preparation internally before running the stop-at-end boundary action, which keeps queue completion source-of-truth in `PlaybackQueueManager` while deleting one forwarding-only owner method. Focused `PlaybackQueueCompletionOwnerTest`, architecture contract, app Kotlin/Java compile, mojibake scan, and MuMu cold-start smoke passed. Current audit after the two 2026-07-04 queue API slices: `EchoPlaybackService.java` is `1376` lines, `MainActivityBase.java` is `2693` lines, strict `private Playback*` Service field lines are `39`, app playback `Playback*Owner.java` files are `39`, and production `fromPlaybackQueueManager(...)` / direct Service `queueStateSnapshot()` supplier hits remain `0`.
- 2026-07-04 queue preparation interface slice: `PlaybackQueueManager.QueuePreparation` and `queuePreparationForNewPlayer()` are removed from the public queue-manager surface. `PlaybackCurrentTrackPreparationQueueOwner` now derives prepared mirrored-queue input from `playbackQueueManager.queueStateSnapshot()` plus `playbackQueueManager.queueSnapshot()` and applies `PlaybackMediaSourceProvider` restore rules there, keeping queue state in the manager and URI/header resolution in the existing media-source owner. Focused `PlaybackQueueManagerTest`, `PlaybackCurrentTrackPreparationQueueOwnerTest`, architecture contract, app Kotlin/Java compile, and mojibake scan passed. Current audit after the three 2026-07-04 queue API slices: `EchoPlaybackService.java` is still `1376` lines, `MainActivityBase.java` is still `2693` lines, strict `private Playback*` Service field lines remain `39`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, and direct Service `queueStateSnapshot()` supplier hits remain `0`.
- 2026-07-04 restore-enable interface slice: `PlaybackQueueManager.setPlaybackRestoreEnabled(...)` is removed from the public queue-manager surface. `PlaybackQueueRestoreOwner` now persists the setting directly through `PlaybackQueueStore.savePlaybackRestoreEnabled(...)`, while `PlaybackQueueManager.restorePlaybackQueue()` reads enablement from `queueStore.loadPlaybackRestoreEnabled()` at call time. This leaves restore-enable source-of-truth in the store instead of duplicating it in both the manager and the persisted setting. Focused `PlaybackQueueManagerTest`, `PlaybackQueueRestoreOwnerTest`, architecture contract, app Kotlin/Java compile, and mojibake scan passed. Current audit after the four 2026-07-04 queue API slices: `EchoPlaybackService.java` is `1377` lines, `MainActivityBase.java` is `2693` lines, strict `private Playback*` Service field lines remain `39`, app playback `Playback*Owner.java` files remain `39`, and production `fromPlaybackQueueManager(...)` / direct Service `queueStateSnapshot()` supplier hits remain `0`.
- 2026-07-04 restore-result interface slice: `PlaybackQueueManager.RestorePlaybackResult` and `restoreLastPlayback(...)` are removed from the public queue-manager surface. `PlaybackQueueRestoreOwner.restoreLastPlayback(...)` now maps `restorePlaybackQueue()` + `queueStateSnapshot()` + `PlaybackQueueStore.loadResumeRequested()` into the existing create-player / publish-state / prepare-current callbacks, keeping queue state in the manager and resume-request persistence in the store while deleting the result wrapper API. Focused `PlaybackQueueManagerTest`, `PlaybackQueueRestoreOwnerTest`, architecture contract, app Kotlin/Java compile, and mojibake scan passed. Current audit after the five 2026-07-04 queue API slices: `EchoPlaybackService.java` is still `1377` lines, `MainActivityBase.java` is still `2693` lines, strict `private Playback*` Service field lines remain `39`, app playback `Playback*Owner.java` files remain `39`, and production `fromPlaybackQueueManager(...)` / direct Service `queueStateSnapshot()` supplier hits remain `0`.
- 2026-07-04 error-recovery queue-state wiring slice: `PlaybackErrorRecoveryCommandOwner` now takes `PlaybackQueueManager` directly, and `EchoPlaybackService.onCreate()` initializes it only after `playbackQueueManager` exists. This removes the temporary `queueStateSnapshotProvider(() -> playbackQueueManager)` wrapper instead of keeping another deferred supplier chain in Service wiring. Focused `PlaybackErrorRecoveryCommandOwnerTest`, architecture contract, app Kotlin/Java compile, and mojibake scan passed. Current audit after the six 2026-07-04 queue/error-recovery slices: `EchoPlaybackService.java` remains `1377` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines are `40`, app playback `Playback*Owner.java` files remain `39`, and production `fromPlaybackQueueManager(...)` / direct Service `queueStateSnapshot()` supplier hits remain `0`.
- 2026-07-04 mirrored-player queue-state wiring slice: `PlaybackQueueMirroredPlayerOwner` now binds `PlaybackQueueManager` explicitly after manager construction instead of accepting `queueStateSnapshotProvider(() -> playbackQueueManager)` for its current-track read path. The mirrored queue matcher still uses `queueSnapshotProvider(() -> playbackQueueManager)` because that constructor cycle is still real; this slice only removes the extra current-track supplier wrapper. Focused `PlaybackQueueMirroredPlayerOwnerTest`, `PlaybackErrorRecoveryCommandOwnerTest`, architecture contract, app Kotlin/Java compile, and mojibake scan passed. Current audit after the seven 2026-07-04 queue/error-recovery/mirrored-player slices: `EchoPlaybackService.java` remains `1377` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, and production `fromPlaybackQueueManager(...)` / direct Service `queueStateSnapshot()` supplier hits remain `0`.
- 2026-07-04 mirrored-player queue-snapshot binding slice: the mirrored queue matcher no longer uses `queueSnapshotProvider(() -> playbackQueueManager)`. `PlaybackQueueMirroredPlayerOwner.queueSnapshotProvider()` now returns a bindable `QueueSnapshotProvider`, and `EchoPlaybackService.onCreate()` binds it explicitly after `playbackQueueManager` exists. This removes one more Service supplier chain without changing queue source-of-truth, MediaItem matching ownership, or cache/resolver boundaries. Focused `PlaybackQueueMirroredPlayerOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and mojibake scan passed; MuMu cold-start smoke is the next verification step before calling the batch stable. Current audit after the next 2026-07-04 mirrored-player slice: `EchoPlaybackService.java` is `1382` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines are `36`, app playback `Playback*Owner.java` files remain `39`, and production `fromPlaybackQueueManager(...)` / direct Service `queueStateSnapshot()` supplier hits remain `0`.
- 2026-07-04 mirrored-player helper cleanup slice: the temporary `PlaybackQueueMirroredPlayerOwner.QueueSnapshotProvider` helper is removed again. `PlaybackQueueMirroredPlayerOwner` now wires mirrored queue matching through its own `this::queueSnapshot` method, so `EchoPlaybackService.onCreate()` no longer needs a separate `playbackQueueSnapshotProvider` local or bind call. This deletes one transient helper and one Service wiring step while keeping queue source-of-truth in `PlaybackQueueManager` and MediaItem matching ownership in the existing mirrored-track matcher/media-source provider. Focused `PlaybackQueueMirroredPlayerOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and mojibake scan passed. Current audit after the follow-up mirrored-player cleanup: `EchoPlaybackService.java` is `1376` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `36`, app playback `Playback*Owner.java` files remain `39`, and production `fromPlaybackQueueManager(...)` / direct Service `queueStateSnapshot()` supplier hits remain `0`.
- 2026-07-04 precache-derived-read slice: `PlaybackQueueManager.upcomingTracksForPrecache(...)` is removed from the public queue API. `PlaybackPrecacheManager` now combines `PlaybackQueueManager.queueSnapshot()`, `PlaybackQueueManager.queueStateSnapshot()`, and `PlaybackRuntimeStateManager.repeatMode()` inside the existing cache policy owner to derive the upcoming tracks it precaches. This deletes one derived queue method without adding a facade or new state mirror, and it keeps repeat-mode semantics owned by runtime state instead of re-encoding them in Service wiring. Focused `PlaybackQueueManagerTest`, `PlaybackPrecacheManagerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and mojibake scan passed. Current audit after the queue/precache slice: `EchoPlaybackService.java` is `1377` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `36`, app playback `Playback*Owner.java` files remain `39`, and production `fromPlaybackQueueManager(...)` / direct Service `queueStateSnapshot()` supplier hits remain `0`.
- 2026-07-04 completion forwarding cleanup slice: `PlaybackQueueCompletionOwner.prepareStopAndClearPlaybackState()` and `prepareStopAtEndOfQueue()` are removed. `EchoPlaybackService.stopAndClear()` and `stopAtEndOfQueue()` now call the corresponding `PlaybackQueueManager` methods directly at the lifecycle boundary, while `PlaybackQueueCompletionOwner` keeps only actual completion-specific behavior. This deletes two forwarding-only owner methods and two `Service -> owner -> manager` chains without adding a new owner or changing queue source-of-truth. Focused `PlaybackQueueCompletionOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and mojibake scan passed. Current audit after the completion cleanup slice: `EchoPlaybackService.java` is `1381` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `36`, app playback `Playback*Owner.java` files remain `39`, and production `fromPlaybackQueueManager(...)` / direct Service `queueStateSnapshot()` supplier hits remain `0`.
- 2026-07-04 restore forwarding cleanup slice: `PlaybackQueueRestoreOwner.restorePlaybackQueue()` is removed. `PlaybackQueueRestoreOwner.restoreLastPlayback(...)` now restores queue state directly through `PlaybackQueueManager.restorePlaybackQueue()`, while `EchoPlaybackService.onCreate()` restores the persisted queue snapshot directly at the service lifecycle boundary before notification and lyrics startup. This deletes one forwarding-only owner method and one `Service -> owner -> manager` startup chain without adding a facade or changing queue source-of-truth. Focused `PlaybackQueueRestoreOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and mojibake scan passed. Current audit after the restore cleanup slice: `EchoPlaybackService.java` is `1383` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `36`, app playback `Playback*Owner.java` files remain `39`, and production `fromPlaybackQueueManager(...)` / direct Service `queueStateSnapshot()` supplier hits remain `0`.
- 2026-07-04 queue-command wiring slice: `PlaybackQueueCommandOwner` now binds `PlaybackQueueManager` explicitly after manager construction instead of accepting `Supplier<PlaybackQueueManager>` in its constructor. This removes one constructor parameter and one queue-manager supplier field while keeping `PlaybackQueueCommandOwner` as the semantic `prepareCurrent/publishState` boundary for `PlaybackQueueManager.QueuePlaybackActions`. Focused `PlaybackQueueCommandOwnerTest`, `PlaybackErrorRecoveryCommandOwnerTest`, `PlaybackQueueMirroredPlayerOwnerTest`, architecture contract, app Kotlin/Java compile, and mojibake scan passed. Current audit after the eight 2026-07-04 queue wiring/API slices: `EchoPlaybackService.java` remains `1377` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, and production `fromPlaybackQueueManager(...)` / direct Service `queueStateSnapshot()` supplier hits remain `0`.
- 2026-07-04 play-history wiring slice: `PlaybackPlayHistoryRecorder.recordIfPlaybackStartedAction(...)` now accepts `PlaybackQueueManager` directly, and `EchoPlaybackService` creates `recordPlaybackStartHistoryAction` only after `playbackQueueManager` exists. This removes one more `Supplier<PlaybackQueueManager>` chain without changing the actual history-recording owner or queue state source of truth. Focused `PlaybackPlayHistoryRecorderTest`, `PlaybackQueueCommandOwnerTest`, `PlaybackErrorRecoveryCommandOwnerTest`, `PlaybackQueueMirroredPlayerOwnerTest`, architecture contract, app Kotlin/Java compile, and mojibake scan passed. Current audit after the nine 2026-07-04 queue/history wiring slices: `EchoPlaybackService.java` remains `1377` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, and production `fromPlaybackQueueManager(...)` / direct Service `queueStateSnapshot()` supplier hits remain `0`.
- 2026-07-04 current-track preparation callback slice: `PlaybackCurrentTrackPreparationOwner` dropped the single-method `QueuePreparationController` interface, and `EchoPlaybackService` now passes `playbackCurrentTrackPreparationQueueOwner::replaceCurrentQueueTrack` directly into `fromMediaSourceProvider(...)`. `PlaybackCurrentTrackPreparationQueueOwner` keeps queue preparation behavior local but no longer exports a forwarding interface or extra public contract surface. Focused `PlaybackCurrentTrackPreparationOwnerTest`, `PlaybackCurrentTrackPreparationQueueOwnerTest`, architecture contract, app Kotlin/Java compile, and mojibake scan passed. Current audit after the ten 2026-07-04 queue/history/preparation slices: `EchoPlaybackService.java` remains `1377` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` / direct Service `queueStateSnapshot()` supplier hits remain `0`, and production/test playback code now has `0` remaining `QueuePreparationController` hits outside architecture contract assertions.
- 2026-07-04 queue retain derivation slice: `PlaybackQueueManager.retainTracksById(...)` is removed from the feature playback public API, and `PlaybackQueueMutationOwner.retainTracksById(...)` now derives `trackIdsToRemove` from `playbackQueueManager.queueSnapshot()` before reusing `removeTracksById(...)`. This keeps queue source-of-truth in the manager while deleting one derivable keep/remove wrapper from the queue API. Focused `PlaybackQueueManagerTest`, `PlaybackQueueMutationOwnerTest`, architecture contract, app Kotlin/Java compile, and mojibake scan passed. Current audit after the eleven 2026-07-04 queue/history/preparation slices: `EchoPlaybackService.java` remains `1377` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` / direct Service `queueStateSnapshot()` supplier hits remain `0`, and production playback code now has `0` remaining `PlaybackQueueManager.retainTracksById(...)` call sites.
- 2026-07-04 queue completion audit: keep `PlaybackQueueCompletionOwner` as the stop/end boundary for now. The old `PlaybackQueueStopClearOwner` remains absent and guarded by architecture contracts, while the remaining completion methods (`preparePlaybackCompletionAction`, `prepareStopAtEndOfQueue`, `prepareStopAfterAutomaticAdvance`, `prepareStopAndClearPlaybackState`) still mutate queue/runtime source-of-truth inside `PlaybackQueueManager` rather than wrapping derivable reads. Do not reintroduce a separate stop/clear owner unless it removes a real state source or queue-manager API.
- 2026-07-04 app-shell wiring contract slice: `MainActivityArchitectureContractTest` now explicitly guards `initializePlaybackLifecycleControllers()` so `PlaybackStateEventController` keeps taking `playbackStore` only after `initializeRouteStoresAndStatus()`, while queue/status collaborators in that early lifecycle wiring remain provider/lambda based instead of being passed as raw host fields.
- 2026-07-04 position-manager wiring slice: `PlaybackPositionManager.stateProviderFromPlaybackState(...)` dropped its `Supplier<PlaybackQueueManager?>` parameter, and `EchoPlaybackService` now binds `playbackQueueManager` explicitly after manager construction through `playbackPositionStateProvider.bindPlaybackQueueManager(playbackQueueManager)`. This removes one more queue-manager supplier chain while keeping position source-of-truth on `PlaybackQueueManager.QueueStateSnapshot` plus `playbackPlayerStateOwner.positionMs()`. Focused `PlaybackPositionManagerTest`, architecture contract, app Kotlin/Java compile, and mojibake scan passed. Current audit after the twelve 2026-07-04 queue/history/preparation/position slices: `EchoPlaybackService.java` is `1379` lines, `MainActivityBase.java` is `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, and production `fromPlaybackQueueManager(...)` / direct Service `queueStateSnapshot()` supplier hits remain `0`.
- 2026-07-04 runtime-state wiring slice: `PlaybackRuntimeStateManager.stateProviderFromPlaybackState(...)` dropped its `Supplier<PlaybackQueueManager?>` parameter, and `EchoPlaybackService` now binds `playbackQueueManager` explicitly after manager construction through `playbackRuntimeStateProvider.bindPlaybackQueueManager(playbackQueueManager)`. This removes another queue-manager supplier chain while keeping replay-gain/current-track-volume source-of-truth on `PlaybackQueueManager.QueueStateSnapshot` plus the existing player/mirror suppliers. Focused `PlaybackRuntimeStateManagerTest`, architecture contract, app Kotlin/Java compile, and mojibake scan passed. Current audit after the thirteen 2026-07-04 queue/history/preparation/runtime slices: `EchoPlaybackService.java` remains `1379` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, and production `fromPlaybackQueueManager(...)` / direct Service `queueStateSnapshot()` supplier hits remain `0`.
- 2026-07-04 queue-manager binding batch recheck: the current combined focused run for `PlaybackPositionManagerTest`, `PlaybackRuntimeStateManagerTest`, `PlaybackQueueCommandOwnerTest`, `PlaybackErrorRecoveryCommandOwnerTest`, `PlaybackPlayHistoryRecorderTest`, `PlaybackCurrentTrackPreparationOwnerTest`, `PlaybackQueueMutationOwnerTest`, `PlaybackStateSnapshotOwnerTest`, and `MainActivityArchitectureContractTest` passed together with `:app:compileDebugKotlin :app:compileDebugJavaWithJavac`, and the UTF-8 mojibake scan reported `TOTAL=0`. Current batch metrics are `EchoPlaybackService.java` `1383` lines, `MainActivityBase.java` `2693` lines, strict `private Playback*` Service field lines `36`, app playback `Playback*Owner.java` files `39`, production `fromPlaybackQueueManager(...)` hits `0`, and direct Service `queueStateSnapshot()` supplier hits `0`. Treat this as the stable checkpoint before the next queue/state interface audit.
- 2026-07-04 current-track replacement recovery slice: `PlaybackQueueManager.replaceCurrentTrackAndResume(...)` now returns only `restoredPositionMs`, `PlaybackCurrentTrackReplacementOwner` now passes `replacement + restoredPositionMs` directly to `PlaybackStreamingDiagnosticsRecorderOwner.recordRecovery(...)`, and the recovery scheduler path is a plain `Runnable` that always schedules `PlaybackRecoveryScheduler.scheduleCurrentPlaybackRecovery(true)`. This deletes the `CurrentTrackReplacementRecovery` wrapper type and one constant-boolean callback parameter without changing queue state ownership, diagnostics ownership, or the public `NowPlaying`/service command surface. Focused `PlaybackQueueManagerTest`, `PlaybackCurrentTrackReplacementOwnerTest`, `PlaybackStreamingDiagnosticsRecorderOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and mojibake scan passed. Current batch metrics remain `EchoPlaybackService.java` `1383` lines, `MainActivityBase.java` `2693` lines, strict `private Playback*` Service field lines `36`, app playback `Playback*Owner.java` files `39`, production `fromPlaybackQueueManager(...)` hits `0`, and direct Service `queueStateSnapshot()` supplier hits `0`.
- 2026-07-04 mirrored-transition result slice: `PlaybackQueueManager.applyMirroredTransitionIndex(...)` now returns only `Boolean?` for `stopAfterAutomaticAdvance`, and `PlaybackQueueMirroredTransitionOwner` derives `completedIndex` from the pre-transition queue snapshot before assembling its existing owner-level `Transition` object. This deletes the `MirroredTransitionResult` wrapper type while keeping repeat-off stop logic and mirrored-transition playback-state preparation inside `PlaybackQueueManager`, not in `EchoPlaybackService`. Focused `PlaybackQueueManagerTest`, `PlaybackQueueMirroredTransitionOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and mojibake scan passed. Current batch metrics remain `EchoPlaybackService.java` `1383` lines, `MainActivityBase.java` `2693` lines, strict `private Playback*` Service field lines `36`, app playback `Playback*Owner.java` files `39`, production `fromPlaybackQueueManager(...)` hits `0`, and direct Service `queueStateSnapshot()` supplier hits `0`.
- 2026-07-04 play-first queue audit: do not mechanically replace `PlaybackQueueManager.playFirstQueuedTrack()` with `playQueue(queueSnapshot(), 0, ...)`. A new focused test now proves that `playFirstQueuedTrack()` preserves the existing `resumeRequested` flag instead of forcing it to `true`, which means the API still carries distinct restore semantics even though it has only one production caller. Keep this method until a later slice explicitly re-homes that persistence behavior with matching tests.
- 2026-07-05 feature dependency-direction contract slice: `MainActivityArchitectureContractTest` now blocks `feature` main-source imports of `app.yukine.playback.EchoPlaybackService` and `app.yukine.playback.Playback*Owner` via an explicit regex contract. The earlier whitelist already blocked named app implementation classes, but this closes the wildcard gap for future playback-owner imports while keeping current feature modules clean.
- 2026-07-05 app-shell re-audit plus queue completion boundary slice: current worktree still initializes `initializeRouteStoresAndStatus()` before `initializeNowPlayingGateways()`, `initializeLibraryGateway()`, and `initializePlaybackLifecycleControllers()`, and the focused UTF-8/mojibake scan over `app/src/main/java`, `app/src/test/java`, and `feature/ui-common/src/main/java` remains `TOTAL=0`. In the same batch, `PlaybackQueueCompletionOwner` took back `stopAndClearPlayback()` and `stopAtEndOfQueue()` queue preparation, so `EchoPlaybackService` no longer calls `playbackQueueManager.prepareStopAndClearPlaybackState()` / `prepareStopAtEndOfQueue()` directly and instead wires only `stopAndClearBoundary()` / `stopAtEndOfQueueBoundary()` into the existing completion owner. Focused `PlaybackQueueCompletionOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and mojibake scan passed. Current metrics after this batch: `EchoPlaybackService.java` `1381` lines, `MainActivityBase.java` `2693` lines, strict `private Playback*` Service field lines `36`, app playback `Playback*Owner.java` files `39`, production `fromPlaybackQueueManager(...)` hits `0`, and direct Service `queueStateSnapshot()` supplier hits `0`.
- 2026-07-05 now-playing queue visibility slice: `NowPlayingPlaybackGateway` and `NowPlayingPlaybackServicePort` no longer expose raw `queueSnapshot(): List<Track>` only to support `NowPlayingViewModel.hasQueue()`. That path now uses semantic `hasQueue()`, while `PlaybackServiceHostPort` keeps `queueSnapshot()` for the separate host/streaming path that still consumes full queue contents. This reduces one service/UI raw queue read without adding a facade or changing queue source-of-truth. Focused `NowPlayingPlaybackGatewayAdapterTest`, `NowPlayingViewModelTest`, `QueueActionControllerTest`, `PlaybackActionControllerTest`, `MainPlaybackServiceHostTest`, `MainNetworkActionsListenerTest`, `MainNowPlayingGatewayTest`, `MainActivityArchitectureContractTest`, and app Kotlin/Java compile passed; the focused mojibake scan remained `TOTAL=0`. Current metrics after this batch: `EchoPlaybackService.java` `1385` lines, `MainActivityBase.java` `2693` lines, strict `private Playback*` Service field lines `36`, app playback `Playback*Owner.java` files `39`, production `fromPlaybackQueueManager(...)` hits `0`, and direct Service `queueStateSnapshot()` supplier hits `0`.
- 2026-07-05 streaming queue-store wiring slice: `MainPlaybackStore` now exposes `queueSnapshot()`, and `MainActivityBase` wires `StreamingPlaybackController` through `playbackStore.queueSnapshot()` instead of routing that read back through `playbackService.queueSnapshot()`. `PlaybackStateEventController` still keeps the service-backed `queueSnapshotSource` because it is the path that republishes fresh queue state into `PlaybackViewModel`. This removes one more `Activity -> service` queue-read use while keeping queue source-of-truth in `PlaybackQueueManager` and UI-visible queue state in `PlaybackViewModel`. Focused `MainStreamingPlaybackListenerTest`, `StreamingPlaybackControllerTest`, `MainActivityArchitectureContractTest`, and app Kotlin/Java compile passed; the focused mojibake scan remained `TOTAL=0`. Current metrics after this batch: `EchoPlaybackService.java` `1385` lines, `MainActivityBase.java` `2693` lines, strict `private Playback*` Service field lines `36`, app playback `Playback*Owner.java` files `39`, production `fromPlaybackQueueManager(...)` hits `0`, and direct Service `queueStateSnapshot()` supplier hits `0`.
- 2026-07-05 current-track preparation callback slice: `PlaybackCurrentTrackPreparationQueueOwner.replaceCurrentQueueTrack(...)` is removed, and `EchoPlaybackService` now passes the existing `PlaybackQueueManager.replaceCurrentQueueTrack(...)` call as a minimal callback directly into `PlaybackCurrentTrackPreparationOwner.fromMediaSourceProvider(...)`. The queue-preparation owner keeps only mirrored queue/media-source preparation, while current-slot mutation stays with the queue manager. Focused `PlaybackQueueManagerTest`, `PlaybackCurrentTrackPreparationOwnerTest`, `PlaybackCurrentTrackPreparationQueueOwnerTest`, `MainActivityArchitectureContractTest`, and app Kotlin/Java compile passed; the focused mojibake scan remained `TOTAL=0`. Current metrics after this batch: `EchoPlaybackService.java` `1389` lines, `MainActivityBase.java` `2693` lines, strict `private Playback*` Service field lines `36`, app playback `Playback*Owner.java` files `39`, production `fromPlaybackQueueManager(...)` hits `0`, and direct Service `queueStateSnapshot()` supplier hits `0`.
- 2026-07-05 queue UI store-read slice: `MainActivityBase.bindQueueViewModelInputs()` and `renderQueue()` now consume `MainPlaybackStore.queueSnapshot()` instead of reading queue contents directly from `playbackService.queueSnapshot()`. `playbackQueueSnapshot()` remains only for store refresh and the unresolved-streaming resolve path. This keeps queue UI consumers on the app-shell playback store without changing queue source-of-truth or adding a facade. Focused `MainActivityArchitectureContractTest` and app Kotlin/Java compile passed; the focused mojibake scan remained `TOTAL=0`. Current metrics after this batch: `EchoPlaybackService.java` `1389` lines, `MainActivityBase.java` `2693` lines, strict `private Playback*` Service field lines `36`, app playback `Playback*Owner.java` files `39`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` supplier hits `0`, and `MainActivityBase.playbackQueueSnapshot()` call sites `3`.
- 2026-07-05 service has-queue snapshot slice: `EchoPlaybackService.hasQueue()` now returns `snapshot().queueSize > 0` instead of `!queueSnapshot().isEmpty()`. This removes one more raw Service queue-list read while keeping `queueSnapshot()` only on the host path that still needs full queue contents. `MainActivityArchitectureContractTest` now explicitly guards the semantic snapshot-based implementation. Focused `MainActivityArchitectureContractTest`, `NowPlayingPlaybackGatewayAdapterTest`, `NowPlayingViewModelTest`, app Kotlin/Java compile, and focused mojibake scan passed. Current metrics after this batch: `EchoPlaybackService.java` `1389` lines, `MainActivityBase.java` `2693` lines, strict `private Playback*` Service field lines `36`, app playback `Playback*Owner.java` files `39`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` supplier hits `0`, and current Service `queueSnapshot()` literal hits `2`.
- 2026-07-05 current-streaming resolve store-read slice: `MainActivityBase.resolveCurrentStreamingQueueTrackIfNeeded()` now passes `playbackStore.queueSnapshot()` into `StreamingViewModel.prepareCurrentStreamingQueueResolveTarget(...)` instead of `playbackQueueSnapshot()`. This removes one more host action-path service queue read while keeping `playbackQueueSnapshot()` only for the real service -> store refresh paths. Focused `MainActivityArchitectureContractTest`, `StreamingViewModelTest`, `MainPlaybackActionListenerTest`, app Kotlin/Java compile, and focused mojibake scan passed. Current metrics after this batch: `EchoPlaybackService.java` `1389` lines, `MainActivityBase.java` `2693` lines, current Service `queueSnapshot()` literal hits `2`, `MainActivityBase.playbackQueueSnapshot()` call sites `2`, and direct `MainActivityBase` `playbackService.queueSnapshot()` occurrences `1`.
- 2026-07-05 queue replacement return-value slice: `PlaybackQueueManager.replaceQueuedTrackById(...)` no longer returns `Boolean`, and `PlaybackQueueMutationOwner.replaceQueuedTrackById(...)` no longer uses a stop/clear branch based on that return value. The replacement/collapse behavior remains owned by `PlaybackQueueManager`, while the mutation owner becomes a pure semantic command delegate instead of also interpreting a dead stop request. Focused `PlaybackQueueManagerTest`, `PlaybackQueueMutationOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed. Current metrics after this batch: `EchoPlaybackService.java` `1389` lines, `MainActivityBase.java` `2693` lines, app playback `Playback*Owner.java` files `39`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` supplier hits `0`, current Service `queueSnapshot()` literal hits `2`, `MainActivityBase.playbackQueueSnapshot()` call sites `2`, and direct `MainActivityBase` `playbackService.queueSnapshot()` occurrences `1`.
- 2026-07-05 queue removal return-value slice: `PlaybackQueueManager.removeTracksById(...)` no longer returns `Boolean`, and `PlaybackQueueMutationOwner` now decides whether to run `stopAndClear()` by comparing `queueStateSnapshot().isQueueEmpty()` before and after the mutation. This keeps empty-queue interpretation at the owner/service boundary while deleting one more queue-manager return-value contract. Focused `PlaybackQueueManagerTest`, `PlaybackQueueMutationOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed. Current metrics after this batch: `EchoPlaybackService.java` `1389` lines, `MainActivityBase.java` `2693` lines, app playback `Playback*Owner.java` files `39`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` supplier hits `0`, current Service `queueSnapshot()` literal hits `2`, `MainActivityBase.playbackQueueSnapshot()` call sites `2`, and direct `MainActivityBase` `playbackService.queueSnapshot()` occurrences `1`.
- 2026-07-05 current-track nullability slice: `PlaybackQueueManager.replaceCurrentTrackAndResume(...)` and `replaceCurrentQueueTrack(...)` now take non-null `Track`. Nullable guards remain in upper UI/service entry points, but the queue manager itself no longer accepts nullable current-track replacements. Focused `PlaybackQueueManagerTest`, `PlaybackCurrentTrackReplacementOwnerTest`, `PlaybackCurrentTrackPreparationQueueOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed. Current metrics after this batch: `EchoPlaybackService.java` `1389` lines, `MainActivityBase.java` `2693` lines, app playback `Playback*Owner.java` files `39`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` supplier hits `0`, current Service `queueSnapshot()` literal hits `2`, `MainActivityBase.playbackQueueSnapshot()` call sites `2`, and direct `MainActivityBase` `playbackService.queueSnapshot()` occurrences `1`.
- 2026-07-05 mirrored-transition tri-state slice: `PlaybackQueueManager.applyMirroredTransitionIndex(...)` now returns plain `Boolean`, and `PlaybackQueueMirroredTransitionOwner` now rejects invalid/same-index transitions locally before calling the queue manager. This keeps Media3 reason interpretation and ignore/no-op semantics in the existing mirrored-transition owner while deleting one more tri-state queue-manager contract. Focused `PlaybackQueueManagerTest`, `PlaybackQueueMirroredTransitionOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed. Current metrics after this batch: `EchoPlaybackService.java` `1389` lines, `MainActivityBase.java` `2693` lines, app playback `Playback*Owner.java` files `39`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` supplier hits `0`, current Service `queueSnapshot()` literal hits `2`, `MainActivityBase.playbackQueueSnapshot()` call sites `2`, and direct `MainActivityBase` `playbackService.queueSnapshot()` occurrences `1`.
- 2026-07-05 completion null-contract slice: `PlaybackQueueManager.preparePlaybackCompletionAction()` now returns explicit `PlaybackCompletionAction.REPEAT_CURRENT` instead of `null`, and `PlaybackQueueCompletionOwner.playAfterCompletion()` now treats that enum case as the no-boundary repeat-current path. This deletes one more nullable queue-manager contract while keeping completion decision logic in the queue manager and service-boundary action mapping in the existing completion owner. Focused `PlaybackQueueManagerTest`, `PlaybackQueueCompletionOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed. Current metrics after this batch: `EchoPlaybackService.java` `1389` lines, `MainActivityBase.java` `2693` lines, app playback `Playback*Owner.java` files `39`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` supplier hits `0`, current Service `queueSnapshot()` literal hits `2`, `MainActivityBase.playbackQueueSnapshot()` call sites `2`, and direct `MainActivityBase` `playbackService.queueSnapshot()` occurrences `1`.
- 2026-07-05 service startup wiring slice: `EchoPlaybackService` now constructs `PlaybackQueueManager` before `PlaybackNotificationStateOwner` and `PlaybackLyricsStateOwner`, and those two owners now require a non-null queue manager instead of carrying local null-to-empty fallback helpers. This removes two startup-time by-value null captures in notification/lyrics wiring without adding a new facade or owner. Focused `PlaybackLyricsStateOwnerTest`, `PlaybackNotificationStateOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, focused mojibake scan, `:app:assembleDebug`, MuMu install, and launch smoke passed; post-launch `logcat` filtering showed no `FATAL EXCEPTION` / `AndroidRuntime` hits on device `127.0.0.1:16480`.
- 2026-07-05 error-recovery owner nullability slice: `PlaybackErrorRecoveryCommandOwner` now requires a non-null `PlaybackQueueManager` and reads `queueStateSnapshot()` directly instead of converting a missing manager into `QueueStateSnapshot.empty()`. `EchoPlaybackService` already constructs this owner after queue-manager initialization, so the slice removes a migration-era null contract without widening lifecycle wiring. Focused `PlaybackErrorRecoveryCommandOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 session-command owner nullability slice: `PlaybackSessionCommandOwner` now requires a non-null `PlaybackQueueManager` and reads `queueStateSnapshot()` directly instead of converting a missing manager into `QueueStateSnapshot.empty()`. The owner is already constructed as an `onCreate()` local after queue-manager initialization, so this is another pure nullability cleanup rather than a runtime wiring change. Focused `PlaybackSessionCommandOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 queue-restore owner nullability slice: `PlaybackQueueRestoreOwner` now requires a non-null `PlaybackQueueManager`, calls `restorePlaybackQueue()` directly, and reads `queueStateSnapshot()` directly instead of converting a missing manager into `QueueStateSnapshot.empty()`. The service still creates the owner lazily through `playbackQueueRestoreOwner()`, but every production call site already passes the real queue manager, so this is another migration-residue cleanup. Focused `PlaybackQueueRestoreOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 playback-state snapshot owner nullability slice: `PlaybackStateSnapshotOwner` now requires a non-null `PlaybackQueueManager` and reads `queueStateSnapshot()` directly instead of converting a missing manager into `QueueStateSnapshot.empty()`. The service already constructs this owner after queue-manager initialization, so the slice removes another migration-only fake empty-state path without changing the higher-level service snapshot fallback. Focused `PlaybackStateSnapshotOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 current-track preparation queue owner nullability slice: `PlaybackCurrentTrackPreparationQueueOwner` now requires a non-null `PlaybackQueueManager` and reads `queueSnapshot()` / `queueStateSnapshot()` directly instead of converting a missing manager into empty queue state. The service already constructs this owner after queue-manager initialization, so this is another direct-construction residue cleanup that leaves media-source resolution in `PlaybackMediaSourceProvider`. Focused `PlaybackCurrentTrackPreparationQueueOwnerTest`, `PlaybackQueueManagerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 crossfade-state owner nullability slice: `PlaybackCrossfadeStateOwner` now requires a non-null `PlaybackQueueManager` and reads `queueStateSnapshot()` directly instead of converting a missing manager into `QueueStateSnapshot.empty()`. The service already constructs this owner after queue-manager initialization, so this is another direct-construction residue cleanup that preserves existing crossfade policy semantics. Focused `PlaybackCrossfadeStateOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 play-history action nullability slice: `PlaybackPlayHistoryRecorder.recordIfPlaybackStartedAction(...)` now still allows `recorder == null` to return a no-op, but once a recorder exists it requires a non-null `PlaybackQueueManager` and reads `queueStateSnapshot()` directly instead of converting a missing manager into a null current track. The service already wires this action after queue-manager initialization, so this removes another direct queue-state residue without changing history semantics. Focused `PlaybackPlayHistoryRecorderTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 favorite-command nullability slice: `PlaybackFavoriteCommandOwner.toggleCurrentFavorite(...)` now requires a non-null `PlaybackQueueManager` and reads `queueStateSnapshot()` through `Objects.requireNonNull(...)` instead of treating a missing manager as a valid no-op current-track path. The service already calls this owner only after queue-manager initialization, so this removes another direct queue-state residue without changing favorite persistence semantics. Focused `PlaybackFavoriteCommandOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 queue-command owner nullability slice: `PlaybackQueueCommandOwner.bindPlaybackQueueManager(...)` now requires a non-null `PlaybackQueueManager`, and current-track reads now use `Objects.requireNonNull(...).queueStateSnapshot()` instead of converting a missing binding into `QueueStateSnapshot.empty()`. Production service wiring already binds the owner immediately after queue-manager construction, so this removes another fake missing-manager runtime mode without widening queue-command behavior. Focused `PlaybackQueueCommandOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 mirrored-player owner nullability slice: `PlaybackQueueMirroredPlayerOwner.bindPlaybackQueueManager(...)` now requires a non-null `PlaybackQueueManager`, and both mirrored queue reads now use `Objects.requireNonNull(...)` instead of converting a missing binding into `Collections.emptyList()` or `QueueStateSnapshot.empty()`. Production service wiring already binds this owner immediately after queue-manager construction, so this removes another fake missing-manager runtime mode without changing mirrored queue reuse strategy. Focused `PlaybackQueueMirroredPlayerOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 mirrored-transition owner nullability slice: `PlaybackQueueMirroredTransitionOwner` now requires a non-null `PlaybackQueueManager` in its constructor and reads `queueStateSnapshot()` directly instead of converting a missing manager into `QueueStateSnapshot.empty()` or treating `null` as a valid no-op transition mode. Production service wiring already constructs this owner with the real queue manager in the Media3 transition callback path, so this removes another fake missing-manager runtime mode without changing mirrored transition semantics. Focused `PlaybackQueueMirroredTransitionOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 queue-mutation owner nullability slice: `PlaybackQueueMutationOwner` now requires a non-null `PlaybackQueueManager` in its constructor and reads `queueStateSnapshot()` directly instead of converting a missing manager into `QueueStateSnapshot.empty()`. Production service wiring already creates this owner with the real queue manager in `EchoPlaybackService.playbackQueueMutationOwner()`, so this removes another fake missing-manager runtime mode without changing queue mutation behavior. Focused `PlaybackQueueMutationOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 current-track replacement owner nullability slice: `PlaybackCurrentTrackReplacementOwner` now requires a non-null `PlaybackQueueManager` in its constructor instead of treating a missing manager as a silent no-op in `replaceCurrentTrackAndResume(...)`. Production service wiring already constructs this owner inline with the real queue manager in `EchoPlaybackService.replaceCurrentTrackAndResume(...)`, so this removes another fake missing-manager runtime mode without changing replacement/recovery behavior. Focused `PlaybackCurrentTrackReplacementOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 queue-completion owner nullability slice: `PlaybackQueueCompletionOwner` now requires a non-null `PlaybackQueueManager` in its constructor instead of treating a missing manager as implicit `STOP_AND_CLEAR` / boundary-only behavior. Production service wiring already constructs this owner with the real queue manager in `EchoPlaybackService.playbackQueueCompletionOwner()`, so this removes another fake missing-manager runtime mode without changing completion behavior. Focused `PlaybackQueueCompletionOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 app-shell queue refresh slice: `MainActivityBase.playbackQueueSnapshot()` now falls back to `playbackStore.queueSnapshot()` when `playbackService` is absent, and `publishPlaybackStore()` now publishes only when both store and service exist. This keeps one direct host `playbackService.queueSnapshot()` read, reuses the existing `MainPlaybackStore` queue snapshot when the service reference is temporarily absent, and leaves disconnect clearing solely to `playbackStore.reset()`. Focused `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 app-shell initialization contract slice: `MainActivityArchitectureContractTest` now locks `initializeRouteStoresAndStatus()` ahead of `initializePlatformControllers()`, `initializeNavigationRendering()`, and `initializePlaybackControllers()`, plus the local host-field creation order `uiShellController -> routeController -> playbackStore -> statusMessageController -> trackShareLauncher`. It also guards `initializePlaybackLifecycleControllers()` to keep disconnect clearing on `() -> playbackStore.reset()` instead of recreating or nulling the store through a hidden lambda. Focused `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 queue manager API contract slice: `MainActivityArchitectureContractTest` now snapshots the full public `PlaybackQueueManager` surface and requires it to stay at the current semantic-command-plus-snapshots set. This blocks future re-expansion into derived public reads such as `currentTrack`, `currentIndex`, `hasQueue`, `persistQueueState`, `restoreLastPlayback`, or `queuePreparationForNewPlayer` while keeping source-of-truth in `QueueStateSnapshot` / `queueSnapshot()`. Focused `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 completion API slice: `PlaybackQueueManager.prepareStopAtEndOfQueue()` is now private, and stop-at-end cleanup runs inside `preparePlaybackCompletionAction()` when the manager returns `STOP_AT_END`. `PlaybackQueueCompletionOwner` now maps that semantic action directly to the service boundary runnable instead of calling a second public prep helper. Focused `PlaybackQueueManagerTest`, `PlaybackQueueCompletionOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 mirrored-transition completion API slice: `PlaybackQueueManager.prepareStopAfterAutomaticAdvance(...)` is now private, and automatic-advance stop preparation runs inside `applyMirroredTransitionIndex(...)` when mirrored transition handling decides to stop at queue end. `PlaybackQueueMirroredTransitionOwner` no longer exports `completedIndex`, `EchoPlaybackService` no longer forwards it, and `PlaybackQueueCompletionOwner.stopAfterAutomaticAdvance()` is now boundary-only. Focused `PlaybackQueueManagerTest`, `PlaybackQueueCompletionOwnerTest`, `PlaybackQueueMirroredTransitionOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 mirrored-transition boundary cleanup slice: `PlaybackQueueCompletionOwner.stopAfterAutomaticAdvance()` is now removed as a forwarding-only wrapper. `EchoPlaybackService` calls `stopAtEndOfQueueBoundary()` directly after `PlaybackQueueMirroredTransitionOwner` returns `stopAfterAutomaticAdvance() == true`, keeping queue/transition semantics in the existing owners while deleting one extra app-layer hop. Focused `PlaybackQueueCompletionOwnerTest`, `PlaybackQueueMirroredTransitionOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 completion-cluster audit: after the three completion slices, `PlaybackQueueManager` public API is down to `17` methods, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` / `playbackQueueStateOwner` supplier hits remain `0`, strict `private Playback*` Service field lines remain `36`, and app playback `Playback*Owner.java` files remain `39`. Do not keep cutting completion APIs mechanically; the next higher-signal target is `EchoPlaybackService` wiring/supplier reduction unless a new queue cut removes a real boundary rather than moving queue strategy back into the service.
- 2026-07-05 current-track preparation wiring slice: `EchoPlaybackService` no longer stores `PlaybackCurrentTrackPreparationOwner` / `PlaybackCurrentTrackPreparationQueueOwner` as fields. Their two call sites now build them through private helpers when `prepareCurrent(...)` / `prepareMirroredQueue(...)` need them, using call-time-safe metadata lambdas around `playbackNotificationManager`. This removes two low-usage service wiring fields without changing queue, restored-position, or media-source ownership. Focused `PlaybackCurrentTrackPreparationOwnerTest`, `PlaybackCurrentTrackPreparationQueueOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 snapshot/diagnostics wiring slice: `EchoPlaybackService` no longer stores `PlaybackStateSnapshotOwner` / `PlaybackStreamingDiagnosticsRecorderOwner` as fields. `snapshot()`, buffering publication, and replacement recovery now build those wrappers through private helpers from the same queue/runtime/visualization/diagnostics owners, which removes two more low-usage service fields without changing snapshot or diagnostics source-of-truth. Focused `PlaybackStateSnapshotOwnerTest`, `PlaybackCurrentTrackReplacementOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 realtime visualization wiring slice: `EchoPlaybackService` no longer stores `PlaybackRealtimeVisualizationOwner` as a field. `realtimeBeat()`, `realtimeBands()`, and the state-snapshot helper now build it through a private helper from `playbackPlayerStateOwner` plus `realtimeBassDetector`, removing one more low-usage service wiring field without changing realtime source-of-truth. Focused `PlaybackRealtimeVisualizationOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 diagnostics adapter deletion slice: `PlaybackStreamingDiagnosticsRecorderOwner` is removed entirely. `EchoPlaybackService` now records buffering and recovery directly through `PlaybackStreamingDiagnostics` with `PlaybackMediaSourceProvider` quality lookup lambdas, deleting one forwarding-only owner file while preserving the same diagnostics source-of-truth. Focused `PlaybackStatePublisherTest`, `PlaybackCurrentTrackReplacementOwnerTest`, `PlaybackStreamingDiagnosticsTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed.
- 2026-07-05 queue-navigation owner nullability slice: `PlaybackQueueNavigationOwner` now requires a non-null `PlaybackQueueManager` and directly delegates queue navigation/reuse calls instead of accepting a missing manager as silent no-op behavior. Production service wiring already creates the owner after queue-manager initialization, so this removes another fake migration state path without changing queue source-of-truth or mirrored-queue reuse semantics. Focused `PlaybackQueueNavigationOwnerTest`, `MainActivityArchitectureContractTest`, and app Kotlin/Java compile tasks passed; current metrics remain `EchoPlaybackService.java` `1408` lines, strict `private Playback*` Service field lines `31`, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` supplier hits `0`, current Service `queueSnapshot()` literal hits `2`, and navigation-owner missing-manager null checks `0`.
- 2026-07-05 precache manager nullability slice: `PlaybackPrecacheManager` now requires a non-null `PlaybackQueueManager` and reads queue state directly for current/upcoming precache policy instead of carrying missing-manager empty fallbacks. This keeps cache policy in the cache owner and queue source-of-truth in `PlaybackQueueManager` while deleting another fake migration runtime mode. Focused `PlaybackPrecacheManagerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile tasks, and focused mojibake scan passed; current metrics remain `EchoPlaybackService.java` `1408` lines, strict `private Playback*` Service field lines `31`, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` supplier hits `0`, current Service `queueSnapshot()` literal hits `2`, and `PlaybackPrecacheManager` missing-manager fallback hits `0`.
- 2026-07-05 feature playback state-provider nullability slice: `PlaybackPositionManager.PlaybackStateProvider.bindPlaybackQueueManager(...)` and `PlaybackRuntimeStateManager.PlaybackStateProvider.bindPlaybackQueueManager(...)` now take non-null `PlaybackQueueManager`. The internal reference remains nullable only for the legitimate pre-bind startup phase, so current-track reads still return `null` before binding and then read `PlaybackQueueManager.queueStateSnapshot()` after binding. This removes two cross Java/Kotlin nullable binding entry points without adding a facade, owner, or state mirror. Focused `PlaybackPositionManagerTest`, `PlaybackRuntimeStateManagerTest`, `MainActivityArchitectureContractTest`, feature/app Kotlin-Java compile tasks, and focused mojibake scan passed; current metrics remain `EchoPlaybackService.java` `1408` lines, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` supplier hits `0`, current Service `queueSnapshot()` literal hits `2`, and nullable queue-manager binding signatures are guarded at `0`.
- 2026-07-05 precache runtime-state source slice: `PlaybackPrecacheManager` now requires `PlaybackRuntimeStateManager` in its constructors and `fromMediaSourceProvider(...)` factory, removing the old null fallback that treated a missing runtime owner as repeat-all behavior. Queue state still comes from `PlaybackQueueManager`, repeat mode still comes from `PlaybackRuntimeStateManager`, and URI/cache-key rules still belong to `PlaybackMediaSourceProvider` / `PlaybackMediaCacheOperations`. Focused `PlaybackPrecacheManagerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile tasks, and focused mojibake scan passed; current metrics remain `EchoPlaybackService.java` `1408` lines, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` supplier hits `0`, current Service `queueSnapshot()` literal hits `2`, and precache runtime-null fallback hits `0`.
- 2026-07-05 service queue-manager post-init guard slice: `EchoPlaybackService` now calls `playbackQueueManager.restorePlaybackQueue()` and `playbackQueueManager.replaceCurrentQueueTrack(track)` directly on paths that only run after queue-manager construction. The public `queueSnapshot()` / `snapshot()` defensive null guards remain in place for app-shell reads. Focused `PlaybackQueueRestoreOwnerTest`, `PlaybackCurrentTrackPreparationOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile tasks, and focused mojibake scan passed; current metrics are `EchoPlaybackService.java` `1402` lines, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` supplier hits `0`, current Service `queueSnapshot()` literal hits `2`, and direct service `playbackQueueManager == null` guards are limited to the two public snapshot paths.
- 2026-07-05 app-shell gateway binding contract slice: `MainActivityArchitectureContractTest` now guards the full `onCreate()` shell phase order from route/status/store creation through now-playing/download/library gateway binding and then platform/playback/navigation controllers. The same test now blocks late host playback/service/store/controller fields from being passed by value into now-playing/library gateway construction. No source-of-truth changed: route/navigation still belong to `MainRouteController` / `NavigationViewModel`, playback snapshots still flow through `MainPlaybackStore` / service reads, and settings still flow through `settingsStore` providers. Focused `MainActivityArchitectureContractTest` and focused mojibake scan passed.
- 2026-07-05 media-cache interface audit slice: `MainActivityArchitectureContractTest` now snapshots the full `PlaybackMediaCacheOperations` method surface, keeping it to cache/reuse operations only. URI / MediaItem / playback preparation remain in `PlaybackMediaSourceProvider`, and cache policy remains in `PlaybackPrecacheManager`; the cache operations adapter is explicitly blocked from becoming a resolution/cache facade. Focused `MainActivityArchitectureContractTest` and focused mojibake scan passed; current metrics are `EchoPlaybackService.java` `1304` lines, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` supplier hits `0`, and current Service `queueSnapshot()` literal hits `2`.
- 2026-07-05 playback service port contract slice: `MainActivityArchitectureContractTest` now locks the Kotlin method sets for `PlaybackServiceHostPort`, `NowPlayingPlaybackServicePort`, and `SettingsPlaybackServicePort`. This keeps the host port to listener/app-visibility/realtime/queue-snapshot boundary reads and blocks the now-playing/settings service ports from quietly becoming broader `EchoPlaybackService` or Android `Context` facades. Source-of-truth remains unchanged; focused `MainActivityArchitectureContractTest` and focused mojibake scan passed. Current metrics are `EchoPlaybackService.java` `1304` lines, strict `private Playback*` Service field lines `31`, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` supplier hits `0`, and current Service `queueSnapshot()` literal hits `2`.
- 2026-07-05 now-playing queue-presence interface slice: `NowPlayingPlaybackServicePort.hasQueue()` and `EchoPlaybackService.hasQueue()` are removed. `NowPlayingViewModel.hasQueue()` derives the same UI semantic from `PlaybackStateSnapshot.queueSize`, whose source-of-truth remains `PlaybackQueueManager.QueueStateSnapshot`. This removes one service-port method, one gateway method, and one concrete Service public method without exposing `queueSnapshot()` to the now-playing UI path. Focused `NowPlayingViewModelTest`, `NowPlayingPlaybackGatewayAdapterTest`, `QueueActionControllerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile tasks, and focused mojibake scan passed. Current metrics are `EchoPlaybackService.java` `1301` lines, strict `private Playback*` Service field lines `31`, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` supplier hits `0`, current Service `queueSnapshot()` literal hits `2`, and concrete Service `hasQueue()` hits `0`.
- 2026-07-05 now-playing connection interface slice: `NowPlayingPlaybackGateway.serviceConnected()` is removed. `NowPlayingViewModel` now uses `player.snapshot() != null` as the connection boundary, matching `NowPlayingPlaybackGatewayAdapter.snapshot()` returning `null` when no service is bound. This removes one ViewModel gateway method and one adapter override without adding state, mirrors, or facades. Focused `NowPlayingViewModelTest`, `NowPlayingPlaybackGatewayAdapterTest`, `QueueActionControllerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile tasks, and focused mojibake scan passed. Current metrics remain `EchoPlaybackService.java` `1301` lines, strict `private Playback*` Service field lines `31`, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` supplier hits `0`, current Service `queueSnapshot()` literal hits `2`, and production `serviceConnected()` hits `0`.
- 2026-07-05 now-playing gateway contract slice: `MainActivityArchitectureContractTest` now snapshots the full `NowPlayingPlaybackGateway` method surface, keeping it to semantic playback commands plus `PlaybackStateSnapshot`. This blocks reintroducing broad service state reads such as `hasQueue`, `serviceConnected`, or `queueSnapshot` into the ViewModel gateway without an explicit reviewed contract change. Focused `MainActivityArchitectureContractTest` and focused mojibake scan passed.
- 2026-07-05 precache runtime-source guard test slice: `PlaybackPrecacheManagerTest` now explicitly verifies that `PlaybackPrecacheManager` rejects a missing `PlaybackRuntimeStateManager`, matching the required queue-manager guard. This keeps repeat-mode source-of-truth in `PlaybackRuntimeStateManager` and protects the earlier removal of the implicit repeat-all null fallback. Focused `PlaybackPrecacheManagerTest`, `MainActivityArchitectureContractTest`, and focused mojibake scan passed.
- 2026-07-05 MuMu startup smoke: `:app:assembleDebug` passed, the debug APK installed successfully on MuMu `127.0.0.1:7555`, and `adb shell monkey -p app.yukine 1` launched `app.yukine/.MainActivity` without `FATAL EXCEPTION`, process-death, or force-finish lines in filtered logcat. Treat this as startup/app-shell wiring evidence only; notification, lyrics, queue restore, streaming playback, and background playback still need their own targeted smoke before high-risk runtime slices.
- 2026-07-05 playback service-host settings snapshot slice: `MainPlaybackServiceHostFactory` / `PlaybackServiceHostController.Host` now expose one `PlaybackServiceConnectionSettings` source instead of six separate connection-setting suppliers. `MainActivityBase` still reads the same `settingsStore` source-of-truth and preserves the previous null-safe defaults, but the Activity-to-host wiring surface and Host interface are narrower. Focused `MainPlaybackServiceHostTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile tasks, and focused mojibake scan passed.
- 2026-07-05 playback service-host method-set contract slice: `MainActivityArchitectureContractTest` now snapshots `PlaybackServiceHostController.Host` through `kotlinInterfaceFunNames(...)`, locking it to the current settings snapshot, attach/detach lifecycle, pending playback, and chrome-render callbacks. This is a boundary-test slice only: no source-of-truth changes, no new owner, and no service behavior change. Focused `MainActivityArchitectureContractTest` and focused mojibake scan passed.
- 2026-07-05 playback service-host chrome callback slice: the service-host chrome refresh path now uses one `renderPlaybackChrome()` callback and one `PlaybackChromeRenderer` factory argument instead of separate selected-tab and now-bar callbacks. This narrows the app-shell lifecycle wiring surface while keeping selected-tab/now-bar state in their existing render owners. Focused `MainPlaybackServiceHostTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile tasks, and focused mojibake scan passed.
- 2026-07-05 playback service-host detach callback slice: the service-host disconnect path now uses one `detachPlaybackService()` callback and one `PlaybackServiceDetacher` factory argument instead of separate service-clear and playback-store-reset callbacks. The same Activity helper still clears the bound service reference and resets `MainPlaybackStore`; this only narrows the lifecycle wiring surface. Focused `MainPlaybackServiceHostTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile tasks, and focused mojibake scan passed.
- 2026-07-05 playback service-host connect callback slice: `PlaybackServiceHostController.Host` is now locked to `attachPlaybackService(...)`, `detachPlaybackService()`, and `renderPlaybackChrome()`. `MainPlaybackServiceHost.attachPlaybackService(...)` applies the connection settings snapshot and pending playback replay, so the controller no longer exposes separate settings or pending-playback Host methods. Source-of-truth is unchanged, and focused `MainPlaybackServiceHostTest`, `MainActivityArchitectureContractTest`, and focused mojibake scan passed.
- 2026-07-05 queue-manager interface audit slice: `MainActivityArchitectureContractTest` now classifies the `PlaybackQueueManager` public API by responsibility and blocks derived reads / historical migration residue from re-entering the public surface. This is an audit/contract slice only; queue source-of-truth and runtime behavior are unchanged. Focused `MainActivityArchitectureContractTest` and focused mojibake scan passed.
- 2026-07-05 resolver/cache boundary audit slice: `MainActivityArchitectureContractTest` now prevents `PlaybackPrecacheManager` from drifting into a media-source/cache facade. URI, MediaItem, cache-key, headers, and cache data-source resolution stay with `PlaybackMediaSourceProvider` / `PlaybackMediaCacheOperations`, while cache scheduling policy stays in `PlaybackPrecacheManager`. Focused `MainActivityArchitectureContractTest` and focused mojibake scan passed.
- 2026-07-05 current-track preparation runtime wiring slice: `EchoPlaybackService` no longer stores `PlaybackCurrentTrackPreparationRuntimeOwner` as a field. The Service builds the lightweight adapter through `playbackCurrentTrackPreparationRuntimeOwner()` at call sites, keeping preparing/error-message source-of-truth in `PlaybackRuntimeStateManager` and preserving the owner boundary without adding a facade or state mirror. Focused `PlaybackCurrentTrackPreparationRuntimeOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed; current metrics are `EchoPlaybackService.java` `1366` lines, `MainActivityBase.java` `2685` lines, broad `private Playback*` Service fields `35`, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` hits `0`, current Service `queueSnapshot()` hits `2`, and private-final current-track preparation runtime owner field hits `0`.
- 2026-07-05 queue snapshot end-of-queue API slice: `PlaybackQueueManager.QueueStateSnapshot` no longer exposes `isAtEndOfQueue`; completion and crossfade policy derive that semantic from `currentIndex` and `queueSize` at their decision sites. Queue source-of-truth remains `PlaybackQueueManager`, no Service queue-state supplier or queue provider was added, and `isQueueEmpty` remains in place for notification/restore/mutation paths. Focused `PlaybackQueueManagerTest`, `PlaybackCrossfadeStateOwnerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile, and focused mojibake scan passed; current metrics are `EchoPlaybackService.java` `1366` lines, `MainActivityBase.java` `2685` lines, broad `private Playback*` Service fields `35`, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` hits `0`, current Service `queueSnapshot()` hits `2`, and production `QueueStateSnapshot.isAtEndOfQueue` hits `0`.
- 2026-07-05 network render initialization contract slice: `MainActivityArchitectureContractTest` now keeps `initializeNetworkRendering(...)` inside `initializeNetworkOwners(...)` after the network request/dialog/settings-context owners are created, and prevents moving it directly into `onCreate()`. This makes the by-value host-field dependency explicit without adding a new owner or state mirror. Focused `MainActivityArchitectureContractTest` and focused mojibake scan passed.
- 2026-07-05 playback mode runtime-state contract slice: `PlaybackModeSettingsStore` no longer accepts a missing `PlaybackRuntimeStateManager` as a valid fallback state for shuffle/repeat mode. The store now requires the runtime state owner for restore, mutation, application, and repeat reads, keeping runtime mode source-of-truth in `PlaybackRuntimeStateManager` and repository persistence in `ModeSettings`. Focused `PlaybackModeSettingsStoreTest`, `MainActivityArchitectureContractTest`, and focused mojibake scan passed.
- 2026-07-05 playback runtime settings contract slice: `PlaybackRuntimeSettingsStore` no longer accepts a missing `PlaybackRuntimeStateManager` as a valid fallback state for speed, app volume, replay gain, concurrent playback, current-track volume, playback parameter application, or audio-focus handling. The store now requires the runtime state owner and leaves only the outer Service startup fallback in `EchoPlaybackService`. Focused `PlaybackRuntimeSettingsStoreTest`, `MainActivityArchitectureContractTest`, and focused mojibake scan passed; runtime-state mini-audit after the two settings slices: `EchoPlaybackService.java` `1398` lines, strict `private Playback*` fields `41`, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` hits `0`, current Service `queueSnapshot()` hits `2`, and mode/runtime settings-store `runtimeStateManager == null` hits `0`.
- 2026-07-05 playback settings Service null-guard cleanup slice: `EchoPlaybackService` no longer wraps mode/runtime settings-store reads, writes, apply calls, or crossfade state suppliers in Service-level store-null strategy branches. Settings source-of-truth remains `PlaybackRuntimeStateManager` plus the two settings stores; the Service only wires the already-initialized owners. Focused `PlaybackModeSettingsStoreTest`, `PlaybackRuntimeSettingsStoreTest`, `PlaybackCrossfadeStateOwnerTest`, `MainActivityArchitectureContractTest`, and focused mojibake scan passed. Current metrics: `EchoPlaybackService.java` `1365` lines, strict `private Playback*` fields `41`, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` hits `0`, current Service `queueSnapshot()` hits `2`, and settings-store null-guard hits in `EchoPlaybackService` are all `0`.
- 2026-07-05 late-bound queue-state provider slice: `EchoPlaybackService.onCreate()` now creates the initial player after binding `PlaybackQueueManager` into the runtime and position playback-state providers. `PlaybackRuntimeStateManager.PlaybackStateProvider.currentTrack()` and `PlaybackPositionManager.PlaybackStateProvider.currentTrack()` now fail before binding instead of silently returning null, while empty bound queues still return null. Current-track source-of-truth remains `PlaybackQueueManager.QueueStateSnapshot`; no owner or facade was added. Focused `PlaybackPositionManagerTest`, `PlaybackRuntimeStateManagerTest`, `MainActivityArchitectureContractTest`, and focused mojibake scan passed. Current metrics: `EchoPlaybackService.java` `1365` lines, strict `private Playback*` fields `39`, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` hits `0`, current Service `queueSnapshot()` hits `2`, and production nullable `playbackQueueManager?.queueStateSnapshot()` hits `0`.
- 2026-07-05 media-cache provider boundary slice: `PlaybackMediaCacheOperations.fromMediaSourceProvider(...)` now requires a non-null `PlaybackMediaSourceProvider`, and `PlaybackMediaSourceProviderCacheOperations` no longer carries provider-null no-op branches. Cache keys and cache data sources still delegate to `PlaybackMediaSourceProvider`; cache scheduling still belongs to `PlaybackPrecacheManager`; no resolution/cache facade was added. Focused `PlaybackMediaCacheOperationsTest`, `MainActivityArchitectureContractTest`, and focused mojibake scan passed. Current metrics remain `EchoPlaybackService.java` `1365` lines, strict `private Playback*` fields `39`, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` hits `0`, current Service `queueSnapshot()` hits `2`, and media-cache provider-null branch hits `0`.
- 2026-07-05 queue snapshot empty API slice: `PlaybackQueueManager.QueueStateSnapshot` no longer exposes `isQueueEmpty`; notification state, restore, mutation, mirrored transition, and completion checks derive empty/non-empty semantics from `queueSize` at their owning decision sites. Queue source-of-truth remains `PlaybackQueueManager`, no owner/facade/state mirror/Service strategy branch was added, and `PlaybackNotificationManager.StateProvider.isQueueEmpty()` remains only as the notification semantic API. Focused `PlaybackQueueManagerTest`, `PlaybackQueueMutationOwnerTest`, `PlaybackQueueRestoreOwnerTest`, `PlaybackNotificationStateOwnerTest`, `PlaybackQueueCompletionOwnerTest`, `PlaybackQueueMirroredTransitionOwnerTest`, `MainActivityArchitectureContractTest`, and focused mojibake scan passed; current metrics are `EchoPlaybackService.java` `1366` lines, broad `private Playback*` Service fields `35`, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` hits `0`, production `QueueStateSnapshot.isQueueEmpty` usage hits `0`, and production `QueueStateSnapshot.isAtEndOfQueue` hits `0`.
- 2026-07-05 startup queue restore owner slice: `EchoPlaybackService.onCreate()` now routes startup queue restore through `PlaybackQueueRestoreOwner.restoreQueueForStartup()` instead of calling `playbackQueueManager.restorePlaybackQueue()` directly. Queue source-of-truth remains `PlaybackQueueManager`, persisted queue/restore settings still come from `PlaybackQueueStore`, and no new owner/facade/state mirror was added. Focused `PlaybackQueueRestoreOwnerTest` and `MainActivityArchitectureContractTest` passed; the run also recompiled app Kotlin/Java.
- 2026-07-05 core dependency boundary contract slice: `MainActivityArchitectureContractTest` now prevents `core:model` and `core:common` main source and Gradle files from depending on app shell, feature implementations, playback manager implementations, concrete playback service classes, or app/feature modules. This is a dependency-direction guard only; focused `MainActivityArchitectureContractTest` passed.
- 2026-07-05 precache cache-owner boundary slice: `PlaybackPrecacheManager` now requires non-null `PlaybackMediaCacheOperations`, and its provider factory requires non-null `PlaybackMediaSourceProvider`. This removes missing-cache-owner and missing-media-source-owner fallback branches while keeping URI/MediaItem resolution in `PlaybackMediaSourceProvider`, cache operations in `PlaybackMediaCacheOperations`, and scheduling policy in `PlaybackPrecacheManager`. Focused `PlaybackPrecacheManagerTest`, `MainActivityArchitectureContractTest`, and app Kotlin/Java compile tasks passed. Current metrics remain `EchoPlaybackService.java` `1365` lines, strict `private Playback*` fields `39`, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` hits `0`, current Service `queueSnapshot()` hits `2`, and precache cache/media-source null branch hits `0`.
- 2026-07-05 visualization cache-owner boundary slice: `PlaybackVisualizationCacheManager` now requires non-null `PlaybackMediaCacheOperations`, and its provider factory requires non-null `PlaybackMediaSourceProvider`. This removes the visualization-cache missing-owner no-op mode while keeping URI/MediaItem/cache-key resolution in `PlaybackMediaSourceProvider` / `PlaybackMediaCacheOperations`; the visualization manager only owns cache-window scheduling and cancellation. Focused `PlaybackVisualizationCacheManagerTest` and `MainActivityArchitectureContractTest` passed.
- 2026-07-05 current-track preparation resolver boundary slice: `PlaybackCurrentTrackPreparationOwner.fromMediaSourceProvider(...)` now requires non-null `PlaybackMediaSourceProvider` instead of installing null-returning preparation/media-source lambdas. This keeps preparation and MediaSource creation in the existing media-source owner and removes one nullable Java/Kotlin resolver boundary.
- 2026-07-05 current-track queue preparation resolver boundary slice: `PlaybackCurrentTrackPreparationQueueOwner` now requires non-null `PlaybackMediaSourceProvider` on the production media-source constructor path instead of installing null-returning queue-restore/media-sources lambdas. Queue source-of-truth stays in `PlaybackQueueManager`; mirrored source creation stays in `PlaybackMediaSourceProvider`.
- 2026-07-05 queue streaming restore resolver boundary slice: `PlaybackQueueStreamingRestoreOwner` now requires non-null `PlaybackMediaSourceProvider` on the production constructor path instead of installing null-returning restored-track/header actions. Queue restore still flows through `PlaybackQueueManager.StreamingRestoreProvider`.
- 2026-07-05 mirrored queue matcher resolver boundary slice: `PlaybackMirroredQueueTrackMatcherOwner` now requires non-null `PlaybackMediaSourceProvider` on the production constructor path instead of treating a missing media-source owner as "no mirrored match." MediaItem/track identity matching remains in `PlaybackMediaSourceProvider`.
- 2026-07-05 current-track preparation runtime boundary slice: `PlaybackCurrentTrackPreparationRuntimeOwner.fromRuntimeStateManager(...)` now requires non-null `PlaybackRuntimeStateManager` instead of installing no-op runtime-state setters and a false preparing supplier. Preparing/error-message source-of-truth remains `PlaybackRuntimeStateManager`; the preparation owner only bridges current-track preparation events into that runtime state.
- 2026-07-05 playback snapshot runtime provider boundary slice: `PlaybackStateSnapshotOwner.fromRuntimeStateManager(...)` now requires non-null `PlaybackRuntimeStateManager` instead of converting a missing runtime owner into default snapshot values. Snapshot runtime state source-of-truth remains `PlaybackRuntimeStateManager`; direct constructor-level runtime-provider defaults are left as a separate, explicitly scoped path.
- 2026-07-05 playback snapshot owner runtime-provider constructor slice: `PlaybackStateSnapshotOwner` now requires non-null `RuntimeStateProvider` and no longer takes a `defaultRepeatMode` constructor argument. Snapshot runtime fields come from `PlaybackRuntimeStateManager` through the provider path; missing runtime state is no longer a valid constructor mode.
- 2026-07-05 playback snapshot visualization-provider constructor slice: `PlaybackStateSnapshotOwner` now requires non-null `VisualizationProvider`, and `fromVisualizationAnalyzer(...)` requires non-null `PlaybackVisualizationAnalyzer`. Waveform/spectrum source-of-truth remains `PlaybackVisualizationAnalyzer`; missing visualization ownership is no longer represented as empty snapshots.
- 2026-07-05 queue state snapshot derived-API slice: `PlaybackQueueManager.QueueStateSnapshot.hasCurrentTrack` is removed, and the only production use now derives from `currentTrack == null` in `PlaybackQueueRestoreOwner`. Queue source-of-truth remains `PlaybackQueueManager.queueStateSnapshot()` with `currentTrack`, `currentIndex`, and `queueSize`; no queue provider, facade, or Service state mirror was added. Focused `PlaybackQueueManagerTest`, `PlaybackQueueRestoreOwnerTest`, `MainActivityArchitectureContractTest`, and focused mojibake scan passed.
- 2026-07-05 queue state snapshot multi-track derived-API slice: `PlaybackQueueManager.QueueStateSnapshot.hasMultipleTracks` is removed. Crossfade and error-recovery policies now derive from `queueSize`, keeping queue source state in `currentTrack`, `currentIndex`, and `queueSize` without adding a queue provider, facade, or Service mirror. Focused `PlaybackQueueManagerTest`, `PlaybackCrossfadeStateOwnerTest`, `PlaybackErrorRecoveryCommandOwnerTest`, and `MainActivityArchitectureContractTest` passed.
- 2026-07-05 queue/resolver/cache audit: `QueueProvider`, `hasCurrentTrack`, and `hasMultipleTracks` remain absent; `EchoPlaybackService` still has no direct `queueStateSnapshot()` reads; `PlaybackPrecacheManager` still delegates cache key/range behavior through `PlaybackMediaCacheOperations` and does not hold `PlaybackMediaSourceProvider`; `PlaybackMediaCacheOperations` still has no MediaItem/source creation surface. No new owner, facade, state mirror, or Service branch was added.
- 2026-07-05 queue mutation / precache mini-audit: after requiring `PlaybackQueueMutationOwner` to receive a non-null stop/clear boundary action and centralizing `PlaybackPrecacheManager` queue-state reads behind one private helper, source-of-truth is unchanged. Queue remains `PlaybackQueueManager`, cache scheduling remains `PlaybackPrecacheManager`, cache operations remain `PlaybackMediaCacheOperations`, and URI/MediaItem resolution remains `PlaybackMediaSourceProvider`. Current metrics: `EchoPlaybackService.java` `1366` lines, `MainActivityBase.java` `2685` lines, broad `private Playback*` Service fields `33`, Service-held `Playback*Owner` fields `8`, app playback `Playback*Owner.java` files `38`, `PlaybackQueueManager` public methods `17`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` hits `0`, current Service `queueSnapshot()` hits `2`, `PlaybackPrecacheManager` direct `playbackQueueManager.queueStateSnapshot();` hits `1`, and chained `playbackQueueManager.queueStateSnapshot().getCurrentTrack()` hits `0`. Guarded by focused `PlaybackQueueMutationOwnerTest`, `PlaybackPrecacheManagerTest`, `MainActivityArchitectureContractTest`, app Kotlin/Java compile tasks, and focused mojibake scans.
- 2026-07-05 current-track supplier mini-audit: `PlaybackVisualizationCacheStateOwner` and `PlaybackWifiLockManager` now read current track from `PlaybackQueueManager.queueStateSnapshot()` instead of receiving the Service `currentTrackSupplier`. Current-track source-of-truth remains `PlaybackQueueManager`; visualization cache and Wi-Fi lock policy stay in their existing owners; notification artwork is intentionally deferred with P4 notification work. Current metrics: `EchoPlaybackService.java` `1366` lines, `MainActivityBase.java` `2685` lines, broad `private Playback*` Service fields `31`, Service-held `Playback*Owner` fields `8`, app playback `Playback*Owner.java` files `38`, production `fromPlaybackQueueManager(...)` hits `0`, direct Service `queueStateSnapshot()` hits `0`, Service `currentTrackSupplier` hits `2`, and current Service `queueSnapshot()` hits `2`. Guarded by focused `PlaybackVisualizationCacheStateOwnerTest`, `PlaybackWifiLockManagerTest`, `MainActivityArchitectureContractTest`, app/feature compile tasks, and focused mojibake scans.
- 2026-07-05 app-shell initialization contract slice: `MainActivityArchitectureContractTest` now explicitly rejects binding now-playing gateways, download request wiring, library gateway, or playback lifecycle wiring before `initializeRouteStoresAndStatus()`. This guards the Kotlin non-null `LibraryRouteActions` route-controller boundary and the status/playback-store host callbacks without adding an owner, facade, state mirror, or runtime behavior change.
- 2026-07-05 replacement recovery diagnostics resolver slice: `EchoPlaybackService.replaceCurrentTrackAndResume(...)` now requires the initialized `PlaybackMediaSourceProvider` for `streamingQualityForTrack(...)` instead of carrying a `mediaSourceProvider == null` fallback. Streaming quality remains owned by `PlaybackMediaSourceProvider`, replacement remains owned by `PlaybackCurrentTrackReplacementOwner` / `PlaybackQueueManager`, and diagnostics remain in `PlaybackStreamingDiagnostics`.
- 2026-07-05 service snapshot queue-manager boundary slice: `EchoPlaybackService.queueSnapshot()` and `EchoPlaybackService.snapshot()` now require the initialized `PlaybackQueueManager` instead of converting a missing manager into empty queue/snapshot state. Queue source-of-truth remains `PlaybackQueueManager`; snapshot assembly remains `PlaybackStateSnapshotOwner`; `playbackQueueManager == null` hits in the Service are now `0`.
- 2026-07-05 recovery diagnostics final-owner slice: `EchoPlaybackService.replaceCurrentTrackAndResume(...)` no longer guards the final `PlaybackStreamingDiagnostics` owner with `streamingDiagnostics == null`. Replacement diagnostics still belong to `PlaybackStreamingDiagnostics`, and the remaining null guard is only for a null recovered track.
- 2026-07-05 warmup coordinator post-init slice: `EchoPlaybackService` now calls the initialized `PlaybackWarmupCoordinator` directly from `warmPlaybackTrack(...)`, mirrored queue preparation, and single-track preparation. Shutdown/no-op behavior remains owned by `PlaybackWarmupCoordinator.release()`, while cache scheduling remains in `PlaybackPrecacheManager` and visualization cache scheduling remains in `PlaybackVisualizationCacheManager`. This removes three Service-level fake missing-owner checks without adding an owner, facade, state mirror, notification/lyrics/shutdown behavior change, or package move.
- 2026-07-05 warmup fanout cache-owner slice: `EchoPlaybackService` now wires `PlaybackWarmupCoordinator` directly to the initialized `PlaybackPrecacheManager` and `PlaybackVisualizationCacheManager` instead of carrying nullable fanout wrappers. Release/no-op behavior remains in those existing owners and the coordinator; this removes two more Service-level missing-owner strategy checks without adding a facade or changing resolver/cache ownership.
- 2026-07-05 position/progress required-owner slice: `EchoPlaybackService.persistCurrentPlaybackPosition(...)`, `startProgressUpdates()`, and `stopProgressUpdates()` now call `PlaybackPositionManager` / `PlaybackProgressUpdateManager` directly. Position source state still flows from `PlaybackQueueManager` plus `PlaybackPlayerStateOwner`, persisted position still goes through `PlaybackQueueStore`, and progress callback cancellation remains in `PlaybackProgressUpdateManager.release()`. This removes three Service-level missing-owner checks without adding a facade or state mirror.
- 2026-07-05 audio-effect settings required-store slice: `EchoPlaybackService.audioEffectSettings()` and `applyAudioEffectSettings(...)` now call `PlaybackAudioEffectSettingsStore` directly instead of carrying a missing-store fallback. The settings current value and null normalization remain in `PlaybackAudioEffectSettingsStore`, persistence still flows through repository via the store, and runtime binding still belongs to `PlaybackAudioEffectManager`. This removes two Service-level missing-store strategy branches without adding an owner, facade, or state mirror.
- 2026-07-05 sleep-timer command-owner binding slice: `PlaybackSleepTimerCommandOwner` now requires its `PlaybackSleepTimerManager` binding for start, cancel, and remaining-time reads instead of silently no-oping when unbound. Sleep timer state and scheduling still belong to `PlaybackSleepTimerManager`; the command owner remains only the action bridge to pause/publish. This removes three unbound-manager fallback paths without adding an owner, facade, or state mirror.
- 2026-07-05 required-owner mini-audit: after the audio-effect settings and sleep-timer binding slices, `EchoPlaybackService.java` is `1239` lines, strict `private Playback*` Service fields are `40`, Service-held `Playback*Owner` fields are `17`, app/feature `Playback*Owner` files are `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, `QueueProvider` remains absent, `QueueStateSnapshot` still exposes only `currentTrack/currentIndex/queueSize`, `PlaybackMediaSourceResolutionOwner` production hits remain `0`, and `PlaybackPrecacheManager` still does not hold a `PlaybackMediaSourceProvider` field. Next cuts should avoid notification/lyrics/shutdown and focus on low-risk queue-state or resolver/cache contracts.
- 2026-07-05 session command-owner constructor slice: `PlaybackSessionCommandOwner` now requires its command, seek, repeat-mode, controller-media-items, queue-manager, and metadata delegates at construction. Queue state still comes from `PlaybackQueueManager`, controller media item mapping remains in `PlaybackControllerMediaItemsOwner`, and metadata remains behind `PlaybackNotificationManager`; this removes five late Media3 callback nullable-boundary risks without adding an owner, facade, or state mirror.
- Do not expand `PlaybackQueueManager.QueueProvider` or similar large interfaces without a prior split/merge/inline plan.
- String-based architecture contracts are not enough for fragile flows; pair them with behavior tests, dependency-direction checks, integration smoke, or device evidence.
- Continue P1/P2 only after a slice demonstrably reduces net files, methods, state sources, dependencies, or call-chain length.
## 2026-06-27 ????

- ???????????/????????????? owner ????
- `EchoPlaybackService` ???? `MainActivity` ???????????/???????? launcher intent?
- `PlaybackQueueManager.QueueProvider` ??????????????????????????????
- ?????????????????????????????
- ???????? `docs/ARCHITECTURE_STABILIZATION_PIVOT_2026-06-27.md`???? P1/P2 ????????????????
