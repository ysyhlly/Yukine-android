# Yukine Android MVVM 迁移接手清单

日期�?026-06-23  
状态：架构审查后形成的接手文档，未要求一次性重写�? 
适用范围：`app/src/main/java/app/yukine` 下当前单 Activity + Compose �?+ Java/Kotlin 迁移中间态�? 

---

## 2026-06-29 Codex continuation note

- Launcher component `.MainActivity` now resolves to Kotlin `MainActivity.kt`;
  the deleted Java `MainActivity.java` is guarded by
  `MainActivityArchitectureContractTest`.
- `MainActivityBase` is now a public legacy base behind the Kotlin shell.
  ViewModel acquisition moved to Kotlin `by viewModels()` delegates and
  `MainActivityViewModels`, so the Java base no longer calls
  `ViewModelProvider`.
- The provisional `ShellViewModel` / `ShellState` / `ShellAction` line was
  removed after review because it was not consumed by `MainActivityBase`,
  `NavigationViewModel`, `MainRouteController`, or `EchoNavHostState`. The
  active shell path remains Kotlin `MainActivity.kt` delegating into the
  existing route/nav owners until a future slice can make `ShellState` the
  single runtime state source instead of a parallel model.
- Hilt Activity-scoped modules now own several stable Activity dependencies:
  `ShellModule` provides `MainExecutors`, `PlatformModule` provides the main
  `Handler`, `StreamingModule` provides streaming playback scheduling,
  streaming playback resolution, and streaming track matching, and
  `LibraryModule` provides favorite toggling and playlist-track loading use
  cases.
- `PlaybackUiModule` now provides `MainPlaybackStoreFactory`; the Java base
  no longer constructs `MainPlaybackStore` directly and only passes the
  Kotlin-shell-owned `PlaybackViewModel` into the injected factory.
- Now-playing transport/favorite/status gateway policy moved out of the Java
  base anonymous `NowPlayingGateway` block into `MainNowPlayingGateway`, which
  is created through `PlaybackUiModule` and covered by
  `MainNowPlayingGatewayTest`.
- Playback action listener policy moved out of the Java base anonymous
  `PlaybackActionController.Listener` block into `MainPlaybackActionListener`,
  which is created through `PlaybackUiModule` and covered by
  `MainPlaybackActionListenerTest`.
- Queue action listener policy moved out of the Java base anonymous
  `QueueActionController.Listener` block into `MainQueueActionListener`, which
  is created through `PlaybackUiModule` and covered by
  `MainQueueActionListenerTest`.
- Queue render listener policy moved out of the Java base anonymous
  `QueueRenderController.Listener` block into `MainQueueRenderListener`, which
  is created through `PlaybackUiModule` and covered by
  `MainQueueRenderListenerTest`.
- Home dashboard render listener policy moved out of the Java base anonymous
  `HomeDashboardRenderController.Listener` block into
  `MainHomeDashboardRenderListener`, which is created through `ShellModule` and
  covered by `MainHomeDashboardRenderListenerTest`.
- Now-playing state listener policy moved out of the Java base anonymous
  `NowPlayingStateController.Listener` block into `MainNowPlayingStateListener`,
  which is created through `PlaybackUiModule` and covered by
  `MainNowPlayingStateListenerTest`.
- Recommendation action callbacks moved out of the Java base anonymous
  `RecommendationActionCallbacks` block into `MainRecommendationActionCallbacks`,
  which is created through `StreamingModule` and covered by
  `MainRecommendationActionCallbacksTest`.
- Streaming playlist dialog callbacks moved out of the Java base anonymous
  `StreamingPlaylistDialogController.Listener` block into
  `MainStreamingPlaylistDialogListener`, which is created through
  `StreamingModule` and covered by `MainStreamingPlaylistDialogListenerTest`.
- Streaming playlist controller listener policy moved out of the Java base
  anonymous `StreamingPlaylistController.Listener` block into
  `MainStreamingPlaylistListener`, which is created through `StreamingModule`
  and covered by `MainStreamingPlaylistListenerTest`.
- Streaming playlist import dialog callbacks moved out of the Java base
  anonymous `StreamingPlaylistImportDialogController.Listener` block into
  `MainStreamingPlaylistImportDialogListener`, which is created through
  `StreamingModule` and covered by
  `MainStreamingPlaylistImportDialogListenerTest`.
- Streaming manual cookie listener callbacks moved out of the Java base
  anonymous `StreamingManualCookieController.Listener` block into
  `MainStreamingManualCookieListener`, which is created through
  `StreamingModule` and covered by `MainStreamingManualCookieListenerTest`.
- Streaming search render listener policy moved out of the Java base anonymous
  `StreamingSearchRenderController.Listener` block into
  `MainStreamingSearchRenderListener`, which is created through
  `StreamingModule` and covered by `MainStreamingSearchRenderListenerTest`.
- Migration direction update: pause mechanical listener extraction after this
  high-value streaming search listener slice. Next architecture slices should
  run in two lanes but land in small batches: first record a checkpoint, then
  prefer Hilt construction migration in `initializeStoresAndDataGateways()`
  for `LibraryPlaylistActionGateway`, `StreamingLocalPlaylistOperations`,
  `LoadSettingsPreferencesUseCase`, `ImportStreamingPlaylistUseCase`,
  `SyncStreamingPlaylistUseCase`, `EnsureStreamingLoginPlaylistUseCase`, and
  `GetStreamingPlaylistLinkUseCase` before adding more one-hop listener
  factories.
- New owner acceptance is now stricter: a new owner must delete a Java
  anonymous business callback, reduce Activity manual construction, shorten a
  call chain, split a large interface, or add real behavior coverage. Listener
  delegation tests can remain, but every 2-3 listener slices need a behavior
  test for a real streaming/playback/data flow.
- Hilt construction migration has started for
  `initializeStoresAndDataGateways()`: `StreamingPlaylistSyncStore`,
  `ImportStreamingPlaylistUseCase`, `SyncStreamingPlaylistUseCase`,
  `EnsureStreamingLoginPlaylistUseCase`, `GetStreamingPlaylistLinkUseCase`,
  and `StreamingLocalPlaylistOperations` are now provided by
  `StreamingModule`. `MainActivityBase` only binds the injected
  `StreamingLocalPlaylistOperations` into `StreamingViewModel`; the previous
  Java anonymous `StreamingLocalPlaylistOperations` block and four manual
  use case constructors were removed. `MainStreamingLocalPlaylistOperations`
  covers the owner behavior with `MainStreamingLocalPlaylistOperationsTest`.
- The download directory picker unavailable message now uses
  `AppLanguage.text(..., "download.directory.picker.unavailable")` instead of
  a hard-coded Chinese string, preserving English parity.
- P0 baseline freeze recheck (2026-06-29): `git status --short` currently
  reports four modified entries:
  `M docs/ARCHITECTURE_REMEDIATION_PLAN_2026-06-26.md`,
  `M docs/ARCHITECTURE_STABILIZATION_PIVOT_2026-06-27.md`,
  `M docs/MVVM_MIGRATION_HANDOFF.md`, and `M gradle.properties`; there are no
  current deleted test files under `app/src/test` or `app/src/androidTest`,
  so the deleted-test replacement map is empty for this checkpoint. Current
  counts are `MainActivityBase.java` 2968 lines,
  `EchoPlaybackService.java` 2469 lines, `feature:data`
  `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines,
  root-package `*Bindings*` 0, root-package `*Controller*` 44,
  root-package files 170, and root-package Java files 15.
  `ShellViewModel` / `ShellState` / `ShellAction` source files are absent;
  remaining mentions are historical correction notes or contract-test guards.
  Serial verification passed with default Gradle settings:
  `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`
  and
  `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`.
  Do not default to `--no-daemon` or `--max-workers=1`; use them only after a
  reproducible daemon, KSP, or lock issue.
- P1 `initializeStoresAndDataGateways()` playlist action gateway slice
  (2026-06-29): `LibraryModule` now provides `LibraryPlaylistActionGateway`
  as `MainLibraryPlaylistActionGateway`, backed by
  `MusicLibraryPlaylistActionOperations(repository, syncStore)`. The playlist
  action contracts moved from the large `LibraryViewModel.kt` file into
  `LibraryPlaylistActionContracts.kt`, while `LibraryViewModel` remains the UI
  state/action owner. `MainActivityBase` now only calls
  `libraryViewModel.bindPlaylistActionGateway(libraryPlaylistActionGateway)`;
  the previous anonymous `new LibraryPlaylistActionGateway() { ... }`, the
  local `MusicLibraryPlaylistActionOperations`, seven playlist action use case
  constructors, and the Activity-owned `StreamingPlaylistSyncStore` field were
  removed from the host. This lowers `MainActivityBase.java` to 2927 lines and
  keeps root-package `*Bindings*` at 0 and root-package `*Controller*` at 44;
  root-package files are now 172 after adding the focused gateway and contract
  files. Guarded by `MainLibraryPlaylistActionGatewayTest` and
  `MainActivityArchitectureContractTest`. Focused tests passed with
  `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainLibraryPlaylistActionGatewayTest --tests app.yukine.MainActivityArchitectureContractTest --rerun-tasks --console=plain`
  after a default cached run exposed stale transformed class output; compile
  passed with
  `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`.
- P1 library collection/import gateway slice (2026-06-29):
  `LibraryModule` now provides `LibraryCollectionGateway` through
  `MainLibraryCollectionGateway` and `LibraryImportGateway` through
  `MainLibraryImportGateway`. The concrete owners live beside the existing
  library collection/import use cases, so no new root-package files or root
  `*Bindings*` / `*Controller*` files were added. `MainActivityBase` now binds
  the injected collection/import gateways directly; the previous anonymous
  `new LibraryCollectionGateway() { ... }` and
  `new LibraryImportGateway() { ... }` blocks, the host-owned
  `MusicLibraryCollectionOperations`, the repeated import use case
  constructors, and the private `toLibraryLoadResultUi(...)` helper were
  removed.
  Current recheck: `MainActivityBase.java` 2873 lines,
  `EchoPlaybackService.java` 2469 lines, `feature:data`
  `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines,
  root-package `*Bindings*` 0, root-package `*Controller*` 44,
  root-package files 172, and root-package Java files 15. Guarded by
  `LibraryCollectionUseCasesTest`,
  `LibraryImportUseCasesTest`, and `MainActivityArchitectureContractTest`.
  Verification passed with
  `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.LibraryCollectionUseCasesTest --tests app.yukine.LibraryImportUseCasesTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`
  and
  `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`.
- P1 library document gateway provider slice (2026-06-29):
  `LibraryModule` now provides `LibraryDocumentGateway` through
  `ContentResolverLibraryDocumentGateway` with an application `ContentResolver`
  and `MusicLibraryImportOperations(repository)`. `MainActivityBase` now binds
  the injected `libraryDocumentGateway` and no longer constructs
  `MusicLibraryImportOperations` or
  `ContentResolverLibraryDocumentGateway(getContentResolver(), ...)` locally.
  This finishes the current library import/document assembly path in Hilt
  without adding root `*Bindings*` or `*Controller*` files. Current recheck:
  `MainActivityBase.java` 2872 lines, `EchoPlaybackService.java` 2469 lines,
  `feature:data` `EchoDatabaseHelper.java` 2117 lines,
  `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0,
  root-package `*Controller*` 44, root-package files 172, and root-package
  Java files 15. This slice reduces Activity manual construction and call-chain
  length; Activity field count is not claimed as a P1 exit win because the
  injected document gateway replaces local assembly. Guarded by
  `ContentResolverLibraryDocumentGatewayTest` and
  `MainActivityArchitectureContractTest`. Verification passed with
  `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.ContentResolverLibraryDocumentGatewayTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`
  and
  `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`.
- P1 settings preference provider slice (2026-06-29):
  `SettingsModule` now provides `LoadSettingsPreferencesUseCase` and
  `ApplySettingsPreferenceUseCase` from the existing settings preference
  operations. `MainActivityBase` now loads settings through the injected
  `loadSettingsPreferencesUseCase` and binds persistence through
  `applySettingsPreferenceUseCase::execute`; the host no longer constructs
  `MusicLibrarySettingsPreferenceLoadOperations`,
  `LoadSettingsPreferencesUseCase`, `MusicLibrarySettingsPreferenceOperations`,
  or `ApplySettingsPreferenceUseCase`. This keeps the Settings path as
  `SettingsViewModel -> preference/runtime applier` and does not add a settings
  gateway, coordinator, controller, or bindings layer. Current recheck:
  `MainActivityBase.java` 2866 lines, `EchoPlaybackService.java` 2469 lines,
  `feature:data` `EchoDatabaseHelper.java` 2117 lines,
  `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0,
  root-package `*Controller*` 44, root-package files 173, and root-package
  Java files 15. The root file count increases by one for the feature-specific
  Hilt module; the Activity manual-construction count and call-chain length
  decrease. Guarded by `LoadSettingsPreferencesUseCaseTest`,
  `ApplySettingsPreferenceUseCaseTest`, and
  `MainActivityArchitectureContractTest`. Verification passed with
  `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.LoadSettingsPreferencesUseCaseTest --tests app.yukine.ApplySettingsPreferenceUseCaseTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`
  and
  `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`.
- P1 library store factory slice (2026-06-29):
  `MainLibraryStore` now has `MainLibraryStoreFactory`, mirroring the existing
  `MainPlaybackStoreFactory` pattern for ViewModel-bound stores. `LibraryModule`
  now provides `LibrarySearchUseCase` and `MainLibraryStoreFactory`, so
  `MainActivityBase` only calls `libraryStoreFactory.create(viewModel)`.
  The host no longer constructs `MainLibraryStore`, `LibrarySearchUseCase`, or
  `MusicLibrarySearchOperations` directly. Current recheck:
  `MainActivityBase.java` 2864 lines, `EchoPlaybackService.java` 2469 lines,
  `feature:data` `EchoDatabaseHelper.java` 2117 lines,
  `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0,
  root-package `*Controller*` 44, root-package files 173, and root-package
  Java files 15. This slice reduces Activity store/search manual construction;
  Activity field count is not claimed as a win because the injected factory
  replaces local assembly. Guarded by `LibrarySearchUseCaseTest`,
  `NetworkLibraryStoreDirectAccessTest`, `PlayHistoryActionControllerTest`,
  and `MainActivityArchitectureContractTest`. The first focused-test attempt
  hit the tool timeout at 120s; a process check showed only Gradle/Kotlin
  daemons, then the same command passed with a longer timeout:
  `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.LibrarySearchUseCaseTest --tests app.yukine.NetworkLibraryStoreDirectAccessTest --tests app.yukine.PlayHistoryActionControllerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`.
  Compile passed with
  `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`.
- P1 streaming search action handler factory slice (2026-06-29):
  `DefaultStreamingSearchActionHandler` now exposes
  `MainStreamingSearchActionHandlerFactory`, provided by `StreamingModule`.
  `MainActivityBase` now calls
  `streamingSearchActionHandlerFactory.create(streamingViewModel, streamingActionGateway)`
  and no longer directly constructs `DefaultStreamingSearchActionHandler` in
  production code. Current recheck: `MainActivityBase.java` 2865 lines,
  `EchoPlaybackService.java` 2469 lines, `feature:data`
  `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines,
  root-package `*Bindings*` 0, root-package `*Controller*` 44,
  root-package files 173, and root-package Java files 15. This slice reduces
  one Activity streaming search manual-construction point; line count and field
  count are not claimed as wins because the injected factory replaces local
  construction. Guarded by `DefaultStreamingSearchActionHandlerTest` and
  `MainActivityArchitectureContractTest`; compile passed with
  `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`.
- P1 track share operations provider slice (2026-06-29):
  `ShellModule` now provides `TrackShareOperations` through the existing
  `TrackShareManagerOperations(trackShareManager, nativeMusicShareManager)`.
  `MainActivityBase` injects `TrackShareOperations` and no longer holds
  `TrackShareManager` / `NativeMusicShareManager` fields or constructs
  `TrackShareManagerOperations` locally when creating `TrackShareLauncher`.
  Current recheck: `MainActivityBase.java` 2862 lines,
  `EchoPlaybackService.java` 2469 lines, `feature:data`
  `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines,
  root-package `*Bindings*` 0, root-package `*Controller*` 44,
  root-package files 173, and root-package Java files 15. This slice reduces
  Activity fields and share operations manual construction without adding root
  files, bindings, or controllers. Guarded by `TrackShareLauncherTest` and
  `MainActivityArchitectureContractTest`; compile passed with
  `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`.
- P1 settings store provider slice (2026-06-29):
  `SettingsModule` now provides the Activity-scoped `MainSettingsStore`.
  `MainActivityBase` injects `settingsStore` directly and no longer constructs
  `new MainSettingsStore()` inside `initializeStoresAndDataGateways()`.
  Current recheck: `MainActivityBase.java` 2861 lines,
  `EchoPlaybackService.java` 2469 lines, `feature:data`
  `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines,
  root-package `*Bindings*` 0, root-package `*Controller*` 44,
  root-package files 173, and root-package Java files 15. This slice reduces
  Activity manual construction without adding root files, bindings, or
  controllers. Guarded by `LoadSettingsPreferencesUseCaseTest`,
  `ApplySettingsPreferenceUseCaseTest`, and `MainActivityArchitectureContractTest`;
  compile passed with
  `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`.
- P1 lyrics settings use-case provider slice (2026-06-29):
  `SettingsModule` now provides `LoadLyricsSettingsUseCase` through the
  existing `MusicLibraryLyricsSettingsOperations(repository)`. `MainActivityBase`
  injects `loadLyricsSettingsUseCase` and no longer constructs
  `LoadLyricsSettingsUseCase` or `MusicLibraryLyricsSettingsOperations` locally
  while configuring `LyricsViewModel`. Current recheck:
  `MainActivityBase.java` 2858 lines, `EchoPlaybackService.java` 2469 lines,
  `feature:data` `EchoDatabaseHelper.java` 2117 lines,
  `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0,
  root-package `*Controller*` 44, root-package files 173, and root-package
  Java files 15. This slice reduces Activity manual construction without adding
  root files, bindings, or controllers. Guarded by
  `LoadLyricsSettingsUseCaseTest` and `MainActivityArchitectureContractTest`;
  compile passed with
  `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`.
- P1 lyrics loader provider slice (2026-06-29):
  `SettingsModule` now provides the `LyricsLoader` used by `LyricsViewModel`
  through the existing `LoadTrackLyricsUseCaseLyricsLoader`,
  `LoadTrackLyricsUseCase`, and `LyricsRepositoryLoadOperations` chain.
  `MainActivityBase` injects `lyricsLoader` and no longer constructs
  `LoadTrackLyricsUseCaseLyricsLoader`,
  `LoadTrackLyricsUseCase`, or `LyricsRepositoryLoadOperations` while
  configuring `LyricsViewModel`. Current recheck:
  `MainActivityBase.java` 2857 lines, `EchoPlaybackService.java` 2469 lines,
  `feature:data` `EchoDatabaseHelper.java` 2117 lines,
  `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0,
  root-package `*Controller*` 44, root-package files 173, and root-package
  Java files 15. This slice reduces Activity manual construction without
  adding root files, bindings, controllers, or a new lyrics manager. Guarded by
  `LoadTrackLyricsUseCaseTest`, `LyricsViewModelTest`, and
  `MainActivityArchitectureContractTest`; compile passed with
  `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`.
- P1 network action use-case provider slice (2026-06-29):
  `LibraryModule` now provides `NetworkActionUseCases` from the existing
  `MusicLibraryWebDavSourceOperations(repository)` and
  `MusicLibraryNetworkLibraryOperations(repository)` owners. `MainActivityBase`
  injects `networkActionUseCases` and no longer constructs
  `NetworkActionUseCases`, `MusicLibraryWebDavSourceOperations`,
  `MusicLibraryNetworkLibraryOperations`, or the 11 WebDAV/network library use
  cases in `initializeNetworkOwners()`. Current recheck:
  `MainActivityBase.java` 2840 lines, `EchoPlaybackService.java` 2469 lines,
  `feature:data` `EchoDatabaseHelper.java` 2117 lines,
  `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0,
  root-package `*Controller*` 44, root-package files 173, and root-package
  Java files 15. This slice reduces Activity manual construction without
  adding root files, bindings, controllers, or a network gateway. Guarded by
  `NetworkActionsViewModelTest`, `NetworkLibraryUseCasesTest`,
  `WebDavSourceUseCasesTest`, and `MainActivityArchitectureContractTest`;
  compile passed with
  `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`.
- P1 library gateway policy owner slice (2026-06-29):
  `LibraryModule` now provides `MainLibraryGatewayFactory`, and
  `MainLibraryGateway` owns the former `LibraryGateway` host policy for
  track-list play, status key resolution, favorite refresh, playlist add,
  library group routing, search, import, and scan commands. `MainRouteController`
  implements the narrow `LibraryRouteActions` interface so Activity does not
  need a replacement anonymous routing block. `MainActivityBase` now only wires
  existing host/platform capabilities into the factory and no longer contains
  `libraryViewModel.bindGateway(new LibraryGateway() { ... })`. Current recheck:
  `MainActivityBase.java` 2789 lines, `EchoPlaybackService.java` 2469 lines,
  `feature:data` `EchoDatabaseHelper.java` 2117 lines,
  `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0,
  root-package `*Controller*` 44, root-package files 174, and root-package
  Java files 15. Root files increase by one for a behavior owner, while the
  Activity anonymous policy block and call-chain policy move into focused
  coverage. Guarded by `MainLibraryGatewayTest`, `LibraryViewModelTest`,
  `MainRouteControllerTest`, and `MainActivityArchitectureContractTest`;
  focused tests and compile passed with default daemon/workers.
- P1 streaming action gateway policy owner slice (2026-06-29):
  `StreamingModule` now provides `MainStreamingActionGatewayFactory`, and
  `MainStreamingActionGateway` owns the former Java anonymous
  `MainActivityStreamingActionGateway` policy for selected quality, language,
  auth launch delegation, resolved-track playback, login-success playlist
  handling, and manual cookie import ordering. `MainActivityBase` now wires
  existing host/platform capabilities into the factory and no longer contains
  `new MainActivityStreamingActionGateway()`. Current recheck:
  `MainActivityBase.java` 2769 lines, `EchoPlaybackService.java` 2469 lines,
  `feature:data` `EchoDatabaseHelper.java` 2117 lines,
  `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0,
  root-package `*Controller*` 44, root-package files 175, and root-package
  Java files 15. Root files increase by one for a behavior owner, while the
  Activity anonymous streaming policy block moves into focused coverage.
  Guarded by `MainStreamingActionGatewayTest`,
  `DefaultStreamingSearchActionHandlerTest`, and
  `MainActivityArchitectureContractTest`; focused tests and compile passed
  with default daemon/workers.
- P1 now-playing playback gateway service-start slice (2026-06-29):
  `PlaybackUiModule` now provides `MainNowPlayingPlaybackGatewayFactory`, and
  `NowPlayingPlaybackServiceStarter` owns the Android
  `Intent(context, EchoPlaybackService::class.java)` start path. `MainActivityBase`
  now only binds `nowPlayingPlaybackGatewayFactory.create(() -> playbackService)`
  and no longer constructs `NowPlayingPlaybackGatewayAdapter` or the playback
  service start `Intent` locally. Current recheck: `MainActivityBase.java` 2762
  lines, `EchoPlaybackService.java` 2469 lines, `feature:data`
  `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines,
  root-package `*Bindings*` 0, root-package `*Controller*` 44,
  root-package files 175, and root-package Java files 15. Guarded by
  `NowPlayingPlaybackGatewayAdapterTest`, `MainNowPlayingGatewayTest`,
  `NowPlayingViewModelTest`, and `MainActivityArchitectureContractTest`;
  focused tests and compile passed with default daemon/workers.
- P1 play-history action controller factory slice (2026-06-29):
  `LibraryModule` now provides `MainPlayHistoryActionControllerFactory`, and
  `MainActivityBase.initializeStoresAndDataGateways()` creates the existing
  `PlayHistoryActionController` through that injected factory instead of
  `new PlayHistoryActionController(...)`. The real owner and behavior stay in
  `PlayHistoryActionController`: it still clears through `LibraryViewModel`,
  updates `PlayHistoryStateStore`, publishes localized status, and reloads
  collections. Current recheck: `MainActivityBase.java` 2760 lines,
  `EchoPlaybackService.java` 2469 lines, `feature:data`
  `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines,
  root-package `*Bindings*` 0, root-package `*Controller*` 44,
  root-package files 175, and root-package Java files 15. Guarded by
  `PlayHistoryActionControllerTest` and `MainActivityArchitectureContractTest`;
  focused tests and compile passed with default daemon/workers.
- P1 network actions listener policy slice (2026-06-29):
  The long `NetworkActionsViewModel.Listener` anonymous block moved out of
  `MainActivityBase.initializeNetworkOwners()` into `MainNetworkActionsListener`,
  provided by `LibraryModule` through `MainNetworkActionsListenerFactory`.
  `NetworkActionsViewModel` still owns use-case execution; the new listener
  only maps action results to library replacement, now-playing queue retain or
  replace, network navigation, collections reload, and status publication.
  `MainActivityBase` now only binds the injected listener factory with existing
  host callbacks. Current recheck: `MainActivityBase.java` 2697 lines,
  `EchoPlaybackService.java` 2469 lines, `feature:data`
  `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines,
  root-package `*Bindings*` 0, root-package `*Controller*` 44,
  root-package files 175, and root-package Java files 15. Guarded by
  `MainNetworkActionsListenerTest`, `NetworkActionsViewModelTest`, and
  `MainActivityArchitectureContractTest`; focused tests and compile passed
  with default daemon/workers.
- Streaming playback listener policy moved out of the Java base anonymous
  `StreamingPlaybackController.Listener` block into
  `MainStreamingPlaybackListener`, which is created through `PlaybackUiModule`
  and covered by `MainStreamingPlaybackListenerTest`.
- Playback start listener policy and pending playback state moved out of the
  Java base anonymous `PlaybackStartController.Listener` block into
  `MainPlaybackStartListener`, which is created through `PlaybackUiModule` and
  covered by `MainPlaybackStartListenerTest`.
- Playback state event listener policy moved out of the Java base anonymous
  `PlaybackStateEventController.Listener` block into
  `MainPlaybackStateEventListener`, which is created through
  `PlaybackUiModule` and covered by `MainPlaybackStateEventListenerTest`.
- Playback service host policy moved out of the Java base anonymous
  `PlaybackServiceHostController.Host` block into `MainPlaybackServiceHost`,
  which is created through `PlaybackUiModule` and covered by
  `MainPlaybackServiceHostTest`.
- Track-list render listener policy moved out of the Java base anonymous
  `TrackListRenderController.Listener` block into
  `MainTrackListRenderListener`, which is created through `LibraryModule` and
  covered by `MainTrackListRenderListenerTest`.
- Heartbeat recommendation listener policy moved out of the Java base
  anonymous `HeartbeatRecommendationController.Listener` block into
  `MainHeartbeatRecommendationListener`, which is created through
  `StreamingModule` and covered by `MainHeartbeatRecommendationListenerTest`.
- Document picker platform listener policy moved out of the Java base
  anonymous `DocumentPickerController.Listener` block into
  `MainDocumentPickerListener`, which is created through `PlatformModule` and
  covered by `MainDocumentPickerListenerTest`.
- Background image picker listener policy moved out of the Java base
  anonymous `BackgroundImagePickerController.Listener` block into
  `MainBackgroundImagePickerListener`, which is created through
  `PlatformModule` and covered by `MainBackgroundImagePickerListenerTest`.
- Permission result listener policy moved out of the Java base anonymous
  `MainPermissionController.Listener` block into `MainPermissionListener`,
  which is created through `PlatformModule` and covered by
  `MainPermissionListenerTest`.
- `MainActivityBase` no longer hand-constructs those dependencies; it only
  binds the injected owners into the existing feature ViewModels/controllers.
- Phase 5.6 active-download host boundary was tightened without changing
  download behavior.
- `TrackDownloadController` and `TrackDownloadActionResult` now live in
  `feature/navigation/src/main/java/app/yukine/DownloadsDestinationContracts.kt`.
- `TrackDownloadManager` stays in `:app` as the concrete download queue,
  directory, Android `DownloadManager`, and app-managed segmented download
  owner through `TrackDownloadRequestQueue`.
- `EchoNavHostState` still consumes `TrackDownloadController?`, but no longer
  depends on an app-owned contract definition for active download snapshots.
- Guarded by `MainActivityArchitectureContractTest` plus focused downloads and
  navigation tests.
- The remaining navigation shell files have now moved to `:feature:navigation`:
  `EchoNavGraph.kt`, `EchoNavHostState.kt`, and `EchoNavHostBridge.kt`.
- `:app` still owns ViewModels, platform launchers, and state/action
  production, while `EchoAppHost` wires those contracts into the feature
  navigation shell.
- Phase 6 has started as stabilization-first coordinator reduction rather than
  new event-bus expansion: forwarding-only `DownloadsCoordinator` and
  behaviorless `FileIOCoordinator` were deleted, with download track requests
  wired directly to the existing `DownloadRequestController`.
- The unused `NetworkCoordinator` has also been deleted; the live network
  request/menu/source/render wiring was already owned directly by
  `MainActivityBase` and guarded by `MainActivityArchitectureContractTest`.
- The unused `LibraryCoordinator` and `NavigationCoordinator` were deleted as
  dead assembly layers: both were constructed in `MainActivityBase` but never
  called. The active library and route/search/back/swipe paths remain in the
  existing host methods until a later slice can move behavior to a true owner.
- `SettingsCoordinator` was deleted as a duplicate runtime binding layer:
  `MainActivityBase` already constructs `SettingsRuntimeApplier` and binds it
  directly to `SettingsViewModel`; the coordinator only rebound the same
  listener and its render method had no caller.
- `PlaybackCoordinator` was narrowed to playback-start delegation plus service
  connection lifecycle. It no longer owns action/result application, ViewModel
  or store dependencies, state controllers, or its own pending playback cache;
  pending playback remains owned by `PlaybackStartController`.
- `PlaybackCoordinator` was then deleted after becoming pure forwarding:
  `MainActivityBase` now calls `PlaybackStartController`,
  `PlaybackServiceConnectionController`, and
  `EchoPlaybackService.setAppVisible(...)` directly.
- `NowPlayingQueueCoordinator` was narrowed to the live queue intent listener
  binding. It no longer owns unused Now Playing effect handling, queue render
  helpers, queue input binding, store/settings/status dependencies, or
  render/state controller constructor parameters.
- `NowPlayingQueueCoordinator` was then deleted after confirming
  `mountNavHostShell()` already installs the actual
  `queueViewModel.bindIntentListener(...)` path and overwrote the coordinator
  binding. Queue intents remain handled through the existing NavHost shell path
  and `QueueActionController`.
- `StreamingCoordinator` was deleted after its only live production use was
  forwarding initial/new intents to `StreamingAuthCallbackController`. The host
  now calls that controller directly, while the existing `MainActivityBase`
  streaming refresh, gateway endpoint, playback, quality, and source-switch
  methods remain the active implementations.
- `NetworkMenuRenderController`, `NetworkTrackListRenderController`,
  `NetworkSourcesRenderController`, and `StreamingSearchRenderController`
  remain focused render owners consumed by `NetworkRenderCoordinator`, but
  `MainActivityBase` now constructs them as local assembly values instead of
  retaining Activity fields.

---

## 0. 迁移原则

### 0.1 目标架构

目标不是套一个笨重框架，而是把当前混合架构收敛成清晰、轻量、可测试�?MVVM�?
```text
Compose UI
  -> Feature ViewModel
  -> UseCase / Feature Coordinator
  -> Repository / Platform Gateway / Playback Boundary
  -> Database / MediaStore / Service / Network / Android API
```

### 0.2 禁止方向

- 不要�?`MainActivity` 继续当总调度中心�?- 不要�?`MainActivity` 拆成另一个万�?`AppCoordinator` / `GodManager`�?- 不要为了 MVVM 增加无意义转发层�?- 不要让一个按钮点击经�?7�? 层只为了保存一个设置�?- 不要�?Compose 同时�?ViewModel state �?Activity 手动同步 state�?- 不要�?ViewModel 直接持有 `Context`、`Intent`、Dialog、Activity Result 等平台细节�?- 不要为了架构整洁破坏播放、后台服务、通知、小部件、歌词、下载、流媒体登录、首次引导�?
### 0.3 迁移验收总目�?
- `MainActivity.java` 从当前约 3171 行降到宿主级别，目标小于 500 行，长期目标小于 300 行�?- `EchoPlaybackService.java` 保留播放服务职责，但 UI 不直接依赖具�?service�?- 单个 ViewModel 目标小于 500 行；复杂 feature 可拆多个 ViewModel�?- UI 只消�?`UiState`，只发�?`UiAction` / `UiEvent`�?- 平台能力通过小型 owner 暴露：picker、permission、backup、dialog、share、system setting�?- 播放、设置、曲库、队列、歌词、流媒体、推荐都能单独单测�?
---

## 1. 当前架构事实

### 1.1 大类规模

当前重点大类�?
| 文件 | 当前角色 | 风险 |
|---|---|---|
| `MainActivity.java` | 应用宿主 + 路由 + 平台 API + �?feature 装配 + 大量回调 + 手动状态同�?| 上帝类，新增功能容易继续挂线 |
| `EchoPlaybackService.java` | Media3 播放服务、队列、通知、小部件、播放状态、部分音�?| 播放边界合理，但 UI/Activity 直接触碰过多 |
| `StreamingViewModel.kt` | 流媒体搜索、登录、导入、播放解析、推荐等大块逻辑 | 仍偏大，feature 边界未完全拆开 |
| `SettingsViewModel.kt` | 设置 UI state、事件、保存偏好、状态文�?| 已有 MVVM 雏形，但外围转发链过�?|
| `EchoNavHostState.kt` | Compose NavHost �?ViewModel 集合 + 大量 mutable chrome/actions/callback | �?ViewModel state 重复，Activity 手动同步 |

### 1.2 MainActivity 当前职责

`MainActivity.java` 目前至少承担这些职责�?
- 创建和绑定多�?ViewModel�?- 创建和持有几十个 Controller / Bindings�?- 保存 `MainRouteController`、`MainSettingsStore`、`MainLibraryStore`、`MainPlaybackStore`�?- 绑定播放服务，直接读�?`EchoPlaybackService`�?- 处理权限、Activity Result、文档选择、图片选择、备份导入导出�?- 创建多个 Dialog：网络源、歌单、流媒体 Cookie、流媒体歌单导入等�?- 持有 Compose 导航 chrome 临时状态：`navTrackListActions` 等；`navSettingsActions` 已清退，`navNetworkMenuActions` 已由 `NetworkMenuViewModel` 接管�?- 手动调用 `renderSelectedTab()`、`renderNowBar()`、`syncNavHostState()`�?- 处理曲库、歌单、播放、搜索、网络、设置、推荐多�?feature 的具体业务流程�?
### 1.3 当前事件路径示例

#### 设置项点�?
```text
SettingsScreen
  -> SettingsAction.onClick
  -> SettingsViewModel.onEvent
  -> SettingsEffect / SettingsPreferenceGateway / SettingsRuntimeEffectListener
  -> settingsStore / repository / runtime applier
  -> renderCurrentPage / state update
```

问题：链路太长，很多层只转发，无策略�?
#### 导航/页面 chrome

```text
RenderController 生成 actions
  -> MainActivity nav* 字段
  -> syncNavHostState()
  -> EchoNavHostState mutableStateOf
  -> EchoNavGraph / Destination
```

问题：页�?state 不由 feature ViewModel 直接拥有，Activity 变成 Compose state 中转站�?
#### 播放状�?
```text
EchoPlaybackService.snapshot()
MainPlaybackStore.snapshot()
PlaybackViewModel.playback
NowPlayingViewModel state
QueueViewModel bind(...)
MainActivity publishNowPlayingState(...)
```

问题：播放状态有多个读点和镜像点，容易出现谁是事实源不明确�?
---

## 2. 目标包与命名建议

不要第一步就大搬家。先�?feature 迁移，稳定后再整理包�?
建议长期包结构：

```text
app.yukine
  core/
    model/
    ui/
    platform/
    result/
  data/
    settings/
    library/
    playback/
    lyrics/
    streaming/
    backup/
  domain/
    settings/
    library/
    playback/
    lyrics/
    streaming/
    recommendation/
  feature/
    shell/
    home/
    library/
    playlists/
    player/
    queue/
    settings/
    network/
    streaming/
    downloads/
    onboarding/
  playback/
    service/
  navigation/
  ui/
    components/
    theme/
```

短期不强制移动已有文件。迁移新 owner 时优先放到现有相邻包，避免大规模 import churn�?
---

## 3. 全局迁移步骤

### 3.1 �?0 阶段：建立迁移护�?
#### 3.1.1 写架构契约测�?
目标：阻止继续往 `MainActivity` 塞新业务�?
具体任务�?
- 更新或新�?`MainActivityArchitectureContractTest.java`�?- 增加断言�?  - `MainActivity.java` 不新增新�?`showXxxDialog`，除非是平台 owner 的安装入口�?  - `MainActivity.java` 不新增新�?`startActivityForResult`�?  - `MainActivity.java` 不新增新�?`nav*Actions` 字段�?  - 新设置项不得要求同时修改 `SettingsGateway`、`SettingsGatewayBindings`、`SettingsActionController`、`MainActivity` 四层以上�?
验收�?
- 新增一个纯偏好设置时，不需要改 `MainActivity.java`�?- 契约测试失败信息能指出应该放到哪�?owner�?
#### 3.1.2 建立迁移工作�?
每个迁移 PR/提交必须写清�?
- 移走哪个职责�?- 原入口和新入口�?- 状态事实源是谁�?- 事件路径缩短了几层�?- 保留哪些兼容 facade�?- 删除哪些旧转发�?- 跑了哪些测试�?
---

## 4. MainActivity 收缩清单

### 4.1 �?Activity Result / Picker 能力

#### 现状

`MainActivity` 直接处理�?
- 音频文件选择�?- 音频文件夹选择�?- M3U/M3U8 导入导出�?- LX 源文件导入�?- 背景图片选择�?- 下载目录选择�?- 备份导入导出�?
已有 `DocumentPickerController`，但仍有�?picker 直接回流 Activity�?
#### 目标

Activity 只注�?launcher，所�?picker 逻辑由独�?owner 管：

```text
MainActivity
  -> PlatformLauncherRegistry
  -> DocumentPickerController
  -> BackupRestoreLauncher
  -> BackgroundImagePickerController
  -> DocumentPickerController.openDownloadFolderPicker
```

#### 具体任务

1. 新增 `BackgroundImagePickerController`�?   - 输入：`page: PageBackgroundTarget`�?   - 输出：`Flow` / callback `BackgroundImagePicked(page, uri)`�?   - 内部处理 `ACTION_OPEN_DOCUMENT`、`image/*`、持久读权限�?   - Activity 只调�?`backgroundImagePicker.open(page)`�?
2. 新增 `BackupRestoreLauncher`�?   - 替代 `exportBackup()`、`importBackup()`、`handleBackupResult()`�?   - 输出 `BackupEffect.ShowStatus` �?callback�?   - 保持 `BackupManager.INSTANCE.export/restore` 不变�?
3. 收敛 `DocumentPickerController`�?   - 把所�?`REQUEST_*` 常量集中�?controller�?   - Activity �?`onActivityResult` 只调用一�?`platformResultDispatcher.dispatch(...)`�?
4. 后续改用 Activity Result API�?   - 先封装旧 `startActivityForResult`，再迁到 `registerForActivityResult`�?   - 不要�?feature ViewModel 中直接使�?launcher�?
#### 文件

- `MainActivity.java`
- `DocumentPickerController.kt`
- 新增 `BackgroundImagePickerController.kt`
- 新增 `BackupRestoreLauncher.kt`
- 新增 `PlatformActivityResultDispatcher.kt`

#### 验收

- `MainActivity.java` 中不再出现新增业�?picker �?`Intent` 构造�?- `MainActivity.java` �?`onActivityResult` 不包含业务判断，只做分发�?- 背景图片、备份、音频导入、M3U 导入全部原行为可用�?
#### 测试

- `BackgroundImagePickerControllerTest`：page 归一化、空 uri、持久权限失败降级�?- `BackupRestoreLauncherTest`：导�?导入 success/fail 映射状态�?- 手动回归：选图后重启仍显示；备份导�?导入仍返回状态�?
#### 迁移进展�?026-06-23 / 2026-06-24 更新�?
- 背景图片选择已迁�?`BackgroundImagePickerController` / `BackgroundImagePickerBindings`�?- 备份导入导出已迁�?`BackupRestoreLauncher` / `BackupRestoreBindings`�?- 状态消息本地化和最新状态事实源已迁�?`StatusMessageViewModel`；`StatusMessageController` 保留�?Activity 兼容门面�?- 设置页新�?`SettingsPage` typed model，`SettingsBackStack` 已改�?typed page；外�?route 字符串暂保留为兼容层�?- 新增 `SettingsPageStateBuilder`，已承接 `title + metrics + actions -> SettingsUiState` �?mapper，并已迁�?`Home` / `AboutGroup` / `AppearanceGroup` / `SourcesGroup` / `PlaybackGroup` / `LyricsGroup` / `Theme` / `AdvancedTheme` / `Accent` / `Language` / `PageBackground` / `AudioEffects` / `ConcurrentPlayback` / `NowPlayingGestures` / `PlaybackRestore` / `ReplayGain` / `StatusBarLyrics` / `FloatingLyrics` / `SleepTimer` / `PlaybackSpeed` / `AppVolume` / `StreamingAudioQuality` / `ShareStyle` / `StreamingGateway` / `Library` / `Lyrics` 页面装配；后续可继续处理其他 group 页等剩余设置页�?- `SettingsViewModel` 已具�?`SettingsState(page, preferences, runtime, actions, ui)` 状态源，`NavigateSettingsPage` 事件会直接基于当�?`SettingsPreferencesSnapshot` / `RuntimeSettingsStatus` 调用 `renderCurrentPage(...)` 重建页面，不再通过 `SettingsGateway` / `MainActivity.navigateSettingsPage(...)` 回流。`SettingsDestination` 已直接消�?`SettingsViewModel.state` 中的 `actions/ui`，`MainActivity` / `EchoNavHostState` 不再持有 `navSettingsActions`；`SettingsRenderCoordinator`、`SettingsPageEventController`、`SettingsPageChromeBindings`、`SettingsScrollStateSink` 已删除，`SettingsPageRenderController` 现在只作为滚动兼容门面，`SettingsViewModel` 自身持有 `scrollState`，不再把设置列表滚动状态放在页面渲染器里�?- `NetworkMenuViewModel` 已接管网络页标题 / metrics / actions 状态；`NetworkMenuChromeBindings` 只负责把导航 chrome 同步进该 ViewModel，`NetworkDestination` 直接消费�?`uiState`，`EchoNavHostState` 不再保存 `networkMenuTitle` / `networkMenuMetrics` / `networkMenuActions` 这类页面临时 state�?- `HomeDashboardViewModel` 已接管首�?`HomeDashboardActions`；`HomeDashboardRenderBindings` 直接�?actions 发布�?ViewModel，`HomeDestination` �?`MainActivityHomeDashboardUiState` 同时消费内容�?actions，`EchoNavHostState` 不再保存 `homeActions` / `navHomeActions`�?- `SettingsGatewayBindings` 已删除：主题、强调色、语言、播放速度、应用音量、流媒体音质、分享样式、并发播放、在线歌词、歌词偏移、音效、状态栏歌词、悬浮歌词、播放页手势、播放恢复、ReplayGain 等纯偏好/运行时设置事件由 `SettingsViewModel.onEvent(...)` 直接调用自身 apply/set 方法并通过 `SettingsPreferenceGateway` 保存；睡眠计时、加载曲库、歌词重载、流媒体网关 endpoint 等外部协作也已改�?`SettingsEffect`。`SettingsActionController` / `SettingsActionBindings` 已删除�?- 设置平台 effect 已继续收敛：`OpenNetworkSources` / `OpenDownloads`、加载曲库、音频文�?文件夹选择、悬浮歌词权限入口、页面背景选择、备份导入导出、歌词重载、睡眠计时、流媒体网关 endpoint 应用都由 `SettingsViewModel` 发出 `SettingsEffect`，`MainActivity` 直接�?effect 接到 `SettingsEffectBindings` 执行页面切换、平�?launcher 或播�?歌词边界调用，不再占�?`SettingsGatewayBindings` / `SettingsActionController` 构造参数；清除页面背景�?`SettingsViewModel` 基于当前 `SettingsPreferencesSnapshot.pageBackgrounds` 直接更新、保存并重建当前设置页�?- 设置运行时副作用 owner 已继续推进：`SettingsRuntimeApplier` 承接主题 surface、语言 surface、播�?service 控制、歌词开�?偏移与悬浮歌词权限应用；运行时应用已类型化为 `SettingsRuntimeEffect`，并�?`SettingsViewModel.applyXxx(...)` 保存路径通过 `SettingsRuntimeEffectListener` 直接发出，`MainActivity` 只接�?`settingsViewModel.bindRuntimeEffectListener(settingsRuntimeApplier::apply)`。`SettingsViewModel` 现在会在纯偏�?运行时设置应用后同步更新自身 `SettingsPreferencesSnapshot` / `RuntimeSettingsStatus` 并重建当前页，通过 `SettingsStoreMirror` 同步 `MainSettingsStore` 兼容镜像，并直接发出 `SettingsEffect.ShowStatus` / `SettingsEffect.ReloadCurrentLyrics`；`SettingsAppliedListenerBindings` 已删除，状态文案和歌词重载不再需要额�?applied listener 兼容层�?- `MainActivity` 只创�?controller、调�?`open(page)`，并�?`onActivityResult` 委托 `handleActivityResult(...)`�?- `MainActivity` 不再持有背景图片/备份选择�?request code、pending page、`image/*` �?`application/zip` Intent 构造�?- 音频文件、音频文件夹、歌�?M3U 导入、网络页 M3U 导入这类无额外兜底状态的 picker 入口已继续直�?`DocumentPickerController.openAudioFilePicker/openAudioFolderPicker/openPlaylistM3uFilePicker/openM3uFilePicker`；`MainActivity` 不再保留 `openAudioFilePicker()` / `openAudioFolderPicker()` / `openPlaylistM3uFilePicker()` / `openM3uFilePicker()` 单行 wrapper。下载目录选择的兜底反馈已迁入 `DownloadDirectoryPickerController`，`DownloadsEffect.OpenDirectoryPicker` 通过 `EchoNavHostState.openDownloadDirectoryPickerAction` 调用�?controller，`MainActivity` 不再保留 `openDownloadFolderPicker()` wrapper。洛雪源导入已迁�?`LuoxueSourceImportController` / `LuoxueSourceImportDialogController`：文�?picker 缺失兜底、URL 输入、后台读�?拉取、解析保存、状态提示和 streaming provider 刷新均由�?owner 承接，`MainActivity` 只保留装配和 `DocumentPickerController`/`ContentResolver` 平台适配，不再保�?`openLuoxueSourceFilePicker()` / `showLuoxueSourceUrlDialog()` / `importLuoxueSourcesFromUrls()` / `saveImportedLuoxueSources()` 等业务方法�?- 流媒体手�?Cookie 输入弹窗已迁�?`StreamingManualCookieDialogController`，`StreamingManualCookieController` 继续负责认证请求和登录成功回调；`MainActivity` 只装�?dialog owner，不再保�?`showStreamingCookieDialogContent(...)` 或直接拼�?Cookie `EditText` / `EchoDialog`�?- 流媒体歌单链接输入弹窗已迁入 `StreamingPlaylistImportDialogController`：普�?provider 的链接输入、洛�?provider 的源导入分流、确认后进入 `StreamingPlaylistController.importStreamingPlaylistFromLink(...)` 均由�?owner 承接；`MainActivity` 不再保留 `showImportStreamingPlaylistDialog()` 或直接拼装歌单链�?`EditText` / `EchoDialog`�?- 流媒体歌单导�?provider picker、导入完成提示、账号歌单多选导入、收藏导�?provider picker 已迁�?`StreamingPlaylistDialogController`；`StreamingPlaylistController` 保持业务编排，`MainActivity` 只装�?dialog owner，不再保�?`showStreamingProviderPicker(...)` / `showStreamingPlaylistLoadedDialog(...)` / `showAccountPlaylistImportPicker(...)` / `showImportStreamingFavoritesProviderPicker()` / `accountPlaylistLabel(...)`�?- 结果回调继续进入 `SettingsViewModel.applyPageBackgrounds(...)`；页面背景状态事实源仍是 `PageBackgrounds` / settings preferences�?- 备份结果回调继续映射 `backup.*` 状�?key，并�?Activity 现有状态栏入口展示�?- 2026-06-24 审查补丁：启动首屏实时频谱轮询改为仅在播放中运行，未播放/服务未连接时复用空频谱，避免 app 打开动画期间每帧触发 Compose 重组；QQ 音乐登录态校验收紧为需�?`qqmusic_key` / `qm_keyst` / `psrf_qqaccess_token` 等真实凭证，避免只含 `uin/p_uin` 的假登录态进入播放解析�?
---

### 4.2 �?Dialog 能力

#### 现状

`MainActivity` 直接创建多个 `EchoDialog`�?
- WebDAV/网络源编辑�?- 歌单创建/重命�?删除确认�?- 添加到歌单�?- 流媒�?provider picker�?- 流媒�?Cookie 手动输入�?- LX 源导入方式选择�?- 账号歌单导入多选�?- 下载音质选择�?
#### 目标

Dialog 变成 UI effect，由 feature ViewModel �?feature coordinator 请求�?
```text
FeatureViewModel emits Effect.ShowDialog(...)
  -> Activity / DialogHost collects effect
  -> DialogController renders
  -> Result sent back as ViewModel action
```

#### 具体任务

1. 定义通用 effect�?
```kotlin
sealed interface UiEffect {
    data class ShowMessage(val text: String) : UiEffect
    data class ShowDialog(val request: DialogRequest) : UiEffect
    data class LaunchPlatformAction(val request: PlatformRequest) : UiEffect
}
```

2. 每个 feature 定义自己�?dialog request�?   - `PlaylistDialogRequest.Create`
   - `PlaylistDialogRequest.Rename`
   - `PlaylistDialogRequest.DeleteConfirm`
   - `StreamingDialogRequest.ProviderPicker`
   - `StreamingDialogRequest.CookieInput`
   - `DownloadDialogRequest.QualityPicker`

3. 新增 `DialogHostController`�?   - 只负责把 request 渲染�?`EchoDialog`�?   - 不做业务�?   - 用户确认后调�?`onDialogResult(result)`�?
4. 删除 `MainActivity` 中直接拼 dialog layout 的逻辑�?
#### 文件

- `MainActivity.java`
- `NetworkDialogController.java`
- `PlaylistDialogController.java`
- `ConfirmationDialogController.kt`
- `StreamingManualCookieController.kt`
- 新增 `DialogRequest.kt`
- 新增 `DialogHostController.kt`

#### 验收

- feature 业务不依�?`EchoDialog`�?- `MainActivity` 不再知道 Cookie 输入框、账号歌单多选布局、下载音质选项细节�?- Dialog 结果可单测�?
---

### 4.3 抽状态消息能�?
#### 现状

多处直接 `setStatus(AppLanguage.text(...))`�?
#### 目标

统一为：

```text
ViewModel / UseCase -> UiEffect.ShowMessage(messageKey or message)
StatusMessageViewModel -> Compose Snackbar/Status UI
```

#### 具体任务

1. 新增 `StatusMessageViewModel` 或复�?`StatusMessageController` 但让 Compose collect�?2. 定义�?
```kotlin
data class StatusMessage(
    val text: String,
    val level: StatusLevel = Info,
    val source: String = ""
)
```

3. 新增 `MessageTextResolver`�?   - �?UI 层根�?`languageMode` 解析 key�?   - Repository/UseCase 不拼 UI 文案�?
4. 逐步替换 `MainActivity.setStatus` 调用�?
#### 验收

- �?feature 不直接调�?`MainActivity.setStatus`�?- 状态消息可以在单元测试中断言 effect�?
迁移进展�?026-06-24）：

- `showActionFeedback(...)` 的空消息过滤与发布已下沉�?`StatusMessageController.showFeedback(...)`；下载请求、下载目录兜底、在线播放解�?音源切换、分享反馈等入口直接调用状态消�?owner，`MainActivity` 不再保留 `showActionFeedback(...)` �?wrapper�?- `MessageTextResolver` 已引入作�?UI �?message key 解析入口；备份导�?导出�?`backup.*` statusKey 现在通过 `StatusMessageController.setStatusKey(...)` 发布，`MainActivity` 不再�?`BackupRestoreLauncher` 直接调用 `AppLanguage.text(...)`�?- 设置返回栈已继续 typed 化：`MainBackNavigationPolicy` 直接接收 `SettingsPage`，`MainRouteController` 先把当前 settings route 转成 typed page 再参�?back policy，`SettingsBackStack.parentPage(String)` 兼容适配器已移除�?
---

## 5. 设置模块迁移清单

设置模块是最适合先完�?MVVM 化的样板�?
### 5.1 当前问题

当前设置链路分散�?
- `SettingsViewModel` 已同时持�?`SettingsState`、`SettingsUiState` 和滚动状态，事件大多已在 ViewModel 内处理，少量平台能力仍通过 effect 走外�?owner�?- `SettingsPageRenderController` 只保留页面滚动兼容入口与少量 label helper�?- `SettingsRenderCoordinator` 根据 `settingsPage` 调不�?render 方法�?- `SettingsGatewayBindings` 是很长的构造参数列表�?- `SettingsAppliedListenerBindings` 同时更新 `MainSettingsStore`、service、lyrics、状态消息、重新渲染�?- `MainSettingsStore`、DB settings、`SettingsViewModel`、`EchoNavHostState` 都参与状态�?
### 5.2 目标

设置页终态：

```text
SettingsScreen
  collect SettingsViewModel.uiState
  send SettingsAction

SettingsViewModel
  owns current page
  owns displayed settings state
  calls SettingsUseCases
  emits SettingsEffect

SettingsRepository
  load/save persistent preferences

SettingsRuntimeApplier
  applies runtime side effects: theme, playback speed, lyrics switches
```

### 5.3 具体任务

#### 5.3.1 合并页面渲染进入 ViewModel

当前�?
```text
SettingsViewModel.onEvent(NavigateSettingsPage(page))
  -> renderCurrentPage(page, preferences, runtime)
  -> state/ui 更新
```

目标�?
```text
SettingsViewModel.onAction(NavigatePage(page))
  -> state = buildPageState(page, preferences, runtimeStatus)
```

执行步骤�?
1. 新增 `SettingsPageStateBuilder`�?   - �?`SettingsPageRenderController` 迁出�?mapper 逻辑�?   - 输入 `SettingsPreferencesSnapshot`、`RuntimeSettingsStatus`、`languageMode`、`page`�?   - 输出 `SettingsUiState`�?
2. �?`SettingsViewModel` 持有�?
```kotlin
data class SettingsState(
    val page: SettingsPage,
    val preferences: SettingsPreferencesSnapshot,
    val runtime: RuntimeSettingsStatus,
    val ui: SettingsUiState
)
```

3. `SettingsRenderCoordinator` 降级或删除�?
验收�?
- 设置页切换不需�?`MainActivity.navigateSettingsPage()`�?- `SettingsPageRenderController` 不再直接调用 `viewModel.updatePage`，页面滚动状态由 `SettingsViewModel.scrollState` 持有�?
#### 5.3.2 替换 string page �?typed page

当前�?
- `MainRoutes.SETTINGS_*` 字符串�?- `SettingsBackStack.parentPage(settingsPage: String)`�?
目标�?
```kotlin
sealed interface SettingsPage {
    data object Home : SettingsPage
    data object AppearanceGroup : SettingsPage
    data object Theme : SettingsPage
    data object Accent : SettingsPage
    data object Language : SettingsPage
    data object PageBackground : SettingsPage
    ...
}
```

执行步骤�?
1. 新增 `SettingsPage.kt`�?2. 新增 `SettingsPage.fromRoute(route: String)` 兼容旧路由�?3. 修改 `SettingsBackStack` 接收 `SettingsPage`�?4. Compose 设置页只发�?typed page�?5. 保留�?`MainRoutes.SETTINGS_*` 作为导航兼容层，最后删除�?
验收�?
- 新设置页不再新增裸字符串 route�?- 设置返回栈单测覆盖每�?page�?
#### 5.3.3 合并 SettingsGateway/Bindings

当前 `SettingsGatewayBindings` 有大量一行转发�?
目标�?
```kotlin
SettingsViewModel.onAction(SettingsAction.ApplyTheme(mode))
  -> applySettingUseCase(...)
  -> emit RuntimeEffect.ApplyTheme(mode)
```

需要保留的边界�?
- 打开系统权限页�?- 打开文件选择器�?- 打开目录选择器�?- 打开网络源页面�?- 打开下载页面�?
其他纯偏好保存不应通过 `SettingsGateway`�?
执行步骤�?
1. 定义�?
```kotlin
sealed interface SettingsPlatformEffect {
    data object OpenAudioFilePicker : SettingsPlatformEffect
    data object OpenAudioFolderPicker : SettingsPlatformEffect
    data object OpenFloatingLyricsPermission : SettingsPlatformEffect
    data class ChoosePageBackground(val page: PageBackgroundTarget) : SettingsPlatformEffect
    data object OpenDownloads : SettingsPlatformEffect
    data object OpenNetworkSources : SettingsPlatformEffect
}
```

2. `SettingsViewModel` 对纯设置直接保存�?3. 对平台行�?emit effect�?4. Activity/DialogHost �?collect effect�?5. 删除 `SettingsGatewayBindings` 中非平台方法�?
验收�?
- 应用主题/强调�?语言/播放速度/音量/ReplayGain 不经�?Activity�?- 平台选择器仍通过 Activity/launcher 执行�?
#### 5.3.4 建立 SettingsRepository

当前偏好�?`MusicLibraryRepository -> EchoDatabaseHelper`�?
目标�?
```kotlin
interface SettingsRepository {
    val preferences: Flow<SettingsPreferencesSnapshot>
    suspend fun save(update: SettingsPreferenceUpdate)
}
```

执行步骤�?
1. 新增 `SettingsPreferencesSnapshot`，包含：
   - themeMode
   - accentMode
   - languageMode
   - playbackSpeed
   - appVolume
   - streamingAudioQuality
   - concurrentPlaybackEnabled
   - audioEffectSettings
   - statusBarLyricsEnabled
   - floatingLyricsEnabled
   - nowPlayingGesturesEnabled
   - playbackRestoreEnabled
   - replayGainEnabled
   - shareStyle
   - pageBackgrounds

2. 新增 `DatabaseSettingsRepository`�?3. 先内部复�?`MusicLibraryRepository` �?`EchoDatabaseHelper`，后续再移出�?4. 删除 `LoadSettingsPreferencesUseCase` �?`ApplySettingsPreferenceUseCase` 中重复接口，或让它们依赖 `SettingsRepository`�?
验收�?
- 设置读写有唯一 Repository�?- `MainSettingsStore` 不再是主要状态源�?
#### 5.3.5 设置运行时副作用 owner

当前 `SettingsAppliedListenerBindings` 同时处理�?
- 更新 `MainSettingsStore`�?- 应用主题�?- 更新语言�?- �?playback service�?- �?lyrics view model�?- �?floating lyrics service�?- setStatus�?- renderSelectedTab/renderNowBar�?
目标拆分�?
```text
SettingsRuntimeApplier
  -> ThemeRuntimeApplier
  -> PlaybackRuntimeSettingsApplier
  -> LyricsRuntimeSettingsApplier
  -> FloatingLyricsRuntimeApplier
```

执行步骤�?
1. 新增 `SettingsRuntimeEffect`�?
```kotlin
sealed interface SettingsRuntimeEffect {
    data class ApplyTheme(val mode: String, val accent: String) : SettingsRuntimeEffect
    data class ApplyPlaybackSpeed(val speed: Float) : SettingsRuntimeEffect
    data class ApplyAppVolume(val volume: Float) : SettingsRuntimeEffect
    data class ApplyAudioEffects(val settings: AudioEffectSettings) : SettingsRuntimeEffect
    data class SetStatusBarLyrics(val enabled: Boolean) : SettingsRuntimeEffect
    data class SetFloatingLyrics(val enabled: Boolean) : SettingsRuntimeEffect
}
```

2. `SettingsViewModel` 保存成功�?emit effect�?3. Activity 只把 effect 交给 `SettingsRuntimeApplier`�?4. `SettingsRuntimeApplier` 不做 UI 渲染，只做运行时应用�?5. UI 刷新�?`SettingsViewModel.uiState` 自然更新�?
验收�?
- `SettingsAppliedListenerBindings` 已删除，后续不得恢复；设置应用后的状态提示通过 `SettingsEffect.ShowStatus`，在线歌词重载通过 `SettingsEffect.ReloadCurrentLyrics`�?- 应用设置不调�?`renderSelectedTab()`�?
---

## 6. 导航�?Compose Shell 迁移清单

### 6.1 当前问题

`EchoNavHostState` 同时持有�?
- 多个 ViewModel�?- 当前 tab�?- 每个页面 action 列表�?- 每个页面 labels/metrics/empty text�?- 多个 callback�?- NowPlaying 手势开关�?- 页面背景�?- 搜索和下载目�?action�?
它本质是 Activity �?Compose 的大状态包�?
### 6.2 目标

```text
EchoAppHost
  -> EchoNavGraph
      collect NavigationViewModel.state
      each Destination collect its own ViewModel.state
```

`EchoNavHostState` 最终删除，或只保留很薄的：

```kotlin
data class EchoNavHostState(
    val appChromeState: AppChromeState
)
```

### 6.3 具体任务

#### 6.3.1 Shell typed state 暂缓（不要恢复 AppShellViewModel）

2026-06-29 correction: the provisional `ShellViewModel` / `ShellState` /
`ShellAction` line has been deleted and is guarded by
`MainActivityArchitectureContractTest`. The old sketch below is historical
context only, not an active migration step. Do not reintroduce an
`AppShellViewModel` unless it replaces the current `NavigationViewModel` /
`MainRouteController` / `EchoNavHostState` path as the single shell state
source.

职责�?
- 当前 tab�?- 是否显示 onboarding�?- bottom nav labels�?- NowBar 是否显示�?- 页面背景选择�?- 全局状态消息�?
不负责：

- 曲库 action�?- 设置 action�?- 网络 action�?- 具体页面业务�?
新增�?
```kotlin
data class AppShellUiState(
    val selectedTab: TabRoute,
    val tabs: List<EchoTabItem>,
    val showOnboarding: Boolean,
    val pageBackgrounds: PageBackgrounds,
    val nowBarVisible: Boolean
)
```

验收�?
- `EchoNavGraph` 不再�?`EchoNavHostState` 读取页面 action�?- tab 切换可单测�?
#### 6.3.2 页面 action 回归页面 ViewModel ✅ 已完成（2026-06-25 验证）

验证结论：以下所有 nav* 字段均已从 `MainActivity.java` 删除，grep 无匹配。曲库 action 已类型化为 `LibraryEvent`，队列为 `QueueIntent`，各页面 ViewModel 直接持有 UiState。

补充记录：`QueueIntentBindings` 已移除；2026-06-28 进一步删除 `QueueIntentController`，`QueueViewModel.bindIntentListener(...)` 现在直接把 `QueueIntent` 分发到 `LibraryEvent`、`QueueActionController` 和页面 owner，避免队列意图再经过一层纯转发 controller。

补充记录：`StreamingTrackMatchStoreBindings` 已移除，`StreamingTrackMatchUseCase` 直接实现 `StreamingTrackMatchStore`，`StreamingViewModel`、`StreamingRecommendationViewModel` 和心跳推荐 seed resolver 共享同一个 use case/store 实例，减少 use case -> bindings -> store 的纯转发层。

补充记录：`HeartbeatRecommendationSeedResolverBindings` 已移除，`HeartbeatRecommendationSeedResolver` 直接实现 `HeartbeatSeedRequestProvider` 并接收 service/store/viewModel/library 快照 provider，心动推荐 seed 请求不再经过额外 supplier 聚合绑定层。

补充记录：`LuoxueSourceImportBindings` 已移除，`ContentResolverLuoxueSourceDocumentReader` 归入 `LuoxueSourceImportController.kt`，洛雪源导入的文档读取平台适配留在该 feature owner 内，不再单独保留一层 binding 文件。

补充记录：`LyricsStateListenerBindings` 已移除，歌词状态刷新策略改为 `LyricsStateRefreshListener` 并归入 `LyricsViewModel.kt`，now bar 刷新和播放页内容刷新仍由可测 listener 保护，但不再保留独立 bindings 文件。

补充记录：`ConfirmationDialogBindings` 已移除，`ConfirmationDialogController.Listener` 由 `MainActivity` 直接提供，播放历史清空、队列清空、网络流媒体删除和远程源删除确认不再经过独立纯转发 binding 文件。

要删除的 Activity 字段（全部已删除）：
- `navTrackListActions`
- `navTrackListHeaderMetrics`
- `navTrackListHeaderActions`
- `navTrackListEmptyText`
- `navTrackListModeActions`
- `navTrackListLabels`
- `navLibraryGroupActions`
- `navLibraryGroupEmptyText`
- `navLibraryGroupModeActions`
- `navCollectionsActions`
- `navSettingsActions`
- `navSettingsScrollState`
- `navNetworkSourceActions`
- `navNetworkSourceHeaderActions`
- `navNetworkSourceEmptyText`
- `navNetworkSourceLabels`
- `navNetworkMenuTitle`
- `navNetworkMenuMetrics`
- `navNetworkMenuActions`
- `navStreamingSearchLabels`
- `navStreamingSearchActions`
- `navSearchActions`

迁移方式�?
- `HomeDashboardViewModel` 输出 `HomeDashboardUiState` + action event�?- `LibraryViewModel` 输出 `LibraryUiState`，包�?mode actions/header actions�?- `SettingsViewModel` 输出 `SettingsUiState`，包�?settings actions�?- `NetworkSourcesViewModel` 输出 `NetworkSourcesUiState`�?- `StreamingSearchViewModel` 输出 `StreamingSearchUiState`�?
验收�?
- `syncNavHostState()` 不再�?30 多个 setter�?- 新页�?action 不需要加 Activity 字段�?
#### 6.3.3 路由状态单源化

当前�?
- `MainRouteController`
- `NavigationViewModel`
- `EchoNavHostState.selectedTabRoute`
- Compose pager state

目标�?
- `NavigationViewModel` �?Compose Navigation 是唯一事实源�?- `MainRouteController` 只保留兼容层，逐步删除�?
任务�?
1. 定义 typed route�?
```kotlin
sealed interface AppRoute {
    data object Home : AppRoute
    data object Library : AppRoute
    data object Player : AppRoute
    data object Settings : AppRoute
    data class Network(val page: NetworkPage) : AppRoute
    data class Collection(val id: Long?) : AppRoute
}
```

2. `MainRoutes.java` 字符串只作为旧桥�?3. `MainRouteController` 迁成 Kotlin，并只负�?route reducer�?4. `EchoNavGraph` 根据 `NavigationState` 渲染�?
验收�?
- 返回键逻辑单测覆盖�?- tab 切换、设置子页、网络子页、播放沉浸页不依赖业�?ViewModel 副作用�?
---

## 7. 播放模块迁移清单

### 7.1 当前问题

播放状态和命令分散�?
- `EchoPlaybackService`
- `MainPlaybackStore`
- `PlaybackViewModel`
- `NowPlayingViewModel`
- `QueueViewModel`
- `NowPlayingStateController`
- `PlaybackStateUpdateController`
- `PlaybackStateUpdateController` is now a stateless `object`; `MainActivityBase`
  no longer keeps or constructs it, and `PlaybackStateEventController` calls the
  policy directly.
- `PlaybackActionController`
- `PlaybackStartController`
- Activity 手动 publish/render

### 7.2 目标

建立播放边界�?
```kotlin
interface PlaybackController {
    val state: StateFlow<PlaybackStateSnapshot>
    val queue: StateFlow<List<Track>>
    suspend fun playQueue(tracks: List<Track>, index: Int)
    suspend fun toggle()
    suspend fun next()
    suspend fun previous()
    suspend fun seekTo(positionMs: Long)
    suspend fun setShuffle(enabled: Boolean)
    suspend fun setRepeatMode(mode: Int)
    suspend fun moveQueueItem(from: Int, to: Int)
    suspend fun removeQueueItem(trackId: Long)
}
```

Service 是实现细节：

```text
EchoPlaybackService
  -> PlaybackServiceController : PlaybackController
```

### 7.3 具体任务

#### 7.3.1 PlaybackController facade（已退役）

1. 2026-06-28：`app.yukine.playback.PlaybackController`、`PlaybackServiceController`、`FakePlaybackController` 已删除。
2. 当前生产树继续使用根包 `app.yukine.PlaybackController` 及其现行调用链。
3. 旧的 7.3.1 只保留为历史记录，不再作为当前迁移目标。
验收：
- UI/ViewModel 不再引用已退役的 playback facade。
- 播放路径继续以当前 service/adapter 边界为准。
#### 7.3.2 统一播放 UiState

目标�?
```kotlin
data class PlaybackUiState(
    val currentTrack: Track?,
    val queue: List<Track>,
    val playing: Boolean,
    val positionMs: Long,
    val durationMs: Long,
    val shuffle: Boolean,
    val repeatMode: Int,
    val lyricsLine: String,
    val waveform: WaveformState,
    val spectrum: SpectrumState
)
```

任务�?
- `NowBar`、`NowPlayingScreen`、`QueueScreen` 使用同一 `PlaybackUiState` 派生 state�?- 删除 `publishNowPlayingState` 这种 Activity 中转�?- `QueueViewModel.bind(...)` 不再每次�?Activity 手动�?state�?
验收�?
- NowBar、播放页、队列页显示一致�?- 播放进度和当前曲不依赖手�?render�?
现状与阻塞（2026-06-25 分析，详见 `docs/TASK_3_UNIFIED_PLAYBACK_STATE_ANALYSIS.md`）：
- `NowPlayingViewModel` / `QueueViewModel` 已各自持有独立 `StateFlow<UiState>`，且已以 `PlaybackStateSnapshot` 作为数据传输对象，未直接耦合 service。
- 仍为"推送式"而非"响应式"：`MainActivity` 中有约 9 处手动 `nowPlayingStateController.renderNowBar()` 调用，状态变化经 `NowPlayingStateController.publish(snapshot)` 手动推送到 ViewModel，而非 ViewModel 自动 `combine` 多个状态流。
- 阻塞点：要做成响应式（删除手动 `renderNowBar()`），需先把 7.3.1 的 `PlaybackController` 真正集成进 `MainActivity` 并由 ViewModel 注入 `combine` 播放/收藏/歌词/语言流。该集成是独立大任务，未完成前 7.3.2 不宜开工。
- 因此本节标记为"待 7.3.1 集成完成后启动"，避免半截改动破坏播放链路。

#### 7.3.3 保留服务稳定性约�?
迁移时禁止改动：

- MediaLibraryService 对外能力�?- 前台服务通知�?- 小部�?action�?- 队列恢复�?- 耳机/车机控制�?- 歌词通知�?- 音效 session 绑定�?
测试�?
- 编译�?- 播放本地文件�?- 后台播放�?- 通知栏控制�?- 小部件控制�?- 杀进程恢复�?
---

## 8. 曲库与歌单迁移清�?
### 8.1 当前问题

曲库数据、路由和 UI action 混合�?
- `MainLibraryStore` 保存可见曲目、收藏、选中歌单等�?- `LibraryViewModel` 已存在，但仍�?Activity �?`publishLibraryState`、`handleLibraryEvent`�?- `TrackListRenderController`、`LibraryGroupsRenderController`、`LibraryPlaylistsRenderController` 输出 chrome，再�?Activity 塞入 nav state�?
### 8.2 目标

```text
LibraryScreen
  -> LibraryViewModel.uiState
  -> LibraryViewModel.onAction
  -> LibraryUseCases
  -> LibraryRepository
```

歌单独立�?
```text
PlaylistListScreen / PlaylistTrackScreen
  -> PlaylistViewModel
  -> PlaylistUseCases
```

### 8.3 具体任务

#### 8.3.1 替换 MainLibraryStore

1. 定义 `LibraryState`�?
```kotlin
data class LibraryState(
    val allTracks: List<Track>,
    val visibleTracks: List<Track>,
    val favorites: Set<Long>,
    val mode: LibraryMode,
    val selectedGroup: LibraryGroup?,
    val selectedPlaylistId: Long,
    val searchQuery: String
)
```

2. `LibraryViewModel` 持有 `MutableStateFlow<LibraryState>`�?3. `MainLibraryStore` 只作为兼容读取，逐步删除�?4. `replaceLibrary`、`loadCollections`、`refreshLibraryAfterStreamingImport` 迁入 ViewModel/use case�?
验收：切换曲库模式不需要 `routeController.setLibraryMode` 后再 `renderSelectedTab`；收藏状态更新自动刷新列表和 NowBar。

迁移进展（2026-06-25）：
- ✅ `MainLibraryStore` 写接口已收敛：`setFavorite` / `toggleFavorite` / `clearPlayHistory` 等写方法移出，`MainLibraryStore` 退化为只读兼容 facade，所有读取都从 `MainActivityViewModel.library` 这一份 `MutableStateFlow<LibraryStoreState>` 派生。
- ✅ 收藏写路径改走 `MainActivityViewModel.setFavorite(...)`，播放历史清空改走 `MainActivityViewModel.clearPlayHistory()`，`PlayHistoryActionController` 通过 `PlayHistoryStateStore { viewModel.clearPlayHistory() }` 单向同步，不再回写 store。
- ✅ 契约测试 `MainActivityArchitectureContractTest` 已更新断言为 `libraryStateStore.clearPlayHistory()`，`PlayHistoryActionControllerTest` 已对齐到 `libraryStateStore` 装配。
- 🔲 仍待迁移：`replaceLibrary` / `loadCollections` / `refreshLibraryAfterStreamingImport` 仍经 store 转发到 ViewModel；完整 `LibraryState`（mode / selectedGroup / searchQuery 等）尚未独立成型，仍由 `LibraryStoreState` + 外部 store 共担。

#### 8.3.2 曲库 action typed �?
当前 action 是多�?`Runnable` / actions list�?
目标�?
```kotlin
sealed interface LibraryAction {
    data object Scan : LibraryAction
    data class Search(val query: String) : LibraryAction
    data class SelectMode(val mode: LibraryMode) : LibraryAction
    data class SelectGroup(val group: LibraryGroup) : LibraryAction
    data class ToggleFavorite(val track: Track) : LibraryAction
    data class Play(val tracks: List<Track>, val index: Int) : LibraryAction
    data class AddToPlaylist(val track: Track) : LibraryAction
}
```

验收�?
- `TrackListScreen` 不接�?`List<TrackRowActions>`，而是 `onAction(LibraryAction)`�?
#### 8.3.3 歌单独立 ViewModel

新增 `PlaylistViewModel`，负责：

- 歌单列表�?- 收藏歌单虚拟入口�?- 播放历史�?- 最�?最多播放�?- 歌单详情�?- 添加、移除、移动、重命名、删除�?- M3U 导入导出请求�?
验收�?
- 歌单 dialog 结果直接回到 `PlaylistViewModel.onAction`�?- Activity 不再处理 `createPlaylist`、`renamePlaylist`、`deletePlaylist`、`moveSelectedPlaylistTrack`�?
迁移进展（2026-06-25）：
- ? ?? CRUD ?"? Activity ?"????`MainActivity` ???? `createPlaylist` / `renamePlaylist` / `deletePlaylist` / `addTrackToPlaylist` / `moveSelectedPlaylistTrack` / `removeSelectedPlaylistTrack` ?????`PlaylistDialogController` ?????????
- ? CRUD ?????? `LibraryViewModel`?`createPlaylist/renamePlaylist/deletePlaylist/addTrackToPlaylist/moveSelectedPlaylistTrack` + presentation ????`MainActivity` ??????????????????? collections reload?`PlaylistDialogController` ??????
- ?? ?????????????**??** `LibraryViewModel` ???????? `PlaylistViewModel`????????????????????????????????????
- 判定：本节"Activity 不再处理 CRUD"验收项已满足；"新增独立 PlaylistViewModel"作为长期目标保留。

---

## 9. 流媒体迁移清�?
已有 `docs/STREAMING_VIEWMODEL_SPLIT_PLAN.md`，这里给接手版拆分点�?
### 9.1 当前问题

`StreamingViewModel` 已经从旧全局 ViewModel 中拆出，但仍偏大，职责包含：

- provider 列表和状态�?- 搜索�?- 登录/认证�?- Cookie 导入�?- 歌单导入�?- 播放 URL 解析�?- 推荐�?- 音质选择状态参与�?- 与本地歌�?匹配信息交互�?
### 9.2 目标拆分

建议拆成�?
| �?owner | 职责 |
|---|---|
| `StreamingProviderViewModel` | provider 列表、选择、登录状�?|
| `StreamingSearchViewModel` | 在线搜索、分页、搜索结果、搜索错�?|
| `StreamingAuthViewModel` | 登录、Cookie、本�?auth store、认证回�?|
| `StreamingPlaylistImportViewModel` | 账号歌单、URL/源歌单导入、本地落�?|
| `StreamingPlaybackResolver` | 播放 URL 解析、质量降级、预解析 |
| `StreamingRecommendationViewModel` | 每日推荐、心动推荐、推荐种�?|

### 9.3 具体任务

1. 先拆纯搜索�?   - 输入：query、provider、media type�?   - 输出：`StreamingSearchUiState`�?   - 保留�?`StreamingViewModel` facade 一轮�?
2. 再拆认证�?   - `StreamingAuthLauncher` 仍是平台 owner�?   - ViewModel �?emit `LaunchAuth(provider)` effect�?
3. 再拆歌单导入�?   - 导入确认 dialog 变成 effect�?   - 导入结果落到 `PlaylistRepository`�?
4. 最后拆播放解析�?   - `StreamingPlaybackResolver` 不依�?Activity�?   - 播放入口通过 `PlaybackController.playQueue`�?
验收�?
- 新增 provider 不改 `MainActivity.java`�?- 搜索测试不需要构造完�?`StreamingViewModel`�?- 登录测试不需要播�?controller�?- 播放解析测试不需�?UI�?
---

## 10. 下载模块迁移清单

### 10.1 当前问题

下载已有 `DownloadsViewModel`，但下载触发、音质选择、结�?status 仍有 Activity 参与�?
### 10.2 目标

```text
DownloadAction
  -> DownloadsViewModel
  -> TrackDownloadUseCase
  -> TrackDownloadManager
  -> DownloadUiState / DownloadEffect
```

### 10.3 具体任务

1. 新增 `DownloadRequest`�?   - current track
   - playlist
   - current list
   - with quality

2. `DownloadsViewModel` 负责�?   - 创建下载请求�?   - 暂停/继续/全部暂停/全部继续�?   - 打开下载目录 effect�?   - 下载状态消�?effect�?
3. Activity 只处理：
   - 目录选择平台 effect�?   - Android system permission/platform result�?
4. 下载音质 dialog 迁入 effect�?
验收�?
- `downloadTrackWithQuality`、`downloadTracks`、`showDownloadQualityDialog` 不在 Activity�?- 下载状态可单测�?
迁移进展�?026-06-24）：

- 下载触发、下载音质选择、未解析流媒体下载地址解析与入队刷新已迁入 `DownloadRequestController`�?- 下载音质 `EchoDialog` 渲染已迁�?`DownloadQualityDialogController`；`MainActivity` 只负责装�?chooser / resolver / status sink�?- `TrackDownloadManager` 通过 `TrackDownloadRequestQueue` 暴露下载入队与状态能力，便于 `DownloadRequestControllerTest` 使用 fake 队列覆盖普通单曲、未解析流媒体单曲、歌单批量下载路径�?- 下载目录预设切换与“选择目录”入口已迁入 `DownloadsViewModel` action / `DownloadsEffect.OpenDirectoryPicker`；`DownloadsDestination` 只消�?state/effect，目录能力由 `TrackDownloadDirectoryController` 暴露�?- `MainActivity` 已不再包�?`downloadTrackWithQuality`、`downloadTracks`、`enqueueTrackDownload`、`showDownloadQualityDialog`、`DownloadQualityCallback` 等下载业务方法�?- 歌单 CRUD / 曲目移除 / 曲目移动 / 添加到歌单的结果处理入口已继续收敛到 `MainActivity` ????：`MainActivity` 不再保留 `createPlaylist(...)`、`renamePlaylist(...)`、`deletePlaylist(...)`、`removeSelectedPlaylistTrack(...)`、`moveSelectedPlaylistTrack(...)`、`addTrackToPlaylist(...)` 这组纯业务转发方法；Dialog/Collections 入口直接调用 `MainActivity` ????，由其进�?`LibraryViewModel` 并统一发布状态、选中歌单�?collections reload�?- 歌单 dialog 入口继续下沉�?`PlaylistDialogController`：Library / Queue / Collections / NowPlaying 入口现在直接调用 `showAddToPlaylist(track)`，当前歌单列表由 `PlaylistProvider` 提供，无歌单时默认添加分支也�?dialog controller 回调 `addToDefaultPlaylist(track)`；创建、重命名、删除确认入口也直接进入 `PlaylistDialogController`，`MainActivity` 不再保留 `showCreatePlaylistDialog(...)`、`showRenamePlaylistDialog(...)`、`showAddToPlaylistDialog(...)`、`confirmDeletePlaylist(...)` 这组 dialog wrapper�?- 队列清空确认入口保留�?`QueueActionController.confirmClearQueue()` 做空队列判断，确认弹窗由 `QueueActionBindings` 进入 `ConfirmationDialogController.confirmClearQueue()`，确认后的执行动作通过 `ConfirmationDialogBindings` 调用 `QueueActionController.clearQueue()`；`MainActivity` 不再保留 `confirmClearQueue()` / `clearQueue()` 纯转发方法�?- 播放历史清空结果处理已迁�?`PlayHistoryActionController`：Collections 入口直接打开 `ConfirmationDialogController.confirmClearPlayHistory()`，确认后的执行动作通过 `ConfirmationDialogBindings` 调用 `PlayHistoryActionController.clearPlayHistory()`，由其进�?`LibraryViewModel.clearPlayHistory(...)`、同�?`MainLibraryStore`、发布状态并刷新 collections；`MainActivity` 不再保留 `confirmClearPlayHistory()` / `clearPlayHistory()` 业务 wrapper�?- 网络流媒体编辑和删除确认的薄转发继续移除：TrackList 入口直接调用 `NetworkDialogController.showEditStream(...)` / `ConfirmationDialogController.confirmDeleteTrack(...)`，分组删除直接调�?`ConfirmationDialogController.confirmDeleteTracks(...)`，确认后的删除动作直接进�?`NetworkRequestController.deleteAllStreams/deleteTrack/deleteTracks/deleteRemoteSource`；`MainActivity` 不再保留 `showEditStreamDialog(...)`、`confirmDeleteTrack(...)`、`confirmDeleteTracks(...)`、`deleteAllStreams()`、`deleteTrack(...)`、`deleteTracks(...)`、`deleteRemoteSource(...)` 这组网络业务 wrapper�?- WebDAV 远程源测�?同步已由 `NetworkSourcesEventController` 直接调用 `NetworkRequestController.testRemoteSource(...)` / `syncRemoteSource(sourceId, remoteSourceName)`，源名称�?`NetworkSourcesLibrarySourceBindings` 提供；远程源曲目列表页的同步 action 也直接进�?`NetworkRequestController.syncRemoteSource(sourceId, remoteSourceName(sourceId))`；`MainActivity` 中不再保�?`testRemoteSource(...)` / `syncRemoteSource(...)` wrapper�?
---

## 11. 歌词模块迁移清单

### 11.1 当前问题

`LyricsViewModel` 已存在，但歌�?reload、当前曲监听、通知/悬浮歌词开关仍�?Activity/Service/Settings 多点连接�?
### 11.2 目标

```text
LyricsViewModel
  -> LoadTrackLyricsUseCase
  -> LyricsRepository
  -> LyricsUiState
```

运行时发布：

```text
PlaybackController.currentTrack
  -> LyricsViewModel.load(track)
  -> LyricsPublisher
      -> notification lyrics
      -> floating lyrics
```

### 11.3 具体任务

1. `LyricsReloadController` 合并�?`LyricsViewModel` �?`LoadLyricsForCurrentTrackUseCase`�?2. `SettingsViewModel` 不直接控制歌�?ViewModel，改�?`LyricsSettingsChanged` effect�?3. `FloatingLyricsService` 启停�?`FloatingLyricsRuntimeApplier` 处理�?4. `LiveLyricsNotificationService` 保持平台边界�?
验收�?
- 改歌词偏移只影响 Lyrics state，不触发全页�?render�?- 当前曲变化自动加载歌词�?- 无歌词时通知/悬浮歌词优雅降级�?
迁移进展�?026-06-24）：

- `LyricsReloadController` / `LyricsReloadBindings` 已合并进 `LyricsViewModel.bindReloadGateway(...)` �?`LyricsViewModel.reloadCurrentLyrics(...)`；设置页重新加载歌词现在直接进入歌词 ViewModel，状态提示仍通过绑定�?status sink 输出�?- `SettingsActionController` 保留设置入口和睡眠计时边界，但不再通过 listener 转发歌词 reload；`SettingsActionBindings` 已移�?`lyricsReloader`�?
---

## 12. 推荐模块迁移清单

### 12.1 当前问题

每日推荐已收敛到 `StreamingRecommendationViewModel` + typed `RecommendationAction`；心动推荐、种子选择、播放入口仍有部分兼容边界：

- `HeartbeatRecommendationController`
- `StreamingViewModel`
- `MainActivity` helper 方法
- `playbackStartController`

### 12.2 目标

```text
RecommendationViewModel
  -> RecommendationUseCases
  -> StreamingRepository / LibraryRepository
  -> PlaybackController
```

### 12.3 具体任务

1. 定义�?
```kotlin
sealed interface RecommendationAction {
    data class PlayDaily(val provider: StreamingProviderName) : RecommendationAction
    data class PlayHeartbeat(val provider: StreamingProviderName) : RecommendationAction
}
```

2. 建立 `RecommendationSeedResolver`�?   - 当前播放歌曲�?   - 当前队列�?   - 当前歌单�?   - 收藏/最近播放�?
3. `RecommendationViewModel` 输出�?   - loading�?   - empty status�?   - ready status�?   - playable tracks�?
4. 播放通过 `PlaybackController`，不�?Activity�?
验收�?
- `heartbeatRecommendationSeedRequest` 不在 Activity�?- 推荐失败、空结果、成功播放可单测�?
迁移进展�?026-06-24）：

- 每日推荐已先拆到 `StreamingRecommendationViewModel` + `StreamingDailyRecommendationUseCase`：provider 选择、loading/empty/ready 状态、仓库拉取和 presentation 生成不再�?`DailyRecommendationController` 直接调用 `StreamingViewModel`�?- `DailyRecommendationController` / `DailyRecommendationBindings` 兼容壳已删除；每日推荐入口只�?typed `RecommendationAction.PlayDaily` -> `StreamingRecommendationViewModel.onAction(...)` -> presentation callback，且 `DailyRecommendationPlayer` 已由 NOTE 63 删除�?- `MainActivity` 仅保�?ViewModel 接线与播�?presentation 的平台尾部；provider 刷新后同步给 `StreamingRecommendationViewModel.updateProviders(...)`�?- 已补 `StreamingRecommendationViewModelTest`、`RecommendationActionControllerTest` 和架构契约；�?`DailyRecommendationControllerTest` / `DailyRecommendationBindingsTest` 已随兼容壳删除。下一步继续收敛心动推荐续�?refill，或�?dialog/settings 切片继续减少 `MainActivity` UI/platform 细节�?- 心动推荐种子请求已拆�?`HeartbeatRecommendationSeedResolver` / `HeartbeatRecommendationSeedResolverBindings`：候选队列合并、随机候选、direct seed、miss 诊断不再�?`StreamingViewModel.prepareHeartbeatRecommendationSeedRequest(...)` �?`MainActivity.heartbeatRecommendationSeedRequest(...)` 承担；`MainActivity` 只提�?service/store/viewModel/library context 快照来源�?- 心动推荐 owner 已继续迁�?`StreamingRecommendationViewModel` / `HeartbeatRecommendationPlayer`：`HeartbeatRecommendationController` 不再依赖整颗 `StreamingViewModel`，`resolveHeartbeatRecommendationSeed(...)`、`fetchHeartbeatRecommendations(...)`、refill/loading 状态、playlist/append presentation �?track-match store 绑定均由推荐 VM 持有；推荐播放尾部已收敛�?`PlaybackStartController.playRecommendation(...)` / `playHeartbeatRecommendation(...)` �?presentation 边界，`MainActivity` 不再保留 `playHeartbeatRecommendationTracks(...)` �?`openRecommendationPlaylist(...)` 兼容 helper�?- 推荐入口已整理为 typed `RecommendationAction` 并下沉到 `StreamingRecommendationViewModel.onAction(...)`：首页与流媒体页发�?`PlayDaily(provider)` / `PlayHeartbeat(provider)` action；`MainActivity` 不再持有 `DailyRecommendationController`�?- 推荐播放启动边界已先引入小型 `PlaybackController` facade：`PlaybackStartController` 不再直接持有 `resolveAndPlayStreamingTrack` / `playTrackList` / `applyPlaybackActionResult` 三段 listener 回调，而是通过 `PlaybackController.playTrackList(...)` 进入 `PlaybackStartControllerAdapter`；MainActivity 只装�?adapter �?`StreamingPlaybackController` + `NowPlayingViewModel` + result applier。该 facade 已由 NOTE 47 删除�?- 播放缓存已改为当前播放优先的并发切片调度：当前歌曲首段缓存使用最高优先级并立即启动，当前歌曲后续切片其次，下一首轻量预取最低；切换当前歌曲时会取消�?`CacheWriter`，下一�?URL 预解析使用独立低优先级通道，不再挡住用户刚点击的当前歌曲解析�?- now-playing 状态发布入口再收一层：`MainActivity` 已不再保�?`publishNowPlayingState(...)` 这类�?wrapper，事件回放时直接交给 `NowPlayingStateController.publish(snapshot)`；`NowPlayingStateController` 继续作为 UI state/floating lyrics 的事实源�?
---

## 13. 数据层迁移清�?
### 13.1 当前问题

`MusicLibraryRepository` 承担过多�?
- 曲库�?- 歌单�?- 设置�?- 播放队列�?- 远程源�?- WebDAV�?- 部分播放设置�?
### 13.2 目标 Repository

| Repository | 职责 |
|---|---|
| `SettingsRepository` | 所有设置偏�?|
| `LibraryRepository` | 本地曲库、扫描、搜索、分�?|
| `PlaylistRepository` | 歌单 CRUD、歌单曲目排序、M3U 导入导出 |
| `PlaybackQueueRepository` | 队列持久化、恢�?|
| `RemoteSourceRepository` | WebDAV/远程�?|
| `LyricsRepository` | 本地/在线歌词 |
| `StreamingRepository` | provider/gateway/search/playlists |
| `DownloadRepository` | 下载记录、下载状�?|

### 13.3 具体任务

1. 先只加接口，不搬 DB�?
```kotlin
interface SettingsRepository { ... }
interface PlaylistRepository { ... }
interface LibraryRepository { ... }
```

2. 实现类内部暂时委�?`MusicLibraryRepository`�?3. ViewModel/UseCase 改依赖新接口�?4. 当调用方迁完后，�?`MusicLibraryRepository` 拆薄�?
验收�?
- 设置 feature 不依�?`MusicLibraryRepository`�?- 歌单 feature 不依赖曲库扫描方法�?- 远程�?feature 不依赖设置方法�?
---

## 14. 测试迁移清单

### 14.1 必须新增�?fake

- `FakeSettingsRepository`
- `FakeLibraryRepository`
- `FakePlaylistRepository`
- `FakePlaybackController`
- `FakeStreamingRepository`
- `FakeLyricsRepository`
- `FakePlatformEffectCollector`
- `FakeStatusMessageSink`

### 14.2 每个 feature 必测

每个 ViewModel 至少覆盖�?
- 初始 state�?- loading state�?- 成功 state�?- �?state�?- 错误 state�?- 用户 action�?- 一次�?effect�?- 语言切换或文�?key 映射�?
### 14.3 回归命令

轻量编译�?
```powershell
.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain
```

单测�?
```powershell
.\gradlew.bat :app:testDebugUnitTest --console=plain
```

打包�?
```powershell
.\gradlew.bat :app:assembleDebug --console=plain
```

完整检查：

```powershell
.\gradlew.bat :app:check --console=plain
```

### 14.4 关键手动回归

- 首次启动引导�?- 扫描本地音乐�?- 播放本地歌曲�?- 后台播放�?- 通知栏控制�?- 桌面小部件控制�?- 播放队列恢复�?- 歌词加载和偏移�?- 状态栏/悬浮歌词�?- 流媒体登录�?- 在线搜索和播放�?- 歌单导入�?- 下载暂停/继续�?- 设置主题/语言/音效/ReplayGain�?- 备份导入导出�?
---

## 15. 推荐执行顺序

### 15.1 第一批：低风险、收益高

1. �?`BackgroundImagePickerController`�?2. �?`BackupRestoreLauncher`�?3. �?`StatusMessageViewModel` 或统一 status effect�?4. 设置�?typed `SettingsPage`�?5. `SettingsPageStateBuilder` 替代 render controller 的纯 mapper�?
预期结果�?
- 新设置不再加�?Activity�?- 设置迁移成为后续 feature 样板�?
### 15.2 第二批：去掉 Activity 手动同步

1. `SettingsViewModel` 直接拥有设置 page state�?2. 删除 `SettingsRenderCoordinator`�?3. 删除大部�?`SettingsGatewayBindings`�?4. `EchoNavHostState` 不再保存 settings actions�?
预期结果�?
- 设置 feature 完整 MVVM�?
### 15.3 第三批：播放状态单源化

1. 引入 `PlaybackController`�?2. `PlaybackViewModel` 收敛播放 state�?3. NowBar/NowPlaying/Queue 使用同一 state�?4. Activity 不再 publish now playing state�?
预期结果�?
- 播放 UI 行为更稳定，测试更好写�?
### 15.4 第四批：曲库/歌单

1. `LibraryViewModel` 接管 `MainLibraryStore`�?2. `PlaylistViewModel` 接管歌单 CRUD/dialog result�?3. 删除 `navTrackList*`、`navLibraryGroup*`、`navCollectionsActions`�?
预期结果�?
- 曲库新增功能不碰 Activity�?
### 15.5 第五批：流媒体拆�?
1. 搜索拆出�?2. 认证拆出�?3. 歌单导入拆出�?4. 播放解析拆出�?5. 推荐拆出�?
预期结果�?
- `StreamingViewModel` 不再是第二个上帝 ViewModel�?
### 15.6 第六批：导航独立

2026-06-29 correction: this old navigation batch is not the current next
step. `AppShellViewModel` was removed after review because it would create a
parallel shell model. Keep the current route/nav owners until a future slice
can make typed shell state the single runtime source and update the
architecture contract in the same slice.

1. `AppShellViewModel`�?2. typed `AppRoute`�?3. 删除 `MainRouteController` 或降级为兼容 adapter�?4. 删除 `EchoNavHostState` 大状态包�?
预期结果�?
- Compose shell 真正�?ViewModel/Navigation state 驱动�?
---

## 16. 每次接手前检查表

开始前�?
- [ ] `git status --short`，确认他人改动�?- [ ] 读本文件对应章节�?- [ ] 读相�?feature ViewModel/Controller/Bindings�?- [ ] 写出当前事件路径�?- [ ] 写出目标事件路径�?- [ ] 明确状态事实源�?- [ ] 明确哪些旧类保留兼容�?
改动中：

- [ ] 不新增万�?manager/coordinator�?- [ ] 不新增只转发一行的 binding�?- [ ] 不把平台 API 放进 ViewModel�?- [ ] 不让 Repository 返回 UI 文案�?- [ ] 不让 Activity 新增业务分支�?- [ ] 不扩大播放服务职责�?
提交前：

- [ ] 编译通过�?- [ ] 对应 ViewModel/use case 测试通过�?- [ ] 关键手动路径不变�?- [ ] 删除不再使用的旧转发�?- [ ] 更新架构契约测试�?- [ ] 如果迁移了公共行为，更新本文件或相关 handoff�?
---

## 17. 完成定义

这轮 MVVM 迁移完成时应满足�?
- `MainActivity` 只负责宿主、launcher 注册、生命周期、Compose root、service bind�?- 业务事件不再回流�?Activity 编排�?- `EchoNavHostState` 被删除或变成极薄 app shell state�?- `MainSettingsStore`、`MainLibraryStore`、`MainPlaybackStore` 删除或只作为兼容 facade�?- 设置、曲库、歌单、播放、队列、歌词、流媒体、下载、推荐都有独�?ViewModel 和测试�?- 播放 service 通过 `PlaybackController` 暴露能力�?- 新增一个普通功能不需要修�?`MainActivity.java`�?- 新增一个设置不需要修改超�?3 个核心文件�?- 没有新的上帝类、上帝进程、万能协调器�?
2026-06-24 note: streaming search chrome state now lives in StreamingViewModel and is consumed by NetworkDestination directly; EchoNavHostState no longer mirrors streamingSearchLabels / streamingSearchActions.
2026-06-25 note: EchoNavHostState no longer mirrors nowPlayingGesturesEnabled / pageBackgrounds; EchoNavGraph reads both directly from SettingsViewModel.state.preferences and MainActivity no longer syncs them manually.
2026-06-25 note: openSearchAction was removed from EchoNavHostState; EchoNavGraph now derives the local search navigation callback itself and passes it to the library destinations.
2026-06-25 note: openDownloadDirectoryPickerAction / closeNowPlayingAction / nowPlayingEventHandler were also moved out of EchoNavHostState; EchoNavGraph/EchoNavHostBridge now receive those shell callbacks directly from EchoAppHost/MainActivity.
2026-06-25 note: openLibraryModeFromHome/openCollectionsFromHome/openSearchFromHome/openFavoritesCollectionFromLibrary thin wrappers were collapsed into the render bindings, keeping MainActivity as a direct callback host instead of a forwarding layer.
2026-06-25 note: openLibraryModeFromHome/openCollectionsFromHome/openSearchFromHome/openFavoritesCollectionFromLibrary thin wrappers were removed from MainActivity; call sites now enter route/navigation or library event paths directly.
2026-06-25 note: syncRouteFieldsFromViewModel was removed; MainRouteController already restores its initial state, so MainActivity no longer keeps a one-off route restoration wrapper.
2026-06-25 note: selectPlaylistFromCollections was removed; the collections binding now writes the selected playlist and reloads collections inline instead of going through a one-off MainActivity helper.
2026-06-25 note: openSelectedPlaylistExportDocument was removed; the collections binding now called PlaylistExportController directly with the selected playlist snapshot. This was later simplified by NOTE 64.
2026-06-25 note: finishOnboarding/openStreamingFromOnboarding now share completeOnboarding as the common shell tail, reducing repeated onboarding cleanup in MainActivity.
2026-06-25 note: searchViewModel was removed from EchoNavHostState; EchoAppHost now provides it as a direct NavHost bridge parameter, keeping the shell state focused on shared navigation state only.
2026-06-25 note: openNowPlayingImmersive was moved out of EchoNavHostState and now lives as local state inside EchoNavGraph, since it only serves the current shell session's immersive handoff.



2026-06-25 note: persistRouteFields was removed; MainActivity now calls MainRouteController.persist() directly at render/navigation persistence boundaries instead of keeping a forwarding-only helper.
2026-06-25 goal alignment note: the project audit report from C:\Users\31283\.codex\attachments\18684c6a-7b8d-4bc7-bd9b-430f25ee48f6\pasted-text.txt is now part of the migration target interpretation.
2026-06-25 goal alignment note: treat runtime stability, startup/playback smoothness, streaming/cache correctness, and shell-to-ViewModel ownership cleanup as higher priority than headline completion percentages.
2026-06-25 note: dead recommendation-stream code removed. `MainLibraryStore` lost its only mutable state fields (`recommendationStreamTitle` / `recommendationStreamTracks`) and the 5 methods around them (`showRecommendationStreamList` / `clearRecommendationStreamList` / `hasRecommendationStreamList` / `recommendationStreamTitle()` / `recommendationStreamTracks()`); the sole writer `showRecommendationStreamList` had zero call sites (the populate path was dropped when daily recommendations moved to StreamingRecommendationViewModel), so `hasRecommendationStreamList()` was always false and the NetworkRenderCoordinator recommendation branch + `NetworkTrackListRenderController.renderRecommendationStreamList` were dead. Removing them also retired the now-unused `Listener.playTrackList` interface method, the `NetworkTrackListRenderBindings.playTrackListAction` param, and two no-op `clearRecommendationStreamList()` calls in MainActivity (navigateNetworkPage / navigateToNetworkTabPage simplified). `MainLibraryStore` is now a pure read-only facade over MainActivityViewModel state (only `combinedSearchUseCase` remains, a stateless val), matching the 8.3.1 target. Verified: compile + full :app:testDebugUnitTest green.
2026-06-25 goal alignment note: use the audit as a prioritization input, not as a source of truth for exact percentages; validate each claimed milestone against current code, tests, and runtime behavior before marking it complete.
2026-06-25 note: LibraryTrackListDestination and LibraryGroupsDestination no longer carry legacy host-injected chrome parameters; both now render directly from LibraryViewModel StateFlow snapshots, keeping the Compose library boundary aligned with ViewModel-owned state.
2026-06-25 note: MainActivity no longer funnels library UI events through handleLibraryEvent/publishLibraryState; library render/queue/collections/now-playing call a narrower dispatchLibraryEvent path, while syncLibraryViewModelState is kept only as the explicit route+library snapshot sync boundary and favorites now read live IDs through LibraryFavoriteIdsProvider.
2026-06-25 note: dispatchLibraryEvent has now been removed as well; library render/queue/collections/now-playing event sinks call LibraryViewModel.onEvent directly, while syncLibraryViewModelState remains the only explicit MainActivity-to-LibraryViewModel snapshot sync boundary.
2026-06-25 note: syncLibraryViewModelState and LibraryViewModel.uiState/updateState have now been removed; production library rendering relies on LibraryViewModel track/group StateFlows plus direct event sinks, while favorite toggles read live IDs from MainLibraryStore through LibraryFavoriteIdsProvider instead of maintaining a separate route/library summary mirror.
2026-06-25 note: PlaybackController facade landed (PlaybackController.kt + PlaybackServiceController.kt) with a FakePlaybackController test double and FakePlaybackControllerTest; Activity/ViewModel integration is deferred to a follow-up so the existing service path stays untouched until 7.3.2 starts. See docs/MVVM_MIGRATION_PROGRESS_2026-06-25.md.
2026-06-25 note: MainLibraryStore write接口 (setFavorite/toggleFavorite/clearPlayHistory) removed; the store is now a read-only compatibility facade over MainActivityViewModel.library, with favorite writes going through MainActivityViewModel.setFavorite and play-history clears through MainActivityViewModel.clearPlayHistory via PlayHistoryStateStore.
2026-06-25 note: fixed two stale test seams left by the write-path move — PlayHistoryActionControllerTest now wires libraryStateStore = PlayHistoryStateStore { viewModel.clearPlayHistory() }, and MainActivityArchitectureContractTest asserts libraryStateStore.clearPlayHistory(). Full :app:testDebugUnitTest --rerun-tasks is green (690 tests).
2026-06-25 note: observed two flaky tests on the first full-suite pass (LyricsViewModelTest.loadPublishesLoadedLyricsState, StreamingViewModelTest.preResolveStreamingQueueWindowResolvesUpcomingTracksAfterNextTrack); both pass in isolation and on clean rerun, tracked as a separate follow-up (coroutine dispatcher timing / shared-state sensitivity), not caused by this session's changes.
2026-06-25 verification note: compileDebugKotlin+Java BUILD SUCCESSFUL; :app:testDebugUnitTest --rerun-tasks BUILD SUCCESSFUL (690 tests) on the current working tree, re-verified per the handoff requirement rather than relying on earlier reports.
2026-06-26 note: BackgroundImagePickerController was refined into a clearer two-stage platform owner. The preview screen now always uses the original picked image for zoom/pan fidelity, while persistence still happens only after Apply via an app-private compressed copy plus BackgroundTransform. This keeps the Activity free of picker/preview/save orchestration detail, preserves restart-safe backgrounds, and avoids regressing the user-visible preview quality. 2026-06-28 update: `BackgroundLanguageModeProvider` and `BackgroundTransformProvider` were removed; the controller now receives direct `() -> String` and `(String) -> BackgroundTransform` dependencies for preview language and transform lookup.
2026-06-26 note: DocumentPickerController now owns its Activity Result launcher instead of using request codes through MainActivity.onActivityResult. Audio import, folder import, download directory, M3U import/export, playlist M3U import, and Luoxue source import still share the same focused picker owner, while MainActivity no longer overrides onActivityResult for document picking. DocumentPickerControllerTest covers launch intents, selected URI dispatch, export URI dispatch, and canceled results.
2026-06-26 note: Track sharing now has a platform owner in TrackShareLauncher. NowPlaying effects call trackShareLauncher.share(track), while chooser creation, native QQ/WeChat music-card attempts, share-style lookup, and share failure/status mapping are no longer implemented in MainActivity. 2026-06-28 update: `TrackShareLanguageProvider` and `TrackShareStyleProvider` were removed; `TrackShareLauncher` now receives direct `() -> String` dependencies for language and share style. TrackShareLauncherTest covers null track feedback, missing share service, native platform-card success, chooser launch, and startActivity failure fallback.
2026-06-26 note: MainPermissionController now owns permission requests through ActivityResultContracts.RequestMultiplePermissions. AppPermissions only computes missing audio/notification permissions, and MainActivity handles the load-library/onboarding remount result tail inline without a separate permission-result adapter. MainPermissionControllerTest covers launch/no-launch behavior and callback dispatch.
2026-06-27 note: CollectionsReloader was removed as a one-hop shim. PlayHistoryActionController now takes a direct reload Runnable, so play-history clearing flows viewModel -> store -> reload without a dedicated forwarding owner.
2026-06-27 note: StreamingAuthCallbackBindings was removed. StreamingAuthCallbackController now owns URI parsing, manual-cookie routing, and auth completion directly from MainActivity wiring, so streaming auth callback handling no longer passes through a forwarding-only adapter.
2026-06-26 note: BackupRestoreBindings was removed as a forwarding-only adapter. 2026-06-28 update: BackupRestoreLauncher now accepts a direct `(String) -> Unit` status-key dependency, so backup result status keys go from the launcher to StatusMessageController without an extra binding or sink wrapper.
2026-06-26 note: DialogLanguageProviderBindings was removed. NetworkDialogController, PlaylistDialogController, and ConfirmationDialogController now share the same DialogLanguageProvider contract, so MainActivity can pass one language lambda directly instead of adapting it through a binding class.
2026-06-26 note: NetworkMenuChromeBindings was removed. Superseded on 2026-06-28 by NOTE 59: NetworkMenuContentSink is also gone, and NetworkMenuEventController updates NetworkMenuViewModel directly instead of routing network menu chrome state through a forwarding binding or sink.
2026-06-26 note: StreamingSearchChromeBindings was removed. StreamingSearchEventController now accepts a narrow StreamingSearchContentSink, and MainActivity wires StreamingViewModel.updateStreamingSearchChrome directly instead of routing streaming search chrome state through a forwarding binding.
2026-06-26 note: MainTabRendererBindings was removed. MainTabRenderDispatcher now owns the tab-to-render-callback mapping directly through Runnable callbacks, so MainActivity no longer constructs a forwarding renderer adapter for selected-tab rendering.
2026-06-26 note: StatusMessageHostBindings was removed. StatusMessageController now accepts a direct language-mode supplier and RawStatusUpdater, while the remaining raw status callback contract lives in StatusMessageContracts.kt for feature paths that still need it.
2026-06-26 note: StreamingGatewayHostBindings was removed. StreamingGatewayEventController now accepts its language provider, selected-tab render action, and status sink directly, so gateway endpoint apply/refresh no longer route through a forwarding host adapter.
2026-06-26 note: NetworkMenuPlayerBindings and NetworkSourcesPlayerBindings were removed. NetworkMenuEventController and NetworkSourcesEventController now receive the shared TrackListPlaybackAction directly, so network playback no longer routes through tiny rename-only adapters.
2026-06-26 note: NetworkMenuDialogBindings was removed. NetworkMenuEventController now receives the add stream, import M3U, and add WebDAV dialog launchers directly as Runnable callbacks from MainActivity, so the network dialog path no longer depends on a forwarding adapter.
2026-06-26 note: NetworkLibrarySourceBindings was removed. NetworkMenuEventController now reads stream tracks, stream counts, WebDAV tracks, and remote sources directly from MainLibraryStore method references, while NetworkSourcesEventController now reads remote source names and source tracks directly from MainLibraryStore method references.
2026-06-26 note: NetworkSourcesRenderBindings was removed. MainActivity now passes NetworkSourcesEventController directly into NetworkSourcesRenderController, so the network sources render path no longer needs an extra one-hop listener wrapper.
2026-06-26 note: PlaybackServiceHostBindings was removed. MainActivity now builds PlaybackServiceHostController with an anonymous Host implementation inline, so playback service attachment, store reset, and post-connect render hooks stay owned by the activity boundary without a forwarding binding.
2026-06-29 note: PlaybackServiceHostController remains the service connect/disconnect policy owner, but MainActivityBase no longer retains it as a field; it is constructed locally and passed directly into PlaybackServiceConnectionController. Later 2026-06-29 update: the anonymous `PlaybackServiceHostController.Host` block moved into `MainPlaybackServiceHost`, provided by `PlaybackUiModule`; MainActivityBase only supplies service/store/render lambdas.
2026-06-29 note: Network render controllers remain real render owners under NetworkRenderCoordinator, but MainActivityBase no longer keeps NetworkMenuRenderController, NetworkTrackListRenderController, NetworkSourcesRenderController, or StreamingSearchRenderController as fields.
2026-06-26 note: PermissionResultBindings was removed. 2026-06-29 update: permission result loading no longer lives inline in an anonymous `MainPermissionController.Listener`; `MainPermissionListener` now owns the permission success path and is provided by `PlatformModule`.
2026-06-29 note: Playback state event listener policy moved out of the Java base anonymous `PlaybackStateEventController.Listener` block into `MainPlaybackStateEventListener`. `MainActivityBase` now injects `MainPlaybackStateEventListenerFactory`, passes a lambda `ServiceQueueSource`, and leaves playback snapshot handling in `PlaybackStateEventController`.
2026-06-29 note: Track-list render listener policy moved out of the Java base anonymous `TrackListRenderController.Listener` block into `MainTrackListRenderListener`, provided by `LibraryModule`. Track-list play/favorite/playlist/download/edit/delete actions and chrome-state copying are now covered by `MainTrackListRenderListenerTest`.
2026-06-29 note: Queue render listener policy moved out of the Java base anonymous `QueueRenderController.Listener` block into `MainQueueRenderListener`, provided by `PlaybackUiModule`. Queue playback/favorite/add/remove/clear/back delegates are now covered by `MainQueueRenderListenerTest`, and the legacy no-op queue chrome callback is preserved in the Kotlin owner.
2026-06-29 note: Recommendation action callback policy moved out of the Java base anonymous `RecommendationActionCallbacks` block into `MainRecommendationActionCallbacks`, provided by `StreamingModule`. Daily recommendation playback, heartbeat seed lookup, heartbeat playback, seed-miss logging, and status routing now have focused `MainRecommendationActionCallbacksTest` coverage.
2026-06-29 note: Home dashboard render listener policy moved out of the Java base anonymous `HomeDashboardRenderController.Listener` block into `MainHomeDashboardRenderListener`, provided by `ShellModule`. Home dashboard navigation, playback continuation, recent-track play, shuffle-all queue creation, recommendation triggers, and action publication are now covered by `MainHomeDashboardRenderListenerTest`.
2026-06-29 note: Now-playing state listener policy moved out of the Java base anonymous `NowPlayingStateController.Listener` block into `MainNowPlayingStateListener`, provided by `PlaybackUiModule`. Store readiness, playback snapshot, favorite ids, lyrics state, language mode, floating-lyrics publication, active lyric selection, and queue input sync are now covered by `MainNowPlayingStateListenerTest`.
2026-06-26 note: LibrarySmallGatewayBindings was removed. MainActivity now binds favorite writing and playlist-track loading directly with lambdas, so the library toggle/load paths no longer need a two-method forwarding wrapper.
2026-06-26 note: NowPlayingStateBindings was removed. Superseded on 2026-06-29: MainActivity no longer wires NowPlayingStateController with an anonymous Listener inline; now-playing state publication inputs, floating-lyrics sync, and active lyric selection now go through `MainNowPlayingStateListener`.
2026-06-26 note: NetworkRequestBindings was removed. MainActivity now supplies NetworkRequestController labels and status callbacks directly with lambdas, so request status localization no longer needs a dedicated forwarding adapter.
2026-06-27 note: NetworkActionsResultBindings was removed. MainActivity now wires NetworkActionsViewModel.Listener inline, so network stream import/update/delete, WebDAV save/sync/test, playback track retention, network page navigation, status updates, and collections reload all stay at the host boundary without a forwarding result adapter.
2026-06-27 note: HomeDashboardRenderBindings was removed. Superseded on 2026-06-29: MainActivity no longer wires HomeDashboardRenderController.Listener inline; home mode switching, continue playback, now-playing refresh, per-track playback, library refresh, queue navigation, shuffle-all, streaming navigation, collections open, search refresh, and recommendation actions now go through `MainHomeDashboardRenderListener`.
2026-06-26 note: PlaylistExportBindings was removed. MainActivity wired PlaylistExportController with an anonymous Listener inline at that point; NOTE 64 later folded that pending export state into DocumentPickerController and removed the controller.
2026-06-26 note: BackNavigationBindings was removed. MainActivity now installs its OnBackPressedCallback inline, so the back-handler shell logic stays at the activity boundary without a forwarding adapter.
2026-06-26 note: SettingsControlsBindings was removed. MainActivity now supplies SettingsPlaybackServiceControls and SettingsLyricsControls inline via anonymous objects, so settings runtime setters stay at the shell boundary without a forwarding adapter.
2026-06-26 note: NowPlayingEffectBindings was removed. Superseded on 2026-06-28 by NOTE 48: the temporary `NowPlayingEffectController` shell dispatcher is also gone, and now-playing effects are drained directly in `MainActivity.handleNowPlayingEffects()` to call the existing platform owners.
2026-06-26 note: DocumentPickerBindings was removed. DocumentPickerController now receives a direct Listener from MainActivity, so audio import, folder import, download folder selection, playlist/M3U import-export, and Luoxue source import no longer pass through a one-hop binding.
2026-06-26 note: BackgroundImagePickerBindings was removed. BackgroundImagePickerController now reports picked page backgrounds directly to SettingsViewModel.applyPageBackgrounds and StatusMessageController, keeping preview/save ownership in the picker controller without a forwarding adapter.
2026-06-26 note: SettingsEffectBindings was removed. MainActivity now binds SettingsViewModel effects with an inline effect dispatcher, so settings navigation, platform launchers, lyrics reload, sleep timer, backup/restore, page background, and streaming gateway endpoint effects are explicit shell-boundary calls instead of a separate forwarding class.
2026-06-27 note: PlaybackQueueManager, PlaybackRuntimeStateManager, PlaybackMediaLibraryCallback, and PlaybackStatePublisher were introduced as playback boundary owners. Queue load/restore/reorder/advance now live in PlaybackQueueManager; playback mode/speed/volume/ReplayGain/audio-focus application now live in PlaybackRuntimeStateManager; MediaItem/Track resolution now lives in PlaybackMediaLibraryCallback; lyric/notification/widget publication and notification triggering now live in PlaybackStatePublisher; EchoPlaybackService shrank to 2740 lines in the current tree. Verified with serial `:app:compileDebugKotlin :app:compileDebugJavaWithJavac` and focused `PlaybackQueueManagerTest` / `PlaybackRuntimeStateManagerTest` / `PlaybackStatePublisherTest`.
2026-06-26 note: PlaylistDialogBindings was removed. PlaylistDialogController now receives a direct Listener that calls `MainActivity` ???? for create, rename, delete, default-add, and add-to-playlist results, while playlist selection data still comes from the PlaylistProvider.
2026-06-26 note: StreamingManualCookieBindings was removed. StreamingManualCookieController now receives a direct Listener for selected provider, dialog display, login-success refresh, and status updates, so the manual cookie auth path no longer depends on a forwarding binding.
2026-06-26 note: PlaybackActionBindings and PlaybackActionResultBindings were removed. MainActivity now wires PlaybackActionController with an inline anonymous Listener implementation, and playback result publication is handled directly inside MainActivity, so playback decision inputs and result commits stay at the shell boundary without an extra forwarding layer.
2026-06-26 note: StreamingSearchNavigatorBindings was removed. MainActivity now wires StreamingSearchEventController.Navigator inline, so network-home navigation, streaming playlist import, account playlist sync, liked-track import, recommendation actions, and import/cookie dialogs no longer pass through a one-hop navigator binding.
2026-06-26 note: StreamingSearchEventController now receives its Navigator inline from MainActivity. Superseded on 2026-06-28 by NOTE 50: recommendation actions no longer flow through `RecommendationActionController`; `MainActivity` calls `StreamingRecommendationViewModel.onAction(...)` directly with the shared callbacks.
2026-06-26 note: StreamingActionGatewayBindings was removed. MainActivity now wires MainActivityStreamingActionGateway inline, so streaming auth launch, playback quality, resolved-track playback, login-success handoff, and manual cookie import stay on the activity shell boundary without a forwarding adapter.
2026-06-26 note: MainActivityStreamingActionGateway is now provided inline by MainActivity, so the streaming auth and playback bridge no longer depends on a standalone gateway binding class.
2026-06-26 note: NowPlayingGatewayBindings, QueueActionBindings, LibraryGatewayBindings, and LibraryPlaylistActionGatewayBindings were removed. MainActivity now wires NowPlayingGateway, QueueActionController.Listener, LibraryGateway, and LibraryPlaylistActionGateway inline, so now-playing, queue, library, and playlist-action shells no longer depend on thin forwarding adapters.
2026-06-26 note: PlaybackServiceQueueSourceBindings was removed. MainActivity now provides PlaybackStateEventController.ServiceQueueSource inline, so playback state publishing no longer depends on a one-method queue-source adapter.
2026-06-26 note: RecommendationActionBindings was removed. Superseded on 2026-06-29: MainActivity no longer provides RecommendationActionCallbacks inline; daily recommendations, heartbeat recommendations, seed lookup, seed-miss logging, and status routing now go through `MainRecommendationActionCallbacks`.
2026-06-26 note: LibraryCollectionGatewayBindings was removed. MainActivity now provides LibraryCollectionGateway inline, so collection loading, play-history clearing, and favorite writes no longer depend on a separate forwarding adapter.
2026-06-26 note: LibraryImportGatewayBindings was removed. MainActivity now provides LibraryImportGateway inline, so cached refresh, audio tree import, URI import, and missing-spec parsing no longer depend on a separate forwarding adapter.
2026-06-26 note: StreamingPlaybackBindings was removed. MainActivity now provides StreamingPlaybackController.Listener inline, so streaming playback quality, queue snapshots, heartbeat append checks, playback result application, and status routing no longer pass through a pure forwarding binding.
2026-06-26 note: HeartbeatRecommendationBindings was removed. HeartbeatSeedRequestProvider now lives with HeartbeatRecommendationSeedResolver, and MainActivity provides HeartbeatRecommendationController.Listener inline, so service availability, queue append/playback, seed-miss logging, and status routing no longer pass through a separate heartbeat forwarding binding.
2026-06-26 note: PlaybackStateEventBindings was removed. MainActivity now provides PlaybackStateEventController.Listener inline, while the shared SettingsSelectedTabProvider contract moved to SettingsControls.kt for LyricsStateRefreshListener. Playback state updates now flow from PlaybackStateEventController directly to the shell listener without a forwarding binding layer.
2026-06-26 note: NetworkTrackListRenderBindings was removed. NetworkTrackListRequest lives beside NetworkTrackListRenderController, and MainActivity provides the render listener inline, so network stream/WebDAV track-list actions no longer pass through a one-hop binding while the request model remains owned by the render boundary. Superseded on 2026-06-28 by NOTE 62: the unused NetworkPageAction wrapper was removed.
2026-06-26 note: QueueRenderBindings was removed. TrackListPlaybackAction remains available for shared network playback actions. Superseded on 2026-06-28 by NOTE 62: the unused QueueTrackAction wrapper was removed. Superseded again on 2026-06-29: MainActivity no longer provides QueueRenderController.Listener inline; queue playback, favorite toggles, add-to-playlist, remove, clear, and back actions now go through `MainQueueRenderListener`.
2026-06-26 note: LibraryPlaylistsRenderBindings was removed. MainActivity now provides LibraryPlaylistsRenderController.Listener inline, keeping playlist/favorites/history events, delete confirmation, chrome publication, and playlist track-list rendering connected directly to LibraryViewModel/dialog/render methods without a forwarding binding.
2026-06-26 note: PlaybackStartBindings was removed. Superseded on 2026-06-28 by NOTE 47: `PlaybackController.kt` and `PlaybackStartControllerAdapter` are now gone; `StreamingTrackListResolver` and `PlaybackTrackListPlayer` live beside `PlaybackStartController.kt`, and playback start uses direct function dependencies rather than a pure forwarding facade.
2026-06-26 note: TrackListRenderBindings was removed. TrackListChromeState now lives beside TrackListRenderController, and MainActivity provides TrackListRenderController.Listener inline for track playback, favorite/add/download/edit/delete actions and chrome publication. Superseded on 2026-06-28 by NOTE 61: the unused LibraryEventSink compatibility type was removed.
2026-06-26 note: LibraryGroupsRenderBindings was removed. LibraryGroupsChromeState and LibraryGroupTrackListRequest now live beside LibraryGroupsRenderController, and MainActivity provides LibraryGroupsRenderController.Listener inline for group selection, favorites, playback, delete confirmation, chrome publication, and group track-list rendering.
2026-06-26 note: CollectionsRenderBindings was removed. CollectionsRenderController now owns the remaining collections action wiring directly, while MainActivity provides CollectionsRenderController.Listener inline for playlist creation/import/export, history clearing, track playback, favorite, download, playlist selection, rename/delete, streaming sync, move/remove, and collections action publication.
2026-06-27 note: PlaybackShutdownCoordinator now separates playback-level stop cleanup from service-level destruction. `stopAndClear()` calls `releasePlaybackResources()` for lyrics, Wi-Fi lock, and player only; `onDestroy()` calls `releaseServiceResources()` for receiver, schedulers, notification artwork, precache, Wi-Fi lock, and player. Do not collapse these paths back together; `PlaybackShutdownCoordinatorTest` guards the distinction. Verified with serial `:app:compileDebugKotlin :app:compileDebugJavaWithJavac` and targeted `:app:testDebugUnitTest --tests app.yukine.playback.PlaybackShutdownCoordinatorTest`.2026-06-27 note: PlaybackPositionManager now owns restored playback position, explicit resume position, clamping, throttled save, reset, and stop/clear position cleanup. EchoPlaybackService no longer keeps restored/last-saved position mutable fields or directly calls queueStore().savePlaybackPosition/loadPlaybackPosition; PlaybackQueueManager.playQueue(..., startPositionMs) now sets the in-memory restored position so immediate prepare honors explicit starts. Verified with serial compile and targeted PlaybackPositionManagerTest, PlaybackQueueManagerTest, and playback queue persistence/mutation contract tests.
2026-06-27 note (line break correction): PlaybackPositionManager now owns restored playback position, explicit resume position, clamping, throttled save, reset, and stop/clear position cleanup. EchoPlaybackService no longer keeps restored/last-saved position mutable fields or directly calls queueStore().savePlaybackPosition/loadPlaybackPosition; PlaybackQueueManager.playQueue(..., startPositionMs) now sets the in-memory restored position so immediate prepare honors explicit starts. Verified with serial compile and targeted PlaybackPositionManagerTest, PlaybackQueueManagerTest, and playback queue persistence/mutation contract tests.

2026-06-27 note: PlaybackSleepTimerManager now owns sleep timer end time, remaining-time calculation, callback scheduling, expiry pause, and cancel/publish policy. EchoPlaybackService no longer keeps sleepTimerEndsAtMs or sleepTimerRunnable and only provides handler scheduling plus pause/publish callbacks. Verified with serial compile and targeted PlaybackSleepTimerManagerTest plus playbackSleepTimerStateIsOwnedOutsideEchoPlaybackService contract.

- 2026-06-27: Playback Wi-Fi lock ownership moved into PlaybackWifiLockManager. EchoPlaybackService no longer stores the WifiLock field or local acquire/release helpers; it creates a narrow Android adapter and delegates streaming playback lock policy to the manager. Verified with serial compile and focused PlaybackWifiLockManagerTest plus MainActivityArchitectureContractTest.playbackWifiLockIsOwnedOutsideEchoPlaybackService.
- 2026-06-27: Playback noisy receiver ownership moved into PlaybackNoisyReceiverManager. EchoPlaybackService no longer stores noisyReceiver/noisyReceiverRegistered or local register/unregister helpers; it now delegates lifecycle to the manager through a narrow Android registrar. Verified with serial compile and focused PlaybackNoisyReceiverManagerTest plus MainActivityArchitectureContractTest.playbackNoisyReceiverIsOwnedOutsideEchoPlaybackService.
- 2026-06-27: Playback progress update ownership moved into PlaybackProgressUpdateManager. EchoPlaybackService no longer stores progressRunnable or direct mainHandler progress scheduling; playback events delegate to the manager for one-second publish/persist ticks. Verified with serial compile and focused PlaybackProgressUpdateManagerTest plus MainActivityArchitectureContractTest.playbackProgressUpdatesAreOwnedOutsideEchoPlaybackService.
- 2026-06-27: Playback state broadcast ownership moved into PlaybackStatePublisher. EchoPlaybackService no longer stores a listener set or manual state/buffering fan-out loops; it delegates register/unregister and publish paths to the publisher. Verified with serial compile and focused PlaybackStatePublisherTest plus MainActivityArchitectureContractTest.playbackStateBroadcastsAreOwnedOutsideEchoPlaybackService.
- 2026-06-27: Playback error recovery ownership moved into PlaybackErrorRecoveryManager. EchoPlaybackService no longer carries the retry-one/skip-on-repeat-failure branch or lastErrorTrackId state; it forwards ready/error events to the manager. Verified with serial compile and focused PlaybackErrorRecoveryManagerTest plus MainActivityArchitectureContractTest.playbackErrorRecoveryIsOwnedOutsideEchoPlaybackService.
## NOTE 29 - Playback mode owner consolidation (2026-06-27)
- Owner clarified: PlaybackRuntimeStateManager now owns shuffleEnabled, repeatMode, playbackSpeed, appVolume, ReplayGain, and concurrent playback state.
- Path shortened: EchoPlaybackService seeds runtime state from MusicLibraryRepository, then delegates playback-mode application and snapshot reads to PlaybackRuntimeStateManager.
- Guard: MainActivityArchitectureContractTest.playbackRuntimeStateIsOwnedOutsideEchoPlaybackService prevents those fields from returning to EchoPlaybackService.
- Verification: serial `gradlew.bat --no-daemon --max-workers=1 :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain` and focused PlaybackRuntimeStateManagerTest / architecture contract passed.

## NOTE 30 - Playback readiness and error state owner consolidation (2026-06-27)
- Owner clarified: PlaybackRuntimeStateManager now owns preparing and errorMessage in addition to playback mode/speed/volume/focus state.
- Path shortened: EchoPlaybackService writes readiness and error state through PlaybackRuntimeStateManager setters and snapshots read the owner directly.
- Guard: PlaybackRuntimeStateManagerTest covers preparing/error transitions; MainActivityArchitectureContractTest asserts EchoPlaybackService no longer declares `private boolean preparing` or `private String errorMessage`.
- Verification: serial `gradlew.bat --no-daemon --max-workers=1 :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain` and focused PlaybackRuntimeStateManagerTest / architecture contract passed.
## NOTE 31 - Playback transition state owner (2026-06-27)
- Owner clarified: PlaybackTransitionStateManager now owns lastMarkedTrack and fadeOutAdvancing state.
- Path shortened: EchoPlaybackService now records playback marking and fade-out transition state through PlaybackTransitionStateManager instead of holding private transition fields.
- Guard: PlaybackTransitionStateManagerTest covers set/clear behavior; MainActivityArchitectureContractTest.playbackTransitionStateIsOwnedOutsideEchoPlaybackService rejects private lastMarkedTrack/fadeOutAdvancing fields returning to EchoPlaybackService.
- Verification: serial `gradlew.bat --no-daemon --max-workers=1 :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain` and focused PlaybackTransitionStateManagerTest / architecture contract passed.
## NOTE 32 - Playback queue runtime mirror state owner (2026-06-27)
- Owner clarified: PlaybackQueueRuntimeStateManager now owns playerMirrorsQueue state.
- Path shortened: EchoPlaybackService no longer carries the private playerMirrorsQueue field; Media3 queue mirror reads/writes go through PlaybackQueueRuntimeStateManager.
- Guard: PlaybackQueueRuntimeStateManagerTest covers mirror state transitions; MainActivityArchitectureContractTest.playbackQueueRuntimeStateIsOwnedOutsideEchoPlaybackService rejects the private field and direct assignments returning to EchoPlaybackService.
- Verification: serial `gradlew.bat --no-daemon --max-workers=1 :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain` and focused PlaybackQueueRuntimeStateManagerTest / architecture contract passed.
- Follow-up: currentIndex remains in EchoPlaybackService and is the next high-value queue state migration target.
## NOTE 33 - Playback queue current index owner (2026-06-27)
- Owner clarified: PlaybackQueueRuntimeStateManager now owns currentIndex alongside playerMirrorsQueue.
- Path shortened: EchoPlaybackService no longer declares a private currentIndex field; queue cursor reads/writes go through currentIndex()/setCurrentIndex()/setClampedCurrentIndex() backed by PlaybackQueueRuntimeStateManager.
- Guard: PlaybackQueueRuntimeStateManagerTest covers currentIndex set/clamp behavior; MainActivityArchitectureContractTest.playbackQueueRuntimeStateIsOwnedOutsideEchoPlaybackService rejects private currentIndex fields and direct currentIndex assignments returning to EchoPlaybackService.
- Verification: serial `gradlew.bat --no-daemon --max-workers=1 :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain` and focused PlaybackQueueRuntimeStateManagerTest / architecture contract passed.
- Follow-up: queue cursor algorithms still live in EchoPlaybackService fallback paths; next migration can move those fallback mutations into PlaybackQueueManager or the queue runtime owner now that the state source is centralized.
## NOTE 34 - Playback queue fallback algorithm removal (2026-06-27)
- Owner clarified: PlaybackQueueManager is now the only owner for public queue mutation algorithms such as append, advance-next, move, remove, retain, clear, and replace queued tracks.
- Path shortened: EchoPlaybackService public queue entry points now delegate to PlaybackQueueManager instead of carrying duplicate fallback algorithms for queue mutation and collapse behavior.
- Guard: MainActivityArchitectureContractTest.playbackQueueMutationKeepsOneClearlyNamedEntryPointCluster rejects old fallback markers such as wasEmpty queue append handling, removedBeforeCurrent removal logic, targetAlreadyQueued collapse logic, random next-index selection, and replaceAndCollapseQueuedTrack returning to EchoPlaybackService.
- Verification: serial `gradlew.bat --no-daemon --max-workers=1 :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain` and focused PlaybackQueueManagerTest / architecture contract passed.
- Follow-up: skipToPrevious and replaceCurrentTrackAndResume still contain direct queue cursor/current-track mutation in EchoPlaybackService and are candidates for the next PlaybackQueueManager slice.
## NOTE 35 - Playback previous and current-track recovery queue owner (2026-06-27)
- Owner clarified: PlaybackQueueManager now owns skip-to-previous queue cursor mutation and current-track replacement/resume queue mutation.
- Path shortened: EchoPlaybackService keeps the >3s seek-to-zero branch and external recovery callbacks, but delegates previous-track cursor movement and replaceCurrentTrackAndResume queue updates to PlaybackQueueManager.
- Guard: PlaybackQueueManagerTest covers skipToPrevious and replaceCurrentTrackAndResume recovery scheduling; MainActivityArchitectureContractTest.playbackQueueMutationKeepsOneClearlyNamedEntryPointCluster rejects direct previous cursor mutation, direct queue.set(currentIndex(), replacement), and inline streaming recovery recording returning to EchoPlaybackService.
- Verification: serial `gradlew.bat --no-daemon --max-workers=1 :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain` and focused PlaybackQueueManagerTest / architecture contract passed.
- Follow-up: remaining queue-facing service code is mostly playback preparation/reuse, current-track replacement helper, and Media3 mirrored-queue integration.
## NOTE 36 - Playback mirrored queue reuse owner (2026-06-27)
- PlaybackQueueManager now owns the mirrored-queue reuse decision and queue-side reuse state reset.
- EchoPlaybackService.seekExistingMirroredQueue(...) now delegates to PlaybackQueueManager.reuseMirroredQueueIfAvailable(...), while Media3 player seek/play remains in the service boundary.
- Added PlaybackQueueManagerTest coverage for successful reuse and failed-seek mirror cleanup; strengthened the playback queue mutation architecture contract.
- Verified serially with `gradlew.bat --no-daemon --max-workers=1 :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain` and focused PlaybackQueueManagerTest / architecture contract.
## NOTE 37 - Playback mirrored queue preparation owner (2026-06-27)
- PlaybackQueueManager now owns mirrored-queue preparation eligibility and header-restore iteration.
- EchoPlaybackService.prepareMirroredQueue(...) now consumes PlaybackQueueManager.mirroredQueueTracksForPreparation() and keeps only MediaSource construction plus ExoPlayer calls.
- PlaybackQueueManagerTest covers snapshot/header restore and unplayable-track rejection; the architecture contract rejects the old queueTrack Uri/header loop in EchoPlaybackService.
- Verified serially with focused PlaybackQueueManagerTest / architecture contract and compileDebugKotlin/compileDebugJavaWithJavac using --max-workers=1.

## NOTE 38 - Streaming task queue owner collapse (2026-06-27)
- `StreamingPlaybackTaskScheduler` now implements `StreamingPlaybackTaskQueue` directly; the thin `StreamingPlaybackTaskQueueAdapter` was removed.
- `MainActivity` now binds the scheduler directly, keeping the streaming task path one hop shorter.
- The former adapter test was renamed into `StreamingPlaybackTaskSchedulerTest` so the queue ordering behavior still has coverage.
- Verified serially with `:app:compileDebugKotlin :app:compileDebugJavaWithJavac` and focused scheduler / architecture contract tests.

## NOTE 39 - Playback service boundary cleanup (2026-06-27)
- `EchoPlaybackService` no longer imports `MainActivity`; launch intent creation now uses the package launch intent path.
- `PlaybackQueueManager` owns the queue recovery comparison directly, and the cache owners compare `contentUri` locally instead of routing through the service.
- `PlaybackQueueManagerTest` remains Robolectric-backed with `sdk = [34]` to keep the URI behavior faithful.
- Verified serially with focused playback tests and the architecture contract after the cleanup.

## NOTE 40 - Playback start controller dependency shortcut (2026-06-27)
- Superseded by NOTE 47 on 2026-06-28.
- This slice first removed `PlaybackStartController.Listener.playbackController()` by injecting the temporary `PlaybackController` facade.
- NOTE 47 then removed that facade and the adapter as well, leaving `PlaybackStartController` with direct function dependencies for streaming resolution, local playback, and result application.

## NOTE 41 - Retired playback facade history (2026-06-28)
- The old `app.yukine.playback.PlaybackController` / `PlaybackServiceController` facade is deleted and kept only as history in the migration ledger.
- The test-only fake facade pair is also deleted.
- Do not treat the old 7.3.1 facade plan as active migration work anymore.
## NOTE 42 - Network dialog forwarding bridge removed (2026-06-28)
- `NetworkRequestController` now implements `NetworkDialogController.Listener` directly.
- `MainActivity` passes `networkRequestController` directly into `NetworkDialogController`; the forwarding-only `NetworkDialogEventController` and its standalone test were deleted.
- Dialog-driven add stream, import M3U, update stream, and WebDAV save still publish status before delegating to `NetworkOperationSink`, now covered by `NetworkRequestControllerTest`.
- Verified serially with focused `NetworkRequestControllerTest` and `MainActivityArchitectureContractTest.mainActivityDelegatesNetworkOperationsThroughRequestController`.
## NOTE 43 - Streaming gateway event bridge removed (2026-06-28)
- Superseded by NOTE 49: `StreamingGatewayController` has also been removed.
- The intermediate slice made `StreamingGatewayController` own repository configure, provider refresh completion rendering, and applied-endpoint status publication directly.
- `StreamingGatewayEventController` and its standalone test were deleted because it only adapted controller callback interfaces to `StreamingViewModel`, render, and status sinks.
- `MainActivity` no longer keeps a streaming gateway field; current startup/auth-refresh paths use `refreshStreamingProviders()` directly.
- Current verification lives in NOTE 49; the old focused `StreamingGatewayControllerTest` was removed with the controller.
## NOTE 46 - Streaming repository configure call moved back to direct owner (2026-06-28)
- `MainActivity` now calls `streamingViewModel.configureStreamingRepository()` directly instead of routing through `StreamingGatewayController.configureRepository()`.
- Superseded by NOTE 49: the remaining refresh/render/status wrapper has also been folded into direct host helpers.
- Current verification lives in NOTE 49.
## NOTE 47 - Playback start adapter folded into controller (2026-06-28)
- `PlaybackStartControllerAdapter` and the single-method `PlaybackController` interface were removed.
- `PlaybackStartController` now directly owns the streaming-resolver -> local-player -> result-applier start path through narrow function dependencies.
- `MainActivity` constructs `PlaybackStartController` with `streamingPlaybackController::resolveAndPlayStreamingTrack`, `nowPlayingViewModel::playTrackList`, and `this::applyPlaybackActionResult`, removing one playback-start hop.
- Verified serially with `PlaybackStartControllerTest`, the playback architecture contract, and compile `:app:compileDebugKotlin :app:compileDebugJavaWithJavac`.
## NOTE 48 - Now-playing effect dispatcher inlined (2026-06-28)
- `NowPlayingEffectController` and `NowPlayingEffectControllerTest` were removed because the controller only forwarded drained effects back to `MainActivity` platform actions.
- `MainActivity.handleNowPlayingEffects()` now drains `NowPlayingViewModel` effects directly and calls the existing owners: tab navigation, `PlaylistDialogController`, `TrackShareLauncher`, `DownloadRequestController`, source switching, and `StatusMessageController`.
- Guarded by `MainActivityArchitectureContractTest`, which rejects the old effect controller and adapter while preserving the direct platform-owner calls.
## NOTE 49 - Streaming gateway controller folded into direct helpers (2026-06-28)
- `StreamingGatewayController` and `StreamingGatewayControllerTest` were removed after its remaining responsibilities shrank to provider refresh completion rendering and endpoint apply status.
- `MainActivity.refreshStreamingProviders()` now calls `StreamingViewModel.refreshStreamingProviders()` directly and syncs recommendation providers when the job completes.
- `MainActivity.applyStreamingGatewayEndpoint(...)` now saves the normalized endpoint through `StreamingGatewaySettingsStore`, reconfigures `StreamingViewModel`, refreshes providers, renders immediately, and publishes the applied-endpoint status without a controller hop.
- Guarded by `MainActivityArchitectureContractTest`, which rejects the old controller/test and protects the direct refresh/apply helper path.
## NOTE 50 - Recommendation action runner folded into direct ViewModel call (2026-06-28)
- The `RecommendationActionController` class and `RecommendationActionRunner` / `RecommendationLanguageProvider` interfaces were removed because they only forwarded typed recommendation actions to `StreamingRecommendationViewModel.onAction(...)`.
- `RecommendationAction` and `RecommendationActionCallbacks` remain as the typed recommendation boundary in `RecommendationAction.kt`; `RecommendationActionHandler` was later removed by NOTE 63 after direct ViewModel calls became the only production path.
- `MainActivity.runRecommendationAction(...)` now passes the action, current language mode, and shared callbacks directly to `StreamingRecommendationViewModel`, preserving daily/heartbeat presentation playback, seed lookup, miss logging, and status routing without a runner hop.
- Guarded by `MainActivityArchitectureContractTest`, which rejects the old runner class while preserving the typed action and handler contracts.
## NOTE 51 - Download directory picker shell folded into DocumentPickerController (2026-06-28)
- `DownloadDirectoryPickerController` and `DownloadDirectoryPickerControllerTest` were removed because the controller only checked for a document picker, published a fixed fallback message, and called `openDownloadFolderPicker()`.
- `DocumentPickerController.openDownloadFolderPicker()` is now the direct owner for the download directory picker launch.
- `MainActivity.openDownloadDirectoryPickerAction()` keeps only the null-guard fallback (`目录选择暂不可用`) before calling the document picker directly.
- Guarded by `MainActivityArchitectureContractTest.downloadRequestsAreOwnedOutsideMainActivity`, which rejects the old controller and preserves the direct picker path.
## NOTE 44 - Now playing stale render bridge removed (2026-06-28)
- `NowPlayingRenderController` was deleted because it no longer published UI state; the active Now page is driven by `NowPlayingViewModel.uiState` through `NowPlayingDestination`.
- `MainActivity.updateNowPlayingContent()` now keeps only the current-track existence check used by the lyrics refresh fallback.
- The unused `renderNowPlaying()` host method was removed.
- Guarded by `MainActivityArchitectureContractTest`, which now rejects the stale render controller and host entry point.
## NOTE 45 - MainActivity one-hop wrappers collapsed (2026-06-28)
- Queue render/intent listeners now call `QueueActionController.removeQueueTrack(...)` and `moveQueueTrack(...)` directly; the private `removeQueueTrack` / `moveQueueTrack` host wrappers were removed.
- Network action result listeners now call `NowPlayingViewModel.replaceQueuedTrack(...)` and `retainTracks(...)` directly; the private queue sync/retention wrappers were removed.
- Collections streaming sync now calls `StreamingPlaylistController.syncSelectedPlaylistFromStreaming()` directly.
- WebDAV source sync now reads `libraryStore.remoteSourceName(sourceId)` inline instead of through a host helper.
- WebDAV source playback now reads `libraryStore.webDavTracksForSource(source.id)` inline, and the unused `streamingProviderTrackId(...)` / `syncStreamingPlaylists(...)` host helpers were removed.
- Playlist dialog and collections row actions now call `LibraryViewModel.*Java(...)` directly and keep only the result handlers in `MainActivity`; the private playlist CRUD/move/add forwarding helpers were removed.
- Unified search load-more now calls `StreamingSearchActionHandler.loadNextPage()` from the action lambda directly; the private host wrapper was removed.
- `StreamingSearchEventController` was removed as a pure forwarding layer. `MainActivity` wires `StreamingSearchRenderController.Listener` inline to the strategy-bearing `DefaultStreamingSearchActionHandler`, playlist/recommendation/dialog owners, and `StreamingViewModel.updateStreamingSearchChrome(...)`.
## NOTE 52 - Streaming search action handler renamed away from Bindings (2026-06-28)
- `StreamingSearchActionHandlerBindings` was renamed to `DefaultStreamingSearchActionHandler` because it is a strategy-bearing handler, not a pure forwarding binding.
- The handler still owns provider selection, search dispatch, auth capability checks, playback capability checks, auth launch clearing, resolved-track playback handoff, and load-more validation.
- `DefaultStreamingSearchActionHandlerTest` keeps the previous behavior coverage under the new owner name.
- Guarded by `MainActivityArchitectureContractTest`, which rejects the old `*Bindings` file and preserves the handler's direct `StreamingViewModel` / action-gateway path.
## NOTE 53 - Library document gateway renamed away from Bindings (2026-06-28)
- `LibraryDocumentGatewayBindings` was renamed to `ContentResolverLibraryDocumentGateway` because it owns document I/O strategy, M3U import fallback, playlist import result mapping, and playlist export text writing.
- `ContentResolverLibraryDocumentGatewayTest` keeps the previous import/export fallback coverage under the new owner name.
- Current P1 follow-up moved that document gateway assembly into `LibraryModule`; `MainActivityBase` now binds the injected `LibraryDocumentGateway`.
- Root-package `*Bindings` count is now zero; the remaining library document behavior is a real gateway owner, not a temporary binding shell.
- WebDAV remote-source playback now reuses `NetworkSourcesEventController.playRemoteSourceTracks(...)` from both sources rows and source track-list actions; the duplicate `MainActivity.playRemoteSourceTracks(...)` helper was removed.
- Onboarding actions now call `OnboardingController` directly from `OnboardingActions`; the private `finish/openStreaming/scanLibrary/importPlaylistFromOnboarding` host wrappers were removed.
- Streaming playlist import refresh now calls `loadLibrary(true)` directly from `StreamingPlaylistController.Listener`; the private `refreshLibraryAfterStreamingImport()` host wrapper was removed.
- Additional host-only wrappers were inlined: manual streaming cookie import now calls `StreamingViewModel` / `StreamingManualCookieController` directly, playback state pre-resolve calls `StreamingPlaybackController` directly, network track-list remote-source clearing calls `RouteController` plus `navigateNetworkPage(...)` directly, and render content now calls `UiShellController` scrolling/tab APIs directly.
- Startup library loading now performs the permission branch inline in the startup path; the private `loadLibraryOnStartup()` wrapper was removed.
- Lyrics state refresh now reads the `LyricsViewModel` snapshot directly in the listener, and the unused `ensureLyricsLoaded(...)` host method was removed.
- `QueueIntentController` was deleted as a pure `QueueIntent -> listener` forwarding layer. `QueueViewModel.bindIntentListener(...)` now dispatches sealed `QueueIntent` values directly to `LibraryViewModel`, `PlaylistDialogController`, `QueueActionController`, and back handling.
- The no-op language bridge was removed completely: `SettingsRuntimeEffect.UpdateLanguage`, `SettingsRuntimeApplier.updateLanguage(...)`, `SettingsRuntimeLanguageUpdater`, and `MainUiShellController.updateLanguage(...)` are gone; language mode still persists through `SettingsViewModel` and is consumed where it actually matters.
- Guarded by `MainActivityArchitectureContractTest` direct-call and no-wrapper assertions.
## NOTE 54 - Playback session forwarding gateway removed (2026-06-28)
- `PlaybackSessionGateway` was removed because it only forwarded `session()`, `bind()`, `refresh()`, and `release()` to `PlaybackSessionManager`.
- `EchoPlaybackService` now holds `PlaybackSessionManager` directly and calls `bind()`, `refreshPlayer()`, `release()`, and `session()` without the extra service-internal gateway hop.
- The media notification platform token now reads from `playbackSessionManager.session()` directly, preserving notification/session behavior while shortening the service path.
- Guarded by `MainActivityArchitectureContractTest.playbackSessionLifecycleIsOwnedOutsideEchoPlaybackService`, which rejects `PlaybackSessionGateway` and preserves the direct manager calls.
## NOTE 55 - Settings render controller collapsed into label formatter (2026-06-28)
- `SettingsPageRenderController` was removed because it no longer rendered settings pages; it only forwarded scroll-to-top to `SettingsViewModel` and hosted static label formatting helpers.
- `MainActivity.requestCurrentDirectoryScrollToTop()` now calls `SettingsViewModel.scrollToTopOnNextRender()` directly when the settings tab is active.
- The remaining label helpers now live in `SettingsLabelFormatter`, which has no `SettingsViewModel`, listener, scroll state, or page render dependency.
- Guarded by `MainActivityArchitectureContractTest`, which rejects the old controller/field and preserves direct `SettingsViewModel` scrolling plus formatter ownership.
## NOTE 56 - Playback queue provider current-track method removed (2026-06-28)
- `PlaybackQueueManager.QueueProvider.currentTrack()` was removed from the large provider interface.
- `PlaybackQueueManager` now derives the current track from its existing `queue()` and `currentIndex()` provider methods, reducing one service/fake implementation method without changing queue behavior.
- `EchoPlaybackService` no longer implements this provider method for the queue manager, and `PlaybackQueueManagerTest` uses the same manager-side current-track calculation as production.
- Guarded by `MainActivityArchitectureContractTest.playbackQueueMutationKeepsOneClearlyNamedEntryPointCluster`, which rejects the provider interface method and preserves the private manager helper.
## NOTE 57 - Download quality callback wrapper removed (2026-06-28)
- `DownloadQualitySelectedCallback` was removed because it only wrapped a single Kotlin callback for download-quality selection.
- `DownloadQualityChooser.choose(...)` now accepts `(StreamingAudioQuality) -> Unit` directly, while `DownloadQualityDialogController` remains the platform dialog owner.
- `DownloadRequestControllerTest` keeps the quality selection flow coverage with the simpler function callback.
- Guarded by `MainActivityArchitectureContractTest.downloadRequestsAreOwnedOutsideMainActivity`, which rejects the old callback wrapper.
## NOTE 58 - Download manager provider wrapper removed (2026-06-28)
- `DownloadManagerProvider` was removed because it only returned the current `TrackDownloadRequestQueue`.
- `DownloadRequestController` now receives `() -> TrackDownloadRequestQueue?` directly, preserving late access to the manager while removing one named compatibility interface.
- `MainActivity` still supplies the current `trackDownloadManager` lazily through the constructor lambda, and `DownloadRequestControllerTest` keeps the queue/enqueue coverage.
- Guarded by `MainActivityArchitectureContractTest.downloadRequestsAreOwnedOutsideMainActivity`, which rejects the old provider wrapper and preserves the direct function dependency.
## NOTE 59 - Network menu content sink removed (2026-06-28)
- `NetworkMenuContentSink` was removed because it only forwarded `title + metrics + actions` into `NetworkMenuViewModel.updateMenu(...)`.
- `NetworkMenuEventController` now receives `NetworkMenuViewModel` directly and updates the true network-menu UI state owner from `publishNetworkMenu(...)`.
- `MainActivity` no longer constructs a three-argument menu chrome lambda for this path; it passes the existing `networkMenuViewModel` to the event controller.
- Guarded by `NetworkMenuEventControllerTest.publishNetworkMenuUpdatesViewModelDirectly` plus `MainActivityArchitectureContractTest`, which rejects the old sink and preserves the direct ViewModel update.
## NOTE 60 - Collections action dead callback removed (2026-06-28)
- `CollectionsActionsSink` was removed because it had no remaining production call sites after `CollectionsRenderController` started updating `CollectionsViewModel` directly.
- The unused `CollectionsRenderController.Listener.publishCollectionsActions(...)` callback and matching `MainActivity` override were removed.
- Collections action state now stays on the direct `CollectionsRenderController -> CollectionsViewModel.updateActions(...)` path.
- Guarded by `MainActivityArchitectureContractTest.collectionsRenderControllerIsKotlinUiStateBoundary`, which rejects the old sink, listener call, and Activity override.
## NOTE 61 - Unused migration callback types removed (2026-06-28)
- Dead compatibility types with no production call sites were removed: `PlaylistIdAction`, `PlaylistAction`, `SelectedPlaylistExportOpener`, `SelectedPlaylistTrackMover`, `SelectedPlaylistTrackRemover`, `LibraryEventSink`, `NowPlayingEventHandler`, and `QueueNoArgAction`.
- These types no longer represented active Java/Kotlin interop boundaries or testable policy; the remaining flows already call their ViewModel/controller owners directly.
- Guarded by `MainActivityArchitectureContractTest` assertions in the collections, playback/queue, and library boundary checks.
## NOTE 62 - More unused action contracts removed (2026-06-28)
- Additional production-dead action contracts were removed after exact reference checks: `QueuePlaybackServiceAvailability`, `QueueStatusProvider`, `QueueStatusSink`, `NetworkPageAction`, `TrackListDownloadAction`, `SettingsStatusSink`, and `QueueTrackAction`.
- `QueueActionContracts.kt` now keeps only `QueuePlaybackActionResultApplier`, the one remaining contract used by `PlaybackStartController`.
- Network track-list header actions and collections download actions already use direct `Runnable` / listener paths, so these named action wrappers no longer represented active boundaries.
- Guarded by `MainActivityArchitectureContractTest`, which now rejects these removed contracts in the playback/queue, collections, and network render checks.
## NOTE 63 - Recommendation self-interfaces removed (2026-06-28)
- `DailyRecommendationPlayer` and `RecommendationActionHandler` were removed because production code no longer holds recommendations through those interfaces.
- `StreamingRecommendationViewModel` keeps the same `onAction(...)` and `playDailyRecommendations(...)` methods directly, while `HeartbeatRecommendationPlayer` remains because `HeartbeatRecommendationController` still depends on that smaller behavior boundary.
- `RecommendationAction.kt` now contains only the typed action model and platform callback contract.
- Guarded by `MainActivityArchitectureContractTest`, which rejects the removed self-interfaces while preserving the direct ViewModel action path and heartbeat player boundary.
## NOTE 64 - Playlist export controller folded into document picker (2026-06-28)
- `PlaylistExportController` was removed because it only stored pending playlist id/name and forwarded the export URI back to `LibraryViewModel`.
- `DocumentPickerController.openPlaylistExportDocument(playlistId, playlistName)` now owns the create-document launch and pending export context, and emits `exportPlaylist(exportUri, playlistId, playlistName)` directly from the result callback.
- `MainActivity` keeps only the selected-playlist empty guard before launching the picker, then calls `LibraryViewModel.exportPlaylistJava(...)` from the existing document picker listener.
- Guarded by `DocumentPickerControllerTest` and `MainActivityArchitectureContractTest.mainSettingsStoreIsKotlinStateHolder`, which reject the removed controller while preserving playlist export document creation and export context delivery.
## NOTE 65 - Empty playlist export callback removed (2026-06-28)
- `LibraryPlaylistExportCallback` was removed because the only production caller passed an empty callback after playlist export.
- `LibraryViewModel.exportPlaylistJava(...)` now exposes the Java shell entry without a callback parameter and delegates to the existing coroutine export path.
- `MainActivity` calls the no-callback export method directly from `DocumentPickerController.Listener.exportPlaylist(...)`, reducing one Java/Kotlin interop interface without changing document export behavior.
- Guarded by `MainActivityArchitectureContractTest.mainSettingsStoreIsKotlinStateHolder`, which rejects the removed callback interface and the old four-argument Activity call.
## NOTE 66 - Root Bindings zero-count guard added (2026-06-28)
- Root-package main/test `*Bindings*` files are now guarded by a directory-level architecture contract instead of only many per-file string checks.
- `MainActivityArchitectureContractTest.rootPackageHasNoMigrationBindingsFiles` counts `app/src/main/java/app/yukine/*Bindings*` and `app/src/test/java/app/yukine/*Bindings*` and requires both to stay at zero.
- This turns the current zero-Bindings state into a structural regression guard for the stabilization pivot: new migration shells must justify themselves outside the old root-package bridge pattern.
## NOTE 67 - Dead play-history Java callback removed (2026-06-28)
- `LibraryPlayHistoryClearedCallback` and `LibraryViewModel.clearPlayHistoryJava(...)` were removed because play-history clearing now flows through `PlayHistoryActionController -> LibraryViewModel.clearPlayHistory { ... }`.
- The active path still updates the collection gateway, clears the `MainActivityViewModel` play-history snapshot via `PlayHistoryStateStore`, publishes status, and reloads collections.
- Guarded by `PlayHistoryActionControllerTest` plus `MainActivityArchitectureContractTest.mainSettingsStoreIsKotlinStateHolder`, which rejects the dead Java callback while preserving the direct Kotlin clear path.
## NOTE 68 - Duplicate collection favorite write path removed (2026-06-28)
- `LibraryViewModel.saveLibraryFavorite(...)`, `saveLibraryFavoriteJava(...)`, `LibraryCollectionGateway.setFavorite(...)`, and `SetLibraryFavoriteUseCase` were removed because production favorite toggles already use `LibraryEvent.ToggleFavorite -> LibraryFavoriteWriter -> ToggleFavoriteUseCase`.
- `LibraryCollectionGateway` now stays focused on loading collections and clearing play history; favorite mutation no longer has a second collection gateway path.
- Guarded by `LibraryViewModelTest` / `ToggleFavoriteUseCaseTest` for the active favorite path and `MainActivityArchitectureContractTest.mainSettingsStoreIsKotlinStateHolder`, which rejects the removed collection favorite write path.
## NOTE 69 - Now-playing playback service function wrappers removed (2026-06-28)
- `PlaybackServiceProvider` and `PlaybackServiceStarter` were removed from `NowPlayingPlaybackGatewayAdapter` because they only wrapped function dependencies.
- `NowPlayingPlaybackGatewayAdapter` now receives `() -> EchoPlaybackService?` and `(String?) -> Unit` directly, preserving the existing `NowPlayingPlaybackGateway` service boundary while reducing two named interop interfaces.
- Guarded by `MainActivityArchitectureContractTest`, which rejects the removed function wrappers and preserves the direct service dependencies.
## NOTE 70 - Backup status sink wrapper removed (2026-06-28)
- `BackupStatusSink` was removed from `BackupRestoreLauncher` because it only wrapped a status-key function.
- `BackupRestoreLauncher` now receives `(String) -> Unit` directly; backup export/import still maps results to `backup.*` status keys and lets `StatusMessageController` localize/publish them.
- Guarded by `BackupRestoreLauncherTest` plus `MainActivityArchitectureContractTest.backupRestoreLauncherIsOwnedOutsideMainActivity`, which rejects the removed sink wrapper and preserves the launcher-owned document intents.
## NOTE 71 - Download status sink wrapper removed (2026-06-28)
- `DownloadStatusSink` was removed from `DownloadRequestController` because it only wrapped a raw status-message function.
- `DownloadRequestController` now receives `(String) -> Unit` directly while keeping download quality selection, streaming download URL resolution, queue enqueue, and `DownloadsViewModel.refresh(...)` ownership unchanged.
- Guarded by `DownloadRequestControllerTest` plus `MainActivityArchitectureContractTest.downloadRequestsAreOwnedOutsideMainActivity`, which rejects the removed sink wrapper and preserves the download owner path.
## NOTE 72 - Track share provider wrappers removed (2026-06-28)
- `TrackShareLanguageProvider` and `TrackShareStyleProvider` were removed from `TrackShareLauncher` because they only wrapped zero-argument string providers.
- `TrackShareLauncher` now receives direct `() -> String` dependencies for language mode and share style while keeping `TrackShareStatusSink` for the two-channel feedback/status boundary.
- Guarded by `TrackShareLauncherTest` plus `MainActivityArchitectureContractTest`, which rejects the removed provider wrappers and preserves chooser/native-share behavior.
## NOTE 73 - Play-history function wrappers removed (2026-06-28)
- `PlayHistoryLanguageModeProvider` and `PlayHistoryStatusSink` were removed from `PlayHistoryActionController` because they only wrapped language-mode and status functions.
- `PlayHistoryActionController` now receives direct `() -> String` and `(String) -> Unit` dependencies while keeping `PlayHistoryStateStore` as the explicit state-clearing boundary.
- Guarded by `PlayHistoryActionControllerTest` plus `MainActivityArchitectureContractTest`, which rejects the removed wrappers and preserves the direct clear-history path.
## NOTE 74 - Background picker provider wrappers removed (2026-06-28)
- `BackgroundLanguageModeProvider` and `BackgroundTransformProvider` were removed from `BackgroundImagePickerController` because they only wrapped preview language and transform lookup functions.
- `BackgroundImagePickerController` now receives direct `() -> String` and `(String) -> BackgroundTransform` dependencies while keeping document/preview activity-result launchers as the real platform boundaries.
- Guarded by `BackgroundImagePickerControllerTest` plus `MainActivityArchitectureContractTest.backgroundImagePickerIsOwnedOutsideMainActivity`, which rejects the removed provider wrappers and preserves picker/preview/save ownership.
## NOTE 75 - Status message language provider wrappers removed (2026-06-28)
- `StatusLanguageModeProvider` and `MessageLanguageModeProvider` were removed because they only wrapped language-mode lookup for status localization.
- `StatusMessageController` and `MessageTextResolver` now share a direct `Supplier<String>` language dependency while keeping `RawStatusUpdater` as the status-output boundary.
- Guarded by `StatusMessageControllerTest`, `MessageTextResolverTest`, and `MainActivityArchitectureContractTest.mainActivityDelegatesStatusLocalizationToStatusMessageController`, which rejects the removed provider wrappers and preserves localized status publishing.
## NOTE 76 - Root-cause audit correction applied (2026-06-28)
- The latest review changes the migration default from "continue deleting wrapper layers" to "reduce the three root hotspots first": `MainActivity` assembly/anonymous callback policy, `EchoPlaybackService` playback policy residue, and `EchoDatabaseHelper` migration risk.
- A controller deletion is no longer accepted if behavior moves back into `MainActivity` private helpers, anonymous listeners, or `onCreate` construction. Host assembly density must go down, or the slice is not net architecture progress.
- Deleted controller tests must be mapped to replacement behavior tests under the new owner; string-based architecture contracts remain useful alarms but are not behavior coverage.
- Database work now starts with migration/transaction tests before Room or repository split work, and concurrency work starts with an inventory of raw Thread/ExecutorService/scheduler ownership and shutdown.
- Reflected in `docs/ARCHITECTURE_STABILIZATION_PIVOT_2026-06-27.md`, `docs/ARCHITECTURE_REMEDIATION_PLAN_2026-06-26.md`, and the `yukine-android-maintenance` skill.
## NOTE 77 - Settings runtime controls moved behind factory (2026-06-29)
- `SettingsModule` now provides `MainSettingsRuntimeApplierFactory`, and the concrete runtime controls live beside `SettingsRuntimeApplier` as `MainSettingsPlaybackServiceControls`, `MainSettingsLyricsControls`, and `MainSettingsFloatingLyricsControls`.
- `MainActivityBase` now only passes the existing theme, playback service, lyrics ViewModel, and permission-controller providers to `settingsRuntimeApplierFactory.create(...)`; it no longer constructs anonymous settings control implementations or calls `FloatingLyricsService` directly from settings runtime setup.
- The Settings path remains `SettingsViewModel -> preference/runtime applier`; no settings gateway, coordinator, controller, event bus, or root `*Bindings*` file was added. Current recheck: `MainActivityBase.java` 2632 lines, `EchoPlaybackService.java` 2469 lines, `feature:data` `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0, root-package `*Controller*` 44, root-package files 175, and root-package Java files 15.
- Guarded by `SettingsRuntimeApplierTest`, `SettingsViewModelTest`, and `MainActivityArchitectureContractTest`; compile passed with `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`.
## NOTE 78 - Collections render listener policy moved out of Activity (2026-06-29)
- `LibraryModule` now provides `MainCollectionsRenderListenerFactory`, and `MainCollectionsRenderListener` owns the collections render action routing that was previously an anonymous `CollectionsRenderController.Listener` in `MainActivityBase`.
- `MainActivityBase` now creates `CollectionsRenderController` with `collectionsRenderListenerFactory.create(...)`; Activity only supplies existing host capabilities such as playlist dialogs, document pickers, download requests, selected-playlist state sources, streaming import actions, and selected-playlist mutation callbacks.
- The selected-playlist export guard moved with the listener: empty or missing selected playlists publish `no.tracks.in.playlist`, while valid exports call `openPlaylistExportDocument(playlistId, playlistName)`. No root `*Bindings*` or root `*Controller*` file was added. Current recheck: `MainActivityBase.java` 2562 lines, `EchoPlaybackService.java` 2469 lines, `feature:data` `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0, root-package `*Controller*` 44, root-package files 176, and root-package Java files 15.
- Guarded by `MainCollectionsRenderListenerTest` and `MainActivityArchitectureContractTest`; compile passed with `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`.
## NOTE 79 - Library groups render listener policy moved out of Activity (2026-06-29)
- `LibraryModule` now provides `MainLibraryGroupsRenderListenerFactory` and `ArtistInfoRepository`. `MainLibraryGroupsRenderListener` owns the library-groups render action routing that was previously an anonymous `LibraryGroupsRenderController.Listener` in `MainActivityBase`.
- `MainActivityBase` now creates `LibraryGroupsRenderController` with `libraryGroupsRenderListenerFactory.create(...)` and the injected `artistInfoRepository`; Activity no longer constructs `new ArtistInfoRepository()` or the library-groups listener inline.
- The listener owns favorite-collection title resolution, chrome state copy construction, and library group track-list request construction, while Activity only supplies existing host callbacks for group open/clear/close, track playback, deletion confirmation, chrome publishing, and track-list rendering. Current recheck: `MainActivityBase.java` 2517 lines, `EchoPlaybackService.java` 2469 lines, `feature:data` `EchoDatabaseHelper.java` 2117 lines, `StreamingViewModel.kt` 2013 lines, root-package `*Bindings*` 0, root-package `*Controller*` 44, root-package files 177, and root-package Java files 15.
- Guarded by `MainLibraryGroupsRenderListenerTest` and `MainActivityArchitectureContractTest`; compile passed with `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`.
## NOTE 80 - Playback P0 baseline freeze captured (2026-06-29)
- New controlling checkpoint: `docs/PLAYBACK_P0_BASELINE_2026-06-29.md`.
- Baseline facts at capture: `EchoPlaybackService.java` was 2469 lines, `PlaybackQueueManager.QueueProvider` had 33 methods, and the dirty worktree already contained the ongoing MainActivity/library-listener migration entries listed in that checkpoint. Those entries are preserved as user-owned migration work.
- Verification passed with default Gradle daemon/workers: `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain` and focused playback tests via `.\gradlew.bat :feature:playback:testDebugUnitTest :app:testDebugUnitTest --tests "app.yukine.playback.*" --tests "app.yukine.*Playback*" --tests "app.yukine.*Queue*" --tests "app.yukine.*NowPlaying*" --tests "app.yukine.*Lyrics*" --tests "app.yukine.*Media*" --tests "app.yukine.*Notification*" --console=plain`.
- Device smoke for local playback, background playback, notification controls, queue restore, lyrics, and streaming playback is explicitly recorded as not run. The next playback implementation slice should start with a small queue-authority change and must not expand `QueueProvider`.
## NOTE 81 - Playback target architecture clarified (2026-06-29)
- Controlling target: `UI / Compose -> ViewModel / UI State -> playback command owner -> PlaybackService boundary -> playback engine managers -> ExoPlayer / MediaSession / Notification`.
- `EchoPlaybackService` should converge toward an Android/Media3 shell for lifecycle, MediaSession, foreground notification, service binding, external intents/media buttons, and ExoPlayer ownership. Queue strategy, playable URI/media item resolution, cache policy, notification action mapping, and lyrics sync should move into small real owners.
- Preferred owners for upcoming slices are `PlaybackQueueManager`, `PlaybackItemResolver`, `PlaybackCachePolicy`, `PlaybackNotificationController`, and `LyricsPlaybackBridge` or existing equivalents. Do not introduce a universal `PlaybackServiceFacade`.
- Queue/current-index/current-track must have one authority, preferably `PlaybackQueueManager`, with Service and UI consuming snapshots instead of mirroring mutable state across Service fields, ViewModels, manager state, and MediaSession queue.
- Large playback interfaces default to reduction. `PlaybackQueueManager.QueueProvider` should lose derivable methods, split by responsibility, or be replaced with a direct state owner dependency; do not add methods without a prior split/merge/inline plan.
- Playback validation priority is queue restore, skip next/previous, replace current track, local vs streaming URI resolution, notification action, background playback, service restart recovery, and cache partial failure cleanup.
## NOTE 82 - Playback queue restore filter moved into queue manager (2026-06-29)
- P1 queue-authority slice: `PlaybackQueueManager` now owns the restored-queue track eligibility rule that was previously exposed through `PlaybackQueueManager.QueueProvider.isRestorableQueueTrack(...)` and implemented by `EchoPlaybackService`.
- `QueueProvider` shrank from 33 to 32 methods. `EchoPlaybackService` no longer implements the provider method or carries the private `isRestorableQueueTrack(Track)` helper; the Service now delegates queue restore to `playbackQueueManager.restorePlaybackQueue()` without providing that policy.
- Behavior preserved: restore still rejects negative ids, blank data paths, missing local URIs, missing file targets, and empty URI strings, while allowing streaming placeholders and valid non-file URIs. `PlaybackQueueManagerTest.restorePlaybackQueueFiltersInvalidTracksInsideManager` covers that boundary.
- Guarded by `PlaybackQueueManagerTest` and `MainActivityArchitectureContractTest`, with app compile passing under default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`. Current recheck: `EchoPlaybackService.java` 2442 lines and `PlaybackQueueManager.QueueProvider` 32 methods.
## NOTE 83 - Playback queue current index moved into queue manager (2026-06-29)
- P1 queue-authority slice: `PlaybackQueueManager` now owns current-index state and clamping. `PlaybackQueueRuntimeStateManager` only owns `playerMirrorsQueue`; it no longer stores or clamps current index.
- `QueueProvider` shrank from 32 to 30 methods by deleting `currentIndex()` and `setCurrentIndex(index)`. `EchoPlaybackService` now reads/writes the queue cursor through `playbackQueueManager.currentIndex()`, `setCurrentIndex(...)`, `clampCurrentIndex(...)`, and `setClampedCurrentIndex(...)`.
- The queue owner now owns index updates for restore, append, next/previous, move, remove, replace, mirrored-queue reuse, and persistence. `PlaybackQueueManagerTest.currentIndexStateIsOwnedByQueueManager` covers set/clamp behavior, while the existing queue tests cover restore, previous, replace-current recovery, and mirrored queue reuse.
- Guarded by `PlaybackQueueManagerTest`, `PlaybackQueueRuntimeStateManagerTest`, and `MainActivityArchitectureContractTest`, with app compile passing under default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest --tests app.yukine.playback.PlaybackQueueRuntimeStateManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`. Current recheck: `EchoPlaybackService.java` 2436 lines and `PlaybackQueueManager.QueueProvider` 30 methods.
## NOTE 84 - Playback queue resume-request persistence moved into queue manager (2026-06-29)
- P1 queue-authority slice: queue-driven resume-request writes now go directly through `PlaybackQueueManager -> PlaybackQueueStore.saveResumeRequested(...)` instead of `PlaybackQueueManager.QueueProvider.savePlaybackResumeRequested(...)`.
- `QueueProvider` shrank from 30 to 29 methods. `EchoPlaybackService` still keeps its private `savePlaybackResumeRequested(...)` helper for service-owned pause/stop/Media3 boundary events, but it no longer supplies that persistence callback to the queue manager.
- `PlaybackQueueManagerTest.queuePlaybackStartPersistsResumeRequestThroughStore` and the mirrored-queue reuse tests cover the queue-owned resume-request path. `MainActivityArchitectureContractTest.playbackQueueMutationKeepsOneClearlyNamedEntryPointCluster` guards against the provider method returning.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`. Current recheck: `EchoPlaybackService.java` 2431 lines and `PlaybackQueueManager.QueueProvider` 29 methods.
## NOTE 85 - Playback sleep timer command owner binding narrowed (2026-07-03)
- Wiring slice: `PlaybackSleepTimerCommandOwner` no longer keeps a `Supplier<PlaybackSleepTimerManager>` just to work around Service initialization order. `EchoPlaybackService` now creates the command owner, creates `PlaybackSleepTimerManager`, and immediately calls `bindPlaybackSleepTimerManager(...)`.
- This removes one supplier forwarding chain from Service without adding an owner or changing sleep-timer runtime policy. The remaining `() -> playbackSleepTimerManager` occurrence belongs to shutdown resource release wiring and is left for a P5 lifecycle audit instead of mixing shutdown behavior into this slice.
- `MainActivityArchitectureContractTest` now rejects the removed supplier/provider field and requires the bind call, while `PlaybackSleepTimerCommandOwnerTest` covers bound delegation and the unbound no-op state.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackSleepTimerCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks for this change. Current recheck: `EchoPlaybackService.java` 1330 lines, `private Playback*` fields 55, `fromPlaybackQueueManager(...)` 0, Service `queueStateSnapshot()` direct suppliers 0, and `Playback*Owner.java` files 43. Device smoke was not run because this slice did not change notification, background, lyrics, shutdown, or service lifecycle behavior.
## NOTE 86 - Playback crossfade command owner binding narrowed (2026-07-03)
- Wiring slice: `PlaybackCrossfadeCommandOwner` no longer keeps a `Supplier<PlaybackCrossfadeAdvanceManager>` for Service initialization order. `EchoPlaybackService` now creates the command owner, creates `PlaybackCrossfadeAdvanceManager`, and immediately calls `bindPlaybackCrossfadeAdvanceManager(...)`.
- This removes a second Service supplier forwarding chain in the same pattern as NOTE 85 without adding an owner or moving crossfade policy back into Service. The remaining `() -> playbackCrossfadeAdvanceManager` occurrence belongs to shutdown resource release wiring and is deferred to the P5 lifecycle audit.
- `MainActivityArchitectureContractTest` now rejects the removed crossfade supplier/provider field and requires the bind call. `PlaybackCrossfadeCommandOwnerTest` covers bound cancel/start delegation plus the unbound no-op state.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCrossfadeCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks for this change. Current two-slice recheck: `EchoPlaybackService.java` 1330 lines, `private Playback*` fields 55, `fromPlaybackQueueManager(...)` 0, Service `queueStateSnapshot()` direct suppliers 0, and `Playback*Owner.java` files 43. Focused tests protecting the batch: `PlaybackSleepTimerCommandOwnerTest`, `PlaybackCrossfadeCommandOwnerTest`, and `MainActivityArchitectureContractTest`. Device smoke was not run because this slice did not change notification, background, lyrics, shutdown, or service lifecycle behavior.
## NOTE 87 - Playback queue state owner manager supplier removed (2026-07-03)
- Queue/state slice: `PlaybackQueueStateOwner` no longer accepts or stores `Supplier<PlaybackQueueManager>`. Production already used default construction plus `bindPlaybackQueueManager(...)`; tests now use a direct manager constructor or explicit bind/rebind instead of preserving a dynamic provider seam.
- This removes a queue-state supplier entry point and one constructor shape from the central queue-state owner without adding another owner or exposing `queueStateSnapshot()` publicly. The state owner still reads queue/current-track/current-index/queue-size from `PlaybackQueueManager` as the authority.
- `MainActivityArchitectureContractTest` now rejects `Supplier<PlaybackQueueManager>` and `playbackQueueManagerSupplier` inside `PlaybackQueueStateOwner`, while requiring the direct manager constructor and bind method. `PlaybackQueueStateOwnerTest` now covers bind/rebind plus empty-state behavior.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.playback.PlaybackCrossfadeStateOwnerTest --tests app.yukine.playback.PlaybackErrorRecoveryCommandOwnerTest --tests app.yukine.playback.PlaybackFavoriteCommandOwnerTest --tests app.yukine.playback.PlaybackLyricsStateOwnerTest --tests app.yukine.playback.PlaybackNotificationStateOwnerTest --tests app.yukine.playback.PlaybackPlayHistoryRecorderTest --tests app.yukine.playback.PlaybackPrecacheStateOwnerTest --tests app.yukine.playback.PlaybackQueueCommandOwnerTest --tests app.yukine.playback.PlaybackQueueMirroredPlayerOwnerTest --tests app.yukine.playback.PlaybackQueueMirroredTransitionOwnerTest --tests app.yukine.playback.PlaybackQueueMutationOwnerTest --tests app.yukine.playback.PlaybackSessionCommandOwnerTest --tests app.yukine.playback.PlaybackStateSnapshotOwnerTest --tests app.yukine.playback.PlaybackVisualizationCacheStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Current recheck: `EchoPlaybackService.java` 1330 lines, `private Playback*` fields 55, `fromPlaybackQueueManager(...)` 0, Service `queueStateSnapshot()` direct suppliers 0, and `Playback*Owner.java` files 43. Device smoke was not run because this slice did not change notification, background, lyrics, shutdown, or service lifecycle behavior.
## NOTE 88 - Playback visualization snapshot supplier removed (2026-07-03)
- Wiring slice: `PlaybackStateSnapshotOwner.fromVisualizationAnalyzer(...)` now receives the already-created `PlaybackVisualizationAnalyzer` directly instead of a `Supplier<PlaybackVisualizationAnalyzer>`. `EchoPlaybackService` still owns analyzer construction, but no longer keeps a lambda forwarding chain for snapshot visualization reads.
- This does not add an owner or move visualization/cache/session policy. It only narrows the snapshot adapter boundary and leaves URI/media item resolution in `PlaybackMediaSourceProvider`, cache policy in existing playback cache owners, and notification/lyrics/shutdown untouched.
- `MainActivityArchitectureContractTest` now requires the direct analyzer adapter and rejects `fromVisualizationAnalyzerProvider(...)`, `Supplier<PlaybackVisualizationAnalyzer>`, and `visualizationAnalyzerProvider`. `PlaybackStateSnapshotOwnerTest.visualizationProviderUsesEmptySnapshotsWhenAnalyzerIsMissing` covers the missing-analyzer fallback.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackStateSnapshotOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Current recheck: `EchoPlaybackService.java` 1425 lines, `private Playback*` fields 43, Service `fromPlaybackQueueManager(...)` count 0, Service `queueStateSnapshot()` direct suppliers 0, and `Playback*Owner.java` files 43. Device smoke remains out of scope because this slice does not change notification, background, lyrics, shutdown, or service lifecycle behavior.
## NOTE 89 - Playback queue completion lambda forwarding removed (2026-07-03)
- Queue/service wiring slice: `EchoPlaybackService` no longer routes queue completion work through `withPlaybackQueueCompletionOwner(Consumer<PlaybackQueueCompletionOwner>)` method references or lambdas. It now creates the short-lived `PlaybackQueueCompletionOwner` through `playbackQueueCompletionOwner()` and invokes the semantic completion/preparation method directly.
- This keeps queue completion policy in `PlaybackQueueCompletionOwner` and `PlaybackQueueManager`, does not resurrect `PlaybackQueueStopClearOwner`, and does not add a Service field or new owner. The slice removes one Service lambda/method-reference forwarding chain while preserving the no-direct-queue-manager rule for stop/clear and completion preparation.
- `MainActivityArchitectureContractTest` now rejects `withPlaybackQueueCompletionOwner(...)` and `Consumer<PlaybackQueueCompletionOwner>`, while requiring direct `playbackQueueCompletionOwner().playAfterCompletion()`, `prepareStopAndClearPlaybackState()`, `prepareStopAtEndOfQueue()`, and `prepareStopAfterAutomaticAdvance(completedIndex)` calls. `PlaybackQueueCompletionOwnerTest` continues to cover completion routing and stop preparation behavior.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCompletionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Current recheck: `EchoPlaybackService.java` 1423 lines, `private Playback*` fields 43, Service `fromPlaybackQueueManager(...)` count 0, Service `queueStateSnapshot()` direct suppliers 0, and `Playback*Owner.java` files 43. Device smoke remains out of scope because this slice does not change notification, background, lyrics, shutdown, or service lifecycle behavior.
## NOTE 90 - Playback queue navigation lambda forwarding removed (2026-07-03)
- Queue/service wiring slice: `EchoPlaybackService` no longer routes queue navigation through `withPlaybackQueueNavigationOwner(Consumer<PlaybackQueueNavigationOwner>)` method references. It now creates the short-lived `PlaybackQueueNavigationOwner` through `playbackQueueNavigationOwner()` and invokes semantic navigation methods directly.
- This also removes the duplicate inline `new PlaybackQueueNavigationOwner(...)` construction in mirrored-queue reuse. Queue navigation policy remains in `PlaybackQueueNavigationOwner` and `PlaybackQueueManager`; Service still does not call `playbackQueueManager.skipToNextImmediately()`, `skipToPrevious()`, `playFirstQueuedTrack()`, or `reuseMirroredQueueIfAvailable(...)` directly.
- `MainActivityArchitectureContractTest` now rejects `withPlaybackQueueNavigationOwner(...)` and `Consumer<PlaybackQueueNavigationOwner>`, while requiring direct `playbackQueueNavigationOwner().skipToNextImmediately()`, `playFirstQueuedTrack()`, `skipToPrevious()`, and mirrored-queue reuse calls. `PlaybackQueueNavigationOwnerTest` continues to cover navigation delegation and mirrored-queue reuse notifications.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueNavigationOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Current recheck: `EchoPlaybackService.java` 1420 lines, `private Playback*` fields 43, Service `fromPlaybackQueueManager(...)` count 0, Service `queueStateSnapshot()` direct suppliers 0, and `Playback*Owner.java` files 43. Device smoke remains out of scope because this slice does not change notification, background, lyrics, shutdown, or service lifecycle behavior.
## NOTE 91 - Playback queue persistence lambda forwarding removed (2026-07-03)
- Queue/service wiring slice: `EchoPlaybackService` no longer routes playback-resume and task-removed queue persistence through `withPlaybackQueuePersistenceOwner(Consumer<PlaybackQueuePersistenceOwner>)`. It now creates a short-lived `PlaybackQueuePersistenceOwner` through `playbackQueuePersistenceOwner()` and invokes persistence methods directly.
- This keeps queue persistence in `PlaybackQueuePersistenceOwner` and `PlaybackQueueManager`; Service still does not call `playbackQueueManager.persistQueueState()` or `savePlaybackResumeRequested(...)` directly. The existing shutdown lifecycle local `PlaybackQueuePersistenceOwner` remains unchanged, so this slice does not move or expand P5 shutdown ownership.
- `MainActivityArchitectureContractTest` now rejects `withPlaybackQueuePersistenceOwner(...)` and `Consumer<PlaybackQueuePersistenceOwner>`, while requiring direct `playbackQueuePersistenceOwner().requestPlaybackResume()`, `clearPlaybackResumeRequest()`, and task-removed persistence calls. `PlaybackQueuePersistenceOwnerTest` and `PlaybackShutdownLifecycleResourcesOwnerTest` protect the owner and lifecycle-store behavior.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueuePersistenceOwnerTest --tests app.yukine.playback.PlaybackShutdownLifecycleResourcesOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Current recheck: `EchoPlaybackService.java` 1419 lines, `private Playback*` fields 43, Service `fromPlaybackQueueManager(...)` count 0, Service `queueStateSnapshot()` direct suppliers 0, and `Playback*Owner.java` files 43. Device smoke remains out of scope because this slice does not change notification, background playback, lyrics, shutdown coordinator behavior, or service destruction behavior.
## NOTE 92 - Playback queue restore lambda forwarding removed (2026-07-03)
- Queue/service wiring slice: `EchoPlaybackService` no longer routes queue restore through `withPlaybackQueueRestoreOwner(Consumer<PlaybackQueueRestoreOwner>)`. It now creates a short-lived `PlaybackQueueRestoreOwner` through `playbackQueueRestoreOwner()` and invokes restore methods directly.
- This keeps restore policy in `PlaybackQueueRestoreOwner` and `PlaybackQueueManager`; Service still does not call `playbackQueueManager.restorePlaybackQueue()`, `restoreLastPlayback(...)`, or `setPlaybackRestoreEnabled(...)` directly. No owner, field, facade, or package move was added.
- `MainActivityArchitectureContractTest` now rejects `withPlaybackQueueRestoreOwner(...)` and `Consumer<PlaybackQueueRestoreOwner>`, while requiring direct `playbackQueueRestoreOwner().restorePlaybackQueue()`, `restoreLastPlayback(playWhenRestored)`, and `setPlaybackRestoreEnabled(enabled)` calls. `PlaybackQueueRestoreOwnerTest` continues to protect restore, publish, prepare, and setting delegation behavior.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueRestoreOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Current recheck: `EchoPlaybackService.java` 1419 lines, `private Playback*` fields 43, Service `fromPlaybackQueueManager(...)` count 0, Service `queueStateSnapshot()` direct suppliers 0, and `Playback*Owner.java` files 43. Device smoke remains out of scope because this slice does not change notification, background playback, lyrics, shutdown, or service lifecycle behavior.
## NOTE 93 - Playback queue mutation lambda forwarding removed (2026-07-03)
- Queue/service wiring slice: `EchoPlaybackService` no longer routes public queue mutation calls through `withPlaybackQueueMutationOwner(Consumer<PlaybackQueueMutationOwner>)`. It now creates the short-lived `PlaybackQueueMutationOwner` through `playbackQueueMutationOwner()` and invokes mutation methods directly for play queue, append, move, remove, retain, clear, and replace-by-id.
- This removes one Service lambda/method-reference forwarding chain and one duplicate inline owner construction in session media-item handling. Mutation policy remains in `PlaybackQueueMutationOwner` and `PlaybackQueueManager`; Service still does not call `playbackQueueManager.playQueue(...)`, `appendToQueue(...)`, `moveQueueTrack(...)`, `removeTracksById(...)`, `retainTracksById(...)`, `clearQueue()`, or `replaceQueuedTrackById(...)` directly. No owner, field, facade, or package move was added.
- `MainActivityArchitectureContractTest` now rejects `withPlaybackQueueMutationOwner(...)` and `Consumer<PlaybackQueueMutationOwner>`, while requiring direct `playbackQueueMutationOwner().playQueue(...)`, `appendToQueue(...)`, `moveQueueTrack(...)`, `removeTracksById(...)`, `retainTracksById(...)`, `clearQueue()`, and `replaceQueuedTrackById(...)` calls. `PlaybackQueueMutationOwnerTest` continues to protect mutation delegation and stop/clear behavior.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMutationOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Current recheck: `EchoPlaybackService.java` 1413 lines, `private Playback*` fields 43, Service `fromPlaybackQueueManager(...)` count 0, Service `queueStateSnapshot()` direct suppliers 0, and `Playback*Owner.java` files 43. Device smoke remains out of scope because this slice does not change notification, background playback, lyrics, shutdown, or service lifecycle behavior.
## NOTE 94 - Playback queue owner/interface audit checkpoint (2026-07-03)
- Audit scope after the queue completion/navigation/persistence/restore/mutation wiring slices: `PlaybackQueueManager.QueueProvider` and `interface QueueProvider` are absent from production playback code, and `MainActivityArchitectureContractTest` already guards against `new PlaybackQueueManager.QueueProvider()`, public `PlaybackQueueManager.currentTrack()`, and Service direct `playbackQueueManager.queueStateSnapshot()` access returning.
- Current queue/state API grouping: true queue commands are `playQueue(...)`, `appendToQueue(...)`, `moveQueueTrack(...)`, `removeTracksById(...)`, `retainTracksById(...)`, `replaceQueuedTrackById(...)`, `replaceCurrentTrackAndResume(...)`, and `replaceCurrentQueueTrack(...)`; navigation/completion commands are `playFirstQueuedTrack()`, `skipToNextImmediately()`, `skipToPrevious()`, `preparePlaybackCompletionAction()`, `prepareStopAtEndOfQueue()`, `prepareStopAfterAutomaticAdvance(...)`, and `prepareStopAndClearPlaybackState()`; persistence/restore commands are `persistQueueState()`, `restorePlaybackQueue()`, `restoreLastPlayback(...)`, and `setPlaybackRestoreEnabled(...)`; read/snapshot outputs are `queueSnapshot()`, `queueStateSnapshot()`, `upcomingTracksForPrecache(...)`, `queuePreparationForNewPlayer()`, `reuseMirroredQueueIfAvailable(...)`, and `applyMirroredTransitionIndex(...)`.
- Current derived-read boundary: `PlaybackQueueManager.QueueStateSnapshot` still stores only `currentTrack`, `currentIndex`, and `queueSize`; `isQueueEmpty`, `hasCurrentTrack`, `hasMultipleTracks`, and `isAtEndOfQueue` remain derived. `PlaybackQueueStateOwner` exposes the small Java-facing read helpers consumed by `PlaybackStateSnapshotOwner`, notification/lyrics/precache/visualization state owners, mirrored queue owners, crossfade, error recovery, and mutation clear checks. No `PlaybackQueueStateOwner` helper was identified as unused in this audit.
- Service wiring audit: `EchoPlaybackService.java` is 1413 lines, has 43 `private Playback*` fields, has 0 Service `fromPlaybackQueueManager(...)` calls, has 0 Service `queueStateSnapshot()` direct suppliers, and `Playback*Owner.java` file count remains 43. The four remaining `playbackQueueStateOwner::currentTrack` constructor dependencies feed distinct owners/managers: runtime state, position persistence, notification artwork, and Wi-Fi lock streaming policy. Do not collapse them into a single `Supplier<Track>` Service field unless the slice also removes a real owner/interface/constructor dependency; otherwise it turns Service into a wiring store.
- Next safe candidates from this audit: first, inspect whether any `PlaybackQueueManager` read output can be narrowed at the consumer boundary without adding a new facade; second, inspect `EchoPlaybackService.queueSnapshot()` callers from UI/app shell and identify whether any can move to an existing semantic playback/queue command owner; third, keep URI/cache audit read-only unless it can add focused resolver/cache behavior tests without expanding `PlaybackMediaSourceResolutionOwner`.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also rechecked app Kotlin/Java compile tasks as up-to-date. Device smoke remains out of scope because this checkpoint is a read-only owner/interface audit and does not change notification, background playback, lyrics, shutdown, service lifecycle, or playback runtime behavior.
## NOTE 95 - Streaming pre-resolve queue snapshot reuse (2026-07-03)
- Queue/read boundary slice: `StreamingPlaybackController.preResolveNextStreamingTrack(...)` now reads `listener.queueSnapshot()` once per active playback-state event and reuses that list for both next-track pre-resolution and queue-window pre-resolution. Before this slice, the controller pulled the same queue snapshot twice, which could route two identical reads through `MainActivityBase -> EchoPlaybackService.queueSnapshot()` for one playback-state event.
- This does not add an owner, facade, field, or package move. The queue source remains the existing `StreamingQueueSnapshotSource`, and streaming URI resolution remains in the existing `StreamingViewModel` / resolver path. The concrete gain is one fewer queue snapshot supplier/Service read per active streaming pre-resolve event.
- Added `StreamingPlaybackControllerTest` coverage for the active path reusing one queue snapshot and the inactive path avoiding queue reads. `MainActivityArchitectureContractTest` now requires the local `val queueSnapshot = listener.queueSnapshot()` and guards the controller against returning to multiple `listener.queueSnapshot()` calls.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.StreamingPlaybackControllerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also rechecked app Kotlin/Java compile tasks. Current recheck: `EchoPlaybackService.java` 1413 lines, `private Playback*` fields 43, Service `fromPlaybackQueueManager(...)` count 0, Service `queueStateSnapshot()` direct suppliers 0, and `Playback*Owner.java` files 43. Device smoke remains out of scope because this slice does not change notification, background playback, lyrics, shutdown, service lifecycle, or playback runtime behavior.
## NOTE 96 - Media source header boundary coverage (2026-07-03)
- URI/cache boundary test slice: `PlaybackMediaSourceProviderTest` now covers `headersForTrack(...)` as a provider-owned rule for both streaming playback headers and WebDAV Basic Auth headers. This locks the intended boundary that playable URI/media item/header resolution remains in `PlaybackMediaSourceProvider`, while cache/precache users consume it through `PlaybackMediaCacheOperations` instead of reimplementing header policy.
- This is a behavior-test strengthening slice only: no production owner, facade, bridge, field, package move, URI resolution path, or cache policy path changed. The concrete gain is stronger focused coverage for a previously undercovered provider boundary, reducing the risk that `PlaybackMediaSourceResolutionOwner` or a cache owner grows into a universal resolver.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackMediaSourceProviderTest --console=plain`. Current recheck: `EchoPlaybackService.java` 1413 lines, `private Playback*` fields 43, Service `fromPlaybackQueueManager(...)` count 0, Service `queueStateSnapshot()` direct suppliers 0, and `Playback*Owner.java` files 43. Device smoke remains out of scope because this slice changes only focused unit coverage and does not change notification, background playback, lyrics, shutdown, service lifecycle, or playback runtime behavior.
## NOTE 97 - Precache cache data source boundary coverage (2026-07-03)
- Cache/precache boundary test slice: `PlaybackPrecacheManagerTest.failedCurrentLeadingRangeCleansActiveRangeForRetry` now asserts that current-track precache opens its cache data source through `PlaybackMediaCacheOperations.cacheDataSourceForTrack(track)`. This pairs with the provider-backed cache-operations/header tests and keeps cache data-source/header policy out of `EchoPlaybackService` and out of a broad media-source resolution owner.
- This is a behavior-test strengthening slice only: no production owner, facade, bridge, field, package move, URI resolution path, or cache policy path changed. The concrete gain is focused coverage for the `PlaybackPrecacheManager -> PlaybackMediaCacheOperations` cache-source boundary, reducing the chance that precache policy slips back into Service wiring.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --console=plain`, which also rechecked app Kotlin/Java compile tasks. Device smoke remains out of scope because this slice changes only focused unit coverage and docs, and does not change notification, background playback, lyrics, shutdown, service lifecycle, or playback runtime behavior.
## NOTE 98 - Playback owner/interface audit after queue read and cache boundary slices (2026-07-03)
- Audit scope after NOTE 95-97: `StreamingPlaybackController` now reuses one queue snapshot per active pre-resolve event, `PlaybackMediaSourceProviderTest` covers provider-owned streaming/WebDAV headers, and `PlaybackPrecacheManagerTest` covers the `PlaybackPrecacheManager -> PlaybackMediaCacheOperations.cacheDataSourceForTrack(track)` cache-source boundary. These are test/read-boundary slices, not owner-count slices.
- Current queue/interface boundary: production playback code still has no `PlaybackQueueManager.QueueProvider` or `interface QueueProvider`; those strings only remain in architecture-contract negative assertions and unrelated recommendation queue-provider names. `PlaybackQueueManager.QueueStateSnapshot` still stores only `currentTrack`, `currentIndex`, and `queueSize`, with queue booleans derived. `PlaybackQueueStateOwner` remains the Java-facing read owner and keeps `queueStateSnapshot()` private.
- Current Service/cache/resolve boundary: `EchoPlaybackService.java` is 1318 lines, has 55 top-level `Playback...` field declarations, 24 top-level `Playback*Owner` field declarations, 5 short-lived queue-owner factory methods, 0 Service `fromPlaybackQueueManager(...)` calls, 0 Service direct `queueStateSnapshot(...)` calls, and 43 `Playback*Owner.java` files. `PlaybackMediaSourceResolutionOwner` has no production code hits; URI/media item/header resolution remains in `PlaybackMediaSourceProvider`, and cache/precache policy remains behind `PlaybackPrecacheManager` plus `PlaybackMediaCacheOperations`.
- Remaining risk and next candidates at this checkpoint: `MainActivityBase` direct `playbackService.queueSnapshot()` call sites should be narrowed without adding a facade (handled in NOTE 99). The four remaining `playbackQueueStateOwner::currentTrack` dependencies still feed distinct runtime-state, position, notification-artwork, and Wi-Fi-lock owners; do not collapse them into a single Service supplier unless that also removes a real constructor/interface dependency.
- Verification passed at T1 with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.StreamingPlaybackControllerTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackMediaSourceProviderTest --console=plain`. Device smoke remains out of scope because this checkpoint is read-only/docs-only and does not change notification, background playback, lyrics, shutdown, service lifecycle, or playback runtime behavior.
## NOTE 99 - MainActivity queue snapshot direct reads narrowed (2026-07-03)
- Queue/read boundary slice: `MainActivityBase` now routes heartbeat seed binding, streaming playback listener queue reads, queue ViewModel binding, queue rendering, and current streaming queue resolution through the existing `playbackQueueSnapshot()` helper instead of repeating `playbackService.queueSnapshot()` at each call site.
- This removes five Activity-to-Service queue snapshot read sites without adding an owner, facade, bridge, field, package move, or changing queue authority. The only remaining direct `playbackService.queueSnapshot()` in `MainActivityBase` is inside `playbackQueueSnapshot()`, so future callers must use the existing source boundary rather than adding new inline null-check suppliers.
- `MainActivityArchitectureContractTest` now asserts there is exactly one `playbackService.queueSnapshot()` occurrence in `MainActivityBase` and that the streaming playback listener uses `this::playbackQueueSnapshot`. Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also recompiled app Kotlin/Java after the Java method-reference changes. Device smoke remains out of scope because this slice changes only host wiring and architecture-contract coverage, not notification, background playback, lyrics, shutdown, service lifecycle, or playback runtime behavior.
## NOTE 100 - Queue state snapshot derived-read contract strengthened (2026-07-03)
- Queue/interface boundary test slice: `PlaybackQueueManagerTest` now directly covers that `QueueStateSnapshot` booleans are derived from only `currentTrack`, `currentIndex`, and `queueSize`. This protects the current narrow snapshot shape instead of adding stored booleans or widening the old provider-style read surface.
- The public API audit found `PlaybackQueueManager.currentIndex()` and `setCurrentIndex(...)` are already private implementation details; no production method was safe to delete in this slice because remaining public methods are consumed by existing queue command, navigation, completion, restore, persistence, mirrored-transition, preparation, or state owners. `MainActivityArchitectureContractTest` now guards the private index helper shape so those cursor methods do not return as public queue API.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this slice changes only focused unit/contract coverage and does not change notification, background playback, lyrics, shutdown, service lifecycle, or playback runtime behavior.
## NOTE 101 - Playback queue/read owner-interface audit after NOTE 99-100 (2026-07-03)
- Audit scope after NOTE 99-100: Activity queue reads were narrowed to the existing `playbackQueueSnapshot()` helper, and `PlaybackQueueManager.QueueStateSnapshot` derived-read behavior plus private cursor helpers are now covered by focused tests/contracts. These two slices did not add owners, facades, bridges, fields, or package moves.
- Current metrics: `EchoPlaybackService.java` is 1318 lines, has 55 top-level `Playback...` field declarations, 24 top-level `Playback*Owner` field declarations, 0 Service `fromPlaybackQueueManager(...)` calls, 0 Service direct `queueStateSnapshot(...)` calls, 43 `Playback*Owner.java` files, 1 direct `MainActivityBase` `playbackService.queueSnapshot()` occurrence inside `playbackQueueSnapshot()`, and 4 `playbackQueueStateOwner::currentTrack` Service dependencies.
- Current boundary findings: `PlaybackQueueManager.currentIndex()` and `setCurrentIndex(...)` remain private implementation details; `QueueStateSnapshot` still stores only `currentTrack`, `currentIndex`, and `queueSize`; production playback code still has no `PlaybackQueueManager.QueueProvider`. The four remaining `playbackQueueStateOwner::currentTrack` dependencies feed feature/runtime owners across module boundaries (runtime gain, position persistence, notification artwork, and Wi-Fi lock policy), so collapsing them into a Service-level supplier would make Service a wiring store without reducing an owner/interface.
- Next safe candidates: inspect whether a feature-module state-provider helper can replace one remaining `currentTrackSupplier` without app-to-feature dependency inversion, or continue with resolver/cache behavior coverage. Defer notification/lyrics/shutdown/background behavior changes until the smoke table is stable.
- Verification for this audit uses the already-passed T1 command from NOTE 100: `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this checkpoint is read-only/docs-only and does not change notification, background playback, lyrics, shutdown, service lifecycle, or playback runtime behavior.
## NOTE 102 - Wi-Fi lock current-track supplier removed from Service wiring (2026-07-03)
- Queue/state wiring slice: `PlaybackWifiLockManager` now receives the feature playback `PlaybackQueueManager` and reads `queueStateSnapshot().currentTrack` internally, instead of receiving `playbackQueueStateOwner::currentTrack` from `EchoPlaybackService`.
- This removes one Service current-track supplier chain without adding an owner, facade, bridge, package move, or moving streaming URI/media item/cache policy into Service. The remaining `playbackQueueStateOwner::currentTrack` Service dependencies are now 3 and still feed runtime state, position persistence, and notification artwork.
- `PlaybackWifiLockManagerTest` now builds current-track state through `PlaybackQueueManager.playQueue(...)`, so the focused behavior test exercises the same queue authority used at runtime. `MainActivityArchitectureContractTest` now requires the Service to pass `playbackQueueManager`, rejects the old Wi-Fi lock `playbackQueueStateOwner::currentTrack` wiring, and locks the remaining Service current-track supplier count at 3.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackWifiLockManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Current recheck: `EchoPlaybackService.java` 1413 lines, strict `private Playback*` field declarations 53, strict `private Playback*Owner` field declarations 22, Service `fromPlaybackQueueManager(...)` 0, Service `queueStateSnapshot()` direct suppliers 0, and `Playback*Owner.java` files 43. Device smoke remains out of scope because this slice does not change notification, background playback, lyrics, shutdown, service lifecycle, or playback runtime behavior.
## NOTE 103 - Position persistence current-track supplier removed from Service wiring (2026-07-03)
- Queue/state wiring slice: `PlaybackPositionManager.stateProviderFromPlaybackState(...)` now receives the feature playback `PlaybackQueueManager` and reads `queueStateSnapshot().currentTrack` internally, instead of receiving `playbackQueueStateOwner::currentTrack` from `EchoPlaybackService`.
- This removes one more Service current-track supplier chain without adding an owner, facade, bridge, package move, or moving position persistence back into Service. The remaining `playbackQueueStateOwner::currentTrack` Service dependencies are now 2 and still feed runtime replay-gain state plus notification artwork.
- `PlaybackPositionManagerTest` now builds current-track state through `PlaybackQueueManager.playQueue(...)`, so position persistence uses the same queue authority path as runtime playback. `MainActivityArchitectureContractTest` now requires the position state provider to receive `playbackQueueManager`, rejects the old position `playbackQueueStateOwner::currentTrack` wiring, and locks the remaining Service current-track supplier count at 2.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackPositionManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Current recheck: `EchoPlaybackService.java` 1413 lines, strict `private Playback*` field declarations 53, strict `private Playback*Owner` field declarations 22, Service `fromPlaybackQueueManager(...)` 0, Service `queueStateSnapshot()` direct suppliers 0, and `Playback*Owner.java` files 43. Device smoke remains out of scope because this slice does not change notification, background playback, lyrics, shutdown, service lifecycle, or playback runtime behavior.
## NOTE 104 - Playback owner/interface audit after NOTE 102-103 (2026-07-03)
- Audit scope after NOTE 102-103: Wi-Fi lock streaming policy and position persistence both now read current-track state from `PlaybackQueueManager.queueStateSnapshot()` inside `feature:playback`, instead of receiving `playbackQueueStateOwner::currentTrack` from `EchoPlaybackService`. These slices did not add owners, facades, bridges, fields, package moves, URI/media item resolution paths, cache policy paths, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior.
- Current metrics: `EchoPlaybackService.java` is 1413 lines, strict `private Playback*` field declarations are 53, strict `private Playback*Owner` field declarations are 22, Service `fromPlaybackQueueManager(...)` calls are 0, Service direct `queueStateSnapshot()` calls are 0, `Playback*Owner.java` files are 43, and Service `playbackQueueStateOwner::currentTrack` dependencies are down from 4 at NOTE 101 to 2.
- Current boundary findings: `PlaybackQueueManager.QueueStateSnapshot` still stores only `currentTrack`, `currentIndex`, and `queueSize`; production playback code still has no `PlaybackQueueManager.QueueProvider`; Service still does not call `playbackQueueManager.queueStateSnapshot()` directly. The two remaining `playbackQueueStateOwner::currentTrack` dependencies feed runtime replay-gain state and notification artwork refresh. Do not collapse them into a global Service supplier; next code slice should only continue if it removes a real constructor/interface dependency or strengthens focused behavior coverage.
- T1 verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackWifiLockManagerTest --tests app.yukine.playback.PlaybackPositionManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this checkpoint is read-only/docs-only and does not change notification, background playback, lyrics, shutdown, service lifecycle, or playback runtime behavior.
## NOTE 105 - Playback queue snapshot interface audit after queue-state owner narrowing (2026-07-03)
- Audit scope after `822cabac4`: `PlaybackQueueStateOwner` now exposes one package-local `queueStateSnapshot()` read boundary plus `currentTrack()`, `queueSnapshot()`, and `upcomingTracksForPrecache(...)`; it no longer exposes `currentIndex()`, `queueSize()`, `isQueueEmpty()`, `hasMultipleTracks()`, or `isAtEndOfQueue()` as separate derived methods. Consumers that need derived booleans or counts read one `PlaybackQueueManager.QueueStateSnapshot` and derive from its source fields. This removed five Java-facing derived queue-state methods without adding an owner, facade, bridge, field, package move, or Service strategy branch.
- Stop/clear completion candidate status: `PlaybackQueueStopClearOwner.java` and `PlaybackQueueStopClearOwnerTest.java` are absent and guarded by `MainActivityArchitectureContractTest`; production has 0 `fromPlaybackQueueManager(...)` calls; `PlaybackQueueCompletionOwner` directly owns the remaining stop/clear, stop-at-end, and automatic-advance preparation calls into `PlaybackQueueManager`. There is no current stop/clear merge slice left that would reduce a field, factory, or `fromPlaybackQueueManager` call without inventing a new layer, so do not keep reopening this candidate mechanically.
- Current queue/API grouping: queue mutation commands remain in `PlaybackQueueMutationOwner`; navigation commands remain in `PlaybackQueueNavigationOwner`; completion preparation remains in `PlaybackQueueCompletionOwner`; persistence/restore remain in `PlaybackQueuePersistenceOwner` and `PlaybackQueueRestoreOwner`; read outputs remain `PlaybackQueueStateOwner` plus direct feature-module manager tests. `PlaybackQueueManager.currentIndex()` and `setCurrentIndex(...)` remain private; `QueueStateSnapshot` still stores only `currentTrack`, `currentIndex`, and `queueSize`; production playback code still has no `PlaybackQueueManager.QueueProvider`.
- Current metrics: `EchoPlaybackService.java` is 1413 lines, strict `private Playback*` field declarations are 53, strict `private Playback*Owner` field declarations are 22, Service `fromPlaybackQueueManager(...)` calls are 0, Service direct `queueStateSnapshot()` calls are 0, `Playback*Owner.java` files are 43, Service `playbackQueueStateOwner::currentTrack` dependencies remain 2, and `PlaybackQueueStateOwner` derived method count for `currentIndex/queueSize/isQueueEmpty/hasMultipleTracks/isAtEndOfQueue` is 0.
- Verification for the code slice passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.playback.PlaybackCrossfadeStateOwnerTest --tests app.yukine.playback.PlaybackErrorRecoveryCommandOwnerTest --tests app.yukine.playback.PlaybackNotificationStateOwnerTest --tests app.yukine.playback.PlaybackQueueMutationOwnerTest --tests app.yukine.playback.PlaybackQueueMirroredTransitionOwnerTest --tests app.yukine.playback.PlaybackStateSnapshotOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. This follow-up audit is docs-only; device smoke remains out of scope because it does not change notification, background playback, lyrics, shutdown, service lifecycle, or playback runtime behavior.
- Next safe candidates: first, inspect the two remaining Service `playbackQueueStateOwner::currentTrack` constructor dependencies separately and only change one if it removes a constructor/interface dependency rather than adding a generic supplier; second, add resolver/cache behavior coverage if a concrete `PlaybackMediaSourceProvider` or `PlaybackPrecacheManager` gap is found; third, continue public queue API grouping only when a method can move from public to private or from Service wiring into an existing owner with focused tests.
## NOTE 106 - Playback owner/interface audit after cache fallback coverage (2026-07-03)
- Audit scope after `f7d645dc5`: `PlaybackPrecacheManagerTest.cacheStateReadFailureFallsBackToManagerOwnedCacheAttempt` now fixes the cache-state read failure behavior at the `PlaybackPrecacheManager -> PlaybackMediaCacheOperations` boundary. This is a behavior-test strengthening slice; it did not add an owner, facade, bridge, field, package move, Service strategy branch, URI/media item resolver path, or cache policy path.
- Current metrics from the working tree: `EchoPlaybackService.java` is 1318 lines, strict `private Playback*` field declarations are 53, strict `private Playback*Owner` field declarations are 22, production `fromPlaybackQueueManager(...)` calls are 0, Service `playbackQueueStateOwner::currentTrack` dependencies remain 2, production `Playback*Owner` files under `app/src/main/java` plus `feature/playback/src/main/java` are 44 by the recursive file metric, and production code still has no `PlaybackMediaSourceResolutionOwner`, `PlaybackQueueManager.QueueProvider`, or `interface QueueProvider` hits.
- Current queue/read boundary: `PlaybackQueueStateOwner.currentTrack()` remains as one package-local current-track convenience read. It is still consumed by multiple Java owners plus the two Service method references, so deleting it now would be a horizontal rewrite unless paired with a real constructor/interface reduction. Do not replace the remaining Service references with a generic `Supplier<Track>` field; that would turn `EchoPlaybackService` into a wiring store without reducing state authority.
- Remaining high-risk edge: the two Service `playbackQueueStateOwner::currentTrack` dependencies feed runtime replay-gain state and notification artwork. The runtime path may be a future candidate only if `PlaybackRuntimeStateManager` can read queue authority without cyclic construction or a broader interface. The notification artwork path stays deferred with P4 unless smoke coverage is stable.
- Verification for the added behavior slice passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --console=plain`. This audit is docs-only; device smoke remains out of scope because it does not change notification, background playback, lyrics, shutdown, service lifecycle, or playback runtime behavior.
## NOTE 107 - Runtime replay-gain current-track supplier removed from Service wiring (2026-07-03)
- Queue/state wiring slice: `PlaybackRuntimeStateManager.stateProviderFromPlaybackState(...)` now receives a `Supplier<PlaybackQueueManager?>` and derives replay-gain current-track state from `queueStateSnapshot().currentTrack` inside `feature:playback`. `EchoPlaybackService` passes `() -> playbackQueueManager` instead of `playbackQueueStateOwner::currentTrack`, so runtime volume policy no longer depends on a Service-level current-track supplier.
- This removes one Service current-track supplier chain without adding an owner, facade, bridge, field, package move, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. The delayed supplier also avoids the earlier field-initialization risk where passing the `playbackQueueManager` field value directly would capture null before `onCreate` binding.
- `PlaybackRuntimeStateManagerTest` now builds replay-gain current-track state through `PlaybackQueueManager.playQueue(...)`, covering the same queue authority path used at runtime. `MainActivityArchitectureContractTest` requires the Service runtime state provider to pass `() -> playbackQueueManager`, rejects the old runtime `playbackQueueStateOwner::currentTrack` wiring, rejects `currentTrackSupplier: Supplier<Track?>?`, and locks Service `playbackQueueStateOwner::currentTrack` occurrences at 1.
- Current metrics after the slice: `EchoPlaybackService.java` is 1318 lines, strict `private Playback*` field declarations are 53, strict `private Playback*Owner` field declarations are 22, production `fromPlaybackQueueManager(...)` calls are 0, Service `playbackQueueStateOwner::currentTrack` dependencies are down from 2 to 1, and production `Playback*Owner` files under `app/src/main/java` plus `feature/playback/src/main/java` are 44 by the recursive file metric. The remaining Service current-track supplier feeds notification artwork and stays deferred with P4 until smoke coverage is stable.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackRuntimeStateManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also recompiled app Kotlin/Java after the feature manager signature change. Device smoke remains out of scope because this slice does not change notification, background playback, lyrics, shutdown, service lifecycle, or playback runtime behavior beyond the tested replay-gain queue-state source.
## NOTE 108 - Playback owner/interface audit after runtime and visualization cache slices (2026-07-03)
- Audit scope after NOTE 107 and `6b0cc0bdd`: runtime replay-gain state now reads current-track state from `PlaybackQueueManager.queueStateSnapshot()` inside `feature:playback`, and `PlaybackVisualizationCacheManagerTest.partiallyCachedVisualizationWindowResumesFromCachedBytes` covers partial-cache resume behavior through `PlaybackMediaCacheOperations.cachedBytesInRange(...)`. These slices did not add owners, facades, bridges, fields, package moves, URI/media item resolver paths, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior.
- Current metrics from the working tree: `EchoPlaybackService.java` is 1318 lines, strict `private Playback*` field declarations are 53, strict `private Playback*Owner` field declarations are 22, production `fromPlaybackQueueManager(...)` calls are 0, Service `playbackQueueStateOwner::currentTrack` dependencies are 1, and production `Playback*Owner` files under `app/src/main/java` plus `feature/playback/src/main/java` are 44 by the recursive file metric. Production code still has no `PlaybackMediaSourceResolutionOwner`, `PlaybackQueueManager.QueueProvider`, or `interface QueueProvider` hits.
- Current boundary findings: queue authority remains in `PlaybackQueueManager`; `PlaybackQueueStateOwner.currentTrack()` remains a package-local Java-facing convenience read for existing owners, but Service no longer uses it for runtime replay gain. Cache policy remains in `PlaybackPrecacheManager` plus focused cache managers consuming `PlaybackMediaCacheOperations`; the visualization cache test now locks the partial cache resume offset and length so this policy does not drift back into Service.
- Remaining high-risk edge: the only Service `playbackQueueStateOwner::currentTrack` method reference feeds notification artwork. Leave that path alone until P4 smoke coverage is stable; replacing it with a generic `Supplier<Track>` field would make Service a wiring store without reducing state authority.
- T1 verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackRuntimeStateManagerTest --tests app.yukine.playback.PlaybackVisualizationCacheManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also rechecked app compile tasks as up-to-date. Device smoke remains out of scope because this checkpoint is docs-only and the covered slices did not change notification, background playback, lyrics, shutdown, or service lifecycle behavior.
## NOTE 109 - Playback owner/interface audit after visualization full-cache coverage (2026-07-03)
- Audit scope after `04aaf7ca0`: `PlaybackVisualizationCacheManagerTest.fullyCachedVisualizationWindowSkipsCacheWriterCreation` now covers the cache-owner full-hit path. This locks the rule that a fully cached visualization window only reads `PlaybackMediaCacheOperations.cachedBytesInRange(...)` and does not create a cache writer. The slice added no owner, facade, bridge, field, package move, URI/media item resolver path, cache policy path, Service strategy branch, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior.
- Current metrics from the working tree: `EchoPlaybackService.java` is 1413 lines, strict `private Playback*` field declarations are 43, strict `private Playback*Owner` field declarations are 19, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot()` / `playbackQueueStateOwner::currentTrack` wiring occurrences are 1, and production `Playback*Owner` files under `app/src/main/java` plus `feature/playback/src/main/java` are 44 by the recursive file metric. Production code still has no `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, `PlaybackQueueManager.QueueProvider`, or `interface QueueProvider` hits.
- Queue/interface audit result: `MainActivityArchitectureContractTest` already guards against `QueueProvider`, `fromPlaybackQueueManager(...)`, public queue cursor helpers, direct Service `playbackQueueManager.queueStateSnapshot()` access, and widening `QueueStateSnapshot` beyond `currentTrack`, `currentIndex`, and `queueSize`. Adding more string-only contract assertions here would duplicate existing coverage; the next queue slice should only proceed if it removes a real public method, constructor dependency, supplier chain, state source, or Service strategy branch.
- Resolver/cache boundary audit result: URI/media item/header resolution remains in `PlaybackMediaSourceProvider`; cache/precache behavior remains behind `PlaybackPrecacheManager`, `PlaybackVisualizationCacheManager`, and `PlaybackMediaCacheOperations`. The only production `PlaybackMediaCacheOperations.fromMediaSourceProvider(...)` calls are the two explicit cache-owner bindings in `PlaybackPrecacheManager` and `PlaybackVisualizationCacheManager`.
- Verification for the added behavior slice passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackVisualizationCacheManagerTest --console=plain`. This audit is docs-only; device smoke remains out of scope because it does not change notification, background playback, lyrics, shutdown, service lifecycle, or playback runtime behavior.
## NOTE 110 - Playback cache/precache boundary audit after unresolved and upcoming coverage (2026-07-03)
- Audit scope after `fe1e0bf30` and `fbeb3d7f7`: `PlaybackMediaCacheOperationsTest.providerBackedOperationsDoNotPrecacheUnresolvedStreamingPlaceholders` now fixes the boundary where provider-owned media identity cache keys do not automatically become precache keys, and `PlaybackPrecacheManagerTest.upcomingPrecacheSkipsTracksWithoutCachePolicyKey` now verifies that the submitted upcoming cache task opens the HTTP streaming candidate, not the local candidate without a cache policy key.
- These are behavior-test strengthening slices only. They added no owner, facade, bridge, field, package move, URI/media item resolver path, cache policy path, Service strategy branch, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, or package structure change. The concrete gain is stronger coverage around the `PlaybackMediaSourceProvider -> PlaybackMediaCacheOperations -> PlaybackPrecacheManager` boundary so unresolved/local candidates do not drift into precache policy.
- Current metrics from the working tree: `EchoPlaybackService.java` is 1413 lines, strict `private Playback*` field declarations are 43, strict `private Playback*Owner` field declarations are 19, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot()` / `playbackQueueStateOwner::currentTrack` wiring occurrences remain 1, and production `Playback*Owner` files under `app/src/main/java` plus `feature/playback/src/main/java` are 44 by the recursive file metric. Production code still has no `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, `PlaybackQueueManager.QueueProvider`, or `interface QueueProvider` hits.
- Queue/API audit status is unchanged from NOTE 109: no safe public `PlaybackQueueManager` method deletion was found in this pass. `restorePlaybackQueue()`, `persistQueueState()`, `queueSnapshot()`, and `upcomingTracksForPrecache(...)` still have real restore/shutdown/UI/precache consumers, so removing them now would be a horizontal rewrite rather than an interface win.
- T1 verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.manager.PlaybackMediaCacheOperationsTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this batch does not change notification, background playback, lyrics, shutdown, service lifecycle, or playback runtime behavior outside the tested cache/precache decisions.
## NOTE 111 - Playback queue restore audit after filtered-index coverage (2026-07-03)
- Audit scope after `e822aeb02`: `PlaybackQueueManager.restorePlaybackQueue()` now clamps the restored current index after invalid queue entries are filtered, and `PlaybackQueueManagerTest.restorePlaybackQueueClampsCurrentIndexAfterFilteringInvalidTracks` covers the queue-authority behavior directly. This is a behavior-test strengthening slice; it added no owner, facade, bridge, field, package move, Service strategy branch, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior.
- Current metrics from the working tree: `EchoPlaybackService.java` is 1413 lines, strict `private Playback*` field declarations are 42, strict `private Playback*Owner` field declarations are 18, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot()` / `playbackQueueStateOwner::currentTrack` wiring occurrences remain 1, and production `Playback*Owner` files under `app/src/main/java` plus `feature/playback/src/main/java` are 43. Production code still has no `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, `PlaybackQueueManager.QueueProvider`, or `interface QueueProvider` hits.
- Stop/clear candidate status: `PlaybackQueueStopClearOwner.java` and `PlaybackQueueStopClearOwnerTest.java` are still absent, Service has no `playbackQueueStopClearOwner` wiring, and `PlaybackQueueCompletionOwner` already owns stop/clear, stop-at-end, and automatic-advance preparation calls into `PlaybackQueueManager`. Do not reopen the stop/clear merge candidate unless a future diff can remove a real field, factory, interface method, supplier chain, or Service strategy branch.
- Queue/API audit status: `QueueStateSnapshot` still stores only `currentTrack`, `currentIndex`, and `queueSize`; `PlaybackQueueManager.currentIndex()` and `setCurrentIndex(...)` remain private implementation details; no safe public `PlaybackQueueManager` method deletion was found in this pass. The next queue slice should target a concrete consumer boundary, not another read-method shuffle.
- Verification for the restored-index slice passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest --console=plain` and `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. This follow-up audit is docs-only; device smoke remains out of scope because it does not change notification, background playback, lyrics, shutdown, service lifecycle, or playback runtime behavior.
## NOTE 112 - Resolver/cache boundary audit after media identity and controller queue slices (2026-07-03)
- Audit scope after `bfba83e8e` and `2c766d847`: `PlaybackMediaSourceProviderTest.mirroredQueueReuseRequiresCacheKeyToMatchWhenBothArePresent` now fixes the MediaItem reuse rule that matching media id and resolved URI are not enough when both sides carry different cache keys, and `PlaybackMediaLibraryCallbackTest.controllerQueueForMediaItemsSkipsUnresolvedItemsAndKeepsResolvedOrder` / `controllerQueueForMediaItemsClampsStartIndexWhenSelectedItemDoesNotResolve` now cover controller MediaItem filtering plus filtered-queue start-index remapping.
- These slices strengthen resolver/queue behavior only. They added no owner, facade, bridge, field, package move, Service strategy branch, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, or package structure change. URI, MediaItem, header, and media identity rules remain in `PlaybackMediaSourceProvider` / `PlaybackMediaLibraryCallback`; cache and precache policy still flow through `PlaybackPrecacheManager` plus `PlaybackMediaCacheOperations`.
- Current metrics from the working tree: `EchoPlaybackService.java` is 1413 lines, strict `private Playback*` field declarations are 42, strict `private Playback*Owner` field declarations are 18, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot()` / `playbackQueueStateOwner::currentTrack` wiring occurrences remain 1, and production `Playback*Owner` files under `app/src/main/java` plus `feature/playback/src/main/java` are 43. Production code still has no `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, `PlaybackQueueManager.QueueProvider`, or `interface QueueProvider` hits.
- Boundary findings: `PlaybackMediaSourceProvider` still owns `mediaItemIdentityMatchesForReuse(...)` and cache-key generation; `PlaybackMediaLibraryCallback` now resolves controller queues with a filtered index instead of leaking raw controller indexes downstream to `PlaybackQueueManager.playQueue(...)`; `PlaybackPrecacheManager.fromMediaSourceProvider(...)` remains the explicit cache policy bridge and should not be widened into a resolver facade.
- T1 verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackMediaSourceProviderTest --console=plain`, `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.manager.PlaybackMediaLibraryCallbackTest --console=plain`, and `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this batch does not change notification, background playback, lyrics, shutdown, service lifecycle, or runtime foreground behavior.
## NOTE 113 - MediaLibrary callback resolution audit after shared-index slices (2026-07-03)
- Audit scope after `86c923703` and `ebd2a5ad9`: `PlaybackMediaLibraryCallback` now routes `onSetMediaItems(...)`, `controllerQueueForMediaItems(...)`, and `onAddMediaItems(...)` through the shared private `resolvedControllerTracks(...)` MediaItem-to-Track filtering path. `mediaItemsWithStartPosition(...)` and `controllerQueueForMediaItems(...)` both consume the same filtered start-index mapping, and `tracksForMediaItems(...)` now reuses that same path for append operations instead of keeping a second filtering loop.
- This is a boundary-tightening slice, not a new-owner slice. It reduced one duplicate callback interpretation of controller MediaItems and kept URI, MediaItem, media identity, header, cache-key, and cache policy decisions inside the existing `PlaybackMediaLibraryCallback`, `PlaybackMediaSourceProvider`, `PlaybackPrecacheManager`, and `PlaybackMediaCacheOperations` owners. It added no owner, facade, bridge, field, package move, Service strategy branch, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, or package structure change.
- Current metrics from the working tree: `EchoPlaybackService.java` is 1413 lines, strict `private Playback*` field declarations are 42, strict `private Playback*Owner` field declarations are 18, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot()` / `playbackQueueStateOwner::currentTrack` wiring occurrences remain 1, and production `Playback*Owner` files under `app/src/main/java` plus `feature/playback/src/main/java` are 43. Production code still has no `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, `PlaybackQueueManager.QueueProvider`, or `interface QueueProvider` hits.
- Drift guard for the next slices: owner count is already high, so do not add or keep a `Playback*Owner` unless it removes a real state source, interface method, supplier chain, forwarding hop, or Service strategy decision, or adds behavior coverage. QueueProvider-era risk is not resolved by moving call sites; keep auditing queue/snapshot methods as real external inputs, snapshot-derived reads, or migration residue before making another queue cut. Also keep watching Service wiring density: line count alone is not enough if fields, factories, suppliers, or initialization blocks grow.
- Verification for the two behavior slices passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.manager.PlaybackMediaLibraryCallbackTest --console=plain`; `86c923703` also passed `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. This audit is docs-only; device smoke remains out of scope because it does not change notification, background playback, lyrics, shutdown, service lifecycle, or runtime foreground behavior.
## NOTE 114 - Queue/precache owner audit after defensive snapshot and diagnostics wiring slices (2026-07-03)
- Audit scope after `801609f5e` and `eb6459ece`: `PlaybackQueueManagerTest.upcomingTracksForPrecacheIsOwnedByQueueManagerAndDefensive` now fixes the queue-to-precache rule that upcoming tracks are a QueueManager-owned defensive read, and `PlaybackPrecacheStateOwner` now receives the existing `PlaybackStreamingDiagnostics` instance directly instead of a `Supplier<PlaybackStreamingDiagnostics>`.
- Concrete win: one Service/precache supplier chain was removed from `EchoPlaybackService` wiring (`() -> streamingDiagnostics`), while the queue/precache read boundary gained focused behavior coverage. The cache/precache owner still gets current track and upcoming tracks through `PlaybackQueueStateOwner` / `PlaybackQueueManager`; it does not own a mutable queue view or derive queue position itself.
- Current metrics from the working tree: `EchoPlaybackService.java` is 1413 lines, strict `private Playback*` field declarations are 42, strict `private Playback*Owner` field declarations are 18, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot()` / `playbackQueueStateOwner::currentTrack` wiring occurrences remain 1, and production `Playback*Owner` files under `app/src/main/java` plus `feature/playback/src/main/java` are 43. Production code still has no `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, `PlaybackQueueManager.QueueProvider`, or `interface QueueProvider` hits.
- Boundary findings: `persistQueueState()` remains a tempting public API candidate but is tied to shutdown lifecycle persistence, so leave it alone until P5/shutdown smoke is in scope. `PlaybackQueueStateOwner.currentTrack()` still has multiple Java owner consumers; deleting it now would be horizontal churn unless paired with a real constructor/interface reduction. The remaining Service `playbackQueueStateOwner::currentTrack` occurrence feeds notification artwork and stays deferred with P4.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest --console=plain` for the defensive upcoming-tracks slice and `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain` for the diagnostics supplier slice. Device smoke remains out of scope because this batch does not change notification, background playback, lyrics, shutdown, service lifecycle, or runtime foreground behavior.
## NOTE 115 - Service supplier wiring audit after diagnostics recorder and widget context slices (2026-07-03)
- Audit scope after `8e340e31a` and `91174f9d0`: `PlaybackStreamingDiagnosticsRecorderOwner` now receives the existing `PlaybackStreamingDiagnostics` instance directly instead of `Supplier<PlaybackStreamingDiagnostics>`, and `PlaybackStatePublisherWidgetOwner` now receives the stable Service `Context` directly through `fromContext(this)` instead of `fromContextProvider(() -> this)`.
- Concrete win: two more Service wiring supplier chains were removed without adding owners, facades, bridges, package moves, queue state sources, URI/media item resolver paths, cache policy paths, notification action behavior, lyrics behavior, shutdown behavior, or background playback behavior. The remaining suppliers in this area either read mutable runtime state such as `player`, belong to notification/artwork P4 paths, or are shutdown lifecycle P5 paths and should not be flattened just for symmetry.
- Current metrics from the working tree: `EchoPlaybackService.java` is 1413 lines, strict `private Playback*` field declarations are 42, strict `private Playback*Owner` field declarations are 18, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot()` / `playbackQueueStateOwner::currentTrack` wiring occurrences remain 1, and production `Playback*Owner` files under `app/src/main/java` plus `feature/playback/src/main/java` are 43. Production code still has no `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, `PlaybackQueueManager.QueueProvider`, or `interface QueueProvider` hits.
- Boundary findings: do not continue flattening the `PlaybackNotificationCommandOwner.notificationUpdaterFromNotificationManagerSupplier(() -> playbackNotificationManager)` or `PlaybackNotificationArtworkManager` current-track supplier until P4 smoke is stable. Do not flatten shutdown resource suppliers until P5. The safe near-term lane is still focused queue/state/API tests or low-risk direct-object wiring where the object is already final/stable.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackStreamingDiagnosticsRecorderOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain` and `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackStatePublisherWidgetOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this batch does not change notification, background playback, lyrics, shutdown, service lifecycle, or runtime foreground behavior.

## NOTE 116 - Progress wiring and queue-preparation audit after local-owner and fallback coverage (2026-07-03)
- Audit scope after `3bede5486` and `c5dcee674`: `EchoPlaybackService` now keeps `PlaybackProgressUpdateCommandOwner` as an `onCreate` local dependency of `PlaybackProgressUpdateManager` instead of a long-lived Service field, and `PlaybackCurrentTrackPreparationQueueOwnerTest.queuePreparationSkipsMirroredSourcesWhenAnyQueuedTrackLacksPlayableUri` fixes the mirrored-queue fallback rule when any queued track lacks a playable URI.
- Concrete wins: one Service `private Playback*` field was removed without adding an owner, facade, bridge, package move, queue state source, URI/media item resolver path, cache policy path, notification action behavior, lyrics behavior, shutdown behavior, or background playback behavior. The queue-preparation fallback test strengthens the existing owner boundary before any future `prepareMirroredQueue(...)` cleanup, so batch media-source resolution is not requested for queues that must fall back to single-track preparation.
- Current metrics from the working tree: `EchoPlaybackService.java` is 1317 lines, strict `private Playback*` field declarations are 42, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot()` mentions are 0, and `Playback*Owner.java` files under `app/src/main/java/app/yukine/playback` are 43. Production code still has no `PlaybackMediaSourceResolutionOwner`, `PlaybackQueueManager.QueueProvider`, or `interface QueueProvider` hits.
- Boundary findings: the old stop/clear candidate remains closed because `PlaybackQueueStopClearOwner` is absent and `PlaybackQueueCompletionOwner` already owns stop/clear, stop-at-end, and automatic-advance preparation calls. `PlaybackCurrentTrackPreparationOwner` and `PlaybackCurrentTrackPreparationQueueOwner` should not be mechanically merged; doing so would risk growing a broad preparation facade unless a future slice also removes a real field, interface method, supplier chain, or Service strategy branch.
- T1 verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused coverage for the batch also passed through `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackProgressUpdateCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain` and `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationQueueOwnerTest --console=plain`. Device smoke remains out of scope because this batch does not change notification, background playback, lyrics, shutdown, service lifecycle, or runtime foreground behavior.
## NOTE 117 - Queue snapshot and resolver invalid-URI audit after focused boundary coverage (2026-07-03)
- Audit scope after `ec05e4ba7` and the current resolver test slice: `PlaybackQueueManagerTest.queueStateSnapshotOwnsSourceStateAndDerivedReads` fixes the queue snapshot rule that `currentTrack`, `currentIndex`, and `queueSize` are the source state while empty/current/multiple/end booleans remain derived reads, and `PlaybackMediaSourceProviderTest.prepareTrackForPlaybackReturnsGenericErrorForLocalTrackWithoutPlayableUri` fixes the invalid local-URI preparation rule inside the media source provider.
- Concrete wins: two focused behavior tests now protect queue/state and URI-resolution boundaries without adding an owner, facade, bridge, field, package move, Service strategy branch, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, or cache policy path. The resolver slice keeps invalid URI handling in `PlaybackMediaSourceProvider.prepareTrackForPlayback(...)` instead of letting `EchoPlaybackService` or a new resolution facade own it.
- Current metrics from the working tree: `EchoPlaybackService.java` is 1317 lines, strict `private Playback*` field declarations are 42, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot()` mentions are 0, and `Playback*Owner.java` files under `app/src/main/java/app/yukine/playback` are 43. Production code still has no `PlaybackMediaSourceResolutionOwner`, `PlaybackQueueManager.QueueProvider`, or `interface QueueProvider` hits.
- Boundary findings: the old stop/clear candidate remains closed; `PlaybackQueueCompletionOwner` owns stop/clear preparation and there is no `PlaybackQueueStopClearOwner` to merge. Service no longer has direct queue snapshot calls, so the next queue slice should only proceed if it removes a real public API, supplier chain, constructor dependency, or strengthens behavior coverage. Resolver/cache work should continue inside `PlaybackMediaSourceProvider`, `PlaybackMediaCacheOperations`, and `PlaybackPrecacheManager`; do not add a broad resolution owner for invalid URI, MediaItem, header, and cache-key policy.
- T1 verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused coverage for the resolver slice also passed via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackMediaSourceProviderTest --console=plain`. Device smoke remains out of scope because this batch does not change notification, background playback, lyrics, shutdown, service lifecycle, or runtime foreground behavior.
## NOTE 118 - Queue-state interface audit after local media reuse and derived-read removal (2026-07-03)
- Audit scope after `415624ead` and the current queue-state interface slice: `PlaybackMediaSourceProviderTest.providerMatchesLocalMediaItemForTrackWithoutCacheKey` fixes the local MediaItem reuse rule that local tracks with no cache key still match by media id and URI, and `PlaybackQueueStateOwner` no longer exposes `isQueueEmpty()`, `hasMultipleTracks()`, or `isAtEndOfQueue()` wrapper methods.
- Concrete wins: three package-local derived read methods were removed from `PlaybackQueueStateOwner`; consumers that need queue booleans now read one `PlaybackQueueManager.QueueStateSnapshot` and use its derived properties directly. This narrows the Java owner interface without adding an owner, facade, bridge, field, package move, Service strategy branch, queue state source, URI/media item resolver path, cache policy path, notification action behavior, lyrics behavior, shutdown behavior, or background playback behavior.
- Current metrics from the working tree: `EchoPlaybackService.java` is 1317 lines, strict `private Playback*` field declarations are 42, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot()` mentions are 0, and `Playback*Owner.java` files under `app/src/main/java/app/yukine/playback` are 43. Production code still has no `PlaybackMediaSourceResolutionOwner`, `PlaybackQueueManager.QueueProvider`, or `interface QueueProvider` hits.
- Boundary findings: `PlaybackQueueStateOwner.currentTrack()` remains because it is still a source-state convenience used by several Java owners; deleting it now would be horizontal churn unless paired with a real constructor/interface reduction. `PlaybackQueueStateOwner.queueSnapshot()` and `upcomingTracksForPrecache(...)` still represent list/precache reads with real consumers. The remaining safe queue direction is to remove a public manager method only when its consumer can move to an existing owner without widening Service wiring.
- Focused verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.playback.PlaybackCrossfadeStateOwnerTest --tests app.yukine.playback.PlaybackErrorRecoveryCommandOwnerTest --tests app.yukine.playback.PlaybackNotificationStateOwnerTest --tests app.yukine.playback.PlaybackQueueMirroredTransitionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. The resolver focused test passed via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackMediaSourceProviderTest --console=plain`. Device smoke remains out of scope because this batch does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or runtime foreground behavior.
## NOTE 119 - Wi-Fi lock queue-state supplier audit after cache boundary coverage (2026-07-03)
- Audit scope after `6f79a731f` and the current Wi-Fi lock wiring slice: `PlaybackMediaCacheOperationsTest.providerBackedOperationsReadCommittedCacheBytesWithSafeOffset` fixes committed-cache span reads and safe negative-offset handling inside existing cache operations, while `PlaybackWifiLockManager` now reads the current track from `PlaybackQueueManager.queueStateSnapshot()` instead of receiving `playbackQueueStateOwner::currentTrack` from `EchoPlaybackService`.
- Concrete win: one more Service current-track supplier chain was removed without adding an owner, facade, bridge, field, package move, queue state source, URI/media item resolver path, cache policy path, notification action behavior, lyrics behavior, shutdown behavior, or background playback behavior. The Wi-Fi lock still owns only lock acquire/release policy; queue authority stays in `PlaybackQueueManager`.
- Current metrics from the working tree: `EchoPlaybackService.java` is 1412 lines, strict `private Playback*` field declarations are 47, strict `private Playback*Owner` field declarations are 23, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot()` mentions are 0, remaining Service `playbackQueueStateOwner::currentTrack` supplier occurrences are 2, and `Playback*Owner.java` files under `app/src/main/java/app/yukine/playback` are 43. Production code still has no `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, `PlaybackQueueManager.QueueProvider`, or `interface QueueProvider` hits.
- Boundary findings: the two remaining Service current-track suppliers feed position persistence and notification artwork. Position persistence should be examined separately because it may be movable into `PlaybackPositionManager` with queue snapshot coverage; notification artwork stays deferred with P4 until notification/background smoke is stable. Do not replace the remaining suppliers with a global Service snapshot facade.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackWifiLockManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. The cache boundary focused test passed earlier via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.manager.PlaybackMediaCacheOperationsTest --console=plain`. Device smoke remains out of scope because this batch does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 120 - Position persistence queue-state supplier audit after feature T1 (2026-07-03)
- Audit scope after the current position persistence wiring slice: `PlaybackPositionManager.stateProviderFromPlaybackState(...)` now receives `PlaybackQueueManager` directly and reads the current track from `queueStateSnapshot().currentTrack`; `EchoPlaybackService` no longer passes `playbackQueueStateOwner::currentTrack` into the position manager state provider.
- Concrete win: another Service current-track supplier chain was removed without adding an owner, facade, bridge, field, package move, queue state source, URI/media item resolver path, cache policy path, notification action behavior, lyrics behavior, shutdown behavior, or background playback behavior. Position persistence continues to live in `PlaybackPositionManager`, while queue/current-track authority remains in `PlaybackQueueManager`.
- Current metrics from the working tree: `EchoPlaybackService.java` is 1412 lines, strict `private Playback*` field declarations are 47, strict `private Playback*Owner` field declarations are 23, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot()` mentions are 0, remaining Service `playbackQueueStateOwner::currentTrack` supplier occurrences are 1, and `Playback*Owner.java` files under `app/src/main/java/app/yukine/playback` are 43. Production code still has no `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, `PlaybackQueueManager.QueueProvider`, or `interface QueueProvider` hits.
- Boundary findings: the single remaining Service current-track supplier feeds `PlaybackNotificationArtworkManager`; leave it deferred with P4 notification/background smoke instead of wrapping it in a global snapshot facade. The safe next queue/API lane is a read-only `PlaybackQueueManager` public API grouping audit or focused behavior coverage for a real queue command.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackPositionManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain` and T1 `.\gradlew.bat :feature:playback:testDebugUnitTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this batch does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 121 - QueueManager public API grouping and mutation behavior audit (2026-07-03)
- Audit scope after the current queue mutation behavior slice: `PlaybackQueueManager` public surface groups into command APIs (`playQueue`, append/play-first/skip/move/remove/retain/replace, completion preparation, mirrored reuse/transition), persistence and restore APIs (`persistQueueState`, `restorePlaybackQueue`, `restoreLastPlayback`, restore-enabled setting), and read APIs (`queueSnapshot`, `queueStateSnapshot`, `upcomingTracksForPrecache`). The read APIs still have real `PlaybackQueueStateOwner` / precache consumers; they are not migration residue to delete blindly.
- Concrete win: `PlaybackQueueManagerTest.removeTracksBeforeCurrentKeepsCurrentTrackAuthorityInQueueManager` now fixes the queue mutation rule that removing tracks before the current item preserves the same current track identity while normalizing `currentIndex` and persisted queue state. This strengthens the queue/current-track single-authority boundary without adding an owner, facade, bridge, field, package move, Service supplier, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior.
- Boundary findings: `retainTracksById(...)` and `removeTracksById(...)` are still command APIs, not simple derived reads, because they can request stop-and-clear when the queue becomes empty and can trigger prepare/publish side effects. `queueSnapshot()` and `upcomingTracksForPrecache(...)` remain externally consumed through `PlaybackQueueStateOwner`; shrinking them requires a real consumer-path reduction, not a visibility-only edit.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest --console=plain`. Device smoke remains out of scope because this slice does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 122 - Owner and Service wiring drift guard after checkpoint audit (2026-07-03)
- Audit scope after the clean checkpoint: `EchoPlaybackService.java` is 1317 lines by the current working tree, production `Playback*Owner.java` files under `app/src/main/java/app/yukine/playback` remain 43, the recursive production owner inventory including `feature/playback` is 44, production `fromPlaybackQueueManager(...)` calls remain 0, Service `queueStateSnapshot()` mentions remain 0, and the only Service `playbackQueueStateOwner::currentTrack` supplier still feeds notification artwork.
- Contract hardening: `MainActivityArchitectureContractTest.playbackOwnerInventoryAndServiceWiringDoNotGrowWithoutAudit` already guards app playback owner count, forbidden resolver/facade/cache-policy files, and resolver/cache ownership. It now also caps Service `private Playback*` wiring declarations including `private final` initialized fields at 54, and caps Service `private Playback*Owner` wiring declarations including final initialized fields at 23. This closes the prior audit gap where the older helper did not count final initialized playback dependencies.
- Boundary findings: no new production `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, `PlaybackQueueManager.QueueProvider`, or `interface QueueProvider` hits were found. URI/media item/header resolution remains in `PlaybackMediaSourceProvider`; cache/precache policy remains behind `PlaybackMediaCacheOperations`, `PlaybackPrecacheManager`, and the existing visualization cache owner. The next code slice should still prove a real reduction or behavior win rather than adding another owner.
- Verification scope for this note is the architecture contract only because production playback behavior is unchanged. Device smoke remains deferred; this slice does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 123 - WebDAV cache operations boundary coverage (2026-07-03)
- Audit scope after the current feature-only test slice: `PlaybackMediaCacheOperationsTest.providerBackedOperationsPrecacheWebDavHttpTracksWithProviderHeaders` now covers the WebDAV path where an HTTP WebDAV track receives a provider-owned `webdav:<sourceId>:...` cache key and both streaming header-store headers plus WebDAV Basic Authorization headers through `PlaybackMediaSourceProvider`.
- Concrete win: this strengthens the existing `PlaybackMediaSourceProvider -> PlaybackMediaCacheOperations -> PlaybackPrecacheManager` boundary without adding an owner, facade, bridge, field, package move, Service supplier, queue state source, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. It specifically prevents a future simplification that treats cache operations as streaming-only and loses WebDAV auth/cache-key behavior.
- Current metrics from the working tree remain: `EchoPlaybackService.java` is 1317 lines, strict non-final `private Playback*` field declarations are 41, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot()` mentions are 0, the only Service `playbackQueueStateOwner::currentTrack` supplier still feeds notification artwork, app playback owner files remain 43, and recursive app+feature playback owner files remain 44.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.manager.PlaybackMediaCacheOperationsTest --console=plain`. Device smoke remains out of scope because this slice is feature test coverage only and does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 124 - Queue completion boundary localized out of Service fields (2026-07-03)
- Audit scope after the current Service wiring slice: `EchoPlaybackService` no longer keeps `playbackQueueCompletionBoundary` as a top-level final field. The `PlaybackQueueCompletionOwner.CompletionBoundary` implementation is now local to `playbackQueueCompletionOwner()`, which is already a short-lived factory for the completion owner.
- Concrete win: one `EchoPlaybackService` `private Playback*` wiring field and one `private Playback*Owner` wiring field were removed without adding an owner, facade, bridge, package move, queue state source, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. Completion behavior still runs through `PlaybackQueueCompletionOwner`; the Service only supplies boundary callbacks.
- Contract update: `MainActivityArchitectureContractTest.playbackOwnerInventoryAndServiceWiringDoNotGrowWithoutAudit` now caps Service `private Playback*` declarations including final initialized fields at 53 and `private Playback*Owner` declarations including final initialized fields at 22. The older strict non-final field metric remains 41.
- Current metrics from the working tree: `EchoPlaybackService.java` is 1315 lines, strict non-final `private Playback*` field declarations are 41, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot()` mentions are 0, the only Service `playbackQueueStateOwner::currentTrack` supplier still feeds notification artwork, app playback owner files remain 43, and recursive app+feature playback owner files remain 44.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCompletionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also recompiled app Kotlin and Java after the Service change. Device smoke remains out of scope because this does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 125 - Playback T1 verification after cache and completion wiring slices (2026-07-03)
- Batch scope: NOTE 123 strengthened the WebDAV cache operations boundary at `PlaybackMediaSourceProvider -> PlaybackMediaCacheOperations -> PlaybackPrecacheManager`; NOTE 124 removed the Service-level `playbackQueueCompletionBoundary` field while keeping completion behavior in `PlaybackQueueCompletionOwner`.
- T1 verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`.
- Current audit metrics after T1 remain: `EchoPlaybackService.java` is 1315 lines, strict non-final `private Playback*` field declarations are 41, Service `private Playback*` declarations including final initialized fields are capped by contract at 53, Service `private Playback*Owner` declarations including final initialized fields are capped by contract at 22, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot()` mentions are 0, the only Service `playbackQueueStateOwner::currentTrack` supplier still feeds notification artwork, app playback owner files remain 43, and recursive app+feature playback owner files remain 44.
- Batch result: one behavior boundary test was added and one Service wiring field/supplier object was localized out of the top-level Service field set. No owner, facade, bridge, package move, queue state source, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior was added.
- Next safe lane: continue only with slices that remove another real Service wiring dependency, shrink a queue/state API, or add behavior coverage for a concrete resolver/cache edge. Keep the remaining notification artwork current-track supplier deferred to P4 smoke coverage.
## NOTE 126 - Precache range policy boundary coverage (2026-07-03)
- Audit scope after the current test-only slice: `PlaybackPrecacheManagerTest` now verifies that the current-leading precache path reads committed cache state through the existing `PlaybackMediaCacheOperations` boundary using the provider-owned precache cache key, position `0`, and `PlaybackPrecacheManager.PRECACHE_BYTES` length.
- Concrete win: this strengthens the existing cache/precache owner boundary without adding an owner, facade, bridge, field, package move, Service supplier, queue state source, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. It specifically protects the rule that cache range policy belongs in `PlaybackPrecacheManager` and cache operations remain behind `PlaybackMediaCacheOperations`, not `EchoPlaybackService` or a new resolution facade.
- Production metrics and architecture caps are intentionally unchanged because this is a focused behavior-test slice. Do not treat this as owner shrink progress; it is boundary coverage that makes the next cache or resolver cleanup safer.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --console=plain`. Device smoke remains out of scope because this slice does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 127 - Queue mutation clear uses derived queue state (2026-07-03)
- Audit scope after the current queue-state slice: `PlaybackQueueMutationOwner.clearQueue()` now checks `playbackQueueManager.queueStateSnapshot().isQueueEmpty()` instead of reading `playbackQueueManager.queueSnapshot().isEmpty()`. The owner still delegates real queue mutations to `PlaybackQueueManager`; it no longer reads the full queue list for a boolean empty check.
- Concrete win: one broad queue list snapshot read in the mutation owner was replaced with an existing derived queue-state read, without adding an owner, facade, bridge, field, package move, Service supplier, queue state source, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. `MainActivityArchitectureContractTest` now requires this narrower read and blocks the old `queueSnapshot().isEmpty()` pattern from returning in the mutation owner.
- Current audit metrics from the working tree: `EchoPlaybackService.java` is 1410 lines, strict non-final `private Playback*` field declarations are 41, single-line Service `private Playback*` declarations including final initialized fields are 43, single-line Service `private Playback*Owner` declarations including final initialized fields are 17, production `fromPlaybackQueueManager(...)` calls are 0, Service direct `queueStateSnapshot()` mentions are 0, the only Service `playbackQueueStateOwner::currentTrack` supplier still feeds notification artwork, and recursive app+feature playback owner files remain 44.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMutationOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain` and T1 `.\gradlew.bat :feature:playback:testDebugUnitTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this slice does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 128 - Provider cache data source header boundary coverage (2026-07-03)
- Audit scope after the current feature-only resolver/cache slice: `PlaybackMediaSourceProviderTest.cacheDataSourceForTrackUsesProviderOwnedHeaders` now exercises `PlaybackMediaSourceProvider.cacheDataSourceForTrack(...)` through a local HTTP socket and proves the provider-owned streaming headers are applied to the upstream request while the cache key still comes from `PlaybackMediaSourceProvider.cacheKeyForTrack(...)`.
- Concrete win: this strengthens the `PlaybackMediaSourceProvider -> CacheDataSource` boundary without adding an owner, facade, bridge, field, package move, Service supplier, queue state source, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. It specifically protects the rule that URI/header/cache data-source construction stays in `PlaybackMediaSourceProvider`, not `EchoPlaybackService` or a new resolution facade.
- Production metrics and architecture caps are intentionally unchanged because this is focused feature test coverage. A first attempt using `com.sun.net.httpserver.HttpServer` failed to compile in the Android unit-test classpath; the final test uses `ServerSocket` and passed.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackMediaSourceProviderTest --console=plain`. Device smoke remains out of scope because this slice does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 129 - Favorite command reads queue snapshot directly (2026-07-03)
- Audit scope after the current queue-state forwarding slice: `PlaybackFavoriteCommandOwner.toggleCurrentFavorite(...)` now reads `PlaybackQueueStateOwner.queueStateSnapshot().getCurrentTrack()` directly instead of going through `PlaybackQueueStateOwner.currentTrack()`.
- Concrete win: one non-notification current-track forwarding chain was shortened without adding an owner, facade, bridge, field, package move, Service supplier, queue state source, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. Favorite command behavior still depends on the queue authority snapshot from `PlaybackQueueManager`.
- Current audit metrics from the working tree remain: `EchoPlaybackService.java` is 1410 lines, strict non-final `private Playback*` field declarations are 41, single-line Service `private Playback*` declarations including final initialized fields are 43, production `fromPlaybackQueueManager(...)` calls are 0, Service direct `queueStateSnapshot()` mentions are 0, the only Service `playbackQueueStateOwner::currentTrack` supplier still feeds notification artwork, and recursive app+feature playback owner files remain 44.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackFavoriteCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this slice does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 130 - Queue command owner reads queue snapshot directly after T1 (2026-07-03)
- Audit scope after the current queue command slice: `PlaybackQueueCommandOwner.currentTrack()` now reads `PlaybackQueueStateOwner.queueStateSnapshot().getCurrentTrack()` directly instead of forwarding through `PlaybackQueueStateOwner.currentTrack()`.
- Concrete win: a second non-notification current-track forwarding chain was shortened without adding an owner, facade, bridge, field, package move, Service supplier, queue state source, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. The queue command path still uses the single queue authority snapshot from `PlaybackQueueManager`.
- Current audit metrics from the working tree remain: `EchoPlaybackService.java` is 1410 lines, strict non-final `private Playback*` field declarations are 41, single-line Service `private Playback*` declarations including final initialized fields are 43, production `fromPlaybackQueueManager(...)` calls are 0, Service direct `queueStateSnapshot()` mentions are 0, the only Service `playbackQueueStateOwner::currentTrack` supplier still feeds notification artwork, and recursive app+feature playback owner files remain 44.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain` and T1 `.\gradlew.bat :feature:playback:testDebugUnitTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this batch does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 131 - Error recovery command reads queue snapshot directly (2026-07-03)
- Audit scope after the current queue-state forwarding slice: `PlaybackErrorRecoveryCommandOwner.currentTrack()` now reads `PlaybackQueueStateOwner.queueStateSnapshot().getCurrentTrack()` directly instead of forwarding through `PlaybackQueueStateOwner.currentTrack()`.
- Concrete win: a third non-notification current-track forwarding chain was shortened without adding an owner, facade, bridge, field, package move, Service supplier, queue state source, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. Error recovery already used the same queue snapshot for failed-track skip policy, so this keeps both reads on the single `PlaybackQueueManager` queue authority snapshot.
- Current audit metrics from the working tree remain: `EchoPlaybackService.java` is 1410 lines, strict non-final `private Playback*` field declarations are 41, single-line Service `private Playback*` declarations including final initialized fields are 43, production `fromPlaybackQueueManager(...)` calls are 0, Service direct `queueStateSnapshot()` mentions are 0, the only Service `playbackQueueStateOwner::currentTrack` supplier still feeds notification artwork, and recursive app+feature playback owner files remain 44.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackErrorRecoveryCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this slice does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 132 - Precache state reads queue snapshot directly after owner audit (2026-07-03)
- Owner/interface audit before this slice: `PlaybackQueueManager.QueueProvider` remains absent; `QueueStateSnapshot` still keeps only `currentTrack`, `currentIndex`, and `queueSize` as source state while deriving queue booleans; production `fromPlaybackQueueManager(...)` calls are 0; Service direct `queueStateSnapshot()` mentions are 0; the only Service current-track supplier remains the notification artwork supplier and is deferred with P4.
- Audit metrics from the working tree: `EchoPlaybackService.java` is 1410 lines, strict non-final `private Playback*` field declarations are 47, single-line Service `private Playback*` declarations including final initialized fields are 58, single-line Service `private Playback*Owner` declarations including final initialized fields are 27, and app/feature playback owner source files are 44.
- Audit scope after the current queue-state forwarding slice: `PlaybackPrecacheStateOwner.currentTrack()` now reads `PlaybackQueueStateOwner.queueStateSnapshot().getCurrentTrack()` directly instead of forwarding through `PlaybackQueueStateOwner.currentTrack()`. This does not move cache policy out of `PlaybackPrecacheManager`; it only shortens the state-provider read path for the existing precache state owner.
- Concrete win: a fourth non-notification current-track forwarding chain was shortened without adding an owner, facade, bridge, field, package move, Service supplier, queue state source, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. Focused coverage remains `PlaybackPrecacheStateOwnerTest`, with `MainActivityArchitectureContractTest` blocking the old forwarding call from returning.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, T1 `.\gradlew.bat :feature:playback:testDebugUnitTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, and T2 `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`. Device smoke remains out of scope because this slice does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 133 - Play history action reads queue snapshot directly (2026-07-03)
- Audit scope after the current queue-state forwarding slice: `PlaybackPlayHistoryRecorder.recordIfPlaybackStartedAction(...)` now reads `PlaybackQueueStateOwner.queueStateSnapshot().getCurrentTrack()` via a local `QueueStateSnapshot` before calling `recordIfPlaybackStarted(...)`, instead of forwarding through `PlaybackQueueStateOwner.currentTrack()`.
- Concrete win: a fifth non-notification current-track forwarding chain was shortened without adding an owner, facade, bridge, field, package move, Service supplier, queue state source, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. The existing history behavior remains protected by `PlaybackPlayHistoryRecorderTest.recordIfPlaybackStartedActionUsesRecorderAndLatestState`.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPlayHistoryRecorderTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this slice does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 134 - Visualization cache state reads queue snapshot directly (2026-07-03)
- Owner/interface audit before this slice: `EchoPlaybackService.java` is 1410 lines, strict non-final `private Playback*` field declarations are 47, single-line Service `private Playback*` declarations including final initialized fields are 58, single-line Service `private Playback*Owner` declarations including final initialized fields are 27, production `fromPlaybackQueueManager(...)` calls are 0, Service direct `queueStateSnapshot()` mentions are 0, the only Service current-track supplier remains notification artwork, and app/feature playback owner source files remain 44.
- Audit scope after the current queue-state forwarding slice: `PlaybackVisualizationCacheStateOwner.currentTrack()` now reads `PlaybackQueueStateOwner.queueStateSnapshot().getCurrentTrack()` via a local `QueueStateSnapshot`, instead of forwarding through `PlaybackQueueStateOwner.currentTrack()`. This keeps visualization cache ownership in the existing cache manager/state owner and does not add a snapshot facade or Service supplier.
- Concrete win: a sixth non-notification current-track forwarding chain was shortened without adding an owner, facade, bridge, field, package move, Service supplier, queue state source, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. Focused coverage remains `PlaybackVisualizationCacheStateOwnerTest`, with `MainActivityArchitectureContractTest` blocking the old forwarding call from returning.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackVisualizationCacheStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, T1 `.\gradlew.bat :feature:playback:testDebugUnitTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, and T2 `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`. Device smoke remains out of scope because this slice does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 135 - Session command reads queue snapshot directly (2026-07-03)
- Audit scope after the current queue-state forwarding slice: `PlaybackSessionCommandOwner.currentTrack()` now reads `PlaybackQueueStateOwner.queueStateSnapshot().getCurrentTrack()` via a local `QueueStateSnapshot`, instead of forwarding through `PlaybackQueueStateOwner.currentTrack()`. This keeps MediaSession command delegation in the existing command owner and does not change session lifecycle, notification artwork, or Service wiring.
- Concrete win: a seventh non-notification current-track forwarding chain was shortened without adding an owner, facade, bridge, field, package move, Service supplier, queue state source, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. Focused coverage remains `PlaybackSessionCommandOwnerTest`, including missing queue state and missing queue manager cases.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackSessionCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this slice does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 136 - Mirrored transition reads queue snapshot directly (2026-07-03)
- Audit scope after the current queue-state forwarding slice: `PlaybackQueueMirroredTransitionOwner.applyMirroredTransitionReason(...)` now reads `PlaybackQueueStateOwner.queueStateSnapshot().getCurrentTrack()` via a local `QueueStateSnapshot` before returning the transition current track, instead of forwarding through `PlaybackQueueStateOwner.currentTrack()`. This keeps mirrored transition decisions in the existing owner and matches the owner’s existing `canApplyMirroredTransition()` snapshot read.
- Concrete win: an eighth non-notification current-track forwarding chain was shortened without adding an owner, facade, bridge, field, package move, Service supplier, queue state source, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. Focused coverage remains `PlaybackQueueMirroredTransitionOwnerTest`, including transition current-track results and missing queue-state behavior.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMirroredTransitionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, T1 `.\gradlew.bat :feature:playback:testDebugUnitTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, and T2 `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`. Device smoke remains out of scope because this slice does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 137 - Mirrored player reads queue snapshot directly (2026-07-03)
- Audit scope after the current queue-state forwarding slice: `PlaybackQueueMirroredPlayerOwner.currentTrack()` now reads `PlaybackQueueStateOwner.queueStateSnapshot().getCurrentTrack()` via a local `QueueStateSnapshot`, instead of forwarding through `PlaybackQueueStateOwner.currentTrack()`. This keeps mirrored player seek handling in the existing owner and does not add another queue/current-track state source.
- Concrete win: a ninth non-notification current-track forwarding chain was shortened without adding an owner, facade, bridge, field, package move, Service supplier, queue state source, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. Focused coverage remains `PlaybackQueueMirroredPlayerOwnerTest`, including seek-to waveform reset, player seek, play-when-ready, and failure behavior.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMirroredPlayerOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this slice does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 138 - Playback owner/interface audit after current-track forwarding slices (2026-07-03)
- Audit scope after NOTE 135-137: `PlaybackSessionCommandOwner`, `PlaybackQueueMirroredTransitionOwner`, and `PlaybackQueueMirroredPlayerOwner` now read the queue authority snapshot directly through `PlaybackQueueStateOwner.queueStateSnapshot()` instead of forwarding through `PlaybackQueueStateOwner.currentTrack()`. No new owner, facade, bridge, field, package move, Service supplier, queue state source, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior was added.
- Current metrics from the clean working tree: `EchoPlaybackService.java` is 1410 lines, strict `private Playback*` field declarations are 47, strict `private Playback*Owner` declarations are 23, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot(...)` mentions are 0, and production `Playback*Owner.java` files remain 43.
- Boundary findings: production `queueStateOwner.currentTrack()` forwarding remains only in `PlaybackLyricsStateOwner` and `PlaybackNotificationStateOwner`, plus the single Service `playbackQueueStateOwner::currentTrack` supplier feeding notification artwork. These are P4 notification/lyrics surfaces and should stay deferred until the smoke table is stable; changing them now would not be a low-risk queue/interface slice. The stop/clear candidate is already closed: `PlaybackQueueStopClearOwner` and its test file are absent, and `PlaybackQueueCompletionOwner` has no `fromPlaybackQueueManager(...)` factory. Resolver/cache boundaries are already guarded by `MainActivityArchitectureContractTest`, `PlaybackMediaSourceProviderTest`, `PlaybackMediaCacheOperationsTest`, and `PlaybackPrecacheManagerTest`; production still has no `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, `PlaybackQueueManager.QueueProvider`, or `interface QueueProvider`.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this checkpoint is docs/audit-only and does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 139 - WebDAV media item cache-key boundary coverage (2026-07-03)
- Resolver/cache behavior slice: `PlaybackMediaSourceProviderTest.providerMediaItemForWebDavTrackUsesOwnedCacheKeyRule` now covers the provider-owned WebDAV MediaItem rule directly. A WebDAV track keeps its resolved URI, uses the track id as `mediaId`, and uses the WebDAV `dataPath` as the MediaItem custom cache key.
- Concrete win: stronger focused coverage for the `PlaybackMediaSourceProvider` URI/MediaItem/cache-key boundary without adding an owner, facade, bridge, field, package move, Service supplier, queue state source, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. This complements the existing local, streaming, unresolved streaming, WebDAV header, and `PlaybackMediaCacheOperations` tests so WebDAV media identity does not drift into `EchoPlaybackService` or a new resolution facade.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackMediaSourceProviderTest --console=plain`. Device smoke remains out of scope because this slice changes only focused unit coverage and docs, not notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 140 - Visualization cache consumes provider cache key boundary (2026-07-03)
- Resolver/cache behavior slice: `PlaybackVisualizationCacheManagerTest.visualizationCacheDataSpecUsesMediaCacheOperationsCacheKey` now verifies that visualization cache writes use the cache key returned by `PlaybackMediaCacheOperations.cacheKeyForPrecache(...)` as the `DataSpec.key`. The test uses a WebDAV-style key to lock the consumer boundary without moving cache policy into `PlaybackVisualizationCacheManager`.
- Concrete win: stronger focused coverage for the `PlaybackMediaSourceProvider -> PlaybackMediaCacheOperations -> PlaybackVisualizationCacheManager` cache-key path without adding an owner, facade, bridge, field, package move, Service supplier, queue state source, URI/media item resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. This pairs with NOTE 139 so both MediaItem identity and visualization cache writes stay on provider/cache-operations rules rather than service-side string construction.
- Current two-slice audit metrics remain unchanged: `EchoPlaybackService.java` is 1410 lines, strict `private Playback*` field declarations are 47, strict `private Playback*Owner` declarations are 23, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot(...)` mentions are 0, and production `Playback*Owner.java` files remain 43. Focused tests protecting this batch are `PlaybackMediaSourceProviderTest`, `PlaybackVisualizationCacheManagerTest`, and `MainActivityArchitectureContractTest`.
- Verification passed with default Gradle daemon/workers via T0 `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackVisualizationCacheManagerTest --console=plain` and T1 `.\gradlew.bat :feature:playback:testDebugUnitTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this slice changes only focused unit coverage and docs, not notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 141 - Resolver/cache boundary batch T2 compile checkpoint (2026-07-03)
- Batch scope after NOTE 139-140: the WebDAV MediaItem cache-key rule and visualization-cache `DataSpec.key` source are now covered by focused feature playback tests. These two slices did not change production code, add owners, widen resolver/cache facades, or move policy into `EchoPlaybackService`.
- T2 verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`. Current metrics remain `EchoPlaybackService.java` 1410 lines, strict `private Playback*` field declarations 47, strict `private Playback*Owner` declarations 23, production `fromPlaybackQueueManager(...)` calls 0, Service `queueStateSnapshot(...)` mentions 0, and production `Playback*Owner.java` files 43.
- Next low-risk candidates remain: only add resolver/cache tests where a concrete gap exists; otherwise resume queue/API read-only audit. Do not touch the remaining notification artwork supplier or `PlaybackLyricsStateOwner` / `PlaybackNotificationStateOwner` current-track reads until the smoke table is stable.
## NOTE 142 - Queue/API drift audit before next slice (2026-07-03)
- Audit scope: this is a read-only queue/API and wiring drift checkpoint after the resolver/cache batch. `PlaybackQueueManager.QueueProvider` is no longer available to shrink; the remaining risk is API/call-site drift, owner proliferation, and `EchoPlaybackService` becoming a wiring store.
- Queue/API grouping: real external queue inputs remain command methods such as `playQueue`, `appendToQueue`, `moveQueueTrack`, `removeTracksById`, `retainTracksById`, `replaceQueuedTrackById`, `replaceCurrentTrackAndResume`, navigation/completion methods, and restore/persistence methods. Derived read outputs remain `queueStateSnapshot`, `queueSnapshot`, `upcomingTracksForPrecache`, and `queuePreparationForNewPlayer`. `currentIndex`, `setCurrentIndex`, and `currentTrack` remain private manager implementation details, while `QueueStateSnapshot` still stores only `currentTrack`, `currentIndex`, and `queueSize` and derives booleans from those fields.
- Current drift metrics from the clean working tree: `EchoPlaybackService.java` is 1315 lines; strict non-final `private Playback*` declarations are 46; strict non-final `private Playback*Owner` declarations are 22; final-inclusive `private Playback*` wiring declarations are 57; final-inclusive `private Playback*Owner` wiring declarations are 26; app playback owner Java files remain 43; production `fromPlaybackQueueManager(...)` calls are 0; Service `queueStateSnapshot(...)` mentions are 0; and the only remaining Service `playbackQueueStateOwner::currentTrack` supplier is the notification artwork path.
- Boundary findings: production source still has no `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, `PlaybackQueueManager.QueueProvider`, or `interface QueueProvider`. URI/media item resolution should stay in `PlaybackMediaSourceProvider`; cache/precache policy should stay in `PlaybackPrecacheManager`, `PlaybackMediaCacheOperations`, or a specific cache-policy owner. The notification artwork supplier plus `PlaybackLyricsStateOwner` and `PlaybackNotificationStateOwner` current-track reads remain P4 surfaces and should not be used as a low-risk P1 queue slice before smoke coverage is stable.
- Next safe cut rule: do not add or keep another `Playback*Owner` unless it removes a real state source, interface method, supplier chain, forwarding hop, Service strategy branch, or adds focused behavior coverage. The next queue/API slice should delete or narrow an actual public method/constructor dependency, or be another read-only audit; moving a call site without reducing one of those surfaces is not enough.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this checkpoint is docs/audit-only and does not change notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 143 - Preparation queue owner factory hop removed (2026-07-03)
- Small wiring slice: `EchoPlaybackService` now constructs `PlaybackCurrentTrackPreparationQueueOwner` directly with `PlaybackQueueManager`, `PlaybackMediaSourceProvider`, and the metadata provider. The owner no longer exposes the `fromMediaSourceProvider(...)` factory, removing one production factory/lambda hop while keeping URI/MediaSource resolution in `PlaybackMediaSourceProvider` and queue authority in `PlaybackQueueManager`.
- Concrete win: no owner, facade, bridge, field, state source, queue API, package move, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior was added. The Service no longer calls `PlaybackCurrentTrackPreparationQueueOwner.fromMediaSourceProvider(...)`, and the architecture contract now blocks that factory from returning while still blocking `fromPlaybackQueueManager(...)` and Service-side `tracks -> mediaSourceProvider.mediaSourcesForTracks(...)` wiring.
- Current slice metrics: `EchoPlaybackService.java` is 1314 lines, strict non-final `private Playback*` declarations are 46, strict non-final `private Playback*Owner` declarations are 22, final-inclusive `private Playback*` wiring declarations are 57, production `fromPlaybackQueueManager(...)` calls remain 0, Service `queueStateSnapshot(...)` mentions remain 0, and app playback owner Java files remain 43.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationQueueOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this slice changes only construction wiring for existing queue preparation behavior, not notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 144 - Mirrored queue matcher factory hop removed (2026-07-03)
- Small wiring slice: `EchoPlaybackService` now constructs `PlaybackMirroredQueueTrackMatcherOwner` directly with the player supplier and `PlaybackMediaSourceProvider`. The owner no longer exposes `fromMediaSourceProvider(...)`, removing one production factory/lambda hop while keeping MediaItem reuse matching inside the existing mirrored queue matcher owner and provider boundary.
- Concrete win: no owner, facade, bridge, field, state source, queue API, package move, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior was added. Service still does not call `mediaSourceProvider.mediaItemMatchesTrackForReuse(...)` or pass `mediaSourceProvider::mediaItemMatchesTrackForReuse`; the architecture contract now blocks the removed factory from returning.
- Current slice metrics: `EchoPlaybackService.java` is 1314 lines, strict non-final `private Playback*` declarations are 46, strict non-final `private Playback*Owner` declarations are 22, final-inclusive `private Playback*` wiring declarations are 57, production `fromPlaybackQueueManager(...)` calls remain 0, Service `queueStateSnapshot(...)` mentions remain 0, and app playback owner Java files remain 43.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackMirroredQueueTrackMatcherOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this slice changes only construction wiring for existing mirrored queue matching behavior, not notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 145 - Two-slice wiring batch T2 checkpoint (2026-07-03)
- Batch scope after NOTE 143-144: two production factory/lambda hops were removed from `PlaybackCurrentTrackPreparationQueueOwner` and `PlaybackMirroredQueueTrackMatcherOwner`. Both changes keep URI/MediaSource/MediaItem decisions behind existing provider-backed owners and avoid new owner/facade/bridge files.
- Batch metrics remain: `EchoPlaybackService.java` is 1314 lines, strict non-final `private Playback*` declarations are 46, strict non-final `private Playback*Owner` declarations are 22, final-inclusive `private Playback*` wiring declarations are 57, production `fromPlaybackQueueManager(...)` calls are 0, Service `queueStateSnapshot(...)` mentions are 0, and app playback owner Java files remain 43.
- T2 verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`.
## NOTE 146 - Streaming restore owner factory hop removed (2026-07-03)
- Small wiring slice: `EchoPlaybackService` now constructs `PlaybackQueueStreamingRestoreOwner` directly with `PlaybackMediaSourceProvider`. The owner no longer exposes `fromMediaSourceProvider(...)`, removing one production factory/lambda hop while keeping streaming restored-track and header restore decisions behind `PlaybackMediaSourceProvider`.
- Concrete win: no owner, facade, bridge, field, state source, queue API, package move, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior was added. Service still does not pass `mediaSourceProvider::restoredTrackForPreparation` or `mediaSourceProvider::restoreHeadersForDataPath`, and the architecture contract now blocks the removed factory from returning.
- Current slice metrics: `EchoPlaybackService.java` is 1314 lines, strict non-final `private Playback*` declarations are 46, strict non-final `private Playback*Owner` declarations are 22, final-inclusive `private Playback*` wiring declarations are 57, production `fromPlaybackQueueManager(...)` calls remain 0, Service `queueStateSnapshot(...)` mentions remain 0, and app playback owner Java files remain 43.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueStreamingRestoreOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this slice changes only construction wiring for existing streaming restore behavior, not notification action mapping, background playback, lyrics, shutdown, service lifecycle, or foreground notification behavior.
## NOTE 147 - Factory-hop batch audit and T2 checkpoint (2026-07-03)
- Batch scope after NOTE 143-146: three provider-backed construction hops were removed from `PlaybackCurrentTrackPreparationQueueOwner`, `PlaybackMirroredQueueTrackMatcherOwner`, and `PlaybackQueueStreamingRestoreOwner`. These were narrow wiring reductions only; no owner, facade, bridge, state source, queue API, package move, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior was added.
- Boundary audit: production source still has no `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, `PlaybackQueueManager.QueueProvider`, `interface QueueProvider`, or `fromPlaybackQueueManager(...)` hits. `EchoPlaybackService` still has 0 `queueStateSnapshot(...)` mentions and 1 remaining `playbackQueueStateOwner::currentTrack` supplier for notification artwork, which remains deferred to P4 smoke coverage.
- Current metrics: `EchoPlaybackService.java` is 1314 lines, strict non-final `private Playback*` declarations are 46, strict non-final `private Playback*Owner` declarations are 22, final-inclusive `private Playback*` wiring declarations are 57, final-inclusive `private Playback*Owner` wiring declarations are 26, and app playback owner Java files remain 43.
- Remaining `fromMediaSourceProvider(...)` production hits are not automatically cleanup targets: `PlaybackCurrentTrackPreparationOwner`, `PlaybackVisualizationCacheManager`, `PlaybackPrecacheManager`, and `PlaybackMediaCacheOperations` still own real provider/cache/precache boundaries. The next slice should return to queue/API narrowing or add concrete resolver/cache behavior coverage, not keep mechanically deleting factories.
- T2 verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`.
## NOTE 148 - Queue/API no-op guard after owner drift risk review (2026-07-03)
- Read-only audit scope after NOTE 147: `PlaybackQueueManager.QueueProvider` is absent from production code, production `fromPlaybackQueueManager(...)` calls are 0, `EchoPlaybackService` has 0 direct `queueStateSnapshot(...)` mentions, and `PlaybackQueueStateOwner` still has no local `QueueProvider`, `QueueStateOperations`, `QueueSnapshotOperations`, or `UpcomingTracksOperations` interfaces. This means there is no remaining QueueProvider method list to shrink mechanically.
- Current metrics from the clean working tree: `EchoPlaybackService.java` is 1409 lines, strict non-final `private Playback*` declarations are 47, strict non-final `private Playback*Owner` declarations are 23, final-inclusive `private Playback*` wiring declarations are 58, final-inclusive `private Playback*Owner` wiring declarations are 27, and app playback owner Java files remain 43. The single Service `playbackQueueStateOwner::currentTrack` supplier still feeds notification artwork and remains a P4 smoke-gated surface.
- Queue/API classification: real external inputs remain queue command, restore, persistence, completion, navigation, replacement, mirrored transition, and precache-read APIs with behavior tests or owner consumers. Derived reads remain centralized as `QueueStateSnapshot` fields and properties, `queueSnapshot()`, `upcomingTracksForPrecache(...)`, and `queuePreparationForNewPlayer()`. `currentIndex`, `setCurrentIndex`, and manager `currentTrack()` remain private implementation details; turning those reads into new owner methods or suppliers would be interface growth.
- Boundary findings: `PlaybackQueueStateOwnerTest` already covers manager delegation, missing-manager fallbacks, rebinding, current-track derivation from `queueStateSnapshot()`, and precache read delegation. `MainActivityArchitectureContractTest` already blocks QueueProvider revival, queue-state supplier facades, Service direct queue snapshot reads, and public manager current-track/cursor exposure. The remaining `queueStateOwner.currentTrack()` reads are only `PlaybackLyricsStateOwner` and `PlaybackNotificationStateOwner`; they are P4 notification/lyrics surfaces and should not be touched before smoke is stable.
- Next safe cut rule: do not spend the next slice deleting or wrapping an owner unless it removes a real state source, public method, constructor dependency, supplier chain, forwarding hop, or Service strategy branch, or adds focused behavior coverage for a concrete queue/resolve/cache gap. If none of those is available, keep the next checkpoint read-only rather than moving call sites.
## NOTE 149 - Notification state owner localized out of Service fields (2026-07-03)
- Small Service wiring slice: `PlaybackNotificationStateOwner` is now a final local state provider in `EchoPlaybackService.onCreate()` instead of a top-level Service field. It is still passed directly to `PlaybackNotificationManager`; notification action mapping, artwork refresh, foreground lifecycle, lyrics, shutdown, and background playback behavior are unchanged.
- Concrete win: one `EchoPlaybackService` `private Playback*Owner` wiring field was removed without adding an owner, facade, bridge, supplier, state source, queue API, URI/media item resolver path, cache policy path, package move, or Service strategy branch. The architecture contract now blocks the field from returning while preserving the existing queue/player/preparing/favorite/session-token state-provider inputs.
- Current metrics after the slice: `EchoPlaybackService.java` is 1408 lines, strict non-final `private Playback*` declarations are 46, strict non-final `private Playback*Owner` declarations are 22, final-inclusive `private Playback*` wiring declarations are 57, final-inclusive `private Playback*Owner` wiring declarations are 26, production `fromPlaybackQueueManager(...)` calls remain 0, Service `queueStateSnapshot(...)` mentions remain 0, the single Service `playbackQueueStateOwner::currentTrack` supplier remains notification artwork only, and app playback owner Java files remain 43.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackNotificationStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this changes only construction lifetime for an existing notification state provider, not notification controls, background playback, lyrics, shutdown, service lifecycle, or foreground behavior.
## NOTE 150 - Session refresher localized out of Service fields (2026-07-03)
- Small Service wiring slice: `PlaybackNotificationArtworkBridgeOwner.SessionRefresher` is now a final local value in `EchoPlaybackService.onCreate()` instead of a top-level Service field. The same refresher is still passed to the lyrics notification bridge and the notification artwork bridge, so session refresh behavior remains owned by `PlaybackNotificationArtworkBridgeOwner` and `PlaybackSessionManager`.
- Concrete win: one more `EchoPlaybackService` `private Playback*Owner` wiring field was removed without adding an owner, facade, bridge, supplier, state source, queue API, URI/media item resolver path, cache policy path, package move, notification action behavior, lyrics sync behavior, shutdown behavior, or background playback behavior. The architecture contract now blocks the field from returning while still requiring `sessionRefresherFromPlaybackSessionManager(...)`.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1408 lines, strict non-final `private Playback*` declarations are 45, strict non-final `private Playback*Owner` declarations are 21, final-inclusive `private Playback*` wiring declarations are 56, final-inclusive `private Playback*Owner` wiring declarations are 25, production `fromPlaybackQueueManager(...)` calls remain 0, Service `queueStateSnapshot(...)` mentions remain 0, the single Service `playbackQueueStateOwner::currentTrack` supplier remains notification artwork only, and app playback owner Java files remain 43.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackNotificationArtworkBridgeOwnerTest --tests app.yukine.playback.PlaybackLyricsManagerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. T2 compile also passed via `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`. Device smoke remains out of scope because this changes only construction lifetime for an existing session refresher, not notification controls, background playback, lyrics runtime policy, shutdown, service lifecycle, or foreground behavior.
## NOTE 151 - Notification artwork source localized out of Service fields (2026-07-03)
- Small Service wiring slice: `PlaybackNotificationArtworkSource` is now a final local value in `EchoPlaybackService.onCreate()` instead of a top-level Service field. It still uses `PlaybackNotificationArtworkSource.fromSupplier(() -> playbackNotificationArtworkManager)` and is passed to both `PlaybackNotificationManager` and `PlaybackStatePublisher`, preserving the delayed artwork-manager lookup.
- Concrete win: one `EchoPlaybackService` `private Playback*` wiring field was removed without adding an owner, facade, bridge, state source, queue API, URI/media item resolver path, cache policy path, package move, notification action behavior, lyrics behavior, shutdown behavior, or background playback behavior. The architecture contract now blocks the field from returning while preserving the existing `fromSupplier(...)` boundary.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1408 lines, strict non-final `private Playback*` declarations are 44, strict non-final `private Playback*Owner` declarations are 21, final-inclusive `private Playback*` wiring declarations are 55, final-inclusive `private Playback*Owner` wiring declarations are 25, production `fromPlaybackQueueManager(...)` calls remain 0, Service `queueStateSnapshot(...)` mentions remain 0, the single Service `playbackQueueStateOwner::currentTrack` supplier remains notification artwork only, and app playback owner Java files remain 43.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackNotificationArtworkSourceTest --tests app.yukine.playback.PlaybackStatePublisherTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. T2 compile also passed via `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`. Device smoke remains out of scope because this changes only construction lifetime for an existing artwork source proxy, not notification controls, background playback, lyrics, shutdown, service lifecycle, or foreground behavior.
## NOTE 152 - Notification foreground owner localized out of Service fields (2026-07-03)
- Small Service wiring slice: `PlaybackNotificationForegroundOwner` is now a final local value in `EchoPlaybackService.onCreate()` instead of a top-level Service field. It is still the foreground boundary passed to `PlaybackNotificationCommandOwner` and `PlaybackNotificationManager`; the Service still owns the Android `startForeground`, `stopForeground`, `stopSelf`, and PendingIntent boundary callbacks.
- Concrete win: one `EchoPlaybackService` `private Playback*Owner` wiring field was removed without adding an owner, facade, bridge, supplier, state source, queue API, URI/media item resolver path, cache policy path, package move, notification action behavior, lyrics behavior, shutdown behavior, or background playback behavior. The architecture contract now blocks the foreground owner field from returning while preserving the existing foreground boundary owner.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1408 lines, strict non-final `private Playback*` declarations are 43, strict non-final `private Playback*Owner` declarations are 20, final-inclusive `private Playback*` wiring declarations are 54, final-inclusive `private Playback*Owner` wiring declarations are 24, production `fromPlaybackQueueManager(...)` calls remain 0, Service `queueStateSnapshot(...)` mentions remain 0, the single Service `playbackQueueStateOwner::currentTrack` supplier remains notification artwork only, and app playback owner Java files remain 43.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackNotificationForegroundOwnerTest --tests app.yukine.playback.PlaybackNotificationCommandOwnerTest --tests app.yukine.playback.PlaybackNotificationManagerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; device smoke remains out of scope because this changes only construction lifetime for the existing foreground owner, not notification controls, background playback, lyrics, shutdown, service lifecycle, or foreground behavior.
## NOTE 153 - Notification artwork current-track supplier removed from Service (2026-07-03)
- Queue/state wiring slice: `PlaybackNotificationArtworkManager` now receives the feature playback `PlaybackQueueManager` and reads `queueStateSnapshot().currentTrack` internally, instead of receiving `playbackQueueStateOwner::currentTrack` from `EchoPlaybackService`.
- Concrete win: this removes the last Service current-track supplier chain without adding an owner, facade, bridge, Service-level supplier field, queue API, URI/media item resolver path, cache policy path, package move, notification action mapping, lyrics sync, shutdown behavior, or background playback behavior. Notification artwork loading and refresh callbacks stay in the existing artwork manager/bridge path.
- `PlaybackNotificationArtworkManagerTest` now builds current-track state through `PlaybackQueueManager.playQueue(...)`, so the focused behavior test exercises the same queue authority used at runtime. `MainActivityArchitectureContractTest` now requires the Service to pass `playbackQueueManager`, rejects `playbackQueueStateOwner::currentTrack` returning anywhere in Service, and requires the artwork manager to hold `PlaybackQueueManager` instead of `Supplier<Track>`.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1408 lines, strict non-final `private Playback*` declarations remain 43, strict non-final `private Playback*Owner` declarations remain 20, final-inclusive `private Playback*` wiring declarations remain 54, final-inclusive `private Playback*Owner` wiring declarations remain 24, production `fromPlaybackQueueManager(...)` calls remain 0, production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` mentions remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers are now 0, and app playback owner Java files remain 43.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackNotificationArtworkManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed feature/app compile tasks for this slice. Device smoke remains out of scope because this changes only construction/state-source wiring for existing notification artwork, not notification controls, background playback, lyrics, shutdown, service lifecycle, or foreground behavior.
## NOTE 154 - Precache upcoming queue read removed from StateProvider (2026-07-03)
- Cache/queue interface slice: `PlaybackPrecacheManager.StateProvider` no longer exposes `upcomingTracksForPrecache(int)`. `PlaybackPrecacheManager` now receives the existing `PlaybackQueueManager` and reads `upcomingTracksForPrecache(SEGMENTED_PRECACHE_CONCURRENCY)` directly from the queue authority.
- Concrete win: one interface method was removed from the precache state provider, and `PlaybackPrecacheStateOwner` no longer forwards upcoming queue reads through `PlaybackQueueStateOwner`. This does not add an owner, facade, bridge, Service-level supplier, queue API, URI/media item resolver path, package move, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior.
- `PlaybackPrecacheManagerTest.upcomingPrecacheReadsTracksThroughQueueManager` now builds upcoming state through `PlaybackQueueManager.playQueue(...)`; `MainActivityArchitectureContractTest` blocks `stateProvider.upcomingTracksForPrecache(...)` and requires the manager to hold `PlaybackQueueManager`.
- Current metrics after the slice: `EchoPlaybackService.java` is 1409 lines, strict non-final `private Playback*` declarations remain 43, strict non-final `private Playback*Owner` declarations remain 20, final-inclusive `private Playback*` wiring declarations remain 54, final-inclusive `private Playback*Owner` wiring declarations remain 24, production `fromPlaybackQueueManager(...)` calls remain 0, production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` mentions remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, and app playback owner Java files remain 43.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.playback.PlaybackPrecacheStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Device smoke remains out of scope because this changes only precache queue-read ownership and interface width, not notification controls, background playback, lyrics, shutdown, service lifecycle, or foreground behavior.
## NOTE 155 - Precache current queue read removed from StateProvider (2026-07-03)
- Cache/queue interface slice: `PlaybackPrecacheManager.StateProvider` no longer exposes `currentTrack()`. `PlaybackPrecacheManager` already receives the existing `PlaybackQueueManager`, so current-track reads now go through `playbackQueueManager.queueStateSnapshot().getCurrentTrack()` inside the precache owner.
- Concrete win: one more precache state-provider interface method was removed, and `PlaybackPrecacheStateOwner` dropped its `PlaybackQueueStateOwner` constructor dependency and field. This does not add an owner, facade, bridge, Service-level supplier, queue API, URI/media item resolver path, cache policy path, package move, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior.
- `PlaybackPrecacheManagerTest` now builds current-track precache state through `PlaybackQueueManager.playQueue(...)`; `PlaybackPrecacheStateOwnerTest` covers only player media item and streaming diagnostics state; `MainActivityArchitectureContractTest` blocks `Track currentTrack();`, `stateProvider.currentTrack()`, and `PlaybackPrecacheStateOwner` queue-state dependencies from returning.
- Current metrics after the slice: `EchoPlaybackService.java` is 1408 lines, strict non-final `private Playback*` declarations remain 43, strict non-final `private Playback*Owner` declarations remain 20, final-inclusive `private Playback*` wiring declarations remain 54, final-inclusive `private Playback*Owner` wiring declarations remain 24, production `fromPlaybackQueueManager(...)` calls remain 0, production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` mentions remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, and app playback owner Java files remain 43.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.playback.PlaybackPrecacheStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Device smoke remains out of scope because this changes only precache queue-read ownership and interface width, not notification controls, background playback, lyrics, shutdown, service lifecycle, or foreground behavior.
## NOTE 156 - QueueStateOwner upcoming precache read removed (2026-07-03)
- Queue/read interface slice: `PlaybackQueueStateOwner.upcomingTracksForPrecache(int)` was removed after the precache owner stopped consuming it. Upcoming precache reads now exist only on the real queue authority, `PlaybackQueueManager`, and the precache owner reaches that method directly.
- Concrete win: one Java-facing queue read API and one forwarding method were deleted without adding an owner, facade, bridge, supplier, state source, Service strategy branch, queue manager API, URI/media item resolver path, cache policy path, package move, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior.
- `PlaybackQueueStateOwnerTest` now covers only the remaining Java-facing state reads (`queueStateSnapshot()`, `queueSnapshot()`, and `currentTrack()`); `MainActivityArchitectureContractTest` blocks the removed upcoming-precache method and forwarding call from returning. `PlaybackQueueManagerTest` and `PlaybackPrecacheManagerTest` remain the behavior coverage for upcoming precache ordering and precache consumption.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1408 lines, strict non-final `private Playback*` declarations remain 43, strict non-final `private Playback*Owner` declarations remain 20, final-inclusive `private Playback*` wiring declarations remain 54, final-inclusive `private Playback*Owner` wiring declarations remain 24, production `fromPlaybackQueueManager(...)` calls remain 0, production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` mentions remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, and app playback owner Java files remain 43.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Device smoke remains out of scope because this changes only a Java-facing queue read method, not notification controls, background playback, lyrics, shutdown, service lifecycle, or foreground behavior.
## NOTE 157 - QueueStateOwner current-track helper removed (2026-07-03)
- Queue/read interface slice: `PlaybackQueueStateOwner.currentTrack()` was removed after the remaining production consumers were reduced to `PlaybackLyricsStateOwner` and `PlaybackNotificationStateOwner`. Those state providers now read `queueStateOwner.queueStateSnapshot().getCurrentTrack()` directly, while the Service still has no direct `queueStateSnapshot()` or `playbackQueueStateOwner::currentTrack` wiring.
- Concrete win: one more Java-facing derived queue read method was deleted without adding an owner, facade, bridge, supplier, state source, Service strategy branch, queue manager API, URI/media item resolver path, cache policy path, package move, notification action behavior, lyrics sync behavior, shutdown behavior, or background playback behavior.
- Focused coverage for this slice is `PlaybackQueueStateOwnerTest`, `PlaybackLyricsStateOwnerTest`, `PlaybackNotificationStateOwnerTest`, and `MainActivityArchitectureContractTest`. The contract now blocks `PlaybackQueueStateOwner.currentTrack()` and any `queueStateOwner.currentTrack()` production call from returning.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1408 lines, strict non-final `private Playback*` declarations remain 43, strict non-final `private Playback*Owner` declarations remain 20, final-inclusive `private Playback*` wiring declarations remain 54, final-inclusive `private Playback*Owner` wiring declarations remain 24, production `fromPlaybackQueueManager(...)` calls remain 0, production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` mentions remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, and app playback owner Java files remain 43.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.playback.PlaybackLyricsStateOwnerTest --tests app.yukine.playback.PlaybackNotificationStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Device smoke remains out of scope because this changes only Java-facing queue read plumbing inside existing state providers, not notification controls, background playback, lyrics sync, shutdown, service lifecycle, or foreground behavior.
## NOTE 158 - Resolver/cache boundary test for current player media item (2026-07-03)
- Resolver/cache boundary test slice: `PlaybackPrecacheManagerTest.currentPrecacheMatchesStateProviderMediaItemBeforeCacheRead` now verifies that current-track precache passes `StateProvider.currentPlayerMediaItem()` and the target track into the media-item matcher before deciding that the player can fill the leading cache range.
- Concrete win: this strengthens focused behavior coverage without adding or changing a production owner, facade, bridge, supplier, state source, Service strategy branch, queue API, URI/media item resolver path, cache policy path, package move, notification behavior, lyrics sync behavior, shutdown behavior, or background playback behavior. URI and MediaItem matching remain behind `PlaybackMediaSourceProvider` / the existing provider-backed matcher supplied to `PlaybackPrecacheManager`.
- Current audit metrics after the slice: `EchoPlaybackService.java` remains 1408 lines, strict non-final `private Playback*` declarations remain 43, strict non-final `private Playback*Owner` declarations remain 20, final-inclusive `private Playback*` wiring declarations remain 54, final-inclusive `private Playback*Owner` wiring declarations remain 24, production `fromPlaybackQueueManager(...)` calls remain 0, production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` mentions remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, app playback owner Java files remain 43, and production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also checked the architecture contract and reused passing app Kotlin/Java compile tasks. Device smoke remains out of scope because this is a test-only resolver/cache boundary slice.
## NOTE 159 - Owner/interface audit checkpoint after queue and resolver slices (2026-07-03)
- Read-only audit scope after NOTE 153-158: the queue current-track supplier chains are now gone from `EchoPlaybackService`, precache no longer receives queue reads through `PlaybackPrecacheManager.StateProvider`, `PlaybackQueueStateOwner` now exposes only `queueStateSnapshot()` and `queueSnapshot()`, and the resolver/cache coverage slice did not introduce a resolution facade.
- Current metrics from the clean working tree: `EchoPlaybackService.java` is 1408 lines, strict non-final `private Playback*` declarations are 43, strict non-final `private Playback*Owner` declarations are 20, final-inclusive `private Playback*` wiring declarations are 54, final-inclusive `private Playback*Owner` wiring declarations are 24, production `fromPlaybackQueueManager(...)` calls are 0, production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` mentions are 0, Service `queueStateSnapshot(...)` mentions are 0, Service `playbackQueueStateOwner::currentTrack` suppliers are 0, app playback owner Java files remain 43, production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions are 0, and production `fromMediaSourceProvider(...)` calls are 9.
- Queue/interface classification: there is no remaining `QueueProvider` method list to shrink. The remaining queue reads are real manager APIs with consumers: `queueSnapshot()` for external UI snapshots, `queueStateSnapshot()` for compact current/index/size state, `upcomingTracksForPrecache(...)` for cache scheduling, and `queuePreparationForNewPlayer()` for Media3 queue preparation. Reintroducing helper methods such as `currentTrack()` on Java-facing owners would widen the interface again.
- Owner drift finding: owner count is still the constraint. The next slice should not add or delete a `Playback*Owner` unless it removes a state source, public method, constructor dependency, supplier chain, forwarding hop, Service strategy branch, or adds focused behavior coverage for a concrete resolver/cache/queue gap. `PlaybackMediaSourceResolutionOwner` and `PlaybackItemResolver` are still absent; keep URI/MediaItem resolution in `PlaybackMediaSourceProvider`, and keep cache policy in `PlaybackPrecacheManager` or a specific cache-policy owner.
- Service wiring finding: the line count is no longer sufficient as a win metric. Track private `Playback*` fields, private `Playback*Owner` fields, owner file count, local factory/supplier count, and initialization length before claiming more Service progress. P4/P5 notification, lyrics, lifecycle, foreground, and shutdown surfaces remain smoke-gated and should not be the next default cut.
- Next safe cut rule: prefer a narrow behavior test or an existing-interface reduction over another owner extraction. If a code slice cannot prove one of those wins in 1-3 focused tests, keep the slice read-only and commit the audit instead of moving call sites.
## NOTE 160 - Lyrics state owner localized out of Service fields (2026-07-03)
- Small Service wiring slice: `PlaybackLyricsStateOwner` is now a final local state provider in `EchoPlaybackService.onCreate()` instead of a top-level Service field. It is still passed directly to `PlaybackLyricsManager`, and status-bar lyrics settings still flow through `playbackLyricsManager`; no lyrics notification, floating lyrics, shutdown, background playback, or MediaSession behavior changed.
- Concrete win: one `EchoPlaybackService` `private Playback*Owner` wiring field was removed without adding an owner, facade, bridge, supplier, state source, queue API, URI/media item resolver path, cache policy path, package move, or Service strategy branch. The architecture contract now blocks the lyrics state owner field from returning while preserving the existing `PlaybackLyricsStateOwner.playbackStateProviderFromPlaybackState(...)` boundary.
- Current metrics after the slice: `EchoPlaybackService.java` is 1407 lines, strict non-final `private Playback*` declarations are 42, strict non-final `private Playback*Owner` declarations are 19, final-inclusive `private Playback*` wiring declarations are 53, final-inclusive `private Playback*Owner` wiring declarations are 23, production `fromPlaybackQueueManager(...)` calls remain 0, production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` mentions remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, app playback owner Java files remain 43, and production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0.
## NOTE 161 - Session command owner localized out of Service fields (2026-07-03)
- Small MediaSession wiring slice: `PlaybackSessionCommandOwner` is now a final local delegate in `EchoPlaybackService.onCreate()` instead of a top-level Service field. The existing `PlaybackSessionManager` player factory captures that delegate and still creates `PlaybackSessionPlayer(player, playbackSessionCommandOwner)`, so MediaSession command routing stays behind the same command owner.
- Concrete win: one more `EchoPlaybackService` `private Playback*Owner` wiring field and the lazy `if (playbackSessionCommandOwner == null)` branch were removed without adding an owner, facade, bridge, state source, queue API, URI/media item resolver path, cache policy path, package move, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. The architecture contract now blocks the session command owner field from returning while preserving the existing `PlaybackControllerMediaItemsOwner` and `PlaybackSessionCommandOwner` boundaries.
- Current metrics after the slice: `EchoPlaybackService.java` is 1404 lines, strict non-final `private Playback*` declarations are 41, strict non-final `private Playback*Owner` declarations are 18, final-inclusive `private Playback*` wiring declarations are 52, final-inclusive `private Playback*Owner` wiring declarations are 22, production `fromPlaybackQueueManager(...)` calls remain 0, production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` mentions remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, app playback owner Java files remain 43, and production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0.
## NOTE 162 - Owner/interface audit after two Service wiring slices (2026-07-03)
- Batch audit scope after NOTE 160-161: two Service wiring fields were localized (`PlaybackLyricsStateOwner`, `PlaybackSessionCommandOwner`) and one lazy Service branch was removed. This batch did not add an owner, facade, bridge, state source, queue API, URI/media item resolver path, cache policy path, notification behavior, lyrics runtime behavior, shutdown behavior, background playback behavior, or package move.
- Current metrics from the clean working tree: `EchoPlaybackService.java` is 1404 lines, strict non-final `private Playback*` declarations are 41, strict non-final `private Playback*Owner` declarations are 18, final-inclusive `private Playback*` wiring declarations are 52, final-inclusive `private Playback*Owner` wiring declarations are 22, production `fromPlaybackQueueManager(...)` calls are 0, production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` mentions are 0, Service `queueStateSnapshot(...)` mentions are 0, Service `playbackQueueStateOwner::currentTrack` suppliers are 0, app playback owner Java files remain 43, production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions are 0, production `fromMediaSourceProvider(...)` calls are 9, and production `mediaItemMatchesTrackForReuse(...)` calls are 5.
- Queue/API audit: `PlaybackQueueStateOwner` remains narrowed to `queueStateSnapshot()` and `queueSnapshot()`. `PlaybackQueueManager.currentIndex()` and `setCurrentIndex(...)` remain private, and `currentTrack()` remains a private manager helper used by manager behavior. Remaining public reads (`queueSnapshot()`, `queueStateSnapshot()`, `upcomingTracksForPrecache(...)`, `queuePreparationForNewPlayer()`) have real consumers and should not be wrapped in another Java-facing helper just to reduce call-site text.
- Resolver/cache audit: URI and MediaItem reuse matching still live behind `PlaybackMediaSourceProvider`; `PlaybackPrecacheManager.fromMediaSourceProvider(...)` only wires the provider-backed matcher and `PlaybackMediaCacheOperations`. Cache keys, headers, cache data sources, leading/current/upcoming/segmented precache, and partial failure behavior still belong to `PlaybackPrecacheManager` / `PlaybackMediaCacheOperations`; no `PlaybackMediaSourceResolutionOwner` or `PlaybackItemResolver` production path exists.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest --tests app.yukine.playback.PlaybackMediaSourceProviderTest --tests app.yukine.playback.manager.PlaybackMediaCacheOperationsTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`. Device smoke remains out of scope because this batch and audit did not change notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
- Next safe cut rule: prefer a focused resolver/cache behavior test or a real interface/field reduction. Do not continue localizing Service fields unless the field is proven to be pure wiring and the owning manager/command owner remains the behavior boundary.
## NOTE 163 - Resolver/cache mismatch test for current player media item (2026-07-03)
- Resolver/cache boundary test slice: `PlaybackPrecacheManagerTest.currentPrecacheFallsBackToCacheWhenPlayerMediaItemDoesNotMatchTrack` now verifies that a non-matching `StateProvider.currentPlayerMediaItem()` is still passed to the media-item matcher, and when the matcher rejects it the current-track leading precache falls back to manager-owned cache state and cache data source work.
- Concrete win: this strengthens behavior coverage for the opposite side of NOTE 158 without adding or changing a production owner, facade, bridge, supplier, state source, Service strategy branch, queue API, URI/media item resolver path, cache policy path, package move, notification behavior, lyrics behavior, shutdown behavior, or background playback behavior. URI/MediaItem matching remains behind `PlaybackMediaSourceProvider` / the matcher supplied to `PlaybackPrecacheManager`, and cache policy remains in `PlaybackPrecacheManager`.
- Current metrics after the test-only slice: `EchoPlaybackService.java` remains 1404 lines, strict non-final `private Playback*` declarations remain 41, strict non-final `private Playback*Owner` declarations remain 18, final-inclusive `private Playback*` wiring declarations remain 52, final-inclusive `private Playback*Owner` wiring declarations remain 22, production `fromPlaybackQueueManager(...)` calls remain 0, production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` mentions remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, app playback owner Java files remain 43, and production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --console=plain`; architecture contract is unchanged for this test-only slice.
## NOTE 164 - Streaming diagnostics owner forwarding helper removed (2026-07-03)
- Owner shrink slice: `PlaybackStreamingDiagnosticsRecorderOwner` now reads its existing `streamingDiagnostics` field directly and no longer carries a private `streamingDiagnostics()` accessor that only returned the same field.
- Concrete win: one pure forwarding method was removed from an existing owner without adding an owner, facade, bridge, supplier, state source, Service strategy branch, queue API, URI/media item resolver path, cache policy path, package move, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior. The Service still owns the same diagnostics recorder wiring until a larger replacement/recovery/buffering boundary can prove a clearer win.
- The architecture contract now blocks the private `PlaybackStreamingDiagnostics streamingDiagnostics()` forwarding helper from returning while preserving the existing direct diagnostics field and the provider-backed streaming quality function.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1404 lines, strict non-final `private Playback*` declarations remain 41, strict non-final `private Playback*Owner` declarations remain 18, final-inclusive `private Playback*` wiring declarations remain 52, final-inclusive `private Playback*Owner` wiring declarations remain 22, production `fromPlaybackQueueManager(...)` calls remain 0, production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` mentions remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, app playback owner Java files remain 43, and production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackStreamingDiagnosticsRecorderOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope because this only removes an internal owner forwarding helper and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 165 - Queue state owner manager accessor removed (2026-07-03)
- Queue/state owner shrink slice: `PlaybackQueueStateOwner.queueStateSnapshot()` and `queueSnapshot()` now read the existing `playbackQueueManager` field directly instead of calling a private `playbackQueueManager()` helper that only returned the same field.
- Concrete win: one pure queue-state forwarding method was removed without adding an owner, facade, bridge, supplier, state source, queue API, URI/media item resolver path, cache policy path, Service strategy branch, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move. The remaining public surface is still the narrowed Java-facing pair: `queueStateSnapshot()` and `queueSnapshot()`.
- The architecture contract now blocks `private PlaybackQueueManager playbackQueueManager()` from returning in `PlaybackQueueStateOwner`, while preserving the direct `PlaybackQueueManager` binding and null-safe empty snapshot behavior.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1404 lines, strict non-final `private Playback*` declarations remain 41, strict non-final `private Playback*Owner` declarations remain 18, final-inclusive `private Playback*` wiring declarations remain 52, final-inclusive `private Playback*Owner` wiring declarations remain 22, production `fromPlaybackQueueManager(...)` calls remain 0, production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` mentions remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, app playback owner Java files remain 43, and production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0. `PlaybackQueueStateOwner.java` is now 34 lines.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest --console=plain`. Device smoke remains out of scope because this only removes an internal queue state owner forwarding helper.
## NOTE 166 - Owner/interface audit after forwarding-helper cleanup batch (2026-07-03)
- Batch audit scope after NOTE 164-165: two existing owners were made smaller by deleting private helpers that only returned their own fields (`PlaybackStreamingDiagnosticsRecorderOwner.streamingDiagnostics()` and `PlaybackQueueStateOwner.playbackQueueManager()`). This batch did not add an owner, facade, bridge, state source, queue API, URI/media item resolver path, cache policy path, notification behavior, lyrics runtime behavior, shutdown behavior, background playback behavior, foreground behavior, or package move.
- Current metrics from the clean batch baseline: `EchoPlaybackService.java` remains 1404 lines, strict non-final `private Playback*` declarations remain 41, strict non-final `private Playback*Owner` declarations remain 18, final-inclusive `private Playback*` wiring declarations remain 52, final-inclusive `private Playback*Owner` wiring declarations remain 22, production `fromPlaybackQueueManager(...)` calls remain 0, production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` mentions remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, app playback owner Java files remain 43, production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0, production `fromMediaSourceProvider(...)` calls remain 9, and production `mediaItemMatchesTrackForReuse(...)` calls remain 5.
- Queue/interface finding: `PlaybackQueueStateOwner` remains narrowed to `queueStateSnapshot()` and `queueSnapshot()`, and `PlaybackQueueManager.QueueStateSnapshot` remains the minimal current-track/current-index/queue-size state contract with derived booleans. There is still no `QueueProvider` method list to shrink, and Service still has no direct queue snapshot reads.
- Resolver/cache finding: URI and MediaItem matching remain in `PlaybackMediaSourceProvider`; precache wires provider-backed matching and cache operations through `PlaybackPrecacheManager.fromMediaSourceProvider(...)` and `PlaybackMediaCacheOperations.fromMediaSourceProvider(...)`. Cache key/header/range/data-source decisions are still behind `PlaybackPrecacheManager` and `PlaybackMediaCacheOperations`, not `EchoPlaybackService` or a new resolution facade.
- Focused coverage for the batch: `PlaybackStreamingDiagnosticsRecorderOwnerTest`, `PlaybackQueueStateOwnerTest`, `PlaybackQueueManagerTest`, and `MainActivityArchitectureContractTest`. The next useful slices should avoid more purely mechanical helper deletion unless it shortens a real queue/cache/resolver path or removes a constructor dependency, interface method, supplier chain, or Service strategy branch.
- Batch compile verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`. Device smoke remains out of scope because NOTE 164-165 and this audit did not change notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
- Next safe candidates: audit `PlaybackMediaSourceProvider` tests for invalid URI/cache-miss gaps; inspect `PlaybackPrecacheManager.StateProvider` for any remaining derived state that can move to the queue/cache owner; or find a `Playback*Owner` field in `EchoPlaybackService` that can be localized only if the owning manager still remains the behavior boundary.
## NOTE 167 - Provider rejects stale streaming MediaItem cache key (2026-07-03)
- Resolver/cache boundary test slice: `PlaybackMediaSourceProviderTest.providerRejectsStreamingMediaItemWhenCacheKeyIsStale` now verifies the provider-level `mediaItemMatchesTrackForReuse(...)` path rejects a streaming `MediaItem` with the same media id and resolved URI when its custom cache key still points at an older resolved URL.
- Concrete win: this strengthens focused behavior coverage without adding or changing a production owner, facade, bridge, state source, supplier, queue API, Service strategy branch, URI/media item resolver path, cache policy path, package move, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior. The rule remains owned by `PlaybackMediaSourceProvider` and protects `PlaybackPrecacheManager`/mirrored-queue callers from treating stale player media items as reusable.
- Current metrics after the test-only slice: `EchoPlaybackService.java` remains 1404 lines, strict non-final `private Playback*` declarations remain 41, strict non-final `private Playback*Owner` declarations remain 18, final-inclusive `private Playback*` wiring declarations remain 52, final-inclusive `private Playback*Owner` wiring declarations remain 22, production `fromPlaybackQueueManager(...)` calls remain 0, production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` mentions remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, app playback owner Java files remain 43, and production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackMediaSourceProviderTest --console=plain`. Architecture contract and device smoke remain out of scope because this is a focused feature test-only resolver/cache boundary slice.
## NOTE 168 - Precache state provider diagnostics narrowed (2026-07-03)
- Cache/interface shrink slice: `PlaybackPrecacheManager.StateProvider` no longer exposes `streamingDiagnostics()`. `PlaybackPrecacheStateOwner` now only supplies the current player `MediaItem`, while `PlaybackStreamingDiagnostics` is an explicit `PlaybackPrecacheManager` dependency owned by the cache/precache boundary rather than a state-provider method.
- Concrete win: one `StateProvider` interface method, one `PlaybackPrecacheStateOwner` field, one `PlaybackPrecacheStateOwner` constructor parameter, and the old no-diagnostics `PlaybackPrecacheManager` constructor overloads were removed without adding an owner, facade, bridge, state source, queue API, URI/media item resolver path, cache policy path, Service strategy branch, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1404 lines, strict non-final `private Playback*` declarations remain 41, strict non-final `private Playback*Owner` declarations remain 18, final-inclusive `private Playback*` wiring declarations remain 52, final-inclusive `private Playback*Owner` wiring declarations remain 22, production `fromPlaybackQueueManager(...)` calls remain 0, production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` mentions remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, app playback owner Java files remain 43, production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0, `PrecacheStateProviderStreamingDiagnostics` is 0, and `PlaybackPrecacheStateOwner.java` is now 70 lines.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.playback.PlaybackPrecacheStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Device smoke remains out of scope because this only narrows the precache state interface and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 169 - Precache current-player supplier uses standard Supplier (2026-07-03)
- Cache/interface shrink slice: `PlaybackPrecacheManager.StateProvider` was deleted after it had only one method left. `PlaybackPrecacheManager` now receives a standard `Supplier<MediaItem>` for the current player media item, and `PlaybackPrecacheStateOwner` implements `Supplier<MediaItem>` while keeping the same safe player-state checks for idle, empty, and released player states.
- Concrete win: one custom precache state-provider interface and its only method were removed without adding an owner, facade, bridge, state source, queue API, URI/media item resolver path, cache policy path, Service strategy branch, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move. The state owner still owns the player-to-current-media-item safety policy instead of moving that logic into `EchoPlaybackService`.
- Batch audit after NOTE 167-169: resolver/cache ownership still stays in `PlaybackMediaSourceProvider`, cache/precache policy still stays in `PlaybackPrecacheManager` / `PlaybackMediaCacheOperations`, production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0, production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` mentions remain 0, and there is no remaining stop/clear merge candidate because `PlaybackQueueStopClearOwner` is absent and `PlaybackQueueCompletionOwner` owns the stop/clear preparation calls.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1404 lines, strict non-final `private Playback*` declarations remain 41, strict non-final `private Playback*Owner` declarations remain 18, final-inclusive `private Playback*` wiring declarations remain 52, final-inclusive `private Playback*Owner` wiring declarations remain 22, production `fromPlaybackQueueManager(...)` calls remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, app playback owner Java files remain 43, `PrecacheCustomStateProviderHits` is 0, and `PlaybackPrecacheStateOwner.java` remains 70 lines.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.playback.PlaybackPrecacheStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Device smoke remains out of scope because this only deletes a custom precache state interface and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 170 - Precache state owner instance wrapper removed (2026-07-03)
- Service wiring/supplier shrink slice: `EchoPlaybackService` no longer creates `new PlaybackPrecacheStateOwner(...)` and no longer passes a local `playbackPrecacheStateOwner` wrapper into `PlaybackPrecacheManager.fromMediaSourceProvider(...)`. The Service now passes the existing `PlaybackPrecacheStateOwner.playerMediaItemSupplierFromPlayerSupplier(() -> player)` supplier directly, while the safe idle/empty/released-player checks remain outside Service.
- Concrete win: one pure instance wrapper, one local Service variable, one constructor parameter path, and one forwarding `get()` method were removed without adding an owner, facade, bridge, state source, queue API, URI/media item resolver path, cache policy path, Service strategy branch, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move. `PlaybackPrecacheStateOwnerTest` was deleted because it only covered the removed wrapper; `PlaybackPrecacheStateOwnerPlayerMediaItemTest` remains the behavior coverage for the supplier factory.
- Current metrics after the slice: `EchoPlaybackService.java` is 1401 lines, strict non-final `private Playback*` declarations remain 41, strict non-final `private Playback*Owner` declarations remain 18, final-inclusive `private Playback*` wiring declarations remain 52, final-inclusive `private Playback*Owner` wiring declarations remain 22, production `fromPlaybackQueueManager(...)` calls remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, app playback owner Java files remain 43, production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0, `PrecacheStateOwnerInstanceHits` is 0, and `PlaybackPrecacheStateOwner.java` is now 60 lines.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.playback.PlaybackPrecacheStateOwnerPlayerMediaItemTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Device smoke remains out of scope because this only removes a precache supplier wrapper and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 171 - Precache media-item supplier merged into player state owner (2026-07-03)
- Player-state owner shrink slice: `PlaybackPrecacheStateOwner.java` was removed after it had become only a static current-player `MediaItem` supplier factory. The safe idle/empty/released-player checks now live on `PlaybackPlayerStateOwner`, which already owns safe player-state reads for playing, position, duration, and buffered progress.
- Concrete win: one production `Playback*Owner.java` file, one static utility owner, and one test class tied to the deleted utility owner were removed without adding an owner, facade, bridge, state source, queue API, URI/media item resolver path, cache policy path, Service strategy branch, supplier field, package move, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior. `EchoPlaybackService` still passes only a player supplier into `PlaybackPrecacheManager.fromMediaSourceProvider(...)`.
- Current metrics after the slice: `EchoPlaybackService.java` is 1306 lines, strict non-final `private Playback*` declarations remain 41, strict non-final `private Playback*Owner` declarations remain 18, final-inclusive `private Playback*` wiring declarations remain 52, final-inclusive `private Playback*Owner` wiring declarations remain 22, production `fromPlaybackQueueManager(...)` calls remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, app playback owner Java files are now 42, production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0, and production `PlaybackPrecacheStateOwner` hits are 0.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.playback.PlaybackPlayerStateOwnerTest --tests app.yukine.playback.PlaybackPlayerStateOwnerMediaItemTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Device smoke remains out of scope because this only moves a supplier factory into an existing state owner and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 172 - Streaming MediaItem reuse requires cache-key presence (2026-07-03)
- Resolver/cache behavior slice: `PlaybackMediaSourceProvider.mediaItemIdentityMatchesForReuse(...)` now requires cache-key equality instead of accepting reuse when either side is missing a key. This keeps streaming/webdav reuse tied to the provider-owned cache-key rule, so a player `MediaItem` with the same id and resolved URI but no custom cache key cannot bypass the stale-cache-key guard.
- Concrete win: one resolver/cache boundary behavior was tightened and `PlaybackMediaSourceProviderTest.providerRejectsStreamingMediaItemWhenCacheKeyIsMissing` now fixes the missing-cache-key case without adding an owner, facade, bridge, state source, queue API, URI/media item resolver path, cache policy path, Service strategy branch, supplier field, package move, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior. Local tracks still match when both sides have no cache key.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1306 lines, strict non-final `private Playback*` declarations remain 41, strict non-final `private Playback*Owner` declarations remain 18, final-inclusive `private Playback*` wiring declarations remain 52, final-inclusive `private Playback*Owner` wiring declarations remain 22, production `fromPlaybackQueueManager(...)` calls remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, app playback owner Java files remain 42, and production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackMediaSourceProviderTest --console=plain` and T1 `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackMirroredQueueTrackMatcherOwnerTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Device smoke remains out of scope because this changes only MediaItem reuse identity and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 173 - Precache manager constructor requires MediaItem matcher path (2026-07-03)
- Cache/precache interface shrink slice: the old `PlaybackPrecacheManager` constructor overload that accepted media cache operations without a `BiPredicate<MediaItem, Track>` matcher was removed. Production continues to use `fromMediaSourceProvider(...)`, which injects provider-owned `mediaItemMatchesTrackForReuse(...)`; focused tests that need direct construction now pass an explicit matcher.
- Concrete win: one constructor entry point was removed, reducing the chance of future precache wiring bypassing the `PlaybackMediaSourceProvider` MediaItem/cache-key reuse rule, without adding an owner, facade, bridge, state source, queue API, URI/media item resolver path, cache policy path, Service strategy branch, supplier field, package move, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1306 lines, strict non-final `private Playback*` declarations remain 41, strict non-final `private Playback*Owner` declarations remain 18, final-inclusive `private Playback*` wiring declarations remain 52, final-inclusive `private Playback*Owner` wiring declarations remain 22, production `fromPlaybackQueueManager(...)` calls remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, app playback owner Java files remain 42, and production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks, and T2 `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`. Device smoke remains out of scope because this only deletes a precache constructor overload and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 174 - Realtime visualization factory boundary covered (2026-07-03)
- Test-only owner boundary slice: `PlaybackRealtimeVisualizationOwnerTest.factoryWrapsRealtimeBassDetectorBehindPlaybackGate` now covers `PlaybackRealtimeVisualizationOwner.fromRealtimeBassDetector(...)`. The factory path keeps `RealtimeBassDetector` reads behind the owner and still applies the playback-state gate, empty-bands fallback, and missing-detector fallback.
- Concrete win: focused behavior coverage was strengthened for an existing owner/factory boundary without adding an owner, facade, bridge, state source, queue API, URI/media item resolver path, cache policy path, Service strategy branch, supplier field, package move, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior. This guards against moving realtime beat/bands policy back into `EchoPlaybackService`.
- Current metrics after the test-only slice: `EchoPlaybackService.java` remains 1306 lines, strict non-final `private Playback*` declarations remain 41, strict non-final `private Playback*Owner` declarations remain 18, final-inclusive `private Playback*` wiring declarations remain 52, final-inclusive `private Playback*Owner` wiring declarations remain 22, production `fromPlaybackQueueManager(...)` calls remain 0, Service `queueStateSnapshot(...)` mentions remain 0, Service `playbackQueueStateOwner::currentTrack` suppliers remain 0, app playback owner Java files remain 42, and production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackRealtimeVisualizationOwnerTest --console=plain`, which also reused existing app Kotlin/Java compile outputs. Device smoke remains out of scope because this changes only focused unit coverage and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 175 - App shell early gateway host wiring guarded (2026-07-04)
- App shell wiring batch: `MainActivityBase.onCreate()` still creates route/status/store before binding now-playing and library gateways. The follow-up slices hardened the remaining early gateway callbacks that could observe later host fields: `MainLibraryGateway` now reads language through the same null-safe `settingsStore` provider shape as the other early wiring, `MainActivityStreamingActionGateway` ignores playlist-login callback delivery until `streamingPlaylistController` exists, and `MainLibraryGateway` ignores add-to-playlist delivery until `playlistDialogController` exists.
- Source-of-truth status is unchanged: language still comes from `MainSettingsStore`, route state still comes from `MainRouteController` / `NavigationViewModel`, library actions still enter `LibraryViewModel` / `MainLibraryGateway`, and playlist dialog behavior remains owned by `PlaylistDialogController`. No state mirror, owner, facade, coordinator, bridge, package move, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: three Java/Kotlin early wiring risks are now guarded by `MainActivityArchitectureContractTest`: one nullable settings lookup that crossed into a Kotlin non-null gateway contract, one early streaming playlist callback, and one early library playlist callback. `MainActivityBase.java` is 2358 lines after the batch.
- Next app-shell rule: do not continue by wrapping every host field in null guards. Only change another gateway/listener callback when it is bound before the field owner is created and the slice removes a real initialization-order dependency, constructor non-null risk, state mirror, or pure forwarding hop.
- Verification for the batch passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; the T2 compile checkpoint also passed with `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`. Focused `git diff --check` and mojibake scans over `MainActivityBase.java` plus `MainActivityArchitectureContractTest.java` also passed. Device or emulator smoke remains out of scope because these slices do not change notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 176 - Queue snapshot list read leaves state owner (2026-07-04)
- Queue/state interface shrink slice: `PlaybackQueueStateOwner.queueSnapshot()` was removed. `EchoPlaybackService.queueSnapshot()` now reads `PlaybackQueueManager.queueSnapshot()` directly with an empty-list fallback while `PlaybackQueueStateOwner` stays focused on derived queue state reads from `QueueStateSnapshot`.
- Source-of-truth status is unchanged: queue membership, current index, and current track authority remain in `PlaybackQueueManager`; `EchoPlaybackService.queueSnapshot()` is still only the Android service boundary used by app shell listeners. No state mirror, owner, facade, bridge, queue provider, URI/media resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one Java-facing state-owner method and one forwarding chain were removed. `PlaybackQueueStateOwnerTest` no longer tests list snapshots through the state owner, and `MainActivityArchitectureContractTest` now guards `playbackQueueManager.queueSnapshot()` consumers as `EchoPlaybackService.java` only.
- Current metrics after the slice: `EchoPlaybackService.java` is 1303 lines, strict non-final `private Playback*` declarations are 41, production `fromPlaybackQueueManager(...)` calls are 0, Service direct `queueStateSnapshot()` mentions are 0, and app playback owner Java files remain 42.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain` and `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest --console=plain`. Device or emulator smoke remains out of scope because this only removes a queue-state forwarding read and does not change notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 177 - Queue state owner drops index and size passthroughs (2026-07-04)
- Queue/state interface shrink slice: `PlaybackQueueStateOwner.currentIndex()` and `PlaybackQueueStateOwner.queueSize()` were removed after a focused caller audit showed no production users. Consumers that need queue boundaries still use the derived booleans, while the raw `currentIndex` and `queueSize` fields remain inside `PlaybackQueueManager.QueueStateSnapshot`.
- Source-of-truth status is unchanged: queue membership, current index, current track, and queue size authority remain in `PlaybackQueueManager`; `PlaybackQueueStateOwner` only exposes the Java-facing state reads still used by notification, lyrics, and crossfade owners. No state mirror, owner, facade, bridge, queue provider, URI/media resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: two Java-facing state-owner methods were removed, and `MainActivityArchitectureContractTest` now guards against reintroducing those passthrough methods while continuing to require `QueueStateSnapshot` to keep the raw source fields.
- Current metrics after the slice: `EchoPlaybackService.java` is 1397 lines in the current baseline, strict non-final `private Playback*` declarations are 51, production `fromPlaybackQueueManager(...)` calls are 0, Service direct `queueStateSnapshot()` mentions are 0, `playbackQueueStateOwner::currentTrack` suppliers are 0, app playback owner Java files remain 42, and production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device or emulator smoke remains out of scope because this only removes unused queue-state passthrough reads and does not change notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 178 - Mirrored queue matcher keeps provider cache-key rule (2026-07-04)
- Resolver/cache boundary test slice: `PlaybackMirroredQueueTrackMatcherOwnerTest.mediaSourceProviderConstructorUsesProviderOwnedCacheKeyRule` now covers the app-side owner constructor that accepts `PlaybackMediaSourceProvider`. A provider-built streaming `MediaItem` still matches, while a `MediaItem` with the same id and URI but missing the provider-owned custom cache key is rejected.
- Source-of-truth status is unchanged: URI, `MediaItem`, and cache-key identity remain owned by `PlaybackMediaSourceProvider`; `PlaybackMirroredQueueTrackMatcherOwner` only routes the player `MediaItem` and queue `Track` into that rule. No state mirror, owner, facade, bridge, queue provider, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: focused app-side behavior coverage now guards against mirrored queue reuse bypassing the provider resolver/cache-key rule without expanding `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, or any new resolver facade.
- Current metrics after the test-only slice: `EchoPlaybackService.java` remains 1397 lines, strict non-final `private Playback*` declarations remain 51, production `fromPlaybackQueueManager(...)` calls remain 0, Service direct `queueStateSnapshot()` mentions remain 0, app playback owner Java files remain 42, and production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackMirroredQueueTrackMatcherOwnerTest --console=plain` and `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackMediaSourceProviderTest --console=plain`. Device or emulator smoke remains out of scope because this only strengthens resolver/cache identity tests and does not change notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 179 - App shell gateway order contract tightened (2026-07-04)
- App shell wiring contract slice: `MainActivityArchitectureContractTest.mainActivityCreatesRouteStoresAndStatusBeforeGatewayBinding` now explicitly guards that `statusMessageController` is created in `initializeRouteStoresAndStatus()` before `initializeLibraryGateway()` binds the library gateway status callback. The same test continues to require route/status/store initialization before now-playing, download, library, and playback lifecycle gateway binding.
- Source-of-truth status is unchanged: route state remains in `MainRouteController` / `NavigationViewModel`, status text remains in `StatusMessageController` / `StatusMessageViewModel`, language remains in `MainSettingsStore`, and library actions still flow through `MainLibraryGateway`. No state mirror, owner, facade, coordinator, bridge, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: the architecture contract now protects one more early host-wiring boundary by preventing `statusMessageController` from being passed as a raw constructor argument to the library gateway and by keeping the callback tied to the post-route/status initialization phase.
- Current metrics after the test-only slice: `EchoPlaybackService.java` remains 1397 lines, strict non-final `private Playback*` declarations remain 51, app playback owner Java files remain 42, production `fromPlaybackQueueManager(...)` calls remain 0, and Service direct `queueStateSnapshot()` mentions remain 0.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device or emulator smoke remains out of scope because this only strengthens app shell wiring contracts and does not change notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 180 - Early playback gateway fallback contract tightened (2026-07-04)
- App shell wiring contract slice: `MainActivityArchitectureContractTest.mainActivityCreatesRouteStoresAndStatusBeforeGatewayBinding` now also guards the early playback command path used by streaming and library gateways. Those gateways must call `playTrackListFromHost(...)` instead of directly capturing `playbackStartController`, and `playTrackListFromHost(...)` must keep the pending-playback fallback when `playbackStartController` has not been initialized yet.
- Source-of-truth status is unchanged: pending playback remains owned by `PlaybackStartController.Listener` / `MainActivityPlaybackStartListener`, playback commands still enter `PlaybackStartController` once initialized, and early gateway creation stays provider/lambda based. No state mirror, owner, facade, coordinator, bridge, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: the architecture contract now protects one more host-field initialization boundary and prevents early gateway callbacks from bypassing the safe pending-playback path.
- Current metrics after the test-only slice: `EchoPlaybackService.java` remains 1397 lines, strict non-final `private Playback*` declarations remain 51, app playback owner Java files remain 42, production `fromPlaybackQueueManager(...)` calls remain 0, and Service direct `queueStateSnapshot()` mentions remain 0.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device or emulator smoke remains out of scope because this only strengthens app shell wiring contracts and does not change notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 181 - Queue persistence forwarding owner removed (2026-07-04)
- Owner/interface shrink slice: `PlaybackQueuePersistenceOwner.java` was removed after it had become only a short-lived forwarding wrapper around `PlaybackQueueManager.persistQueueState()` and `PlaybackQueueStore.saveResumeRequested(...)`. `EchoPlaybackService` now writes play/pause resume requests through a private playback-boundary helper backed by `PlaybackQueueStoreImpl`, while task-removed fallback persists the queue through the queue authority, `PlaybackQueueManager`.
- Shutdown source-of-truth status is unchanged: `PlaybackShutdownLifecycleResourcesOwner` still owns the lifecycle resource contract and now exposes `playbackQueueLifecycleStore(playbackQueueManager, queueStore)` for the shutdown coordinator wiring. No new owner, facade, coordinator, queue provider, queue state mirror, URI/media resolver path, cache policy path, notification behavior, lyrics behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one production `Playback*Owner.java` file, one forwarding test class, one short-lived Service owner allocation, and the `playbackQueuePersistenceOwner()` forwarding method were removed. `PlaybackShutdownLifecycleResourcesOwnerTest` now covers the remaining queue lifecycle store behavior directly, and `MainActivityArchitectureContractTest` rejects reintroducing `PlaybackQueuePersistenceOwner`.
- Current metrics after the slice: `EchoPlaybackService.java` is 1394 lines, app playback owner Java files are 41, strict `private Playback*` declarations are 40, production `fromPlaybackQueueManager(...)` calls remain 0, and Service direct `queueStateSnapshot()` mentions remain 0.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackShutdownLifecycleResourcesOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest --console=plain`, which also executed app Kotlin/Java compile tasks. Emulator smoke should be used for later broad shutdown/background/notification batches; this slice only removes a forwarding owner and keeps shutdown coordinator behavior covered by focused tests.
## NOTE 182 - UI common UTF-8 contract reads feature owner paths (2026-07-04)
- Test-only app-shell/UTF-8 guard slice: `MainActivityArchitectureContractTest.uiCommonChineseCopyRemainsUtf8Readable` now reads `HomeDashboardScreen.kt`, `LibraryGroupsScreen.kt`, and `TrackListScreen.kt` from `feature/ui-common/src/main/java/app/yukine/ui/...` instead of relying on the legacy `app/src/main/java/app/yukine/ui/...` compatibility mapping.
- Source-of-truth status is unchanged: the migrated Compose UI files remain owned by `feature:ui-common`; this slice only makes the mojibake contract point at that real owner and asserts the old app UI paths are not raw files. No production UI behavior, playback owner, app shell wiring, state mirror, facade, coordinator, bridge, package move, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: one migration-residue test path was removed from the UTF-8 guard, so future mojibake regressions in the actual feature module cannot be hidden by `read(...)` candidate-path compatibility.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused mojibake scan over the three UI files and the architecture contract returned `TOTAL=0`. Device or emulator smoke remains out of scope because this is test-only and does not touch runtime playback or UI rendering behavior.
## NOTE 183 - Crossfade queue reads reuse queue state owner (2026-07-04)
- Queue/state supplier shrink slice: `PlaybackCrossfadeStateOwner` now receives the existing `PlaybackQueueStateOwner` instead of its own `Supplier<PlaybackQueueManager>`. Crossfade advance policy reads `hasMultipleTracks()` and `isAtEndOfQueue()` from that Java queue-state owner instead of duplicating queue manager snapshot fallback logic.
- Source-of-truth status is unchanged: queue membership, current index, and current track remain owned by `PlaybackQueueManager`; `PlaybackQueueStateOwner` remains the Java-facing derived state reader for notification, lyrics, and now crossfade. No new owner, facade, bridge, state mirror, queue provider, URI/media resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one crossfade-specific queue manager supplier chain, one direct `PlaybackQueueManager` import, and one duplicate `queueStateSnapshot()` fallback block were removed from `PlaybackCrossfadeStateOwner`. `MainActivityArchitectureContractTest` now rejects reintroducing that direct supplier/snapshot path, and `PlaybackCrossfadeStateOwnerTest` still covers repeat/off/end-of-queue behavior through the queue state owner.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1394 lines, app playback owner Java files remain 41, strict `private Playback*` declarations remain 40, production `fromPlaybackQueueManager(...)` calls remain 0, Service direct `queueStateSnapshot()` mentions remain 0, and `PlaybackCrossfadeStateOwner.java` direct `PlaybackQueueManager` / `queueStateSnapshot()` hits are 0.
- Verification passed with default Gradle daemon/workers via `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCrossfadeStateOwnerTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also executed app Kotlin/Java compile tasks. Device or emulator smoke remains out of scope because this only changes crossfade queue-state wiring and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 184 - Error recovery queue reads reuse queue state owner (2026-07-04)
- Queue/state dependency shrink slice: `PlaybackErrorRecoveryCommandOwner` now receives the existing `PlaybackQueueStateOwner` instead of `PlaybackQueueManager`. Error recovery still owns failed-track policy (`failed != null`, valid id, and queue has multiple tracks), but reads current-track and multi-track state through the shared Java queue-state owner rather than duplicating a manager snapshot helper.
- Source-of-truth status is unchanged: queue membership, current index, current track, and derived queue flags remain backed by `PlaybackQueueManager`; `PlaybackQueueStateOwner` is only the Java-facing derived state reader. No new owner, facade, bridge, state mirror, queue provider, URI/media resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one direct `PlaybackQueueManager` import, one direct manager field, and one duplicate `queueStateSnapshot()` fallback block were removed from `PlaybackErrorRecoveryCommandOwner`. `EchoPlaybackService` reuses its existing `playbackQueueStateOwner` field, so Service line count, owner file count, and wiring field count do not grow.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1394 lines, app playback owner Java files remain 41, production `fromPlaybackQueueManager(...)` calls remain 0, Service direct `queueStateSnapshot()` mentions remain 0, and `PlaybackErrorRecoveryCommandOwner.java` direct `PlaybackQueueManager` / `queueStateSnapshot()` hits are 0.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackErrorRecoveryCommandOwnerTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Emulator smoke is only needed once ADB sees the new VM or when a later slice touches notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 185 - Favorite command current-track read reuses queue state owner (2026-07-04)
- Queue/state dependency shrink slice: `PlaybackFavoriteCommandOwner` now receives the existing `PlaybackQueueStateOwner` instead of `PlaybackQueueManager`. Favorite toggling still stays in the focused favorite command owner, while current-track lookup uses the same Java queue-state reader as notification, lyrics, crossfade, and error recovery.
- Source-of-truth status is unchanged: current track is still derived from `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackQueueStateOwner` remains only the Java-facing derived state reader. No new owner, facade, bridge, state mirror, queue provider, URI/media resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one direct `PlaybackQueueManager` import, one manager-shaped command parameter, and one command-local `queueStateSnapshot().getCurrentTrack()` helper were removed from `PlaybackFavoriteCommandOwner`. `EchoPlaybackService.toggleCurrentFavorite()` reuses the existing `playbackQueueStateOwner` field and still does not contain favorite toggle policy.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1394 lines, strict `private Playback*` declarations remain 40, app playback owner Java files remain 41, production `fromPlaybackQueueManager(...)` calls remain 0, Service queue-state snapshot/supplier mentions remain 0, and `PlaybackFavoriteCommandOwner.java` direct `PlaybackQueueManager` / `queueStateSnapshot()` hits are 0.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackFavoriteCommandOwnerTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Device smoke remains out of scope unless the VM appears in ADB or a later slice touches notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 186 - Playback owner/interface audit tightens Service wiring limits (2026-07-04)
- Audit slice after the crossfade, error-recovery, and favorite queue-state read closures: `MainActivityArchitectureContractTest.playbackOwnerInventoryAndServiceWiringDoNotGrowWithoutAudit` now locks `EchoPlaybackService` to the current wiring baseline instead of the older looser threshold. Strict `private Playback*` declarations are capped at 40, final-inclusive `private Playback*` declarations at 51, and final-inclusive `private Playback*Owner` declarations at 21.
- Source-of-truth status is unchanged: queue/current-track state still comes from `PlaybackQueueManager`, Java derived queue-state reads still go through `PlaybackQueueStateOwner`, URI/MediaItem resolution remains in `PlaybackMediaSourceProvider`, and cache/precache policy remains in `PlaybackPrecacheManager` plus `PlaybackMediaCacheOperations`. No production code, owner, facade, bridge, state mirror, queue provider, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one architecture contract now prevents silent Service wiring growth after the latest queue-state cleanup batch. The same audit confirmed production `fromPlaybackQueueManager(...)` remains 0, Service queue-state snapshot/supplier mentions remain 0, app playback owner Java files remain 41, and production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` hits remain 0.
- Important boundary note: `PlaybackPrecacheManager` intentionally still depends on `PlaybackQueueManager` because it owns both current-track cache decisions and `upcomingTracksForPrecache(...)`; moving it to `PlaybackQueueStateOwner` would add queue/precache interface surface without reducing state sources.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus focused UTF-8 checks for the changed contract and added handoff lines. Device smoke remains out of scope because this is a contract/docs-only audit slice.
## NOTE 187 - Play history current-track read reuses queue state owner (2026-07-04)
- Queue/state dependency shrink slice: `PlaybackPlayHistoryRecorder.recordIfPlaybackStartedAction(...)` now receives the existing `PlaybackQueueStateOwner` instead of `PlaybackQueueManager`. Play-history policy remains in `PlaybackPlayHistoryRecorder`, while the current-track lookup uses the shared Java queue-state reader.
- Source-of-truth status is unchanged: current track is still derived from `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackQueueStateOwner` remains only the Java-facing derived state reader. No new owner, facade, bridge, state mirror, queue provider, URI/media resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one direct `PlaybackQueueManager` import, one manager-shaped action parameter, and one play-history-local `queueStateSnapshot().getCurrentTrack()` helper were removed from `PlaybackPlayHistoryRecorder`. `EchoPlaybackService.onCreate()` reuses the existing `playbackQueueStateOwner` field and still does not contain play-history policy.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1394 lines, strict `private Playback*` declarations remain 40, final-inclusive `private Playback*` declarations remain 51, app playback owner Java files remain 41, production `fromPlaybackQueueManager(...)` calls remain 0, Service queue-state snapshot/supplier mentions remain 0, and `PlaybackPlayHistoryRecorder.java` direct `PlaybackQueueManager` / `queueStateSnapshot()` hits are 0.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPlayHistoryRecorderTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`. MuMu is available through `adb connect 127.0.0.1:7555`, but device smoke remains out of scope unless a later slice touches notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 188 - Visualization cache current-track read reuses queue state owner (2026-07-04)
- Queue/state dependency shrink slice: `PlaybackVisualizationCacheStateOwner` now receives the existing `PlaybackQueueStateOwner` instead of `PlaybackQueueManager`. Visualization cache still owns only its state-provider boundary: main handler, current track, and cache task scheduling.
- Source-of-truth status is unchanged: current track is still derived from `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackQueueStateOwner` remains only the Java-facing derived state reader. No new owner, facade, bridge, state mirror, queue provider, URI/media resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one direct `PlaybackQueueManager` import, one manager-shaped constructor parameter, and one visualization-cache-local `queueStateSnapshot().getCurrentTrack()` read were removed from `PlaybackVisualizationCacheStateOwner`. `EchoPlaybackService.onCreate()` reuses the existing `playbackQueueStateOwner` local wiring path and still does not contain visualization cache policy.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1394 lines, strict `private Playback*` declarations remain 40, final-inclusive `private Playback*` declarations remain 51, app playback owner Java files remain 41, production `fromPlaybackQueueManager(...)` calls remain 0, Service queue-state snapshot/supplier mentions remain 0, and `PlaybackVisualizationCacheStateOwner.java` direct `PlaybackQueueManager` / `queueStateSnapshot()` hits are 0.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackVisualizationCacheStateOwnerTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`. MuMu remains available at `127.0.0.1:7555`; device smoke remains out of scope unless a later slice touches notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 189 - UI common Chinese copy contract covers full migrated text set (2026-07-04)
- UTF-8/app-shell contract slice: `MainActivityArchitectureContractTest.uiCommonChineseCopyRemainsUtf8Readable` now requires the migrated `feature:ui-common` home dashboard copy to contain the full visible text set that was previously at risk: title, badge, subtitle fallback, primary/secondary actions, now-playing/recent/empty/weekly labels, recommendation labels, and streaming connection text.
- Source-of-truth status is unchanged: user-visible migrated Compose copy remains in `feature/ui-common/src/main/java/app/yukine/ui/HomeDashboardScreen.kt`, `LibraryGroupsScreen.kt`, and `TrackListScreen.kt`. This slice adds no owner, gateway, facade, bridge, state mirror, package move, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior.
- Concrete win: the UTF-8 contract now proves the key Chinese strings are still present in the feature owner files, instead of only checking a smaller subset plus generic mojibake absence. This reduces the chance that future UI file moves or encoding regressions silently drop visible Chinese copy.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus focused mojibake scan over the changed contract and the three migrated UI files. Device smoke remains out of scope because this is test/docs-only and does not touch runtime playback or rendering behavior.
## NOTE 190 - Queue public API audit separates precache reads (2026-07-04)
- Queue/interface audit slice: `MainActivityArchitectureContractTest.playbackQueueManagerPublicApiStaysExplicitlyAudited` now separates `PlaybackQueueManager.upcomingTracksForPrecache(...)` into a `queuePrecacheApi` group instead of treating it as a generic derived queue read. The existing contract still requires the grouped API set to exactly match every class-level public `fun` on `PlaybackQueueManager`.
- Source-of-truth status is unchanged: queue membership, current index, and current track remain owned by `PlaybackQueueManager`; cache/precache policy remains in `PlaybackPrecacheManager`, with `upcomingTracksForPrecache(...)` as the explicit queue-owned input for that policy. No production code, owner, facade, bridge, state mirror, queue provider, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: the architecture contract now makes the queue/precache boundary visible in the public API audit, reducing the chance that precache-specific reads are later widened into `PlaybackQueueStateOwner` or a new broad queue snapshot facade.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus focused UTF-8 checks for the changed contract and added handoff lines. Device smoke remains out of scope because this is a contract/docs-only audit slice.
## NOTE 191 - Queue command current-track read reuses queue state owner (2026-07-04)
- Queue/state dependency shrink slice: `PlaybackQueueCommandOwner` now receives the existing `PlaybackQueueStateOwner` for current-track lookup instead of keeping its own `Supplier<PlaybackQueueManager>` and duplicate `queueStateSnapshot()` helper. `EchoPlaybackService` wires the existing `playbackQueueStateOwner` into the command owner field.
- Source-of-truth status is unchanged: current track is still derived from `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackQueueStateOwner` remains only the Java-facing derived state reader. No new owner, facade, bridge, state mirror, queue provider, URI/media resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one queue-command-specific manager supplier chain and one duplicate queue snapshot helper were removed from `PlaybackQueueCommandOwner`. The architecture contract now removes `PlaybackQueueCommandOwner.java` from the direct `playbackQueueManager.queueStateSnapshot()` hit list and requires the command owner to read through `PlaybackQueueStateOwner.currentTrack()`.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCommandOwnerTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`. MuMu remains available at `127.0.0.1:7555`; device smoke remains out of scope unless a later slice touches notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 192 - Mirrored transition queue reads reuse queue state owner (2026-07-04)
- Queue/state dependency shrink slice: `PlaybackQueueMirroredTransitionOwner` now receives the existing `PlaybackQueueStateOwner` for `isQueueEmpty()` and current-track lookup, while keeping `PlaybackQueueManager` only for the real mutation command `applyMirroredTransitionIndex(...)`. `EchoPlaybackService.onMediaItemTransition(...)` wires the existing `playbackQueueStateOwner` into the short-lived transition owner.
- Source-of-truth status is unchanged: queue membership, current index, and current track remain owned by `PlaybackQueueManager`; `PlaybackQueueStateOwner` remains only the Java-facing derived state reader. No new owner, facade, bridge, state mirror, queue provider, URI/media resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one mirrored-transition-local `queueStateSnapshot()` helper and two direct snapshot reads were removed from `PlaybackQueueMirroredTransitionOwner`. The architecture contract now removes `PlaybackQueueMirroredTransitionOwner.java` from the direct `playbackQueueManager.queueStateSnapshot()` hit list and requires the owner to read derived queue state through `PlaybackQueueStateOwner`.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMirroredTransitionOwnerTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`. Device smoke remains out of scope because this only changes mirrored transition queue-state wiring and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 193 - App shell gateway initialization contract locks route host order (2026-07-04)
- App-shell wiring contract slice: `MainActivityArchitectureContractTest.mainActivityCreatesRouteStoresAndStatusBeforeGatewayBinding` now treats `MainLibraryGatewayFactory.create(... routeActions: LibraryRouteActions, ...)` as a Kotlin non-null boundary and requires `initializeRouteStoresAndStatus()` to run before `initializeLibraryGateway()` when `MainActivityBase` passes the host `routeController` by value.
- Source-of-truth status is unchanged: navigation state remains owned by `NavigationViewModel` plus `MainRouteController`, library UI actions remain owned by `LibraryViewModel` plus `MainLibraryGateway`, and platform document picking remains behind the existing `DocumentPickerController` provider. No owner, facade, gateway, bridge, state mirror, package move, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: the previous route-controller null crash class is now guarded directly. The contract also keeps `documentPickerController` provider-based because `initializePlatformControllers()` still runs after library gateway binding, preventing a different uninitialized host field from being passed by value across the Java/Kotlin boundary.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus focused UTF-8/mojibake checks for the changed contract and added handoff lines. Device smoke remains out of scope because this is a contract/docs-only app-shell wiring guard and does not touch runtime playback, notification, lyrics, shutdown, foreground behavior, or background playback.
## NOTE 194 - Queue mutation clear snapshot exception is explicitly bounded (2026-07-04)
- Queue/interface audit slice: `MainActivityArchitectureContractTest` now records `PlaybackQueueMutationOwner` as a deliberate direct `PlaybackQueueManager.queueStateSnapshot()` exception only for `clearQueue()` empty-queue guarding. The contract caps that owner at one direct snapshot call and rejects expanding it into current-track reads or `queueSnapshot().isEmpty()` list reads.
- Source-of-truth status is unchanged: queue membership, current index, and current track remain owned by `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackQueueMutationOwner` remains the command owner for queue mutations, and `PlaybackQueueCompletionOwner` remains the stop/clear preparation owner. No owner, facade, bridge, state mirror, queue provider, new `PlaybackQueueManager` public API, URI/media resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: the stop/clear preparation merge candidate is now constrained instead of being mechanically migrated. Passing `PlaybackQueueStateOwner` into `PlaybackQueueMutationOwner` would widen the constructor without reducing a state source, and adding `PlaybackQueueManager.clearQueue()` would widen the public queue API; both are rejected until a later slice can remove a real field, method, supplier chain, or behavior gap.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus focused UTF-8 checks for the changed contract and added handoff lines. Device smoke remains out of scope because this is a contract/docs-only queue/interface audit and does not touch runtime playback, notification, lyrics, shutdown, foreground behavior, or background playback.
## NOTE 195 - Resolver and cache conversion points stay bounded (2026-07-04)
- Resolver/cache boundary audit slice: `MainActivityArchitectureContractTest.playbackOwnerInventoryAndServiceWiringDoNotGrowWithoutAudit` now counts the provider-to-cache conversion points. `EchoPlaybackService` may call `PlaybackPrecacheManager.fromMediaSourceProvider(...)` once, while `PlaybackMediaCacheOperations.fromMediaSourceProvider(...)` is allowed only inside `PlaybackPrecacheManager.java` and `PlaybackVisualizationCacheManager.java`.
- Source-of-truth status is unchanged: URI and MediaItem resolution remain owned by `PlaybackMediaSourceProvider`; cache operations are exposed through `PlaybackMediaCacheOperations`; cache/precache policy remains owned by `PlaybackPrecacheManager`; visualization cache remains owned by `PlaybackVisualizationCacheManager`. No owner, facade, bridge, state mirror, queue provider, new resolver/cache API, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: the contract now prevents `EchoPlaybackService` or a new resolver/cache facade from directly creating cache operations or spreading provider-backed cache policy into another owner. This strengthens the P2/P3 boundary without moving behavior or widening constructor/API surface.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus focused UTF-8 checks for the changed contract and added handoff lines. Device smoke remains out of scope because this is a contract/docs-only resolver/cache audit and does not touch runtime playback, notification, lyrics, shutdown, foreground behavior, or background playback.
## NOTE 196 - Feature modules cannot depend upward on app implementations (2026-07-04)
- Cross-module dependency audit slice: `MainActivityArchitectureContractTest.featureModulesDoNotDependOnAppImplementationClasses` now scans the five feature main source directories and rejects direct references to app shell/service implementation classes such as `MainActivityBase`, `EchoPlaybackService`, service connection/host adapters, app stores, route controllers, and app gateway implementations.
- Source-of-truth status is unchanged: feature modules may keep depending on their own contracts, managers, `core/model`, and `core/common`; app shell assembly remains in `MainActivityBase`; playback service boundaries remain in the app-side host ports and adapters. No owner, facade, bridge, state mirror, queue provider, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: this closes the audit gap where a future feature API could compile by importing app implementation classes, turning the migration into hidden upward coupling. The contract reinforces the rule that new cross-module API must live in core/model, core/common, or an explicit feature contract rather than `feature -> app implementation`.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus focused UTF-8 checks for the changed contract and added handoff lines. Device smoke remains out of scope because this is a contract/docs-only dependency-direction guard and does not touch runtime playback, notification, lyrics, shutdown, foreground behavior, or background playback.
## NOTE 197 - Session command current-track read reuses queue state owner (2026-07-04)
- Queue/state dependency shrink slice: `PlaybackSessionCommandOwner` now receives the existing `PlaybackQueueStateOwner` for `currentTrack()` instead of a `Supplier<PlaybackQueueManager>` plus its own `queueStateSnapshot()` helper. `EchoPlaybackService.onCreate()` wires the already-created `playbackQueueStateOwner` into the session command owner.
- Source-of-truth status is unchanged: current track is still derived from `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackQueueStateOwner` remains only the Java-facing derived state reader. No owner, facade, bridge, state mirror, queue provider, new `PlaybackQueueManager` public API, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one session-command-specific manager supplier chain, one direct `PlaybackQueueManager` import, and one duplicate local `queueStateSnapshot()` fallback block were removed from `PlaybackSessionCommandOwner`. The architecture contract also removes `PlaybackSessionCommandOwner.java` from the direct `playbackQueueManager.queueStateSnapshot()` hit list.
- Current metrics after the slice: `EchoPlaybackService.java` is 1395 lines, strict `private Playback*` declarations remain 40, app playback owner Java files remain 41, production `fromPlaybackQueueManager(...)` calls remain 0, and Service direct `queueStateSnapshot()` mentions remain 0.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackSessionCommandOwnerTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus focused UTF-8 checks for the changed Java contract/test and added handoff lines. Device smoke remains out of scope because this only changes session command current-track wiring and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 198 - Mirrored player current-track read reuses queue state owner (2026-07-04)
- Queue/state dependency shrink slice: `PlaybackQueueMirroredPlayerOwner` now receives the existing `PlaybackQueueStateOwner` for the `seekTo(...)` waveform-reset current-track lookup instead of a `Supplier<PlaybackQueueManager>` plus its own `queueStateSnapshot()` helper. The mirrored queue matcher still receives a queue snapshot supplier because it compares the queue against Media3 player items rather than reading derived current-track state.
- Source-of-truth status is unchanged: current track is still derived from `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackQueueStateOwner` remains only the Java-facing derived state reader; mirrored queue membership still belongs to `PlaybackQueueManager.queueSnapshot()` plus `PlaybackMirroredQueueTrackMatcherOwner`. No owner, facade, bridge, state mirror, queue provider, new `PlaybackQueueManager` public API, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one mirrored-player-specific manager supplier chain and one duplicate local `queueStateSnapshot()` fallback block were removed from `PlaybackQueueMirroredPlayerOwner`. The architecture contract also removes `PlaybackQueueMirroredPlayerOwner.java` from the direct `playbackQueueManager.queueStateSnapshot()` hit list while preserving the matcher's queue snapshot boundary.
- Current metrics after the slice: `EchoPlaybackService.java` is 1395 lines, strict `private Playback*` declarations remain 40, app playback owner Java files remain 41, production `fromPlaybackQueueManager(...)` calls remain 0, Service direct `queueStateSnapshot()` mentions remain 0, and direct manager snapshot files are now `PlaybackPrecacheManager.java`, `PlaybackQueueMutationOwner.java`, `PlaybackQueueStateOwner.java`, and `PlaybackStateSnapshotOwner.java`.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMirroredPlayerOwnerTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus focused UTF-8 checks for the changed Java contract/test and added handoff lines. Device smoke remains out of scope because this only changes mirrored-player current-track wiring and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 199 - Playback snapshot queue-state exception stays bounded (2026-07-04)
- Queue/state audit slice: `MainActivityArchitectureContractTest.playbackQueueMutationKeepsOneClearlyNamedEntryPointCluster` now treats `PlaybackStateSnapshotOwner` as a deliberately bounded direct `PlaybackQueueManager.queueStateSnapshot()` exception. The owner needs `currentTrack`, `currentIndex`, and `queueSize` together to build the public `PlaybackStateSnapshot`, so routing this through `PlaybackQueueStateOwner` would widen the Java state owner into a full snapshot facade.
- Source-of-truth status is unchanged: queue membership, current index, current track, and queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackStateSnapshotOwner` only maps that source state into `PlaybackStateSnapshot`. No production code, owner, facade, bridge, state mirror, queue provider, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: the architecture contract now caps `PlaybackStateSnapshotOwner` at one direct manager snapshot call, requires that call to feed `track/currentIndex/queueSize` into `PlaybackStateSnapshot`, and rejects exposing a full snapshot method through `PlaybackQueueStateOwner`.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus focused UTF-8/mojibake checks for the changed contract and added handoff lines. MuMu remains available at `127.0.0.1:7555`; device smoke remains out of scope because this is a contract/docs-only audit and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 200 - Media source provider public API stays audited (2026-07-04)
- Resolver/cache boundary audit slice: `MainActivityArchitectureContractTest.playbackOwnerInventoryAndServiceWiringDoNotGrowWithoutAudit` now locks the `PlaybackMediaSourceProvider` public class-level methods and companion helper methods as an explicit API set. Future additions to URI, `MediaItem`, header restore, cache-key, or reuse identity behavior must update the contract instead of silently widening the resolver surface.
- Source-of-truth status is unchanged: URI and `MediaItem` resolution remain owned by `PlaybackMediaSourceProvider`; provider-backed cache primitives are exposed to app owners only through `PlaybackMediaCacheOperations`; cache/precache scheduling policy remains in `PlaybackPrecacheManager`. No production code, owner, facade, bridge, state mirror, queue provider, package move, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: the architecture contract now rejects adding queue/precache policy methods such as `upcomingTracksForPrecache(...)` or `precacheTrack(...)` to `PlaybackMediaSourceProvider`, while keeping `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` absent and `PlaybackMediaCacheOperations.fromMediaSourceProvider(...)` conversion points bounded.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus focused UTF-8/mojibake checks for the changed contract and added handoff lines. Device smoke remains out of scope because this is a contract/docs-only resolver/cache audit and does not touch runtime playback, notification, lyrics, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 201 - Progress update adapter moves to manager owner (2026-07-04)
- Pure-forwarding owner removal slice: `PlaybackProgressUpdateCommandOwner.java` and its app-side forwarding test were removed. `PlaybackProgressUpdateManager.actionsFromCallbacks(...)` now owns the tiny adapter from `publishState` / `persistCurrentPlaybackPosition(false)` callbacks into `PlaybackProgressUpdateManager.Actions`, and `EchoPlaybackService.onCreate()` wires that manager-owned factory directly.
- Source-of-truth status is unchanged: progress tick scheduling still belongs to `PlaybackProgressUpdateManager`; playback snapshot publication still enters `EchoPlaybackService.publishState()`; position persistence still enters `PlaybackPositionManager` through `EchoPlaybackService.persistCurrentPlaybackPosition(false)`. No new owner, facade, bridge, state mirror, queue provider, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one pure forwarding `Playback*Owner.java` file and one app-side forwarding test were deleted, while behavior coverage moved to `PlaybackProgressUpdateManagerTest.actionsFromCallbacksPublishesAndPersistsWithoutForce`. Current metrics after the slice: `EchoPlaybackService.java` is 1393 lines, strict `private Playback*` declarations remain 40, app playback owner Java files are 40, production `fromPlaybackQueueManager(...)` calls remain 0, and Service direct `queueStateSnapshot()` mentions remain 0.
- Verification target: `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackProgressUpdateManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus focused UTF-8/mojibake checks for the changed files and added handoff lines. Device smoke remains out of scope because this only removes a forwarding progress-tick adapter and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 202 - Position manager current-track input narrows to queue state owner (2026-07-04)
- Queue/state dependency shrink slice: `PlaybackPositionManager.stateProviderFromPlaybackState(...)` now accepts a `Supplier<Track?>` for current track instead of a `PlaybackQueueManager`. `EchoPlaybackService.onCreate()` wires the existing `playbackQueueStateOwner::currentTrack` into the position manager state provider.
- Source-of-truth status is unchanged: current track remains derived from `PlaybackQueueManager.QueueStateSnapshot` through `PlaybackQueueStateOwner`; playback position still belongs to `PlaybackPlayerStateOwner` and persistence still belongs to `PlaybackPositionManager` plus `PlaybackQueueStore`. No owner, facade, bridge, state mirror, queue provider, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one feature manager no longer depends on `PlaybackQueueManager` or calls `queueStateSnapshot()` just to persist current playback position, and `PlaybackPositionManagerTest` no longer constructs a full queue manager to test the state-provider adapter.
- Verification target: `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackPositionManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus focused UTF-8/mojibake checks for the changed files and added handoff lines. Device smoke remains out of scope because this only changes position-manager current-track wiring and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.

## NOTE 203 - Runtime state manager current-track input narrows to queue state owner (2026-07-04)
- Queue/state dependency shrink slice: `PlaybackRuntimeStateManager.stateProviderFromPlaybackState(...)` now accepts a `Supplier<Track?>` for current track instead of a `Supplier<PlaybackQueueManager?>`. `EchoPlaybackService` wires the existing `playbackQueueStateOwner::currentTrack` into the runtime state manager used for replay-gain current-track volume.
- Source-of-truth status is unchanged: current track remains derived from `PlaybackQueueManager.QueueStateSnapshot` through `PlaybackQueueStateOwner`; runtime playback settings and replay gain still belong to `PlaybackRuntimeStateManager`; queue membership, current index, and current track still belong to `PlaybackQueueManager`. No owner, facade, bridge, state mirror, queue provider, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one feature manager no longer depends on `PlaybackQueueManager` or calls `queueStateSnapshot()` just to compute current-track volume, and `PlaybackRuntimeStateManagerTest` no longer constructs a full queue manager to test the state-provider adapter.
- Current metrics after the slice: `EchoPlaybackService.java` is 1393 lines; Service direct `queueStateSnapshot()` mentions remain 0; app playback `Playback*Owner.java` files remain 40; production `fromPlaybackQueueManager(...)` mentions remain 0; production `playbackQueueManagerSupplier?.get()?.queueStateSnapshot()` mentions are now 0.
- Verification target: `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackRuntimeStateManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus app compile and focused UTF-8/mojibake checks for the changed files and added handoff lines. Device smoke remains out of scope because this only changes runtime-manager current-track input for replay gain and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.

## NOTE 204 - Library gateway delayed now-bar renderer is contract-guarded (2026-07-04)
- App-shell wiring contract slice: `MainActivityArchitectureContractTest.mainActivityCreatesRouteStoresAndStatusBeforeGatewayBinding` now records that `initializeLibraryGateway()` runs before `initializePlaybackControllers()`, where `nowPlayingStateController` is created. The library gateway must therefore receive `() -> nowPlayingStateController.renderNowBar()` as a delayed callback and must not receive `nowPlayingStateController` by value or call `renderNowBar()` during gateway binding.
- Source-of-truth status is unchanged: now-playing state remains owned by `NowPlayingViewModel` plus `NowPlayingStateController`; library actions remain owned by `LibraryViewModel` plus `MainLibraryGateway`; navigation remains owned by `NavigationViewModel` plus `MainRouteController`. No owner, facade, gateway, bridge, state mirror, package move, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: one more cross-initialization host-field risk is guarded alongside the existing route/status/document-picker checks, preventing a future regression where a not-yet-created `nowPlayingStateController` is captured or invoked across the Java/Kotlin gateway boundary.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus focused UTF-8/mojibake checks for the changed contract and added handoff lines. Device smoke remains out of scope because this is a contract/docs-only app-shell wiring guard and does not touch runtime playback, notification, lyrics, shutdown, foreground behavior, or background playback.

## NOTE 205 - Playback state snapshot owner receives queue snapshot directly (2026-07-04)
- Queue/state dependency shrink slice: `PlaybackStateSnapshotOwner` now accepts a `Supplier<PlaybackQueueManager.QueueStateSnapshot>` instead of a `Supplier<PlaybackQueueManager>`. `EchoPlaybackService.onCreate()` wires the already-initialized `playbackQueueManager::queueStateSnapshot` into the snapshot owner, so the owner no longer knows how to fetch a queue manager just to build the public `PlaybackStateSnapshot`.
- Source-of-truth status is unchanged: queue membership, current index, current track, and queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackStateSnapshotOwner` still only maps that source state plus player/runtime/visualization inputs into `PlaybackStateSnapshot`. No owner, facade, bridge, state mirror, queue provider, new queue manager public API, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one manager supplier chain and one owner-local `playbackQueueManager()` helper were removed from `PlaybackStateSnapshotOwner`; `PlaybackStateSnapshotOwnerTest` no longer constructs a full `PlaybackQueueManager`, fake queue store, transition manager, queue actions, or mirrored player just to test snapshot mapping.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1393 lines; strict `private final Playback*` declarations remain 11; app playback `Playback*Owner.java` files remain 40; production `fromPlaybackQueueManager(...)` mentions remain 0; direct production `playbackQueueManager.queueStateSnapshot()` mentions are now 3; `playbackQueueManager::queueStateSnapshot` appears once as explicit Service wiring.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackStateSnapshotOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus app compile and focused UTF-8/mojibake checks for the changed files and added handoff lines. Device smoke remains out of scope because this only changes state-snapshot owner input wiring and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 206 - Queue command owner drops manager compatibility constructor (2026-07-04)
- Queue/interface narrowing slice: `PlaybackQueueCommandOwner` no longer has the historical `PlaybackQueueCommandOwner(PlaybackQueueManager, ...)` constructor that built its own `PlaybackQueueStateOwner`. The only production construction path now passes the already-created `PlaybackQueueStateOwner` from `EchoPlaybackService`.
- Source-of-truth status is unchanged: current-track availability for queue commands still comes from `PlaybackQueueManager.QueueStateSnapshot` through `PlaybackQueueStateOwner`; `PlaybackQueueCommandOwner` remains only the queue playback action adapter for prepare/publish. No owner, facade, bridge, state mirror, queue provider, new queue manager API, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one hidden manager-to-state-owner forwarding constructor and one possible future call path were removed. `MainActivityArchitectureContractTest` now rejects reintroducing that constructor or rebuilding `PlaybackQueueStateOwner` inside `PlaybackQueueCommandOwner`, keeping the queue command owner on the explicit state-owner boundary.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus app compile and focused UTF-8/mojibake checks for the changed files and added handoff lines. Device smoke remains out of scope because this only narrows a constructor path and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 207 - Queue state owner construction is supplier-only (2026-07-04)
- Queue/interface narrowing slice: `PlaybackQueueStateOwner` no longer exposes the direct `PlaybackQueueStateOwner(PlaybackQueueManager)` compatibility constructor. Callers either use the empty owner for missing queue state or pass `Supplier<PlaybackQueueManager>` explicitly, matching the production `EchoPlaybackService` late-bound wiring.
- Source-of-truth status is unchanged: current track, current index, and queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackQueueStateOwner` still only derives `currentTrack`, `isQueueEmpty`, `hasMultipleTracks`, and `isAtEndOfQueue` from that source. No owner, facade, bridge, state mirror, queue provider, queue manager API, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one public-in-package constructor/API path and one implicit manager-to-state-owner forwarding shortcut were removed. Focused tests now use the same supplier-shaped boundary as production or the empty-owner missing-state path, and `MainActivityArchitectureContractTest` rejects reintroducing the direct manager constructor.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1393 lines; strict `private final Playback*` declarations remain 11; app playback `Playback*Owner.java` files remain 40; production `fromPlaybackQueueManager(...)` mentions remain 0; `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` mentions remain 0.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.playback.PlaybackQueueCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus app compile and focused UTF-8/mojibake checks for the changed files and added handoff lines. Device smoke remains out of scope because this only narrows a constructor path and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 208 - Playback lifecycle callbacks are guarded until playback controllers exist (2026-07-04)
- App-shell initialization safety slice: `initializePlaybackLifecycleControllers()` still runs before `initializePlaybackControllers()`, but the callbacks it passes to `MainPlaybackStateEventListener` and `MainPlaybackServiceHost` now go through `playPendingTracksIfReady()`, `renderNowBarIfReady()`, `preResolveNextStreamingTrackIfReady(...)`, and `recoverStreamingBufferingIfReady(...)` instead of directly dereferencing `playbackStartController`, `nowPlayingStateController`, or `streamingPlaybackController`.
- Source-of-truth status is unchanged: playback service attachment still belongs to `PlaybackServiceConnectionController` plus `PlaybackServiceHostController`; pending playback still belongs to `PlaybackStartController`; now-bar rendering still belongs to `NowPlayingStateController`; streaming pre-resolve/recovery still belongs to `StreamingPlaybackController`. No owner, facade, bridge, state mirror, queue provider, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one app-shell initialization-order crash class is removed. A very early service connection or immediate playback-state callback can no longer invoke playback-controller methods before those controllers are created. `MainActivityArchitectureContractTest` now requires guarded callbacks for this lifecycle-before-controllers ordering and rejects direct early lambdas.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus app compile, focused UTF-8/mojibake checks for the changed Java contract and added handoff lines, and a MuMu startup smoke after installing `app-debug.apk`. Deeper notification, lyrics, playback shutdown, foreground service, and background playback smoke remain out of scope for this slice.
## NOTE 209 - Playback state callbacks wait for stores and render owners (2026-07-04)
- App-shell lifecycle safety slice: playback-state callbacks now save playback settings through `savePlaybackSettingsIfReady(...)`, render selected tabs through `renderSelectedTabIfReady()`, and `loadCollections()` itself returns until `libraryStore` exists. Current streaming queue resolution also returns false until `streamingPlaybackController` exists.
- Source-of-truth status is unchanged: playback speed/app volume still belong to `MainSettingsStore`; collection state still belongs to `MainLibraryStore`; selected-tab rendering still belongs to existing render controllers; streaming placeholder resolution still belongs to `StreamingPlaybackController`. No owner, facade, bridge, state mirror, queue provider, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one more app-shell initialization-order crash class is removed. A playback-state event arriving before stores/render owners finish binding can no longer dereference `libraryStore`, `nowPlayingStateController`, `tabRenderDispatcher`, `networkRenderCoordinator`, `settingsContextProvider`, or `streamingPlaybackController` through lifecycle callbacks.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus app compile and focused UTF-8/mojibake checks for the changed Java contract and added handoff lines. Device smoke remains out of scope because this is an app-shell lifecycle guard and does not change notification actions, lyrics runtime behavior, playback shutdown, foreground service behavior, or background playback semantics.
## NOTE 210 - Library gateway callbacks use ready host helpers (2026-07-04)
- App-shell wiring safety slice: `initializeLibraryGateway()` now passes `renderNowBarIfReady`, `renderSelectedTabIfReady`, and `openAudioFilePickerIfReady` into `MainLibraryGateway` instead of callbacks that directly dereference `nowPlayingStateController`, selected-tab rendering, or `documentPickerController`.
- Source-of-truth status is unchanged: route actions still belong to `MainRouteController`; now-bar rendering still belongs to `NowPlayingStateController`; audio import still belongs to `DocumentPickerController`; library state still belongs to `LibraryViewModel` plus `MainLibraryStore`. No owner, facade, bridge, state mirror, queue provider, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one more app-shell initialization-order dependency is removed. A library gateway event can no longer dereference the now-bar renderer or document picker before those host fields finish binding, and selected-tab rendering now shares the same readiness gate as playback-state callbacks.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus app compile and focused UTF-8/mojibake checks for the changed Java contract and added handoff lines.
## NOTE 211 - Playback render listeners use ready host helpers (2026-07-04)
- App-shell wiring safety slice: `initializePlaybackControllers()` now passes ready helpers into home and queue render listeners for now-bar rendering, selected-tab rendering, add-to-playlist, queue-track removal, and queue clear confirmation. The queue-empty text callback also tolerates `settingsStore` not being bound yet.
- Source-of-truth status is unchanged: now-bar rendering still belongs to `NowPlayingStateController`; playlist dialog behavior still belongs to `PlaylistDialogController`; queue actions still belong to `QueueActionController`; route rendering still belongs to `MainRouteController` plus the existing render controllers; queue state still belongs to playback/queue owners. No owner, facade, bridge, state mirror, queue provider, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one more app-shell initialization-order dependency is removed. Home and queue render listeners can no longer dereference `nowPlayingStateController`, `playlistDialogController`, or `queueActionController` before those host fields finish binding, and selected-tab rendering uses the same readiness gate as the earlier lifecycle/library gateway slices.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus app compile and focused UTF-8/mojibake checks for the changed Java contract and added handoff lines.
## NOTE 212 - Precache current-track read uses queue state owner (2026-07-04)
- Queue/state dependency shrink slice: `PlaybackPrecacheManager.currentTrack()` now reads through the existing `PlaybackQueueStateOwner.currentTrack()` boundary instead of directly calling `playbackQueueManager.queueStateSnapshot().getCurrentTrack()`.
- Source-of-truth status is unchanged: queue membership, current index, current track, and queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackQueueStateOwner` owns the Java-facing derived current-track read; `PlaybackPrecacheManager` still owns cache/precache policy and still calls `PlaybackQueueManager.upcomingTracksForPrecache(...)` for the queue-owned prefetch list. No owner, facade, bridge, state mirror, queue provider, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: the last direct production `playbackQueueManager.queueStateSnapshot().getCurrentTrack()` read outside the queue-state owner is removed, and `MainActivityArchitectureContractTest` now rejects reintroducing that direct precache current-track read while preserving the direct queue-manager call for upcoming-track precache selection.
- Current metrics after the slice: `EchoPlaybackService.java` remains 1393 lines; final-inclusive `private Playback*` declarations remain 51; final-inclusive `private Playback*Owner` declarations remain 21; production `fromPlaybackQueueManager(...)` calls remain 0; Service direct `queueStateSnapshot()` mentions remain 0; app playback `Playback*Owner.java` files remain 40.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus app compile and focused UTF-8/mojibake checks for the changed Java contract and added handoff lines. Device smoke remains out of scope because this only changes an internal queue-state read path and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 213 - MuMu startup smoke baseline is available (2026-07-04)
- Runtime smoke checkpoint: MuMu `vmindex 3` (`MuMu安卓设备-MUSIC`) was launched through `E:\QQ\MuMuPlayer\nx_main\mumu-cli.exe`, Android reached `is_android_started=true`, and ADB connected at `127.0.0.1:16480` on Android 12 / `ALN-AL00`.
- Startup result: `app-debug.apk` installed successfully, `adb shell monkey -p app.yukine 1` launched `app.yukine/.MainActivity`, `dumpsys activity` reported it as `topResumedActivity`, and `pidof app.yukine` returned a live process. A narrow logcat scan for `FATAL EXCEPTION`, `ANR in app.yukine`, `Process: app.yukine`, and common Java runtime exceptions returned no hits after launch.
- Visual baseline: a temporary screenshot at `%TEMP%\yukine-mumu-startup-smoke.png` showed the home screen rendered with readable Chinese copy, now-playing controls, bottom navigation, and no blank first viewport. This is startup smoke only; notification controls, lyrics, shutdown, foreground/background playback, and playback recovery still need separate T3 smoke before high-risk P4/P5 runtime slices.
## NOTE 214 - Collections download label mojibake is fixed (2026-07-04)
- UI UTF-8 safety slice: `CollectionsScreen.kt` restored the download mini-action label from mojibake to `下载`, and `MainActivityArchitectureContractTest.uiCommonChineseCopyRemainsUtf8Readable` now covers the Collections screen plus this common label.
- Source-of-truth status is unchanged: no playback queue state, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, owner, facade, bridge, or package move changed in this slice.
- Concrete win: one user-visible corrupted Chinese label is fixed, and the app-shell UTF-8 contract now rejects this class of regression in the migrated UI-common Collections surface instead of relying on manual review.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus `:feature:ui-common:compileDebugKotlin :app:compileDebugKotlin :app:compileDebugJavaWithJavac`, focused mojibake scans for changed source/test files, added-line mojibake scan for this handoff entry, and `git diff --check`.
## NOTE 215 - Precache reuses the service queue state owner (2026-07-04)
- Queue/state wiring shrink slice: `PlaybackPrecacheManager` now receives the already-created `PlaybackQueueStateOwner` from `EchoPlaybackService` instead of constructing `new PlaybackQueueStateOwner(() -> this.playbackQueueManager)` internally. Upcoming-track selection still uses `PlaybackQueueManager.upcomingTracksForPrecache(...)` because queue membership and queue order remain queue-manager owned.
- Source-of-truth status is unchanged: queue membership, current index, and current track remain owned by `PlaybackQueueManager.QueueStateSnapshot`; Java-facing derived current-track reads stay on `PlaybackQueueStateOwner`; cache policy, segmented precache, cache-key checks, and failure cleanup remain in `PlaybackPrecacheManager`. No owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one internal queue-manager supplier wrapper in `PlaybackPrecacheManager` is removed, and `MainActivityArchitectureContractTest` now requires precache wiring to pass `playbackQueueStateOwner` from Service while rejecting the old internal `new PlaybackQueueStateOwner(() -> this.playbackQueueManager)` path.
- Current metrics after the slice: `EchoPlaybackService.java` is 1394 lines; final-inclusive `private Playback*` declarations remain 51; final-inclusive `private Playback*Owner` declarations remain 21; production `fromPlaybackQueueManager(...)` calls remain 0; app playback `Playback*Owner.java` files remain 40.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus app compile and focused UTF-8/mojibake checks for the changed files and added handoff lines. Device smoke remains out of scope because this only changes precache current-track state wiring and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 216 - Queue state owner consumes snapshots, not the queue manager (2026-07-04)
- Queue/state interface narrowing slice: `PlaybackQueueStateOwner` now accepts `Supplier<PlaybackQueueManager.QueueStateSnapshot>` instead of `Supplier<PlaybackQueueManager>`. Service keeps a single null-safe wiring supplier for the late-bound queue manager; tests use `queueManager::queueStateSnapshot` where a queue manager is already local.
- Source-of-truth status is unchanged: queue membership, current index, current track, and queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackQueueStateOwner` still only exposes Java-facing derived reads (`currentTrack`, empty, multiple, and end-of-queue). No state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: `PlaybackQueueStateOwner` no longer has a queue-manager supplier field or private queue-manager helper, so it cannot grow into another manager facade. `MainActivityArchitectureContractTest` now requires the snapshot-supplier boundary and rejects reintroducing `Supplier<PlaybackQueueManager>` inside the state owner.
- Current metrics after the slice: `EchoPlaybackService.java` is 1396 lines; final-inclusive `private Playback*` declarations remain 51; final-inclusive `private Playback*Owner` declarations remain 21; production `fromPlaybackQueueManager(...)` calls remain 0; app playback `Playback*Owner.java` files remain 40.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus app compile and focused UTF-8/mojibake checks for the changed files and added handoff lines. Device smoke remains out of scope because this only narrows the queue-state owner input boundary and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 217 - Queue completion owner drops forwarding boundary interface (2026-07-04)
- Queue/completion wiring shrink slice: `PlaybackQueueCompletionOwner.CompletionBoundary` was removed. `EchoPlaybackService.playbackQueueCompletionOwner()` now passes the three concrete completion callbacks (`stopAndClear`, `stopAtEndOfQueue`, `skipToNext`) as direct method references instead of constructing a three-method anonymous boundary object.
- Source-of-truth status is unchanged: queue membership, current index, current track, queue size, and completion preparation still belong to `PlaybackQueueManager` / `PlaybackQueueManager.QueueStateSnapshot`; completion action routing remains owned by `PlaybackQueueCompletionOwner`. No state mirror, queue provider, resolver/cache boundary, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, owner, facade, bridge, or package move was added.
- Concrete win: one pure forwarding boundary/interface and one Service anonymous wiring block were removed without adding a new owner or Service field. `MainActivityArchitectureContractTest` now rejects `CompletionBoundary` reintroduction and requires the direct method-reference wiring.
- Current metrics after the slice: `EchoPlaybackService.java` is 1383 lines; final-inclusive `private Playback*` declarations remain 51; final-inclusive `private Playback*Owner` declarations remain 21; app playback `Playback*Owner.java` files remain 40; production/test `CompletionBoundary` hits are 0.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCompletionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus app compile and focused UTF-8/mojibake checks for the changed files and added handoff lines. Device smoke remains out of scope because this only removes a queue-completion forwarding interface and does not touch notification controls, lyrics runtime behavior, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 218 - Library gateway add-to-playlist uses ready host helper (2026-07-04)
- App-shell wiring shrink slice: `initializeLibraryGateway()` now passes `this::showAddToPlaylistIfReady` into `MainLibraryGateway` instead of an inline lambda that directly captures and dereferences `playlistDialogController` during early gateway binding.
- Source-of-truth status is unchanged: add-to-playlist presentation remains owned by `PlaylistDialogController`, library UI actions still enter `LibraryViewModel` / `MainLibraryGateway`, and route state remains owned by `NavigationViewModel` plus `MainRouteController`. No state mirror, owner, facade, gateway, bridge, package move, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: one early app-shell callback no longer embeds direct host-field access in `initializeLibraryGateway()`. `MainActivityArchitectureContractTest` now requires the existing ready helper method reference and rejects reintroducing direct `playlistDialogController` access in the gateway binding block.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus app compile and focused UTF-8/mojibake checks for the changed files and added handoff lines. Device smoke remains out of scope because this only changes app-shell callback wiring and does not touch runtime playback, notification controls, lyrics, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 219 - Streaming gateway late-controller callbacks use ready host helpers (2026-07-04)
- App-shell wiring shrink slice: `createStreamingActionGateway()` now passes `this::notifyStreamingLoginSuccessIfReady` and `this::showStreamingCookieDialogIfReady` instead of inline lambdas that directly capture `streamingPlaylistController` and `streamingManualCookieController` before `initializeStreamingOwners(...)` creates those controllers.
- Source-of-truth status is unchanged: streaming provider selection still lives in `StreamingViewModel`, playlist-login delivery remains a late callback into `StreamingPlaylistController`, and manual-cookie presentation remains owned by `StreamingManualCookieController`. No state mirror, owner, facade, gateway, bridge, package move, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: two early streaming gateway callbacks no longer embed direct host-field access in the gateway construction block. `MainActivityArchitectureContractTest` now requires the ready helper method references and rejects reintroducing direct controller access in `createStreamingActionGateway()`.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus app compile and focused UTF-8/mojibake checks for the changed files and added handoff lines. Device smoke remains out of scope because this only changes app-shell callback wiring and does not touch runtime playback, notification controls, lyrics, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 220 - Precache audio cache release belongs to cache operations (2026-07-04)
- Cache/precache wiring shrink slice: `PlaybackMediaCacheOperations` now owns `releaseAudioCache()`, and `PlaybackPrecacheManager` no longer keeps a separate `Runnable audioCacheReleaseAction` constructor parameter, field, or provider release lambda.
- Source-of-truth status is unchanged: URI/MediaItem resolution remains in `PlaybackMediaSourceProvider`; cache key, header, range, data-source, and release lifecycle remain behind `PlaybackMediaCacheOperations`; precache scheduling and policy remain in `PlaybackPrecacheManager`; queue state remains `PlaybackQueueManager.QueueStateSnapshot` plus `PlaybackQueueStateOwner`. No state mirror, owner, facade, bridge, package move, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: one `PlaybackPrecacheManager` constructor parameter, one field, and one provider release lambda forwarding chain were removed while keeping provider release out of `EchoPlaybackService`.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest :feature:playback:testDebugUnitTest --tests app.yukine.playback.manager.PlaybackMediaCacheOperationsTest --tests app.yukine.playback.PlaybackVisualizationCacheManagerTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only changes internal cache/precache release wiring and does not touch runtime playback, notification controls, lyrics, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 221 - Document picker Luoxue import callback uses ready host helper (2026-07-04)
- App-shell wiring safety slice: `initializePlatformControllers()` now passes `this::importSelectedLuoxueSourceUrisIfReady` into `MainDocumentPickerListenerFactory` instead of a lambda that directly dereferences `luoxueSourceImportController` before the controller is created later in the same initialization method.
- Source-of-truth status is unchanged: document picking remains owned by `DocumentPickerController` plus `MainDocumentPickerListener`; Luoxue source parsing/import state remains owned by `LuoxueSourceImportController`; streaming provider refresh remains the existing completion callback. No state mirror, owner, facade, gateway, bridge, package move, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: one early app-shell listener callback no longer embeds direct host-field access to a later-created controller. `MainActivityArchitectureContractTest` now requires the ready helper method reference and rejects the old `uris -> luoxueSourceImportController.importSelectedUris(uris)` callback.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only changes app-shell callback wiring and does not touch runtime playback, notification controls, lyrics, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 222 - Track list stream actions use ready dialog helpers (2026-07-04)
- App-shell wiring safety slice: `initializeNavigationRendering()` now passes `this::showEditStreamIfReady` and `this::confirmDeleteTrackIfReady` into `TrackListRenderController` instead of lambdas that directly dereference `networkDialogController` and `confirmationDialogController` before those dialog owners are created later in startup.
- Source-of-truth status is unchanged: track-list row rendering remains owned by `TrackListRenderController`; stream edit dialogs remain owned by `NetworkDialogController`; delete confirmation remains owned by `ConfirmationDialogController`; delete execution remains in `NetworkRequestController`. No state mirror, owner, facade, gateway, bridge, package move, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: two early app-shell listener callbacks no longer embed direct host-field access to later-created dialog owners. `MainActivityArchitectureContractTest` now requires the ready helper method references and rejects the old direct `track -> networkDialogController.showEditStream(track)` / `track -> confirmationDialogController.confirmDeleteTrack(track)` callbacks.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only changes app-shell callback wiring and does not touch runtime playback, notification controls, lyrics, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 223 - Recommendation playback callbacks use ready playback-start helper (2026-07-04)
- App-shell wiring safety slice: `initializePlaybackControllers()` now passes `this::playRecommendationIfReady` and `this::playHeartbeatRecommendationIfReady` into recommendation callbacks instead of lambdas that directly dereference `playbackStartController` before that controller is created later in the same initialization method.
- Source-of-truth status is unchanged: recommendation state and presentations remain owned by `StreamingRecommendationViewModel` plus `HeartbeatRecommendationController`; playback startup remains owned by `PlaybackStartController`; pending playback still remains in `MainPlaybackStartListener`. No state mirror, owner, facade, gateway, bridge, package move, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: three early recommendation callbacks no longer embed direct host-field access to a later-created playback-start owner. `MainActivityArchitectureContractTest` now requires the ready helper method references and rejects the old direct `presentation -> playbackStartController.playRecommendation(...)` / `playHeartbeatRecommendation(...)` callbacks.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only changes app-shell callback wiring and does not touch runtime playback, notification controls, lyrics, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 224 - Home dashboard all-tracks source uses ready library-store helper (2026-07-04)
- App-shell wiring safety slice: `initializePlaybackControllers()` now passes `this::allLibraryTracksIfReady` into the home dashboard render listener instead of a lambda that directly dereferences `libraryStore` before `initializeStoresAndDataGateways(...)` creates that store later in startup.
- Source-of-truth status is unchanged: home dashboard shuffle-all still reads library tracks from `MainLibraryStore`, whose state comes from `MainActivityViewModel.library`. No state mirror, owner, facade, gateway, bridge, package move, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: one more late host-field dereference is removed from early playback-controller wiring. `MainActivityArchitectureContractTest` now requires the null-safe all-tracks helper and rejects reintroducing `() -> libraryStore.allTracks()` in the home dashboard listener binding.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed source/test files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only changes app-shell callback wiring and does not touch runtime playback, notification controls, lyrics, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 225 - Queue clear confirmation uses ready dialog helper (2026-07-04)
- App-shell wiring safety slice: `initializePlaybackControllers()` now passes `this::showClearQueueConfirmationIfReady` into `MainQueueActionListener` instead of a lambda that directly dereferences `confirmationDialogController` before later startup creates the dialog owner. This is intentionally separate from `confirmClearQueueIfReady`, which is the UI-to-`QueueActionController` command entry and would recurse if reused as the action listener's dialog confirmer.
- Source-of-truth status is unchanged: queue clear command validation remains in `QueueActionController` plus `NowPlayingViewModel`, confirmation UI remains owned by `ConfirmationDialogController`, and actual clear execution remains the existing confirmation listener path. No state mirror, owner, facade, gateway, bridge, package move, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: one more early playback-controller callback no longer embeds direct host-field access to a later-created dialog owner. `MainActivityArchitectureContractTest` now requires the ready dialog helper and rejects reintroducing `() -> confirmationDialogController.confirmClearQueue()` in the queue action listener binding.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed source/test files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only changes app-shell callback wiring and does not touch runtime playback, notification controls, lyrics, shutdown, service lifecycle, foreground behavior, or background playback.
## NOTE 226 - Notification state reads queue snapshot directly (2026-07-04)
- Queue/state interface narrowing slice: `PlaybackNotificationStateOwner` now receives the shared `Supplier<PlaybackQueueManager.QueueStateSnapshot>` from `EchoPlaybackService` instead of consuming `PlaybackQueueStateOwner` and its derived `currentTrack()` / `isQueueEmpty()` helpers.
- Source-of-truth status is unchanged: queue membership, current track, current index, and queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; notification state still only exposes `PlaybackNotificationManager.StateProvider` values. No state mirror, owner, facade, bridge, resolver path, cache policy path, notification action mapping change, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one `PlaybackQueueStateOwner` consumer and constructor dependency is removed from the notification path, making notification state read the same queue snapshot boundary as other narrowed playback state owners instead of encouraging `PlaybackQueueStateOwner` to grow into a derived-read facade. `PlaybackNotificationStateOwnerTest` and `MainActivityArchitectureContractTest` now require the snapshot-supplier boundary and reject the old `PlaybackQueueStateOwner` dependency.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackNotificationStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only changes notification state queue-read wiring and does not touch notification action mapping, foreground/background behavior, lyrics runtime behavior, shutdown, or service lifecycle.
## NOTE 227 - Playlist dialog owner is ready before render listener binding (2026-07-04)
- App-shell initialization safety slice: `initializeStoresAndDataGateways(...)` now creates `playlistDialogController` immediately after `libraryStore` and `settingsStore` are ready, before `initializeRenderOwners(...)` binds `LibraryPlaylistsRenderController` and `CollectionsRenderController` listeners that can later call playlist dialog actions.
- Source-of-truth status is unchanged: playlist mutation state remains owned by `LibraryViewModel` plus `LibraryPlaylistActionGateway`, playlist dialog presentation remains owned by `PlaylistDialogController`, and collection/library rendering remains owned by the existing render controllers. No owner, facade, gateway, bridge, state mirror, package move, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: one app-shell initialization-order implicit dependency is removed without adding a wrapper. `MainActivityArchitectureContractTest` now requires `libraryStore` and `settingsStore` setup before playlist dialog creation, and requires playlist dialog creation before playlists/collections render owner binding.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only changes app-shell object creation order before render listener binding and does not touch playback runtime, notification controls, lyrics, shutdown, foreground behavior, or background playback.
## NOTE 228 - Playback snapshot owner reuses the service queue snapshot supplier (2026-07-04)
- Queue/state wiring shrink slice: `EchoPlaybackService` now passes the existing `queueStateSnapshotSupplier` to `PlaybackStateSnapshotOwner` instead of wiring a second `playbackQueueManager::queueStateSnapshot` method reference.
- Source-of-truth status is unchanged: queue membership, current track, current index, and queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackStateSnapshotOwner` still only maps queue/player/runtime/visualization inputs into `PlaybackStateSnapshot`. No state mirror, owner, facade, bridge, queue provider, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one duplicate queue snapshot supplier chain is removed from `EchoPlaybackService` wiring. `MainActivityArchitectureContractTest` now requires both notification state and playback snapshot state to share `queueStateSnapshotSupplier`, and rejects reintroducing the direct `playbackQueueManager::queueStateSnapshot` snapshot-owner wiring.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackStateSnapshotOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only changes Service snapshot-owner wiring and does not touch playback runtime commands, notification controls, lyrics, shutdown, foreground behavior, or background playback.
## NOTE 229 - Playback queue-state wiring audit after supplier reuse (2026-07-04)
- Read-only owner/interface audit after the notification-state and playback-snapshot queue-supplier slices: `EchoPlaybackService.java` is 1291 lines; strict `private Playback*` declarations are 51; strict `private Playback*Owner` declarations are 21; app playback `Playback*Owner.java` files remain 40; production `fromPlaybackQueueManager(...)`, `PlaybackQueueManager.QueueProvider`, `PlaybackMediaSourceResolutionOwner`, and `PlaybackItemResolver` hits are 0.
- Queue source-of-truth status is unchanged: queue membership, current track, current index, and queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; `EchoPlaybackService` now has one direct `playbackQueueManager.queueStateSnapshot()` mention inside the shared `queueStateSnapshotSupplier` and 0 `playbackQueueManager::queueStateSnapshot` method references.
- Audit decision: the remaining two `playbackQueueStateOwner::currentTrack` suppliers stay for now because they feed the narrower `Supplier<Track>` boundaries for `PlaybackRuntimeStateManager` replay-gain state and `PlaybackPositionManager` position persistence. Broadening those feature-manager inputs to `QueueStateSnapshot` only to reduce a count would make the dependency less precise. `MainActivityArchitectureContractTest` now whitelists exactly those two uses and rejects new Service current-track supplier expansion.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this audit/contract slice changes no runtime behavior.
## NOTE 230 - Playback service host settings suppliers tolerate early connection (2026-07-04)
- App-shell initialization safety slice: `initializePlaybackLifecycleControllers()` now passes `this::playbackSpeedIfReady`, `this::appVolumeIfReady`, `this::concurrentPlaybackEnabledIfReady`, `this::statusBarLyricsEnabledIfReady`, `this::playbackRestoreEnabledIfReady`, and `this::replayGainEnabledIfReady` into `MainPlaybackServiceHost` instead of lambdas that directly dereference `settingsStore`.
- Source-of-truth status is unchanged: persisted playback speed, app volume, concurrent playback, status-bar lyrics, playback restore, and replay-gain settings remain owned by `MainSettingsStore` after it is loaded. The ready helpers only provide the same defaults as `MainSettingsStore` while the store is not initialized yet; no new state mirror, owner, facade, bridge, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: six early playback service host supplier dereferences can no longer crash if `PlaybackServiceConnectionController` receives a service connection before `initializeStoresAndDataGateways(...)` creates and loads `settingsStore`. `MainActivityArchitectureContractTest` now requires the ready helpers and rejects reintroducing direct `() -> settingsStore.*` playback host suppliers.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --tests app.yukine.MainPlaybackServiceHostTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only changes app-shell startup guard defaults and does not alter notification controls, lyrics runtime behavior, shutdown, foreground/background playback, or service action mapping.
## NOTE 231 - Streaming owner startup callbacks use ready host readers (2026-07-04)
- App-shell initialization safety slice: `initializeStreamingOwners(...)` now passes `this::languageModeIfReady`, `this::selectedPlaylistId`, `this::selectedPlaylistTracksIfReady`, and `this::favoriteTracksIfReady` into streaming playlist/manual-cookie controllers instead of callbacks that directly dereference `settingsStore.languageMode()`, `libraryStore.selectedPlaylistTracks()`, or `libraryStore.favoriteTracks()` before `initializeStoresAndDataGateways(...)` creates and loads those stores.
- Source-of-truth status is unchanged: language remains owned by `MainSettingsStore`; selected playlist id remains owned by `MainRouteController` / `NavigationViewModel`; selected playlist tracks and favorites remain owned by `MainLibraryStore`. The ready helpers only provide safe empty/default reads before the real stores are initialized; no new state mirror, owner, facade, gateway, bridge, package move, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: streaming playlist import, manual cookie, and account playlist callbacks can no longer crash on an early callback path because the store fields are not initialized yet. `MainActivityArchitectureContractTest` now requires the ready helpers and rejects reintroducing the direct early `settingsStore.languageMode()` and `libraryStore.*Tracks()` suppliers.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only changes app-shell startup guard defaults and does not alter streaming provider behavior, playback runtime, notification controls, lyrics, shutdown, foreground/background playback, or service action mapping.
## NOTE 232 - Settings lyrics reload effect uses ready language reader (2026-07-04)
- App-shell initialization safety slice: `initializeSettingsEffects()` now calls `lyricsViewModel.reloadCurrentLyrics(languageModeIfReady())` instead of directly dereferencing `settingsStore.languageMode()` from an effect listener that is bound before `initializeStoresAndDataGateways(...)` loads `settingsStore`.
- Source-of-truth status is unchanged: language remains owned by `MainSettingsStore` after load, current lyrics track remains supplied by the existing `LyricsViewModel.bindReloadGateway(...)` current-track provider, and lyrics loading remains owned by `LyricsViewModel`. The ready helper only supplies `AppLanguage.MODE_SYSTEM` before settings are initialized; no new state mirror, owner, facade, gateway, bridge, package move, playback resolver path, cache policy path, notification behavior, lyrics runtime behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: one early settings effect path can no longer crash if `ReloadCurrentLyrics` is emitted before `settingsStore` is ready. `MainActivityArchitectureContractTest` now requires the ready language reader and rejects the direct `lyricsViewModel.reloadCurrentLyrics(settingsStore.languageMode())` call.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only changes app-shell startup guard language selection and does not alter lyrics loading policy, playback runtime, notification controls, shutdown, foreground/background playback, or service action mapping.
## NOTE 233 - Heartbeat recommendation controller uses ready language reader (2026-07-04)
- App-shell initialization safety slice: `initializePlaybackControllers()` now passes `this::languageModeIfReady` into `HeartbeatRecommendationController` instead of a direct `() -> settingsStore.languageMode()` callback while the controller is created before `initializeStoresAndDataGateways(...)` loads `settingsStore`.
- Source-of-truth status is unchanged: language remains owned by `MainSettingsStore` after load, and heartbeat recommendation state/presentations remain owned by `StreamingRecommendationViewModel` plus `HeartbeatRecommendationController`. The ready helper only supplies `AppLanguage.MODE_SYSTEM` before settings are initialized; no new state mirror, owner, facade, gateway, bridge, package move, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: one more early playback-controller callback no longer embeds a direct dereference of a later-loaded host settings store. `MainActivityArchitectureContractTest` now requires the ready language method reference in the heartbeat controller binding and rejects reintroducing the direct `settingsStore.languageMode()` callback.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only changes app-shell startup guard language selection and does not alter recommendation behavior, playback runtime, notification controls, lyrics, shutdown, foreground/background playback, or service action mapping.
## NOTE 234 - Background image picker apply callback uses ready settings helper (2026-07-04)
- App-shell initialization safety slice: `initializePlatformControllers()` now passes `this::applyPageBackgroundIfReady` into `MainBackgroundImagePickerListener` instead of a lambda that directly reads `settingsStore.pageBackgrounds()` while the picker controller is created before `initializeStoresAndDataGateways(...)` loads `settingsStore`.
- Source-of-truth status is unchanged: page background preferences remain owned by `MainSettingsStore` plus `SettingsViewModel.applyPageBackgrounds(...)` and the settings repository persistence path. The helper only skips the impossible early write before settings are initialized; no new state mirror, owner, facade, gateway, bridge, package move, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: one early platform-controller callback no longer embeds a direct dereference of a later-loaded host settings store. `MainActivityArchitectureContractTest` now requires the ready helper in background image picker binding and rejects reintroducing the direct `settingsStore.pageBackgrounds()` callback.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --tests app.yukine.BackgroundImagePickerControllerTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only changes app-shell callback wiring and does not alter runtime playback, notification controls, lyrics, shutdown, foreground/background playback, or service action mapping.
## NOTE 235 - Precache current-track reads queue manager directly (2026-07-04)
- Queue/cache boundary narrowing slice: `PlaybackPrecacheManager` now reads current track from its existing `PlaybackQueueManager.queueStateSnapshot()` dependency instead of holding a separate `PlaybackQueueStateOwner` only for `currentTrack()`. `EchoPlaybackService` no longer passes `playbackQueueStateOwner` into the precache manager.
- Source-of-truth status is unchanged: queue membership/current track/current index/queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; cache key, precache range planning, failure cleanup, and partial/segmented cache policy remain in `PlaybackPrecacheManager` plus `PlaybackMediaCacheOperations`. No new state mirror, owner, facade, bridge, resolver path, cache policy owner, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one `PlaybackPrecacheManager` constructor parameter, one field, and one Service wiring argument were removed while keeping URI/MediaItem matching in `PlaybackMediaSourceProvider` and cache policy in `PlaybackPrecacheManager`. `MainActivityArchitectureContractTest` now rejects reintroducing the precache `PlaybackQueueStateOwner` dependency.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only narrows internal precache queue-state wiring and does not alter runtime playback commands, notification controls, lyrics, shutdown, foreground/background playback, or service action mapping.
## NOTE 236 - Crossfade and error recovery read queue snapshots directly (2026-07-04)
- Queue/state interface narrowing slice: `PlaybackCrossfadeStateOwner` and `PlaybackErrorRecoveryCommandOwner` now consume the existing `queueStateSnapshotSupplier` from `EchoPlaybackService` instead of reading `hasMultipleTracks()` / `isAtEndOfQueue()` through `PlaybackQueueStateOwner`. `PlaybackQueueStateOwner` keeps only the remaining current-track and empty-queue derived reads still used by narrower owners.
- Source-of-truth status is unchanged: queue membership/current track/current index/queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; crossfade repeat/end-of-queue policy remains in `PlaybackCrossfadeStateOwner`, and streaming error retry/skip policy remains in `PlaybackErrorRecoveryCommandOwner` plus `PlaybackErrorRecoveryManager`. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: two derived methods, `PlaybackQueueStateOwner.hasMultipleTracks()` and `PlaybackQueueStateOwner.isAtEndOfQueue()`, were removed from the Java queue-state interface while reusing the existing Service queue snapshot supplier. `MainActivityArchitectureContractTest` now rejects reintroducing those methods and requires the crossfade/error-recovery wiring to use `queueStateSnapshotSupplier`.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCrossfadeStateOwnerTest --tests app.yukine.playback.PlaybackErrorRecoveryCommandOwnerTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only narrows internal queue-state owner wiring and does not alter notification controls, lyrics runtime, shutdown, foreground/background playback, or service lifecycle.
## NOTE 237 - Mirrored transition reads queue snapshot directly (2026-07-04)
- Queue/state interface narrowing slice: `PlaybackQueueMirroredTransitionOwner` now receives the existing `queueStateSnapshotSupplier` from `EchoPlaybackService` instead of consuming `PlaybackQueueStateOwner.isQueueEmpty()` and `PlaybackQueueStateOwner.currentTrack()` during MediaItem transition handling. `PlaybackQueueStateOwner` is now reduced to the remaining `currentTrack()` helper used by owners that still intentionally consume a narrow `Track` read.
- Source-of-truth status is unchanged: queue membership/current track/current index/queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; mirrored transition index updates remain owned by `PlaybackQueueManager.applyMirroredTransitionIndex(...)`, and transition side effects remain in `PlaybackQueueMirroredTransitionOwner` plus the existing Service callback. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: `PlaybackQueueStateOwner.isQueueEmpty()` was removed as a migration residue, and one more owner no longer depends on the Java queue-state facade. The current playback owner/interface audit baseline before this slice was `EchoPlaybackService.java` 1382 lines, strict `private Playback*` fields 51, strict `private Playback*Owner` fields 21, production `fromPlaybackQueueManager(...)` hits 0, Service `queueStateSnapshotSupplier` mentions 6, and app playback `Playback*Owner.java` files 40.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMirroredTransitionOwnerTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only narrows internal queue-state owner wiring and does not alter notification controls, lyrics runtime, shutdown, foreground/background playback, or service lifecycle.
## NOTE 238 - Visualization cache state uses the existing current-track supplier (2026-07-04)
- Queue/state wiring shrink slice: `PlaybackVisualizationCacheStateOwner` now takes a narrow `Supplier<Track>` instead of `PlaybackQueueStateOwner`. `EchoPlaybackService` passes the already-local `currentTrackSupplier` derived from `PlaybackNotificationStateOwner`, so no new Service field, facade, bridge, or queue snapshot helper was introduced.
- Source-of-truth status is unchanged: queue membership/current track/current index/queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`, exposed to notification state through `queueStateSnapshotSupplier`; visualization cache policy and cache scheduling remain in `PlaybackVisualizationCacheManager` plus `PlaybackVisualizationCacheStateOwner`. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification action behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: one `PlaybackQueueStateOwner` constructor dependency and its focused-test queue-manager fixture were removed from the visualization-cache state path while reusing an existing local current-track supplier. `MainActivityArchitectureContractTest` now requires this wiring and rejects reintroducing `playbackQueueStateOwner` into `PlaybackVisualizationCacheStateOwner`.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackVisualizationCacheStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only narrows internal visualization-cache queue-state wiring and does not alter notification controls, lyrics runtime, shutdown, foreground/background playback, or service lifecycle.
## NOTE 239 - Queue command owner reads queue snapshots directly (2026-07-04)
- Queue/state wiring shrink slice: `PlaybackQueueCommandOwner` now takes the existing `Supplier<PlaybackQueueManager.QueueStateSnapshot>` instead of `PlaybackQueueStateOwner`. `EchoPlaybackService` passes the already-existing `queueStateSnapshotSupplier`, so no new Service field, facade, bridge, owner, or snapshot mirror was introduced.
- Source-of-truth status is unchanged: queue membership/current track/current index/queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackQueueCommandOwner` only derives the current track from that snapshot for prepare/fallback decisions. No URI/media item resolution, cache policy, notification action behavior, lyrics behavior, shutdown behavior, foreground/background playback behavior, or package move was changed.
- Concrete win: one more playback command owner no longer depends on the Java `PlaybackQueueStateOwner` facade, and `PlaybackQueueCommandOwnerTest` no longer constructs a full `PlaybackQueueManager` only to supply a current track. `MainActivityArchitectureContractTest` now requires `queueStateSnapshotSupplier` wiring and rejects reintroducing `playbackQueueStateOwner` into `PlaybackQueueCommandOwner`.
- Owner/interface audit after this slice: `EchoPlaybackService.java` remains 1382 lines, strict `private Playback*` fields remain 51, strict `private Playback*Owner` fields remain 21, production `fromPlaybackQueueManager(...)` hits remain 0, Service `queueStateSnapshotSupplier` mentions are 8, Service `playbackQueueStateOwner::currentTrack` refs remain 2, and app playback `Playback*Owner.java` files remain 40. The extra snapshot-supplier mentions are the intended replacement for queue-state facade wiring, not new Service state.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only narrows internal queue-command current-track reads and does not alter notification controls, lyrics runtime, shutdown, foreground/background playback, or service lifecycle.
## NOTE 240 - Favorite command owner reads queue snapshots directly (2026-07-04)
- Queue/state wiring shrink slice: `PlaybackFavoriteCommandOwner.toggleCurrentFavorite(...)` now takes the existing `Supplier<PlaybackQueueManager.QueueStateSnapshot>` instead of `PlaybackQueueStateOwner`. `EchoPlaybackService.toggleCurrentFavorite()` passes the already-existing `queueStateSnapshotSupplier`, so no new Service field, facade, bridge, owner, or snapshot mirror was introduced.
- Source-of-truth status is unchanged: queue membership/current track/current index/queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; favorite toggling only derives the current track from that snapshot before delegating to `ToggleFavoriteUseCase`. No URI/media item resolution, cache policy, notification action behavior, lyrics behavior, shutdown behavior, foreground/background playback behavior, or package move was changed.
- Concrete win: another current-track command path no longer depends on the Java `PlaybackQueueStateOwner` facade, and `PlaybackFavoriteCommandOwnerTest` no longer constructs a full `PlaybackQueueManager` only to supply the current track. `MainActivityArchitectureContractTest` now requires `queueStateSnapshotSupplier` wiring and rejects reintroducing `playbackQueueStateOwner` into `PlaybackFavoriteCommandOwner`.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackFavoriteCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only narrows internal favorite-command current-track reads and does not alter notification controls, lyrics runtime, shutdown, foreground/background playback, or service lifecycle.
## NOTE 241 - Play history recorder reads queue snapshots directly (2026-07-04)
- Queue/state wiring shrink slice: `PlaybackPlayHistoryRecorder.recordIfPlaybackStartedAction(...)` now takes the existing `Supplier<PlaybackQueueManager.QueueStateSnapshot>` instead of `PlaybackQueueStateOwner`. `EchoPlaybackService` passes the already-existing `queueStateSnapshotSupplier`, so no new Service field, facade, bridge, owner, or snapshot mirror was introduced.
- Source-of-truth status is unchanged: queue membership/current track/current index/queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; play-history recording only derives the current track from that snapshot before marking history and updating `PlaybackTransitionStateManager.lastMarkedTrack`. No URI/media item resolution, cache policy, notification action behavior, lyrics behavior, shutdown behavior, foreground/background playback behavior, or package move was changed.
- Concrete win: another current-track side-effect path no longer depends on the Java `PlaybackQueueStateOwner` facade, and `PlaybackPlayHistoryRecorderTest` no longer constructs a full `PlaybackQueueManager` only to supply changing current-track state. `MainActivityArchitectureContractTest` now requires `queueStateSnapshotSupplier` wiring and rejects reintroducing `playbackQueueStateOwner` into `PlaybackPlayHistoryRecorder`.
- Owner/interface audit after the favorite + play-history slice pair: `EchoPlaybackService.java` remains 1382 lines, strict `private Playback*` fields remain 51, strict `private Playback*Owner` fields remain 21, production `fromPlaybackQueueManager(...)` hits remain 0, Service `queueStateSnapshotSupplier` mentions are 10, Service `playbackQueueStateOwner::currentTrack` refs remain 2, and app playback `Playback*Owner.java` files remain 40. The added snapshot-supplier mentions replace queue-state facade wiring and do not add Service state.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPlayHistoryRecorderTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only narrows internal play-history current-track reads and does not alter notification controls, lyrics runtime, shutdown, foreground/background playback, or service lifecycle.
## NOTE 242 - Session command owner reads queue snapshots directly (2026-07-04)
- Queue/state wiring shrink slice: `PlaybackSessionCommandOwner` now takes the existing `Supplier<PlaybackQueueManager.QueueStateSnapshot>` instead of `PlaybackQueueStateOwner`. `EchoPlaybackService` passes the already-existing `queueStateSnapshotSupplier`, so no new Service field, facade, bridge, owner, or snapshot mirror was introduced.
- Source-of-truth status is unchanged: queue membership/current track/current index/queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; MediaSession command delegation remains in `PlaybackSessionCommandOwner`, controller media item mapping remains in `PlaybackControllerMediaItemsOwner`, and session lifecycle remains in `PlaybackSessionManager`. No URI/media item resolution, cache policy, notification action behavior, lyrics behavior, shutdown behavior, foreground/background playback behavior, or package move was changed.
- Concrete win: one more MediaSession delegate path no longer depends on the Java `PlaybackQueueStateOwner` facade, and `PlaybackSessionCommandOwnerTest` no longer constructs a full `PlaybackQueueManager` only to supply the current track. `MainActivityArchitectureContractTest` now requires `queueStateSnapshotSupplier` wiring and rejects reintroducing `playbackQueueStateOwner` into `PlaybackSessionCommandOwner`.
- Owner/interface audit after this slice: `EchoPlaybackService.java` remains 1382 lines, strict `private Playback*` fields remain 51, strict `private Playback*Owner` fields remain 21, production `fromPlaybackQueueManager(...)` hits remain 0, Service `queueStateSnapshotSupplier` mentions are 11, Service `playbackQueueStateOwner::currentTrack` refs remain 2, and app playback `Playback*Owner.java` files remain 40. The added snapshot-supplier mention replaces queue-state facade wiring and does not add Service state.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackSessionCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only narrows internal MediaSession current-track reads and does not alter notification controls, lyrics runtime, shutdown, foreground/background playback, or service lifecycle.
## NOTE 243 - Mirrored player owner reads queue snapshots directly (2026-07-04)
- Queue/state wiring shrink slice: `PlaybackQueueMirroredPlayerOwner` now takes the existing `Supplier<PlaybackQueueManager.QueueStateSnapshot>` instead of `PlaybackQueueStateOwner`. `EchoPlaybackService` passes the already-existing `queueStateSnapshotSupplier`, so no new Service field, facade, bridge, owner, or snapshot mirror was introduced.
- Source-of-truth status is unchanged: queue membership/current track/current index/queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; mirrored queue matching still reads a queue list through the existing matcher input, and mirrored queue reuse still belongs to `PlaybackQueueManager.MirroredQueuePlayer` plus `PlaybackQueueMirroredPlayerOwner`. No URI/media item resolution, cache policy, notification action behavior, lyrics behavior, shutdown behavior, foreground/background playback behavior, or package move was changed.
- Concrete win: another mirrored playback path no longer depends on the Java `PlaybackQueueStateOwner` facade, and `PlaybackQueueMirroredPlayerOwnerTest` no longer constructs a full `PlaybackQueueManager`, runtime-state manager, transition manager, and fake queue store only to supply the current track. `MainActivityArchitectureContractTest` now requires `queueStateSnapshotSupplier` wiring and rejects reintroducing `playbackQueueStateOwner` into `PlaybackQueueMirroredPlayerOwner`.
- Owner/interface audit after this slice: `EchoPlaybackService.java` remains 1382 lines, strict `private Playback*` fields remain 51, strict `private Playback*Owner` fields remain 21, production `fromPlaybackQueueManager(...)` hits remain 0, Service `queueStateSnapshotSupplier` mentions are 12, Service `playbackQueueStateOwner::currentTrack` refs remain 2, total Service `playbackQueueStateOwner` mentions are 4, and app playback `Playback*Owner.java` files remain 40. The added snapshot-supplier mention replaces queue-state facade wiring and does not add Service state.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMirroredPlayerOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only narrows internal mirrored-queue current-track reads and does not alter notification controls, lyrics runtime, shutdown, foreground/background playback, or service lifecycle.
## NOTE 244 - Position manager reads queue snapshots directly (2026-07-04)
- Queue/state wiring shrink slice: `PlaybackPositionManager.stateProviderFromPlaybackState(...)` now takes the existing `Supplier<PlaybackQueueManager.QueueStateSnapshot>` instead of a narrow `Supplier<Track?>`. `EchoPlaybackService` passes the already-existing `queueStateSnapshotSupplier`, so no new Service field, facade, bridge, owner, or snapshot mirror was introduced.
- Source-of-truth status is unchanged: queue membership/current track/current index/queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; position persistence and restored-position clamping remain in `PlaybackPositionManager`. No URI/media item resolution, cache policy, notification action behavior, lyrics behavior, shutdown behavior, foreground/background playback behavior, or package move was changed.
- Concrete win: the playback-position persistence path no longer depends on `PlaybackQueueStateOwner.currentTrack()`, and `EchoPlaybackService` now has one fewer `playbackQueueStateOwner::currentTrack` method-reference forwarding chain. `PlaybackPositionManagerTest` now covers snapshot-backed current-track reads, missing snapshots, empty snapshots, and late-bound snapshot changes.
- Owner/interface audit after this slice: `EchoPlaybackService.java` remains 1382 lines, strict `private Playback*` fields remain 51, strict `private Playback*Owner` fields remain 21, production `fromPlaybackQueueManager(...)` hits remain 0, Service `queueStateSnapshotSupplier` mentions are 13, Service `playbackQueueStateOwner::currentTrack` refs are down to 1, total Service `playbackQueueStateOwner` mentions are down to 3, and app playback `Playback*Owner.java` files remain 40. The added snapshot-supplier mention replaces queue-state facade wiring and does not add Service state.
- Verification target: `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackPositionManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only narrows internal position-persistence current-track reads and does not alter notification controls, lyrics runtime, shutdown, foreground/background playback, or service lifecycle.
## NOTE 245 - Runtime manager reads queue snapshots directly (2026-07-04)
- Queue/state wiring shrink slice: `PlaybackRuntimeStateManager.stateProviderFromPlaybackState(...)` now takes the existing `Supplier<PlaybackQueueManager.QueueStateSnapshot>` instead of a narrow `Supplier<Track?>`. `EchoPlaybackService` passes the already-existing `queueStateSnapshotSupplier`, so no new Service field, facade, bridge, owner, or snapshot mirror was introduced.
- Source-of-truth status is unchanged: queue membership/current track/current index/queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; replay-gain and current-track volume policy remain in `PlaybackRuntimeStateManager`. No URI/media item resolution, cache policy, notification action behavior, lyrics behavior, shutdown behavior, foreground/background playback behavior, or package move was changed.
- Concrete win: the runtime replay-gain/current-volume path no longer depends on `PlaybackQueueStateOwner.currentTrack()`, and `EchoPlaybackService` now has zero `playbackQueueStateOwner::currentTrack` method-reference forwarding chains. `PlaybackRuntimeStateManagerTest` now covers snapshot-backed current-track reads, missing snapshots, empty snapshots, and late-bound snapshot changes.
- Owner/interface audit after this slice: `EchoPlaybackService.java` remains 1382 lines, strict non-final `private Playback*` fields remain 40, strict non-final `private Playback*Owner` fields remain 17, final-inclusive `private Playback*` wiring declarations remain 51, final-inclusive `private Playback*Owner` wiring declarations remain 21, production `fromPlaybackQueueManager(...)` hits remain 0, Service `queueStateSnapshotSupplier` mentions are 14, Service `playbackQueueStateOwner::currentTrack` refs are down to 0, total Service `playbackQueueStateOwner` mentions are down to 2, and app playback `Playback*Owner.java` files remain 40. The added snapshot-supplier mention replaces queue-state facade wiring and does not add Service state.
- Verification target: `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackRuntimeStateManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed code files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only narrows internal runtime-manager current-track reads and does not alter notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 246 - Queue state owner drops no-arg construction (2026-07-04)
- Queue/state interface narrowing slice: `PlaybackQueueStateOwner` no longer exposes a no-arg constructor. The only remaining construction path is the existing `Supplier<PlaybackQueueManager.QueueStateSnapshot>` input used by `EchoPlaybackService` and the focused lyrics state tests.
- Source-of-truth status is unchanged: queue membership/current track/current index/queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackQueueStateOwner` remains a temporary lyrics-only adapter and does not own state. No URI/media item resolution, cache policy, notification action behavior, lyrics runtime behavior, shutdown behavior, foreground/background playback behavior, Service lifecycle behavior, or package move was changed.
- Concrete win: one historical migration fallback entry point was removed from the queue-state owner API without adding a replacement owner or facade. `PlaybackQueueStateOwnerTest` now covers the missing-snapshot case through an explicit null supplier, and `MainActivityArchitectureContractTest` rejects reintroducing the no-arg constructor.
- Owner/interface audit after this slice: `EchoPlaybackService.java` remains 1382 lines, strict non-final `private Playback*` fields remain 40, strict non-final `private Playback*Owner` fields remain 17, final-inclusive `private Playback*` wiring declarations remain 51, final-inclusive `private Playback*Owner` wiring declarations remain 21, production `fromPlaybackQueueManager(...)` hits remain 0, Service `queueStateSnapshotSupplier` mentions remain 14, Service `playbackQueueStateOwner::currentTrack` refs remain 0, total Service `playbackQueueStateOwner` mentions remain 2, and app playback `Playback*Owner.java` files remain 40.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed code files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only narrows an internal constructor API and does not alter runtime playback, notification controls, lyrics runtime behavior, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 247 - Lyrics state reads queue snapshot supplier directly (2026-07-04)
- Queue/state owner deletion slice: `PlaybackLyricsStateOwner` now consumes the existing `Supplier<PlaybackQueueManager.QueueStateSnapshot>` from `EchoPlaybackService` instead of receiving the temporary `PlaybackQueueStateOwner` adapter. `PlaybackQueueStateOwner.java` and `PlaybackQueueStateOwnerTest.java` were deleted rather than replaced with another owner.
- Source-of-truth status is unchanged: queue membership/current track/current index/queue size remain owned by `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackLyricsStateOwner` only derives the current track from that snapshot for the existing lyrics manager state provider. No new state mirror, facade, bridge, resolver path, cache policy path, notification behavior, lyrics sync policy, shutdown behavior, foreground/background playback behavior, Service lifecycle behavior, or package move was added.
- Concrete win: one pure forwarding owner file, one focused forwarding-only test file, and one final Service owner field were removed. The lyrics state constructor now uses the already-existing queue snapshot supplier, so the last production `playbackQueueStateOwner` references are gone while `PlaybackLyricsStateOwnerTest` still covers current-track, playing, preparing, and missing-supplier behavior.
- Owner/interface audit after this slice: `EchoPlaybackService.java` is 1380 lines, strict non-final `private Playback*` fields remain 40, strict non-final `private Playback*Owner` fields remain 17, final-inclusive `private Playback*` wiring declarations are down to 50, final-inclusive `private Playback*Owner` wiring declarations are down to 20, production `fromPlaybackQueueManager(...)` hits remain 0, Service `queueStateSnapshotSupplier` mentions remain 14, Service `playbackQueueStateOwner` mentions are 0, and app playback `Playback*Owner.java` files are down to 39.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackLyricsStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed code files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only removes an internal queue-state adapter and does not alter notification controls, lyrics runtime policy, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 248 - App shell route/status initialization order contract is explicit (2026-07-04)
- App-shell wiring contract slice: `MainActivityArchitectureContractTest.mainActivityCreatesRouteStoresAndStatusBeforeGatewayBinding` now locks the internal `initializeRouteStoresAndStatus()` field creation order as `uiShellController -> routeController -> playbackStore -> statusMessageController`, in addition to the existing `onCreate()` requirement that route/status/store setup runs before now-playing, download, library, and playback lifecycle gateway binding.
- Source-of-truth status is unchanged: navigation route state remains owned by `MainRouteController` / `NavigationViewModel`, playback UI state remains owned by `MainPlaybackStore` / `PlaybackViewModel`, and status message state remains owned by `StatusMessageController` / `StatusMessageViewModel`. No new state mirror, owner, facade, gateway, bridge, package move, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: the already-fixed Kotlin non-null `MainLibraryGatewayFactory.create(... routeActions: LibraryRouteActions, ...)` boundary is now protected against both phase reordering and same-method host field reordering. This is a contract-only slice: `MainActivityBase` production code was not changed because current evidence already shows `initializeRouteStoresAndStatus()` precedes `initializeLibraryGateway()`.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only strengthens app-shell wiring contracts and does not change runtime behavior.
## NOTE 249 - Queue manager mirrored-reuse behavior is pinned before more owner changes (2026-07-04)
- Queue/mirror behavior test slice: `PlaybackQueueManagerTest` now covers previous-track mirrored queue reuse and the negative path where the mirrored player snapshot does not match the queue. This deliberately does not delete `PlaybackQueueNavigationOwner` or `PlaybackMirroredQueueTrackMatcherOwner` yet, because the current evidence shows they still hold Service-boundary notification and Media3 matching responsibilities.
- Source-of-truth status is unchanged: queue membership/current track/current index/restored position remain owned by `PlaybackQueueManager`; `PlaybackQueueManager.MirroredQueuePlayer` is only the player-boundary adapter, and `PlaybackQueueManager.StreamingRestoreProvider` remains the restore/headers adapter into `PlaybackMediaSourceProvider`. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: the queue manager now has focused coverage proving mirrored reuse seeks the existing player and publishes state without preparing new media sources, while a mismatched player snapshot returns before seek, streaming restore, queue persistence, resume-state writes, or prepare calls. This is the baseline needed before any later attempt to shrink navigation or mirrored-player owner wiring.
- Owner/interface audit after this test slice: `EchoPlaybackService.java` remains 1380 lines, strict non-final `private Playback*` fields remain 40, strict non-final `private Playback*Owner` fields remain 17, production `fromPlaybackQueueManager(...)` hits remain 0, Service `queueStateSnapshotSupplier` mentions remain 14, and app playback `Playback*Owner.java` files remain 39.
- Verification target: `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only strengthens queue-manager behavior tests and does not alter notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 250 - Media item reuse identity remains owned by media source provider (2026-07-04)
- Resolver/cache boundary test slice: `PlaybackMediaSourceProviderTest` now directly covers the static `PlaybackMediaSourceProvider.mediaItemMatchesTrackForReuse(MediaItem, trackId, contentUri, cacheKey)` rule used by mirrored queue matching and precache reuse checks. The tests assert that reuse requires matching media id, resolved URI, and cache key, and rejects null or unresolved `MediaItem` input.
- Source-of-truth status is unchanged: URI / MediaItem identity and cache-key derivation remain owned by `PlaybackMediaSourceProvider`; cache policy remains in `PlaybackPrecacheManager` through `PlaybackMediaCacheOperations`; Service, navigation owner, mirrored-player owner, and resolution owner did not gain resolver logic. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: this closes a focused boundary-test gap before any later owner/interface changes around mirrored queue reuse or cache fallback, so the identity rule is protected in the resolver owner instead of being copied into `EchoPlaybackService`, `PlaybackMediaSourceResolutionOwner`, or `PlaybackMirroredQueueTrackMatcherOwner`.
- Owner/interface audit after this test slice: `EchoPlaybackService.java` remains 1380 lines, strict non-final `private Playback*` fields remain 40, strict non-final `private Playback*Owner` fields remain 17, production `fromPlaybackQueueManager(...)` hits remain 0, Service `queueStateSnapshotSupplier` mentions remain 14, and app playback `Playback*Owner.java` files remain 39.
- Verification target: `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackMediaSourceProviderTest --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only strengthens resolver/cache identity tests and does not alter notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 251 - Boundary-test batch closed with T1/T2 verification and no new owner (2026-07-04)
- Batch audit after NOTE 249 and NOTE 250: the queue mirrored-reuse and media-item identity slices are closed as test/contract hardening, not owner extraction. `MainActivityArchitectureContractTest` already protects `PlaybackQueueManager` public API groups, rejects `QueueProvider` / `fromPlaybackQueueManager(...)` reintroduction, and keeps resolver/cache responsibilities in `PlaybackMediaSourceProvider`, `PlaybackMediaCacheOperations`, and `PlaybackPrecacheManager`.
- Source-of-truth status is unchanged: queue membership/current track/current index/restored position remain owned by `PlaybackQueueManager`; URI / MediaItem identity remain owned by `PlaybackMediaSourceProvider`; cache policy remains in `PlaybackPrecacheManager`; app-shell route/playback/status state ownership is unchanged. No new state mirror, owner, facade, bridge, listener, gateway, resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, background playback behavior, or package move was added.
- Concrete win: the two-slice batch has T1 coverage across `PlaybackQueueManagerTest`, `PlaybackMediaSourceProviderTest`, and `MainActivityArchitectureContractTest`, plus an explicit T2 app compile. The follow-up audit found existing coverage for `PlaybackPrecacheManager` current-player fallback and provider-backed `PlaybackMediaCacheOperations`, so no duplicate cache owner/test slice was added.
- Owner/interface audit for this batch: `EchoPlaybackService.java` remains 1380 lines, strict non-final `private Playback*` fields remain 40, strict non-final `private Playback*Owner` fields remain 17, production `fromPlaybackQueueManager(...)` hits remain 0, Service `queueStateSnapshotSupplier` mentions remain 14, and app playback `Playback*Owner.java` files remain 39.
- Verification passed: `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest --tests app.yukine.playback.PlaybackMediaSourceProviderTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain` and `.\gradlew.bat :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this batch only hardens internal queue/resolver/cache boundaries and does not alter runtime playback, notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 252 - Early app-shell gateway host captures are contracted (2026-07-04)
- App-shell wiring contract slice: `MainActivityArchitectureContractTest.mainActivityCreatesRouteStoresAndStatusBeforeGatewayBinding` now also requires early now-playing, playback, and download bindings to reach mutable host fields through providers/lambdas instead of passing `playbackActionController`, `playbackStore`, `playbackService`, or `statusMessageController` by value.
- Source-of-truth status is unchanged: navigation remains owned by `MainRouteController` / `NavigationViewModel`, playback UI state remains owned by `MainPlaybackStore` / `PlaybackViewModel`, playback service state remains owned behind `EchoPlaybackService`, and status messages remain owned by `StatusMessageController` / `StatusMessageViewModel`. No new state mirror, owner, facade, gateway, bridge, package move, playback resolver path, cache policy path, notification behavior, lyrics behavior, shutdown behavior, foreground behavior, or background playback behavior was added.
- Concrete win: the app shell now has a focused regression guard for the Kotlin/Java non-null boundary class of bugs that previously allowed nullable host fields to be captured before their initialization phase. This is a contract-only slice; `MainActivityBase` production code was unchanged because current evidence already uses providers/lambdas for these early host dependencies.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only strengthens app-shell wiring contracts and does not change runtime behavior.
## NOTE 253 - Stop-clear queue merge is deferred, retain-empty behavior is pinned (2026-07-04)
- Queue mutation behavior-test slice: the older `PlaybackQueueStopClearOwner` merge candidate no longer has a direct target in current production code; `EchoPlaybackService` already creates short-lived `PlaybackQueueCompletionOwner` / `PlaybackQueueMutationOwner` instances and there is no Service field or `fromPlaybackQueueManager(...)` path to remove. The stop/clear boundary stays split because queue emptiness is decided by `PlaybackQueueManager`, while player release, progress updates, foreground notification, and self-stop remain Android/Media3 Service boundary work.
- Source-of-truth status is unchanged: queue membership/current index/current track remain owned by `PlaybackQueueManager`; shutdown/notification release remains owned by `EchoPlaybackService` and its existing shutdown/notification owners. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification action mapping, lyrics behavior, foreground/background playback behavior, Service lifecycle behavior, or package move was added.
- Concrete win: `PlaybackQueueMutationOwnerTest` now covers `retainTracksById(...)` emptying the queue and routing through the stop-and-clear Service boundary without preparing another current track. This pins the same empty-queue behavior already covered for remove/clear, so future queue-interface narrowing cannot accidentally leave retain with a stale player.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMutationOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only strengthens queue mutation behavior coverage and does not alter runtime playback, notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 254 - Playback/app-shell owner audit after NOTE 252-253 (2026-07-04)
- Audit scope: this is a read-only checkpoint after the early app-shell host-capture contract and the queue retain-empty behavior test. It deliberately does not add, delete, rename, or move any production owner.
- Current metrics: `EchoPlaybackService.java` is 1385 lines, strict non-final `private Playback*` fields are 40, strict non-final `private Playback*Owner` fields are 17, final-inclusive `private Playback*` wiring declarations are 50, final-inclusive `private Playback*Owner` declarations are 20, production `fromPlaybackQueueManager(...)` hits remain 0, `queueStateSnapshotSupplier` mentions are 11, the only direct `queueStateSnapshot()` call in Service is inside the supplier declaration, and app playback `Playback*Owner.java` files remain 39.
- App-shell metrics: `MainActivityBase.java` is 2693 lines. `onCreate()` still initializes `createStreamingActionGateway()` before streaming owners, then `initializeRouteStoresAndStatus()` before now-playing/download/library gateway binding, then platform/lifecycle/render/playback/settings/streaming/network startup. Contract-covered early nullable host-field captures are 0.
- Queue API grouping: `PlaybackQueueManager` has no `QueueProvider` and exposes 25 public Kotlin functions. Mutations/commands are `playQueue`, `appendToQueue`, `playFirstQueuedTrack`, `skipToNextImmediately`, `skipToPrevious`, `moveQueueTrack`, `removeTracksById`, `retainTracksById`, `replaceQueuedTrackById`, `replaceCurrentTrackAndResume`, and `replaceCurrentQueueTrack`. Restore/completion/player-mirror preparation APIs are `setPlaybackRestoreEnabled`, `applyMirroredTransitionIndex`, `preparePlaybackCompletionAction`, `prepareStopAtEndOfQueue`, `prepareStopAfterAutomaticAdvance`, `prepareStopAndClearPlaybackState`, `reuseMirroredQueueIfAvailable`, `queuePreparationForNewPlayer`, `persistQueueState`, `restorePlaybackQueue`, and `restoreLastPlayback`. Remaining reads are `queueSnapshot`, `queueStateSnapshot`, and `upcomingTracksForPrecache`.
- Resolver/cache boundary check: no `PlaybackMediaSourceResolutionOwner` production hits were found. URI / MediaItem identity still goes through `PlaybackMediaSourceProvider`; cache operations still go through `PlaybackMediaCacheOperations.fromMediaSourceProvider(...)`; cache policy and current/upcoming precache scheduling remain in `PlaybackPrecacheManager`, which owns direct reads from `PlaybackQueueManager` instead of a Service supplier facade.
- Next candidate: prefer a focused queue API/read audit or a `PlaybackQueueManager` behavior test before more production rewiring. Do not start P4/P5 notification, lyrics, shutdown, foreground, or background playback slices until the smoke table remains stable; MuMu is available for T3 when those runtime areas are touched.
## NOTE 255 - Queue manager narrow API call sites are contracted (2026-07-04)
- Queue/interface contract slice: `MainActivityArchitectureContractTest.playbackQueueManagerPublicApiStaysExplicitlyAudited` now pins the only production callers for two narrow `PlaybackQueueManager` public APIs that are easy to grow into migration leftovers: `persistQueueState()` may only be reached through `PlaybackShutdownLifecycleResourcesOwner`, and `replaceCurrentQueueTrack(...)` may only be reached through `PlaybackCurrentTrackPreparationQueueOwner`.
- Source-of-truth status is unchanged: queue membership/current track/current index/queue size remain owned by `PlaybackQueueManager`; shutdown persistence remains in the shutdown lifecycle resources boundary; current-track replacement during preparation remains in the current-track preparation queue owner. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, or package move was added.
- Concrete win: the `onTaskRemoved()` fallback no longer calls `playbackQueueManager.persistQueueState()` directly from `EchoPlaybackService`; it reuses the existing shutdown lifecycle resources owner boundary. The contract now fails future edits that spread these low-level queue APIs back into `EchoPlaybackService`, cache/resolver owners, or another broad facade instead of silently becoming "calling point moved" debt.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackShutdownLifecycleResourcesOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only reroutes an internal queue persistence fallback through the existing shutdown owner and does not alter runtime playback, notification controls, lyrics runtime, shutdown semantics, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 256 - Mirrored queue snapshot fallback moves out of Service wiring (2026-07-04)
- Queue/service wiring shrink slice: `PlaybackQueueMirroredPlayerOwner.queueSnapshotProvider(...)` now owns the null-safe queue snapshot supplier used by the mirrored-player matcher. `EchoPlaybackService` no longer inlines `playbackQueueManager == null ? emptyList : queueSnapshot()` inside `onCreate()` wiring; it passes a queue-manager provider so the still-uninitialized field is not captured by value, and its remaining direct `queueSnapshot()` call is the public `NowPlayingPlaybackServicePort` boundary.
- Source-of-truth status is unchanged: full queue membership remains owned by `PlaybackQueueManager`; mirrored queue matching remains in `PlaybackQueueMirroredPlayerOwner` plus `PlaybackMirroredQueueTrackMatcherOwner`; NowPlaying queue reads still cross the existing service port. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, or package move was added.
- Concrete win: one `EchoPlaybackService` strategy/null-default branch was removed from mirrored queue assembly, and `PlaybackQueueMirroredPlayerOwnerTest` now covers the provider delegate, missing-manager fallback, and defensive queue snapshot behavior. `MainActivityArchitectureContractTest` also requires this helper wiring and keeps the low-level `queueSnapshot()` production callers explicit.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMirroredPlayerOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only moves an internal mirrored-queue supplier fallback and does not alter runtime playback, notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 257 - Playback/app-shell owner audit after NOTE 255-256 (2026-07-04)
- Audit scope: this is a read-only checkpoint after the queue-persistence caller contract and the mirrored queue snapshot fallback move. It deliberately does not add, delete, rename, move, or widen any production owner.
- Current metrics using the `MainActivityArchitectureContractTest` helper口径: `EchoPlaybackService.java` is 1381 lines, strict non-final `private Playback*` fields are 36, final-inclusive `private Playback*` wiring declarations are 46, final-inclusive `private Playback*Owner` wiring declarations are 16, app playback `Playback*Owner.java` files remain 39, production `fromPlaybackQueueManager(...)` hits remain 0, `queueStateSnapshotSupplier` mentions remain 11, and the only direct `playbackQueueManager.queueStateSnapshot()` call in Service is still inside the queue snapshot supplier declaration.
- Queue direct-read status: production `playbackQueueManager.queueSnapshot()` callers are now only `EchoPlaybackService.queueSnapshot()` for the existing `NowPlayingPlaybackServicePort` boundary and `PlaybackQueueMirroredPlayerOwner.queueSnapshotProvider(...)` for mirrored-player matching. Narrow public queue API callers remain pinned: `persistQueueState()` through `PlaybackShutdownLifecycleResourcesOwner`, `replaceCurrentQueueTrack(...)` and `queuePreparationForNewPlayer()` through `PlaybackCurrentTrackPreparationQueueOwner`, and `upcomingTracksForPrecache(...)` through `PlaybackPrecacheManager`.
- App-shell metrics: `MainActivityBase.java` is 2693 lines. `onCreate()` order remains `createStreamingActionGateway()` -> `initializeStreamingPlaybackCoordinator()` -> `initializeRouteStoresAndStatus()` -> `initializeNowPlayingGateways()` -> `initializeDownloadRequests()` -> `initializeLibraryGateway()` -> startup/platform/playback/settings/streaming/network/onboarding phases. The existing architecture contract still guards early now-playing/playback/download bindings against passing nullable host fields by value.
- Resolver/cache boundary check: no `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, `Playback*CachePolicy`, `Playback*CacheFacade`, `Playback*PrecachePolicy`, `PlaybackQueueManager.QueueProvider`, or `fromPlaybackQueueManager(...)` production hits were found. URI / MediaItem identity remains in `PlaybackMediaSourceProvider`; cache operations remain in `PlaybackMediaCacheOperations.fromMediaSourceProvider(...)`; current/upcoming precache policy remains in `PlaybackPrecacheManager`.
- Next candidate: prefer a focused behavior/contract slice around the remaining queue reads before any P4/P5 runtime work. The safest candidates are: keep `EchoPlaybackService.queueSnapshot()` explicitly documented as the NowPlaying port boundary, add or tighten behavior around `PlaybackCurrentTrackPreparationQueueOwner.queuePreparationForNewPlayer()` media-source mapping, or audit `PlaybackPrecacheManager.upcomingTracksForPrecache(...)` cache fallback coverage. Do not start notification, lyrics, shutdown, foreground, or background playback slices until smoke remains stable.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for this handoff entry, added-line scan, and `git diff --check`. Device smoke remains out of scope because this checkpoint is read-only documentation and does not alter runtime behavior.
## NOTE 258 - Current-track preparation media-source fallback is pinned (2026-07-04)
- Queue preparation behavior-test slice: `PlaybackCurrentTrackPreparationQueueOwnerTest` now covers valid mirrored queue preparation when the media-source boundary returns `null`, and when the injected media-source mapper is absent. Both cases preserve the current track and start index while leaving mirrored queue media sources unset.
- Source-of-truth status is unchanged: queue membership/current track/start index remain owned by `PlaybackQueueManager.QueuePreparation`; URI / MediaItem / MediaSource construction remains owned by `PlaybackMediaSourceProvider` and is only injected into `PlaybackCurrentTrackPreparationQueueOwner` as the existing mapping boundary. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, Service lifecycle behavior, or package move was added.
- Concrete win: queue preparation fallback behavior is now covered without adding another owner or moving resolver/cache strategy into `EchoPlaybackService`. This closes the NOTE 257 test-hardening candidate and keeps future queue-interface narrowing from accidentally dropping the prepared current track when mirrored MediaSource resolution is unavailable.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationQueueOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only strengthens internal queue-preparation behavior coverage and does not alter runtime playback, notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 259 - Lyrics state reads queue manager directly (2026-07-04)
- Service wiring shrink slice: `PlaybackLyricsStateOwner` now receives `PlaybackQueueManager` directly and reads `queueStateSnapshot()` inside the owner instead of receiving `EchoPlaybackService.queueStateSnapshotSupplier`. `EchoPlaybackService` no longer forwards that supplier into the lyrics state boundary.
- Source-of-truth status is unchanged: lyrics playback state still comes from `PlaybackLyricsManager.StateProvider`; current track for lyrics is still derived from `PlaybackQueueManager.QueueStateSnapshot`; playing/preparing/app-visible state still comes from the existing playback runtime/service suppliers. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, foreground/background playback behavior, Service lifecycle behavior, or package move was added.
- Concrete win: one Service supplier/lambda forwarding chain is removed from the lyrics path, and `PlaybackLyricsStateOwnerTest` plus `MainActivityArchitectureContractTest` now pin the direct queue-manager dependency and null-manager fallback. Notification state remains intentionally unchanged for this slice to avoid broad P4/P5 runtime movement.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackLyricsStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only removes an internal supplier hop and preserves lyrics manager behavior.
## NOTE 260 - Playback/app-shell owner audit after NOTE 258-259 (2026-07-04)
- Audit scope: this is a read-only checkpoint after the current-track preparation fallback tests and the lyrics queue-state supplier shrink. It deliberately does not add, delete, rename, move, or widen any production owner.
- Current metrics using the local audit script口径: `EchoPlaybackService.java` is 1381 lines, strict non-final `private Playback*` fields are 36, final-inclusive `private Playback*` declarations are 44, final-inclusive `private Playback*Owner` declarations are 16, app playback `Playback*Owner.java` files remain 39, production `fromPlaybackQueueManager(...)` hits remain 0, Service `queueStateSnapshotSupplier` mentions are now 10, the only direct `playbackQueueManager.queueStateSnapshot()` call in Service is still inside the supplier declaration, and the only direct `playbackQueueManager.queueSnapshot()` call in Service remains the `NowPlayingPlaybackServicePort` queue snapshot boundary.
- Queue/API status: `PlaybackQueueManager` still exposes 25 public Kotlin functions, with no `PlaybackQueueManager.QueueProvider` or `interface QueueProvider` production hits. Direct production `playbackQueueManager.queueStateSnapshot()` callers are explicitly audited as `EchoPlaybackService`, `PlaybackLyricsStateOwner`, `PlaybackPrecacheManager`, and `PlaybackQueueMutationOwner`; `PlaybackLyricsStateOwner` is the only new direct queue-state caller from this batch and removes one Service supplier hop.
- App-shell status: `MainActivityBase.java` remains 2693 lines. `onCreate()` order remains `initializeViewModels` -> `createStreamingActionGateway()` -> `initializeStreamingPlaybackCoordinator()` -> `initializeRouteStoresAndStatus()` -> `initializeNowPlayingGateways()` -> `initializeDownloadRequests()` -> `initializeLibraryGateway()` -> startup/platform/playback/settings/streaming/network/onboarding phases. The existing contract still guards the early route/status/playback host fields before gateway binding and the UI-common UTF-8 Chinese copy.
- Resolver/cache boundary check: no `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, `PlaybackCacheFacade`, `PlaybackPrecachePolicy`, `PlaybackQueueManager.QueueProvider`, `interface QueueProvider`, or `fromPlaybackQueueManager(...)` production hits were found. URI / MediaItem identity remains in `PlaybackMediaSourceProvider`; cache operations remain behind `PlaybackMediaCacheOperations.fromMediaSourceProvider(...)`; current/upcoming precache policy remains in `PlaybackPrecacheManager`.
- Batch evidence: NOTE 258 is protected by `PlaybackCurrentTrackPreparationQueueOwnerTest`; NOTE 259 is protected by `PlaybackLyricsStateOwnerTest`; both are guarded by `MainActivityArchitectureContractTest` and app Kotlin/Java compile. Next candidates should continue one narrow supplier/API reduction at a time, preferably `PlaybackNotificationStateOwner` only if its notification smoke risk can stay bounded, or a queue API caller contract/test that does not touch P4/P5 runtime behavior.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for this handoff entry, added-line scan, and `git diff --check`. Device smoke remains out of scope because this checkpoint is read-only documentation and does not alter runtime behavior.
## NOTE 261 - Mirrored transition reads queue manager directly (2026-07-04)
- Service wiring shrink slice: `PlaybackQueueMirroredTransitionOwner` no longer accepts `EchoPlaybackService.queueStateSnapshotSupplier`. It already owns the `PlaybackQueueManager`, so its private `queueStateSnapshot()` helper now reads `playbackQueueManager.queueStateSnapshot()` directly.
- Source-of-truth status is unchanged: queue membership, current index, and current track remain owned by `PlaybackQueueManager`. The mirrored transition decision remains owned by `PlaybackQueueManager.applyMirroredTransitionIndex(...)`; the owner only adapts Media3 transition reasons and applies current-track volume after a successful non-stop transition. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, Service lifecycle behavior, or package move was added.
- Concrete win: one owner constructor parameter, one owner field, one Service supplier argument, and one focused-test supplier helper were removed from the mirrored-transition path. `MainActivityArchitectureContractTest` now guards against reintroducing that supplier and updates the audited direct queue-state caller set.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMirroredTransitionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only removes an internal supplier hop and does not touch notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 262 - Crossfade state reads initialized queue manager directly (2026-07-04)
- Service wiring shrink slice: `PlaybackCrossfadeStateOwner` no longer accepts `EchoPlaybackService.queueStateSnapshotSupplier`. `EchoPlaybackService` now creates `PlaybackQueueManager` before the crossfade state/advance manager pair, and then passes the initialized `PlaybackQueueManager` by value.
- Source-of-truth status is unchanged: crossfade advance eligibility still derives from `PlaybackQueueManager.QueueStateSnapshot` plus repeat mode; transition state remains owned by `PlaybackTransitionStateManager`; player availability, playback state, and current-track volume still come from the existing service/runtime providers. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, Service lifecycle behavior, or package move was added.
- Concrete win: one owner constructor parameter type no longer exposes a queue snapshot supplier, one owner field no longer stores that supplier, one Service supplier argument is removed from the crossfade path, the crossfade test helper now builds a real `PlaybackQueueManager`, and `MainActivityArchitectureContractTest` now pins the initialization order so crossfade state cannot capture an uninitialized queue manager.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCrossfadeStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only removes an internal supplier hop and does not touch notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 263 - Playback owner/interface audit after NOTE 261-262 (2026-07-04)
- Audit scope: this is a read-only checkpoint after the mirrored-transition and crossfade queue-state supplier shrink slices. It deliberately does not add, delete, rename, move, or widen any production owner.
- Current metrics using the `MainActivityArchitectureContractTest` helper口径: `EchoPlaybackService.java` is 1380 lines, strict `private Playback*` fields are 36, final-inclusive `private Playback*` field declarations are 46, final-inclusive `private Playback*Owner` field declarations are 16, app playback `Playback*Owner.java` files remain 39, production `fromPlaybackQueueManager(...)` hits remain 0, Service `queueStateSnapshotSupplier` mentions are now 8, the only direct `playbackQueueManager.queueStateSnapshot()` call in Service is still inside the supplier declaration, and the only direct `playbackQueueManager.queueSnapshot()` call in Service remains the `NowPlayingPlaybackServicePort` queue snapshot boundary.
- Queue/API status: `PlaybackQueueManager.QueueProvider` and `interface QueueProvider` still have 0 production hits. No production `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, `PlaybackCacheFacade`, or `PlaybackPrecachePolicy` hits were found. Direct production `playbackQueueManager.queueStateSnapshot()` callers are explicitly audited as `EchoPlaybackService`, `PlaybackCrossfadeStateOwner`, `PlaybackLyricsStateOwner`, `PlaybackPrecacheManager`, `PlaybackQueueMirroredTransitionOwner`, and `PlaybackQueueMutationOwner`; the two new direct queue-state callers from this batch each removed a Service supplier hop and did not add a state mirror.
- App-shell status: `MainActivityBase.java` remains 2693 lines. `onCreate()` order remains `initializeViewModels` -> `createStreamingActionGateway()` -> `initializeStreamingPlaybackCoordinator()` -> `initializeRouteStoresAndStatus()` -> `initializeNowPlayingGateways()` -> `initializeDownloadRequests()` -> `initializeLibraryGateway()` -> startup/platform/playback/settings/streaming/network/onboarding phases. `initializeLibraryGateway()` and `initializeNowPlayingGateways()` still run after route/status/playback host fields are created, so this batch did not add an early by-value nullable host capture.
- Batch evidence: NOTE 261 is protected by `PlaybackQueueMirroredTransitionOwnerTest`; NOTE 262 is protected by `PlaybackCrossfadeStateOwnerTest`; both are guarded by `MainActivityArchitectureContractTest` and app Kotlin/Java compile. Next low-risk supplier candidate is likely `PlaybackStateSnapshotOwner`, because it is constructed after `PlaybackQueueManager`; avoid `PlaybackQueueCommandOwner` while it remains part of the `PlaybackQueueManager` construction cycle, and keep notification/session/shutdown/background slices deferred until smoke coverage is intentionally run.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for this handoff entry, added-line scan, and `git diff --check`. Device smoke remains out of scope because this checkpoint is read-only documentation and does not alter runtime behavior.
## NOTE 264 - Playback state snapshots read queue manager directly (2026-07-04)
- Service wiring shrink slice: `PlaybackStateSnapshotOwner` no longer accepts `EchoPlaybackService.queueStateSnapshotSupplier`. It is constructed after `PlaybackQueueManager`, so `EchoPlaybackService` now passes the initialized queue manager by value and the owner reads `playbackQueueManager.queueStateSnapshot()` internally.
- Source-of-truth status is unchanged: playback snapshot queue fields (`currentTrack`, `currentIndex`, and `queueSize`) remain owned by `PlaybackQueueManager.QueueStateSnapshot`; position, runtime state, sleep timer, visualization, and realtime beat sources are unchanged. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, or package move was added.
- Concrete win: one owner constructor parameter type no longer exposes a Service snapshot supplier, one owner field no longer stores that supplier, one Service supplier argument is removed from the playback snapshot path, and `PlaybackStateSnapshotOwnerTest` now builds queue state through a real `PlaybackQueueManager` instead of a test-only snapshot supplier.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackStateSnapshotOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only removes an internal supplier hop and does not touch notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 265 - Session command current track reads queue manager directly (2026-07-04)
- Service wiring shrink slice: `PlaybackSessionCommandOwner` no longer accepts `EchoPlaybackService.queueStateSnapshotSupplier`. It is created after `PlaybackQueueManager`, so the service now passes the initialized queue manager and the session delegate reads its current track from `playbackQueueManager.queueStateSnapshot()`.
- Source-of-truth status is unchanged: the session player's `currentTrack()` still derives from `PlaybackQueueManager.QueueStateSnapshot`; session play/pause/seek/repeat/controller-media-item delegation and notification metadata construction are unchanged. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification action mapping, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, or package move was added.
- Concrete win: one owner constructor parameter, one owner field, one Service supplier argument, and one test-only queue snapshot supplier were removed from the MediaSession delegate path. `PlaybackSessionCommandOwnerTest` now covers current-track and empty-queue fallback behavior through a real `PlaybackQueueManager`.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackSessionCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only removes an internal supplier hop from the session delegate and does not touch notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession lifecycle binding.
## NOTE 266 - Playback owner/interface audit after NOTE 264-265 (2026-07-04)
- Audit scope: this is a read-only checkpoint after the playback snapshot and session command queue-state supplier shrink slices. It deliberately does not add, delete, rename, move, or widen any production owner.
- Current metrics using the `MainActivityArchitectureContractTest` helper口径: `EchoPlaybackService.java` is 1380 lines, strict `private Playback*` fields are 36, final-inclusive `private Playback*` field declarations are 46, final-inclusive `private Playback*Owner` field declarations are 16, app playback `Playback*Owner.java` files remain 39, production `fromPlaybackQueueManager(...)` hits remain 0, Service `queueStateSnapshotSupplier` mentions are now 6, and the only direct `playbackQueueManager.queueSnapshot()` callers remain `EchoPlaybackService` and `PlaybackQueueMirroredPlayerOwner`.
- Queue-state direct-read status: production `playbackQueueManager.queueStateSnapshot()` callers are explicitly audited as `EchoPlaybackService`, `PlaybackCrossfadeStateOwner`, `PlaybackLyricsStateOwner`, `PlaybackPrecacheManager`, `PlaybackQueueMirroredTransitionOwner`, `PlaybackQueueMutationOwner`, `PlaybackSessionCommandOwner`, and `PlaybackStateSnapshotOwner`. Remaining production `queueStateSnapshotSupplier` files are `EchoPlaybackService`, `PlaybackErrorRecoveryCommandOwner`, `PlaybackNotificationStateOwner`, `PlaybackQueueCommandOwner`, and `PlaybackQueueMirroredPlayerOwner`.
- Boundary status: `PlaybackQueueManager.QueueProvider`, `interface QueueProvider`, `PlaybackMediaSourceResolutionOwner`, `PlaybackItemResolver`, `PlaybackCacheFacade`, and `PlaybackPrecachePolicy` still have 0 production hits. URI / MediaItem identity remains in `PlaybackMediaSourceProvider`; cache operations and precache policy remain outside `EchoPlaybackService`.
- App-shell status: `MainActivityBase.java` remains 2693 lines. `onCreate()` order remains `initializeViewModels` -> `createStreamingActionGateway()` -> `initializeStreamingPlaybackCoordinator()` -> `initializeRouteStoresAndStatus()` -> `initializeNowPlayingGateways()` -> `initializeDownloadRequests()` -> `initializeLibraryGateway()` -> startup/platform/playback/settings/streaming/network/onboarding phases. This batch did not add an early by-value nullable host capture.
- Batch evidence: NOTE 264 is protected by `PlaybackStateSnapshotOwnerTest`; NOTE 265 is protected by `PlaybackSessionCommandOwnerTest`; both are guarded by `MainActivityArchitectureContractTest` and app Kotlin/Java compile. Next work should not mechanically force the remaining supplier owners: `PlaybackQueueCommandOwner`, `PlaybackErrorRecoveryCommandOwner`, and `PlaybackQueueMirroredPlayerOwner` are tied to queue-manager construction timing, and `PlaybackNotificationStateOwner` should wait for intentional notification smoke coverage. Prefer a focused queue API behavior test or another read-only boundary audit before touching notification, shutdown, foreground/background, or MediaSession lifecycle.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for this handoff entry, added-line scan, and `git diff --check`. Device smoke remains out of scope because this checkpoint is read-only documentation and does not alter runtime behavior.
## NOTE 267 - Visualization cache rejects empty cache-owner keys (2026-07-04)
- Cache-boundary test slice: `PlaybackVisualizationCacheManagerTest.emptyCacheKeyFromMediaCacheOperationsSkipsVisualizationCache` now pins that visualization caching treats an empty `PlaybackMediaCacheOperations.cacheKeyForPrecache(...)` result as a hard no-op, without scheduling a cache task, reading cache ranges, or creating a writer.
- Source-of-truth status is unchanged: cache identity and cache-key eligibility remain owned by `PlaybackMediaCacheOperations`, which is backed by `PlaybackMediaSourceProvider`; visualization cache scheduling only consumes that decision. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, or package move was added.
- Concrete win: cache owner boundary coverage is stronger without moving policy into `EchoPlaybackService` or adding a `PlaybackMediaSourceResolutionOwner` / resolver facade. This protects URI/cache behavior while P2/P3 stay test-first.
- Verification target: `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackVisualizationCacheManagerTest --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this is a feature-module cache-boundary test only and does not alter runtime behavior.
## NOTE 268 - Mirrored player queue-state fallback moves out of Service wiring (2026-07-04)
- Queue/service wiring shrink slice: `PlaybackQueueMirroredPlayerOwner.queueStateSnapshotProvider(...)` now owns the null-safe queue-state supplier used when reusing a mirrored Media3 queue. `EchoPlaybackService` no longer passes the broad `queueStateSnapshotSupplier` into the mirrored-player owner; it passes a queue-manager provider because the owner participates in `PlaybackQueueManager` construction.
- Source-of-truth status is unchanged: queue membership/current index/current track still come from `PlaybackQueueManager.QueueStateSnapshot`; mirrored-player reuse still adapts Media3 player seek/play state and waveform reset only after `PlaybackQueueManager` accepts reuse. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, or package move was added.
- Concrete win: one Service `queueStateSnapshotSupplier` forwarding site was removed from mirrored-player wiring. `EchoPlaybackService.java` remains 1380 lines, Service `queueStateSnapshotSupplier` mentions are now 5, and direct production `playbackQueueManager.queueStateSnapshot()` callers are audited as `EchoPlaybackService`, `PlaybackCrossfadeStateOwner`, `PlaybackLyricsStateOwner`, `PlaybackPrecacheManager`, `PlaybackQueueMirroredPlayerOwner`, `PlaybackQueueMirroredTransitionOwner`, `PlaybackQueueMutationOwner`, `PlaybackSessionCommandOwner`, and `PlaybackStateSnapshotOwner`.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMirroredPlayerOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only removes an internal supplier hop and does not touch notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 269 - Error recovery queue-state fallback moves out of Service wiring (2026-07-04)
- Queue/service wiring shrink slice: `PlaybackErrorRecoveryCommandOwner.queueStateSnapshotProvider(...)` now owns the null-safe queue-state supplier used by retry/skip decisions. `EchoPlaybackService` no longer passes the broad `queueStateSnapshotSupplier` into error-recovery command wiring; it passes a queue-manager provider because this owner is created before `PlaybackQueueManager` is assigned.
- Source-of-truth status is unchanged: queue membership/current index/current track still come from `PlaybackQueueManager.QueueStateSnapshot`. Error recovery still owns only failed-track command behavior and debug text, with no new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, MediaSession action mapping, or package move.
- Concrete win: one more Service `queueStateSnapshotSupplier` forwarding site was removed. `EchoPlaybackService.java` remains 1380 lines, Service `queueStateSnapshotSupplier` mentions are now 4, and direct production `playbackQueueManager.queueStateSnapshot()` callers are audited as `EchoPlaybackService`, `PlaybackErrorRecoveryCommandOwner`, `PlaybackCrossfadeStateOwner`, `PlaybackLyricsStateOwner`, `PlaybackPrecacheManager`, `PlaybackQueueMirroredPlayerOwner`, `PlaybackQueueMirroredTransitionOwner`, `PlaybackQueueMutationOwner`, `PlaybackSessionCommandOwner`, and `PlaybackStateSnapshotOwner`.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackErrorRecoveryCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, and `git diff --check`. Device smoke remains out of scope because this only removes an internal supplier hop and does not touch notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 270 - Owner/interface audit after NOTE 268-269 (2026-07-04)
- Audit scope: this is a read-only checkpoint after the mirrored-player and error-recovery queue-state fallback shrink slices. It deliberately does not add, delete, rename, move, or widen any production owner; it verifies the batch did not drift into owner count progress, QueueProvider expansion, or resolver/cache facade growth.
- Current metrics: `EchoPlaybackService.java` is 1380 lines and `MainActivityBase.java` is 2693 lines. Simple field-scan counts are 40 strict `private Playback*` fields, 50 final-inclusive `private Playback*` field declarations, 20 final-inclusive `private Playback*Owner` field declarations, 39 app playback `Playback*Owner.java` files, 0 production `fromPlaybackQueueManager(...)` hits, 0 production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` hits, 0 production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` / `PlaybackCacheFacade` / `PlaybackPrecachePolicy` hits, 4 Service `queueStateSnapshotSupplier` mentions, 1 direct Service `playbackQueueManager.queueStateSnapshot()` call inside the supplier declaration, and 1 direct Service `playbackQueueManager.queueSnapshot()` call at the `NowPlayingPlaybackServicePort` boundary.
- Queue-state status: direct production `playbackQueueManager.queueStateSnapshot()` callers are explicitly audited as `EchoPlaybackService`, `PlaybackErrorRecoveryCommandOwner`, `PlaybackCrossfadeStateOwner`, `PlaybackLyricsStateOwner`, `PlaybackPrecacheManager`, `PlaybackQueueMirroredPlayerOwner`, `PlaybackQueueMirroredTransitionOwner`, `PlaybackQueueMutationOwner`, `PlaybackSessionCommandOwner`, and `PlaybackStateSnapshotOwner`. Remaining Service queue-state supplier forwarding sites are now only `PlaybackQueueCommandOwner` and `PlaybackNotificationStateOwner`; do not force the command owner while it participates in queue-manager construction, and keep notification changes deferred until the notification/background smoke table is intentionally run.
- App-shell status: `MainActivityBase.onCreate()` order remains `initializeViewModels` -> `createStreamingActionGateway()` -> `initializeStreamingPlaybackCoordinator()` -> `initializeRouteStoresAndStatus()` -> `initializeNowPlayingGateways()` -> `initializeDownloadRequests()` -> `initializeLibraryGateway()` -> startup/platform/playback/settings/streaming/network/onboarding phases. Existing `MainActivityArchitectureContractTest.mainActivityCreatesRouteStoresAndStatusBeforeGatewayBinding` now pins route/status/playback host creation before gateway binding, verifies `routeController` is the non-null `LibraryRouteActions` argument, and guards provider/lambda use for host fields that are intentionally not initialized yet.
- Batch evidence: NOTE 268 is protected by `PlaybackQueueMirroredPlayerOwnerTest`; NOTE 269 is protected by `PlaybackErrorRecoveryCommandOwnerTest`; both are guarded by `MainActivityArchitectureContractTest` and app Kotlin/Java compile. Next low-risk work should be a queue API behavior/interface test or a focused app-shell wiring fix if new evidence appears. Avoid broad notification, lyrics, shutdown, foreground/background, or MediaSession lifecycle changes until smoke coverage is intentionally run.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for this handoff entry, added-line scan, forbidden facade/provider search, and `git diff --check`. Device smoke remains out of scope because this checkpoint is documentation-only and does not alter runtime behavior.
## NOTE 271 - Queue completion public API call sites are contracted (2026-07-04)
- Queue/interface contract slice: `MainActivityArchitectureContractTest.playbackQueueManagerPublicApiStaysExplicitlyAudited` now pins the production callers for the completion/stop-preparation portion of `PlaybackQueueManager` public API. `preparePlaybackCompletionAction()`, `prepareStopAndClearPlaybackState()`, `prepareStopAtEndOfQueue()`, and `prepareStopAfterAutomaticAdvance(...)` may only be called through `PlaybackQueueCompletionOwner`.
- Source-of-truth status is unchanged: queue membership/current index/current track remain owned by `PlaybackQueueManager`; completion routing remains owned by `PlaybackQueueCompletionOwner`; runtime stop/release/foreground boundaries remain in `EchoPlaybackService` and existing shutdown/notification owners. No production code, state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, MediaSession action mapping, or package move was added.
- Concrete win: the wide queue completion/preparation API is now protected against future call-site spread, so later interface narrowing cannot silently turn into "calling point moved" debt. This complements the existing behavior coverage in `PlaybackQueueCompletionOwnerTest` and `PlaybackQueueManagerTest` without touching P4/P5 runtime surfaces.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, forbidden facade/provider search, and `git diff --check`. Device smoke remains out of scope because this is a test-contract slice only and does not alter runtime behavior.
## NOTE 272 - Queue restore and navigation public API call sites are contracted (2026-07-04)
- Queue/interface contract slice: `MainActivityArchitectureContractTest.playbackQueueManagerPublicApiStaysExplicitlyAudited` now also pins the production callers for restore, mirrored transition, and navigation/reuse queue APIs. `restoreLastPlayback(...)`, `restorePlaybackQueue()`, and `setPlaybackRestoreEnabled(...)` may only be called through `PlaybackQueueRestoreOwner`; `applyMirroredTransitionIndex(...)` may only be called through `PlaybackQueueMirroredTransitionOwner`; `reuseMirroredQueueIfAvailable(...)`, `skipToNextImmediately()`, `skipToPrevious()`, and `playFirstQueuedTrack()` may only be called through `PlaybackQueueNavigationOwner`.
- Source-of-truth status is unchanged: queue membership/current index/current track remain owned by `PlaybackQueueManager`; restore gating remains in `PlaybackQueueRestoreOwner`; mirrored transition adaptation remains in `PlaybackQueueMirroredTransitionOwner`; navigation/reuse adaptation remains in `PlaybackQueueNavigationOwner`. No production code, state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, MediaSession action mapping, or package move was added.
- Concrete win: another group of wide queue public APIs is protected against call-site spread, so queue interface narrowing can keep real owners visible instead of letting `EchoPlaybackService` or a new facade become the integration point.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, forbidden facade/provider search, and `git diff --check`. Device smoke remains out of scope because this is a test-contract slice only and does not alter runtime behavior.
## NOTE 273 - Queue mutation public API call sites are contracted (2026-07-04)
- Queue/interface contract slice: `MainActivityArchitectureContractTest.playbackQueueManagerPublicApiStaysExplicitlyAudited` now pins the remaining mutation/current-replacement callers for `PlaybackQueueManager` public API. `playQueue(...)`, `appendToQueue(...)`, `removeTracksById(...)`, `retainTracksById(...)`, `moveQueueTrack(...)`, and `replaceQueuedTrackById(...)` may only be called through `PlaybackQueueMutationOwner`; `replaceCurrentTrackAndResume(...)` may only be called through `PlaybackCurrentTrackReplacementOwner`.
- Source-of-truth status is unchanged: queue membership/current index/current track remain owned by `PlaybackQueueManager`; queue membership mutation remains adapted by `PlaybackQueueMutationOwner`; current-track recovery replacement remains adapted by `PlaybackCurrentTrackReplacementOwner`. No production code, state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, MediaSession action mapping, or package move was added.
- Concrete win: the remaining wide queue mutation APIs are protected against call-site spread, which keeps the next interface-narrowing work from becoming only a Service-to-owner call-site move. This adds contract coverage without changing runtime behavior or adding another owner.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, forbidden facade/provider search, and `git diff --check`. Device smoke remains out of scope because this is a test-contract slice only and does not alter runtime behavior.
## NOTE 274 - Owner/interface audit after NOTE 271-273 (2026-07-04)
- Audit scope: this is a read-only checkpoint after the queue completion, restore/navigation, and mutation/current-replacement API contract slices. It deliberately does not add, delete, rename, move, or widen any production owner; it records whether the contract work avoided owner growth, QueueProvider return, resolver/cache facade drift, and Service wiring expansion.
- Current metrics: `EchoPlaybackService.java` is 1380 lines and `MainActivityBase.java` is 2693 lines. Current scripted counts are 36 strict `private Playback*` fields, 46 final-inclusive `private Playback*` field declarations, 16 final-inclusive `private Playback*Owner` field declarations, 39 app playback `Playback*Owner.java` files, 0 production `fromPlaybackQueueManager(...)` hits, 0 production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` hits, 0 production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` / `PlaybackCacheFacade` / `PlaybackPrecachePolicy` hits, 4 Service `queueStateSnapshotSupplier` mentions, 1 direct Service `playbackQueueManager.queueStateSnapshot()` call inside the supplier declaration, and 1 direct Service `playbackQueueManager.queueSnapshot()` call at the `NowPlayingPlaybackServicePort` boundary.
- Queue-state status: direct production `playbackQueueManager.queueStateSnapshot()` callers remain `EchoPlaybackService`, `PlaybackCrossfadeStateOwner`, `PlaybackErrorRecoveryCommandOwner`, `PlaybackLyricsStateOwner`, `PlaybackPrecacheManager`, `PlaybackQueueMirroredPlayerOwner`, `PlaybackQueueMirroredTransitionOwner`, `PlaybackQueueMutationOwner`, `PlaybackSessionCommandOwner`, and `PlaybackStateSnapshotOwner`. Direct `playbackQueueManager.queueSnapshot()` callers remain `EchoPlaybackService` and `PlaybackQueueMirroredPlayerOwner`.
- Queue public-API status: completion APIs are pinned to `PlaybackQueueCompletionOwner`; restore APIs are pinned to `PlaybackQueueRestoreOwner`; mirrored transition is pinned to `PlaybackQueueMirroredTransitionOwner`; navigation/reuse APIs are pinned to `PlaybackQueueNavigationOwner`; mutation APIs are pinned to `PlaybackQueueMutationOwner`; current-track recovery replacement is pinned to `PlaybackCurrentTrackReplacementOwner`. Source of truth is unchanged: queue membership/current index/current track remain in `PlaybackQueueManager`; the contracts prevent call-site spread while later interface narrowing is planned.
- App-shell status: `MainActivityBase.onCreate()` order remains `initializeViewModels` -> `createStreamingActionGateway()` -> `initializeStreamingPlaybackCoordinator()` -> `initializeRouteStoresAndStatus()` -> `initializeNowPlayingGateways()` -> `initializeDownloadRequests()` -> `initializeLibraryGateway()` -> startup/platform/playback/settings/streaming/stores/network/onboarding phases. The `routeController` by-value `LibraryRouteActions` argument remains after `routeController` construction and is covered by `MainActivityArchitectureContractTest.mainActivityCreatesRouteStoresAndStatusBeforeGatewayBinding`; host fields that are intentionally not initialized yet continue to use provider/lambda guards.
- Verification evidence: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain` passed before this documentation-only audit note, focused UTF-8/mojibake scan passed for `MainActivityArchitectureContractTest.java`, added-line mojibake scan passed, forbidden facade/provider search returned no hits, and `git diff --check` only reported the repository's existing CRLF normalization warnings. A full scan of this historical handoff document still reports pre-existing mojibake outside the added NOTE 274 lines; do not treat that as introduced by this checkpoint.
## NOTE 275 - Queue command owner reads queue manager directly (2026-07-04)
- Service wiring shrink slice: `PlaybackQueueCommandOwner` no longer receives `EchoPlaybackService.queueStateSnapshotSupplier`. Because the command owner is created before `PlaybackQueueManager` is assigned, it now receives only a delayed `Supplier<PlaybackQueueManager>` and reads `queueStateSnapshot()` inside the owner after the manager exists.
- Source-of-truth status is unchanged: current-track readiness for `prepareCurrent(...)`, `prepareCurrentOrRunFallback(...)`, and `runIfCurrentTrackMissing(...)` still derives from `PlaybackQueueManager.QueueStateSnapshot`; the command owner still only adapts `PlaybackQueueManager.QueuePlaybackActions` to Service prepare/publish callbacks. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, MediaSession action mapping, or package move was added.
- Concrete win: one Service snapshot-supplier forwarding site was removed from queue command wiring, and `PlaybackQueueCommandOwnerTest` now builds current-track state through a real `PlaybackQueueManager` instead of a test-only queue snapshot supplier. Notification state intentionally still uses the existing supplier and remains deferred until notification/background smoke is intentionally run.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, forbidden facade/provider search, and `git diff --check`. Device smoke remains out of scope because this only removes an internal supplier hop and does not touch notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 276 - Notification state reads queue manager directly (2026-07-04)
- Service wiring shrink slice: `PlaybackNotificationStateOwner` no longer receives `EchoPlaybackService.queueStateSnapshotSupplier`; it now receives the initialized `PlaybackQueueManager` by value and reads `queueStateSnapshot()` inside the owner. `EchoPlaybackService.currentTrackFromQueueStateSnapshot()` was also simplified to read the queue manager directly, so the Service no longer keeps a queue-state snapshot supplier field only for notification/current-track helper wiring.
- Source-of-truth status is unchanged: notification current-track and empty-queue state still derive from `PlaybackQueueManager.QueueStateSnapshot`; notification favorite lookup, session token lookup, playing state, and preparing state stay on the existing callbacks. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification action mapping, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, MediaSession action mapping, or package move was added.
- Concrete win: the remaining Service `queueStateSnapshotSupplier` field was removed entirely, taking out one notification wiring hop and one current-track helper hop without broadening notification responsibilities. `PlaybackNotificationStateOwnerTest` now builds queue state through a real `PlaybackQueueManager`, matching the production source of truth.
- Verification target: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackNotificationStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, forbidden facade/provider search, and `git diff --check`. Device smoke remains out of scope because this only removes internal queue-state supplier hops and does not alter notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 277 - Owner/interface audit after NOTE 275-276 (2026-07-04)
- Audit scope: this is a read-only checkpoint after the queue-command and notification-state queue-manager wiring shrink slices. It deliberately does not add, delete, rename, move, or widen any production owner; it records whether the batch reduced Service queue-state wiring without reopening QueueProvider, resolver/cache facade drift, or app-shell initialization risk.
- Current metrics: `EchoPlaybackService.java` is 1380 lines and `MainActivityBase.java` is 2693 lines. Current scripted counts are 36 strict `private Playback*` fields, 46 final-inclusive `private Playback*` field declarations, 16 final-inclusive `private Playback*Owner` field declarations, 39 app playback `Playback*Owner.java` files, 0 production `fromPlaybackQueueManager(...)` hits, 0 production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` hits, 0 production `PlaybackMediaSourceResolutionOwner` / `PlaybackItemResolver` / `PlaybackCacheFacade` / `PlaybackPrecachePolicy` hits, 0 Service `queueStateSnapshotSupplier` mentions, 1 direct Service `playbackQueueManager.queueStateSnapshot()` call inside `currentTrackFromQueueStateSnapshot()`, and 1 direct Service `playbackQueueManager.queueSnapshot()` call at the `NowPlayingPlaybackServicePort` boundary.
- Queue-state status: direct production `playbackQueueManager.queueStateSnapshot()` callers are now explicitly audited as `EchoPlaybackService`, `PlaybackCrossfadeStateOwner`, `PlaybackErrorRecoveryCommandOwner`, `PlaybackLyricsStateOwner`, `PlaybackNotificationStateOwner`, `PlaybackPrecacheManager`, `PlaybackQueueCommandOwner`, `PlaybackQueueMirroredPlayerOwner`, `PlaybackQueueMirroredTransitionOwner`, `PlaybackQueueMutationOwner`, `PlaybackSessionCommandOwner`, and `PlaybackStateSnapshotOwner`. Direct `playbackQueueManager.queueSnapshot()` callers remain `EchoPlaybackService` and `PlaybackQueueMirroredPlayerOwner`.
- App-shell status: `MainActivityBase.onCreate()` order remains `initializeViewModels` -> `createStreamingActionGateway()` -> `initializeStreamingPlaybackCoordinator()` -> `initializeRouteStoresAndStatus()` -> `initializeNowPlayingGateways()` -> `initializeDownloadRequests()` -> `initializeLibraryGateway()` -> startup/platform/playback/settings/streaming/stores/network/onboarding phases. This batch did not add any new by-value nullable host capture or shell initialization dependency.
- Verification evidence: `.\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCommandOwnerTest --tests app.yukine.playback.PlaybackNotificationStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain` passed across the batch, focused UTF-8/mojibake scans passed for changed source/test files, added-line mojibake scan passed, forbidden facade/provider search returned no hits, and `git diff --check` only reported the repository's existing CRLF normalization warnings. Notification/background/device smoke remains intentionally deferred because this checkpoint only changes queue-state wiring, not runtime notification behavior.
## NOTE 278 - Position manager reads queue manager directly (2026-07-04)
- Service wiring shrink slice: `PlaybackPositionManager.stateProviderFromPlaybackState(...)` no longer receives `EchoPlaybackService.this::currentTrackFromQueueStateSnapshot`. `EchoPlaybackService` now passes the initialized `PlaybackQueueManager` directly into the position manager state provider, and the feature owner reads `queueStateSnapshot().currentTrack` inside `feature:playback`.
- Source-of-truth status is unchanged: playback-position persistence still uses `PlaybackPositionManager` as the persistence policy owner, while current-track identity still comes from `PlaybackQueueManager.QueueStateSnapshot`. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, MediaSession action mapping, or package move was added.
- Concrete win: one more `EchoPlaybackService.currentTrackFromQueueStateSnapshot()` wiring site is removed, and `PlaybackPositionManagerTest` now proves late-bound current-track reads through a real `PlaybackQueueManager` instead of a test-only `Supplier<Track?>`. The remaining helper usages stay bounded to runtime-state/play-history/favorite paths for later slices.
- Verification target: `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackPositionManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, forbidden facade/provider search, and `git diff --check`. Device smoke remains out of scope because this only changes current-track wiring for position persistence and does not alter notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 279 - Runtime state reads queue manager directly (2026-07-04)
- Service wiring shrink slice: `PlaybackRuntimeStateManager.stateProviderFromPlaybackState(...)` no longer receives `EchoPlaybackService.this::currentTrackFromQueueStateSnapshot`. Because runtime state is created before `PlaybackQueueManager` is assigned, it now receives only a delayed `Supplier<PlaybackQueueManager?>` and reads `queueStateSnapshot().currentTrack` inside `feature:playback`.
- Source-of-truth status is unchanged: replay-gain/current-track volume decisions inside `PlaybackRuntimeStateManager` still derive from the current track in `PlaybackQueueManager.QueueStateSnapshot`; player/mirrored-queue state still comes from the existing player and `PlaybackQueueRuntimeStateManager` suppliers. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, MediaSession action mapping, or package move was added.
- Concrete win: one more `EchoPlaybackService.currentTrackFromQueueStateSnapshot()` wiring site is removed, and `PlaybackRuntimeStateManagerTest` now covers current-track reads through a real `PlaybackQueueManager` provider instead of a raw `Supplier<Track?>`. This keeps replay-gain state aligned with queue source-of-truth without adding another Service helper or facade.
- Verification target: `.\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackRuntimeStateManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, forbidden facade/provider search, and `git diff --check`. Device smoke remains out of scope because this only changes current-track wiring for runtime replay-gain state and does not alter notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 280 - Favorite command reads queue manager directly (2026-07-04)
- Service wiring shrink slice: `PlaybackFavoriteCommandOwner.toggleCurrentFavorite(...)` no longer receives `EchoPlaybackService.this::currentTrackFromQueueStateSnapshot`. The favorite command now reads the current track from `PlaybackQueueManager.queueStateSnapshot()` inside the owner.
- Source-of-truth status is unchanged: current-track identity still comes from `PlaybackQueueManager.QueueStateSnapshot`, while favorite persistence still stays in `ToggleFavoriteUseCase`. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, MediaSession action mapping, or package move was added.
- Concrete win: one more `EchoPlaybackService.currentTrackFromQueueStateSnapshot()` forwarding site is removed, and `PlaybackFavoriteCommandOwnerTest` now covers a real queue-manager-backed current-track read instead of a raw `Supplier<Track>`.
- Verification target: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackFavoriteCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, forbidden facade/provider search, and `git diff --check`. Device smoke remains out of scope because this only changes current-track wiring for the favorite command and does not alter notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 281 - Service current-track helper is removed from play-history and early position wiring (2026-07-04)
- Paired current-track wiring correction slice: `PlaybackPlayHistoryRecorder.recordIfPlaybackStartedAction(...)` now takes a delayed `Supplier<PlaybackQueueManager>` and reads `queueStateSnapshot().currentTrack` inside the owner, while `PlaybackPositionManager.stateProviderFromPlaybackState(...)` now also takes a delayed `Supplier<PlaybackQueueManager?>` instead of a by-value manager because the position manager is created before `playbackQueueManager` is assigned in `EchoPlaybackService`.
- Source-of-truth status is unchanged: play-history dedupe still belongs to `PlaybackTransitionStateManager`, playback-position persistence still belongs to `PlaybackPositionManager`, and current-track identity still comes from `PlaybackQueueManager.QueueStateSnapshot`. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, MediaSession action mapping, or package move was added.
- Concrete wins: the last `EchoPlaybackService.currentTrackFromQueueStateSnapshot()` usage is gone, the helper method itself is deleted, direct Service `playbackQueueManager.queueStateSnapshot()` calls are now `0`, and the position manager no longer captures an uninitialized queue-manager field by value during Service startup.
- Audit checkpoint after NOTE 279-281: `EchoPlaybackService.java` is `1373` lines by the local script, strict `private Playback*` field lines are `40`, final-inclusive `private Playback*` declarations are `61`, final-inclusive `private Playback*Owner` declarations are `21`, `EchoPlaybackService.this::currentTrackFromQueueStateSnapshot` references are `0`, and direct Service `playbackQueueManager.queueStateSnapshot()` calls are `0`.
- Verification target: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackPositionManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPlayHistoryRecorderTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, forbidden facade/provider search, and `git diff --check`. Device smoke remains out of scope because this only changes current-track wiring and Service startup binding, not notification controls, lyrics runtime, shutdown, foreground/background playback, service lifecycle, or MediaSession action mapping.
## NOTE 282 - Shutdown lifecycle owner absorbs queue-persistence mapping (2026-07-04)
- Queue API narrowing slice: `PlaybackQueueManager.persistQueueState()` is removed from the public manager API. `PlaybackShutdownLifecycleResourcesOwner.persistQueueState(...)` now persists shutdown queue state by reading `playbackQueueManager.queueSnapshot()` plus `queueStateSnapshot().currentIndex` and writing through `PlaybackQueueStore.save(...)`.
- Source-of-truth status is unchanged: queue tracks and current index still come from `PlaybackQueueManager`; shutdown persistence only maps that source-of-truth into the existing `PlaybackQueueStore` boundary. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, MediaSession action mapping, or package move was added.
- Concrete win: `PlaybackQueueManager` loses one public method, `PlaybackShutdownLifecycleResourcesOwnerTest` now proves the shutdown owner saves the actual queue snapshot and index, and the Service still avoids direct queue persistence logic.
- Verification target: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackShutdownLifecycleResourcesOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, added-line scan for this handoff entry, forbidden facade/provider search, and `git diff --check`. Device smoke remains out of scope because this only narrows queue persistence API and shutdown mapping, not notification controls, lyrics runtime, foreground/background playback, or service destruction semantics.
## NOTE 283 - Queue completion owner hides automatic-advance prep helper (2026-07-04)
- Queue owner/interface narrowing slice: `PlaybackQueueCompletionOwner.prepareStopAfterAutomaticAdvance(int)` is removed from the owner surface. `stopAfterAutomaticAdvance(int)` now performs the existing `PlaybackQueueManager.prepareStopAfterAutomaticAdvance(...)` call internally before running the stop-at-end boundary action.
- Source-of-truth status is unchanged: automatic-advance completion state still comes from `PlaybackQueueManager`, while the stop-at-end boundary action still belongs to `PlaybackQueueCompletionOwner` and existing `EchoPlaybackService` stop/release wiring. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, MediaSession action mapping, or package move was added.
- Concrete win: one forwarding-only owner method is deleted, the completion owner surface is smaller, and `PlaybackQueueCompletionOwnerTest` now covers the automatic-advance preparation path only through the real semantic entrypoint `stopAfterAutomaticAdvance(...)`.
- Audit checkpoint after NOTE 282-283: `EchoPlaybackService.java` is `1376` lines, `MainActivityBase.java` is `2693` lines, strict `private Playback*` Service field lines are `39`, app playback `Playback*Owner.java` files are `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` supplier/helper hits remain `0`, and `MainActivityBase` still initializes `initializeRouteStoresAndStatus()` before `initializeLibraryGateway()` at lines `206 -> 209`.
- Verification target: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCompletionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files, `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java`, and the existing MuMu cold-start smoke on `127.0.0.1:7555`. Device playback/notification smoke remains out of scope because this slice does not alter notification controls, lyrics runtime, shutdown behavior, foreground/background playback, or MediaSession action mapping.
## NOTE 284 - Queue preparation leaves PlaybackQueueManager public API (2026-07-04)
- Queue API narrowing slice: `PlaybackQueueManager.QueuePreparation` and `PlaybackQueueManager.queuePreparationForNewPlayer()` are removed. `PlaybackCurrentTrackPreparationQueueOwner.queuePreparationForNewPlayer()` now reads the existing `queueStateSnapshot()` plus `queueSnapshot()` directly and performs mirrored-queue preparation locally before calling the existing media-source boundary.
- Source-of-truth status is unchanged: queue membership/current index/current track still come from `PlaybackQueueManager.QueueStateSnapshot` and `PlaybackQueueManager.queueSnapshot()`. Mirrored queue URI/header resolution still belongs to `PlaybackMediaSourceProvider`; this slice only moves the preparation mapping out of the manager and into the owner that already owns current-track preparation.
- Concrete wins: `PlaybackQueueManager` loses one public method and one public data class, `PlaybackCurrentTrackPreparationQueueOwnerTest` now covers mirrored-queue preparation behavior directly, and Service preparation still only talks to `playbackCurrentTrackPreparationQueueOwner` instead of reintroducing queue-preparation logic into `EchoPlaybackService`.
- Audit checkpoint after NOTE 283-284: `EchoPlaybackService.java` remains `1376` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `39`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` supplier/helper hits remain `0`, and `PlaybackQueueManager` derived-read public API is now just `queueSnapshot()` plus `queueStateSnapshot()`.
- Verification target: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationQueueOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files and `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py feature/playback/src/main/java feature/playback/src/test/java app/src/main/java app/src/test/java`. Device playback/notification smoke remains out of scope because this slice does not alter notification controls, lyrics runtime, shutdown behavior, foreground/background playback, or MediaSession action mapping.
## NOTE 285 - Playback-restore enablement leaves PlaybackQueueManager public API (2026-07-04)
- Queue API narrowing slice: `PlaybackQueueManager.setPlaybackRestoreEnabled(...)` is removed. `PlaybackQueueManager.restorePlaybackQueue()` now reads enablement directly from `PlaybackQueueStore`, while `PlaybackQueueRestoreOwner.setPlaybackRestoreEnabled(...)` persists the setting through `PlaybackQueueStore.savePlaybackRestoreEnabled(...)`.
- Source-of-truth status is now clearer: playback-restore enablement belongs to `PlaybackQueueStore` persistence state, while queue membership/current index/current track still belong to `PlaybackQueueManager`. No new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, MediaSession action mapping, or package move was added.
- Concrete wins: `PlaybackQueueManager` loses another public method, restore-enable persistence is owned entirely by the restore owner/store boundary, and the manager no longer keeps a second cached copy of restore-enable state.
- Audit checkpoint after NOTE 283-285: `EchoPlaybackService.java` is now `1377` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `39`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` supplier/helper hits remain `0`, `PlaybackQueueManager` no longer exposes `setPlaybackRestoreEnabled(...)`, and `playbackQueueRestoreOwner()` still stays the only Service boundary for restore commands.
- Verification target: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueRestoreOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files and `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py feature/playback/src/main/java feature/playback/src/test/java app/src/main/java app/src/test/java`. Device playback/notification smoke remains out of scope because this slice does not alter notification controls, lyrics runtime, shutdown behavior, foreground/background playback, or MediaSession action mapping.
## NOTE 286 - Restore result mapping leaves PlaybackQueueManager public API (2026-07-04)
- Queue API narrowing slice: `PlaybackQueueManager.RestorePlaybackResult` and `PlaybackQueueManager.restoreLastPlayback(...)` are removed. `PlaybackQueueRestoreOwner.restoreLastPlayback(...)` now runs `restorePlaybackQueue()`, reads `queueStateSnapshot()`, and combines that with `PlaybackQueueStore.loadResumeRequested()` to decide whether to create the player, publish state, or prepare current playback.
- Source-of-truth status is now sharper: queue restore state still comes from `PlaybackQueueManager.queueStateSnapshot()`, while resume-request persistence still comes from `PlaybackQueueStore`. This slice moves only the restore-orchestration mapping to the restore owner that already owns the Android/service boundary callbacks; no new state mirror, owner, facade, bridge, resolver path, cache policy path, notification behavior, lyrics behavior, foreground/background playback behavior, service lifecycle behavior, MediaSession action mapping, or package move was added.
- Concrete wins: `PlaybackQueueManager` loses one more public method and one more public data class, restore-result decision logic is localized to `PlaybackQueueRestoreOwner`, and focused restore behavior coverage now lives on the owner boundary instead of a manager result wrapper.
- Audit checkpoint after NOTE 283-286: `EchoPlaybackService.java` remains `1377` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `39`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` supplier/helper hits remain `0`, and `PlaybackQueueManager` no longer exposes either `setPlaybackRestoreEnabled(...)` or `restoreLastPlayback(...)`.
- Verification target: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueRestoreOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus focused UTF-8/mojibake checks for changed files and `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py feature/playback/src/main/java feature/playback/src/test/java app/src/main/java app/src/test/java`. Device playback/notification smoke remains out of scope because this slice does not alter notification controls, lyrics runtime, shutdown behavior, foreground/background playback, or MediaSession action mapping.
## NOTE 287 - Error recovery owner reads queue state directly from PlaybackQueueManager (2026-07-04)
- Queue/error-recovery wiring slice: `PlaybackErrorRecoveryCommandOwner` no longer wraps `PlaybackQueueManager` in `queueStateSnapshotProvider(() -> playbackQueueManager)`. `EchoPlaybackService.onCreate()` now constructs `playbackQueueManager` first, then creates `PlaybackErrorRecoveryCommandOwner` with the manager directly.
- Source-of-truth status is unchanged: queue membership/current index/current track still come from `PlaybackQueueManager.QueueStateSnapshot`, and playback error recovery policy still belongs to `PlaybackErrorRecoveryManager`. This slice only removes one supplier/lambda forwarding chain from the app playback boundary; it does not add a facade, owner, bridge, resolver path, cache policy path, state mirror, or new runtime behavior.
- Concrete wins: one queue-state supplier wrapper is deleted, one static helper leaves `PlaybackErrorRecoveryCommandOwner`, and the Service init order now makes the queue-state dependency explicit instead of depending on a deferred provider.
- Audit checkpoint after NOTE 287: `EchoPlaybackService.java` remains `1377` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines are `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, and direct Service `queueStateSnapshot()` supplier/helper hits remain `0`.
- Verification target: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackErrorRecoveryCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java` and `git diff --check`. Device playback/notification smoke remains out of scope because this slice changes only queue/error-recovery wiring and does not alter notification controls, lyrics runtime, shutdown behavior, foreground/background playback, or MediaSession action mapping.
## NOTE 288 - Mirrored-player current-track queue-state wiring uses explicit bind (2026-07-04)
- Queue/mirrored-player wiring slice: `PlaybackQueueMirroredPlayerOwner` no longer accepts `queueStateSnapshotProvider(() -> playbackQueueManager)` for its current-track read path. `EchoPlaybackService.onCreate()` now creates the owner with the existing mirrored queue matcher, constructs `playbackQueueManager`, then calls `playbackQueueMirroredPlayerOwner.bindPlaybackQueueManager(playbackQueueManager)`.
- Source-of-truth status is unchanged: queue membership/current index/current track still come from `PlaybackQueueManager.QueueStateSnapshot`, and mirrored queue matching still uses the existing deferred `queueSnapshotProvider(() -> playbackQueueManager)` because `PlaybackQueueManager` still depends on `MirroredQueuePlayer` at construction time. This slice removes only the current-track supplier chain; it does not add a facade, owner, state mirror, resolver path, cache policy path, or new runtime behavior.
- Concrete wins: one more queue-state supplier wrapper is deleted, one more static helper leaves an app playback owner, and the Service wiring now makes one construction-cycle dependency explicit through a narrow bind step instead of hiding it behind a second deferred provider.
- Audit checkpoint after NOTE 287-288: `EchoPlaybackService.java` remains `1377` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, and direct Service `queueStateSnapshot()` supplier/helper hits remain `0`.
- Verification target: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackErrorRecoveryCommandOwnerTest --tests app.yukine.playback.PlaybackQueueMirroredPlayerOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java` and `git diff --check`. Device playback/notification smoke remains out of scope because this slice changes only queue/mirrored-player wiring and does not alter notification controls, lyrics runtime, shutdown behavior, foreground/background playback, or MediaSession action mapping.
## NOTE 289 - Queue command owner binds PlaybackQueueManager explicitly (2026-07-04)
- Queue command wiring slice: `PlaybackQueueCommandOwner` no longer accepts `Supplier<PlaybackQueueManager>` in its constructor. `EchoPlaybackService` now constructs the owner with only semantic callbacks, creates `playbackQueueManager`, then calls `playbackQueueCommandOwner.bindPlaybackQueueManager(playbackQueueManager)`.
- Source-of-truth status is unchanged: queue membership/current index/current track still come from `PlaybackQueueManager.QueueStateSnapshot`, and `PlaybackQueueCommandOwner` still only owns semantic prepare/publish delegation for queue actions. This slice removes one constructor-level supplier chain and one implicit init-order dependency; it does not add a facade, owner, state mirror, resolver path, cache policy path, or new runtime behavior.
- Concrete wins: one constructor parameter is deleted, one `Supplier<PlaybackQueueManager>` field is deleted, and Service wiring now exposes the queue-command/queue-manager construction dependency through an explicit bind step instead of a deferred supplier capture.
- Audit checkpoint after NOTE 287-289: `EchoPlaybackService.java` remains `1377` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, and direct Service `queueStateSnapshot()` supplier/helper hits remain `0`.
- Verification target: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCommandOwnerTest --tests app.yukine.playback.PlaybackErrorRecoveryCommandOwnerTest --tests app.yukine.playback.PlaybackQueueMirroredPlayerOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java` and `git diff --check`. Device playback/notification smoke remains out of scope because this slice changes only queue command wiring and does not alter notification controls, lyrics runtime, shutdown behavior, foreground/background playback, or MediaSession action mapping.
## NOTE 290 - Play-history recorder action binds PlaybackQueueManager directly (2026-07-04)
- Playback history wiring slice: `PlaybackPlayHistoryRecorder.recordIfPlaybackStartedAction(...)` no longer accepts `Supplier<PlaybackQueueManager>`. `EchoPlaybackService` now creates `playbackQueueManager` first, then builds `recordPlaybackStartHistoryAction` with the manager directly.
- Source-of-truth status is unchanged: current track still comes from `PlaybackQueueManager.QueueStateSnapshot`, and play-history persistence still belongs to `PlaybackPlayHistoryRecorder` plus `PlaybackTransitionStateManager`. This slice removes one queue-manager supplier chain and one earlier pre-manager binding site; it does not add a facade, owner, state mirror, resolver path, cache policy path, or new runtime behavior.
- Concrete wins: one `Supplier<PlaybackQueueManager>` parameter is deleted, one `Supplier` import/helper path leaves `PlaybackPlayHistoryRecorder`, and the Service history-recording wiring no longer needs to be installed before `playbackQueueManager` exists.
- Audit checkpoint after NOTE 287-290: `EchoPlaybackService.java` remains `1377` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, and direct Service `queueStateSnapshot()` supplier/helper hits remain `0`.
- Verification target: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPlayHistoryRecorderTest --tests app.yukine.playback.PlaybackQueueCommandOwnerTest --tests app.yukine.playback.PlaybackErrorRecoveryCommandOwnerTest --tests app.yukine.playback.PlaybackQueueMirroredPlayerOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java` and `git diff --check`. Device playback/notification smoke remains out of scope because this slice changes only play-history/queue wiring and does not alter notification controls, lyrics runtime, shutdown behavior, foreground/background playback, or MediaSession action mapping.
## NOTE 291 - Current-track preparation uses a direct queue replacement callback (2026-07-04)
- Current-track preparation slice: `PlaybackCurrentTrackPreparationOwner` no longer declares `QueuePreparationController`. `EchoPlaybackService` now passes `playbackCurrentTrackPreparationQueueOwner::replaceCurrentQueueTrack` directly into `fromMediaSourceProvider(...)`, and `PlaybackCurrentTrackPreparationQueueOwner` drops the forwarding interface implementation while keeping queue preparation logic local.
- Source-of-truth status is unchanged: queue membership/current index/current track still come from `PlaybackQueueManager.QueueStateSnapshot`, `PlaybackCurrentTrackPreparationQueueOwner` still owns queue preparation for mirrored-player startup, and `PlaybackMediaSourceProvider` still owns restored-track/media-source resolution. This slice removes only one pure forwarding interface from the Java playback boundary; it does not add a facade, owner, state mirror, resolver path, cache policy path, or new runtime behavior.
- Concrete wins: one single-method interface is deleted, one owner stops advertising an unnecessary public contract, and the Service wiring now exposes the actual dependency as a direct method reference instead of a wrapper type.
- Audit checkpoint after NOTE 287-291: `EchoPlaybackService.java` remains `1377` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` supplier/helper hits remain `0`, and production/test playback code now has `0` remaining `QueuePreparationController` hits outside architecture contract assertions.
- Verification target: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationOwnerTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationQueueOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java` and `git diff --check`. Device playback/notification smoke remains out of scope because this slice changes only current-track preparation wiring and does not alter notification controls, lyrics runtime, shutdown behavior, foreground/background playback, or MediaSession action mapping.
## NOTE 292 - Queue retain derivation leaves PlaybackQueueManager public API (2026-07-04)
- Queue API slice: `PlaybackQueueManager.retainTracksById(...)` is removed from the feature-module public API. `PlaybackQueueMutationOwner.retainTracksById(...)` now derives `trackIdsToRemove` from `playbackQueueManager.queueSnapshot()` and reuses the existing `removeTracksById(...)` command instead of calling a dedicated manager wrapper.
- Source-of-truth status is unchanged: queue membership/current index/current track still come from `PlaybackQueueManager` through `queueSnapshot()` and `queueStateSnapshot()`. This slice moves only a derived keep/remove calculation into the existing mutation owner that already owns semantic queue mutation commands; it does not add a facade, owner, state mirror, resolver path, cache policy path, or new runtime behavior.
- Concrete wins: one public queue-manager method is deleted, one queue mutation becomes explicitly derivable from existing queue reads plus the existing remove command, and the queue authority stays narrow instead of accumulating keep/remove convenience wrappers.
- Audit checkpoint after NOTE 287-292: `EchoPlaybackService.java` remains `1377` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` supplier/helper hits remain `0`, and production playback code now has `0` remaining `PlaybackQueueManager.retainTracksById(...)` call sites.
- Verification target: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMutationOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/playback/src/main/java feature/playback/src/test/java` and `git diff --check`. Device playback/notification smoke remains out of scope because this slice changes only queue mutation derivation and does not alter notification controls, lyrics runtime, shutdown behavior, foreground/background playback, or MediaSession action mapping.
- Completion/stop-clear audit after NOTE 292: keep `PlaybackQueueCompletionOwner` as the current stop/end boundary. `PlaybackQueueStopClearOwner` is still absent and contract-guarded, and the remaining completion methods on `PlaybackQueueManager` still perform queue/runtime state mutation rather than exposing another derivable convenience wrapper. Do not split a new stop/clear owner back out unless a later slice removes a real state source or queue-manager API.
- App-shell wiring contract after NOTE 292: `initializePlaybackLifecycleControllers()` is now explicitly contract-guarded to keep `PlaybackStateEventController(playbackStore, ...)` after `initializeRouteStoresAndStatus()`, while queue/service/status dependencies in that wiring continue to use providers or lambdas (`this::playbackQueueSnapshot`, `status -> statusMessageController.setStatus(status)`) instead of early by-value host captures.
## NOTE 293 - PlaybackPositionManager binds PlaybackQueueManager explicitly (2026-07-04)
- Position wiring slice: `PlaybackPositionManager.stateProviderFromPlaybackState(...)` no longer accepts `Supplier<PlaybackQueueManager?>`. `EchoPlaybackService` now creates a `PlaybackPositionManager.PlaybackStateProvider`, constructs `playbackPositionManager` with it, creates `playbackQueueManager`, then calls `playbackPositionStateProvider.bindPlaybackQueueManager(playbackQueueManager)`.
- Source-of-truth status is unchanged: current track for position persistence still comes from `PlaybackQueueManager.QueueStateSnapshot`, and playback position still comes from `playbackPlayerStateOwner.positionMs()`. This slice removes only one queue-manager supplier chain and one earlier implicit init-order dependency; it does not add a facade, owner, state mirror, resolver path, cache policy path, or new runtime behavior.
- Concrete wins: one `Supplier<PlaybackQueueManager?>` parameter is deleted, one queue-state read path becomes an explicit late bind instead of a deferred supplier capture, and the Service wiring now shows the queue-manager dependency the same way as the recent queue command/mirrored-player slices.
- Audit checkpoint after NOTE 287-293: `EchoPlaybackService.java` is now `1379` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` supplier/helper hits remain `0`, and production playback code now has `0` remaining `playbackQueueManagerSupplier?.get()?.queueStateSnapshot()?.currentTrack` hits in `PlaybackPositionManager`.
- Verification target: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackPositionManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/playback/src/main/java feature/playback/src/test/java` and `git diff --check`. Device playback/notification smoke remains out of scope because this slice changes only position-manager wiring and does not alter notification controls, lyrics runtime, shutdown behavior, foreground/background playback, or MediaSession action mapping.
## NOTE 294 - PlaybackRuntimeStateManager binds PlaybackQueueManager explicitly (2026-07-04)
- Runtime-state wiring slice: `PlaybackRuntimeStateManager.stateProviderFromPlaybackState(...)` no longer accepts `Supplier<PlaybackQueueManager?>`. `EchoPlaybackService` now creates a `PlaybackRuntimeStateManager.PlaybackStateProvider` at field initialization, constructs `playbackRuntimeStateManager` from it, creates `playbackQueueManager`, then calls `playbackRuntimeStateProvider.bindPlaybackQueueManager(playbackQueueManager)` inside `onCreate()`.
- Source-of-truth status is unchanged: current track for replay gain / current-track volume still comes from `PlaybackQueueManager.QueueStateSnapshot`, while player and queue-mirroring state still come from the existing player supplier and `PlaybackQueueRuntimeStateManager`. This slice removes only one queue-manager supplier chain and one implicit construction dependency; it does not add a facade, owner, state mirror, resolver path, cache policy path, or new runtime behavior.
- Concrete wins: one more `Supplier<PlaybackQueueManager?>` parameter is deleted, one more queue-state read path becomes an explicit late bind instead of a deferred supplier capture, and the Service wiring now treats runtime-state current-track access the same way as position-manager, queue-command, and mirrored-player wiring.
- Audit checkpoint after NOTE 287-294: `EchoPlaybackService.java` remains `1379` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` supplier/helper hits remain `0`, and production playback code now has `0` remaining `playbackQueueManagerSupplier?.get()?.queueStateSnapshot()?.currentTrack` hits in `PlaybackRuntimeStateManager`.
- Verification target: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackRuntimeStateManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/playback/src/main/java feature/playback/src/test/java` and `git diff --check`. Device playback/notification smoke remains out of scope because this slice changes only runtime-state wiring and does not alter notification controls, lyrics runtime, shutdown behavior, foreground/background playback, or MediaSession action mapping.
## NOTE 295 - Mirrored queue matcher queue snapshots bind PlaybackQueueManager explicitly (2026-07-04)
- Mirrored-queue wiring slice: `PlaybackQueueMirroredPlayerOwner.queueSnapshotProvider()` now returns a bindable `QueueSnapshotProvider` instead of accepting `() -> playbackQueueManager`. `EchoPlaybackService.onCreate()` creates that provider before `PlaybackQueueMirroredPlayerOwner`, passes it into `mirroredQueueMatcher(...)`, constructs `playbackQueueManager`, then calls `playbackQueueSnapshotProvider.bindPlaybackQueueManager(playbackQueueManager)`.
- Source-of-truth status is unchanged: mirrored queue membership/current index/current track still come from `PlaybackQueueManager`, player mirror state still comes from `PlaybackQueueRuntimeStateManager`, and MediaItem-vs-track matching still stays in `PlaybackMirroredQueueTrackMatcherOwner` plus `PlaybackMediaSourceProvider`. This slice removes only the last mirrored-player queue snapshot supplier chain; it does not add a facade, owner, state mirror, resolver path, cache policy path, or new runtime behavior.
- Concrete wins: one more `() -> playbackQueueManager` forwarding chain is deleted from `EchoPlaybackService`, the mirrored matcher now follows the same explicit late-bind pattern as `PlaybackQueueCommandOwner`, `PlaybackPositionManager`, and `PlaybackRuntimeStateManager`, and the still-early `PlaybackQueueMirroredPlayerOwner` wiring no longer hides an init-order dependency behind a lambda.
- Audit checkpoint after NOTE 287-295: `EchoPlaybackService.java` is now `1382` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines are `36`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, and direct Service `queueStateSnapshot()` supplier/helper hits remain `0`.
- Verification target: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMirroredPlayerOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/playback/src/main/java feature/playback/src/test/java` and `git diff --check`. Because this batch also tightened app-shell/service wiring, follow with MuMu cold-start smoke before calling the batch stable.
## NOTE 296 - Mirrored queue snapshot helper collapses back into PlaybackQueueMirroredPlayerOwner (2026-07-04)
- Mirrored-queue cleanup slice: the temporary `PlaybackQueueMirroredPlayerOwner.QueueSnapshotProvider` helper is removed. `PlaybackQueueMirroredPlayerOwner` now builds its own mirrored matcher from `this::queueSnapshot`, and `EchoPlaybackService.onCreate()` no longer needs a separate `playbackQueueSnapshotProvider` local or a second bind call for mirrored-queue snapshots.
- Source-of-truth status is unchanged: mirrored queue membership/current index/current track still come from `PlaybackQueueManager`, player mirror state still comes from `PlaybackQueueRuntimeStateManager`, and MediaItem-vs-track matching still stays in `PlaybackMirroredQueueTrackMatcherOwner` plus `PlaybackMediaSourceProvider`. This slice shortens wiring only; it does not add a facade, owner, state mirror, resolver path, cache policy path, or new runtime behavior.
- Concrete wins: one transient helper type is deleted, one `EchoPlaybackService.onCreate()` local is deleted, one `bindPlaybackQueueManager(...)` call is deleted from Service wiring, and mirrored queue snapshot matching stays inside the owner that already owns mirrored-player reuse behavior.
- Audit checkpoint after NOTE 287-296: `EchoPlaybackService.java` is now `1376` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `36`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, and direct Service `queueStateSnapshot()` supplier/helper hits remain `0`.
- Verification target: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMirroredPlayerOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/playback/src/main/java feature/playback/src/test/java` and `git diff --check`. MuMu cold-start smoke is optional for this follow-up because the previous batch already exercised startup successfully and this slice only collapses mirrored-player wiring inside the existing owner.
## NOTE 297 - PlaybackPrecacheManager owns upcoming-track derivation (2026-07-04)
- Queue/precache interface slice: `PlaybackQueueManager.upcomingTracksForPrecache(...)` is removed from the feature playback public API. `PlaybackPrecacheManager` now derives upcoming tracks from `playbackQueueManager.queueSnapshot()`, `playbackQueueManager.queueStateSnapshot()`, and `playbackRuntimeStateManager.repeatMode()` inside the existing cache policy owner.
- Source-of-truth status is explicit and unchanged at the owner level: queue membership/current index/current track still come from `PlaybackQueueManager`, repeat mode still comes from `PlaybackRuntimeStateManager`, and cache/precache scheduling policy stays in `PlaybackPrecacheManager`. This slice removes a derived queue convenience read instead of adding a facade, state mirror, or new Service policy branch.
- Concrete wins: one `PlaybackQueueManager` public method is deleted, precache-specific queue derivation moves into the owner that already decides upcoming-cache scheduling, and `EchoPlaybackService` only adds one direct runtime-state dependency to `PlaybackPrecacheManager.fromMediaSourceProvider(...)` instead of another supplier/facade layer.
- Audit checkpoint after NOTE 287-297: `EchoPlaybackService.java` is now `1377` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `36`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` supplier/helper hits remain `0`, and `PlaybackQueueManager` public API no longer contains `upcomingTracksForPrecache(...)`.
- Verification target: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/playback/src/main/java feature/playback/src/test/java` and `git diff --check`. Device smoke remains out of scope because this slice changes only queue/precache derivation and does not alter notification, lyrics, shutdown, foreground, or background playback boundaries.
## NOTE 298 - Stop preparation leaves PlaybackQueueCompletionOwner forwarding path (2026-07-04)
- Completion/service boundary slice: `PlaybackQueueCompletionOwner.prepareStopAndClearPlaybackState()` and `prepareStopAtEndOfQueue()` are removed. `EchoPlaybackService.stopAndClear()` and `stopAtEndOfQueue()` now call `playbackQueueManager.prepareStopAndClearPlaybackState()` / `prepareStopAtEndOfQueue()` directly at the lifecycle boundary, while `PlaybackQueueCompletionOwner` keeps only actual completion semantics (`playAfterCompletion()` and `stopAfterAutomaticAdvance(...)`).
- Source-of-truth status is unchanged: queue stop/clear mutation still belongs to `PlaybackQueueManager`, completion-action selection still belongs to `PlaybackQueueCompletionOwner`, and `EchoPlaybackService` only performs the lifecycle-boundary sequencing around player release, foreground teardown, and notification shutdown. This slice removes forwarding-only owner methods; it does not add a facade, owner, state mirror, resolver path, cache policy path, or new runtime behavior.
- Concrete wins: two forwarding-only owner methods are deleted, two `Service -> owner -> manager` chains become direct lifecycle-boundary calls, and `PlaybackQueueCompletionOwner` gets narrower instead of remaining a mixed completion-plus-stop-preparation bucket.
- Audit checkpoint after NOTE 287-298: `EchoPlaybackService.java` is now `1381` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `36`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, and direct Service `queueStateSnapshot()` supplier/helper hits remain `0`.
- Verification target: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCompletionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/playback/src/main/java feature/playback/src/test/java` and `git diff --check`. Device smoke remains out of scope because this slice changes only queue stop-preparation wiring and does not alter notification, lyrics, shutdown ownership, or background playback policy.
## NOTE 299 - Queue restore snapshot leaves PlaybackQueueRestoreOwner forwarding path (2026-07-04)
- Restore/service boundary slice: `PlaybackQueueRestoreOwner.restorePlaybackQueue()` is removed. `PlaybackQueueRestoreOwner.restoreLastPlayback(...)` now restores queue state directly through `playbackQueueManager.restorePlaybackQueue()`, while `EchoPlaybackService.onCreate()` restores the persisted queue snapshot at the lifecycle boundary with a direct `playbackQueueManager.restorePlaybackQueue()` call before notification/lyrics startup.
- Source-of-truth status is unchanged: queue restore state still belongs to `PlaybackQueueManager` plus `PlaybackQueueStore`, resume-request persistence still belongs to `PlaybackQueueStore`, and `PlaybackQueueRestoreOwner` keeps only the real restore semantics that decide whether to publish state, create the player, or prepare current playback. This slice removes one forwarding-only owner method; it does not add a facade, owner, state mirror, resolver path, cache policy path, or new runtime behavior.
- Concrete wins: one `PlaybackQueueRestoreOwner` method is deleted, one `Service -> owner -> manager` chain is removed from startup wiring, and the owner surface now stays focused on semantic restore behavior instead of also carrying a pass-through queue snapshot restore hook.
- Audit checkpoint after NOTE 287-299: `EchoPlaybackService.java` is now `1383` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `36`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, and direct Service `queueStateSnapshot()` supplier/helper hits remain `0`.
- Verification target: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueRestoreOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/playback/src/main/java feature/playback/src/test/java` and `git diff --check`. Device smoke remains out of scope because this slice changes only restore wiring and does not alter notification, lyrics, shutdown ownership, or background playback policy.
## NOTE 300 - Queue-manager binding batch closes remaining supplier captures in Service wiring (2026-07-04)
- Wiring batch recheck: `PlaybackQueueCommandOwner`, `PlaybackErrorRecoveryCommandOwner`, `PlaybackPlayHistoryRecorder.recordIfPlaybackStartedAction(...)`, `PlaybackPositionManager.PlaybackStateProvider`, and `PlaybackRuntimeStateManager.PlaybackStateProvider` now all rely on explicit post-construction `PlaybackQueueManager` binding or direct manager input instead of `Supplier<PlaybackQueueManager>` / `queueStateSnapshotProvider(() -> playbackQueueManager)` constructor-time capture. In the same batch, `PlaybackCurrentTrackPreparationOwner` keeps queue replacement as a direct `Consumer<Track>` callback and `PlaybackQueueMutationOwner` derives retain-by-id removals from `queueSnapshot()` plus `removeTracksById(...)` instead of re-expanding `PlaybackQueueManager` public API.
- Source-of-truth status is explicit and unchanged: queue membership/current index/current track stay in `PlaybackQueueManager` via `queueSnapshot()` and `queueStateSnapshot()`, playback position still comes from `playbackPlayerStateOwner::positionMs`, runtime replay-gain/current-track volume still use the existing player/mirror suppliers plus queue-manager current-track lookup, and no new queue or runtime state mirror is introduced.
- Concrete wins: multiple constructor-time queue-manager supplier captures are removed from `EchoPlaybackService` wiring, two feature-module state-provider factories lose `Supplier<PlaybackQueueManager?>` parameters, one direct queue-manager convenience API (`retainTracksById(...)`) stays deleted, and one forwarding-only queue replacement interface stays deleted without replacing it with a new facade.
- Batch verification: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackPositionManagerTest --tests app.yukine.playback.PlaybackRuntimeStateManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCommandOwnerTest --tests app.yukine.playback.PlaybackErrorRecoveryCommandOwnerTest --tests app.yukine.playback.PlaybackPlayHistoryRecorderTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationOwnerTest --tests app.yukine.playback.PlaybackQueueMutationOwnerTest --tests app.yukine.playback.PlaybackStateSnapshotOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain` passed, and `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/playback/src/main/java feature/playback/src/test/java` reported `TOTAL=0`.
- Audit checkpoint after NOTE 300: `EchoPlaybackService.java` is `1383` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines are `36`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, and direct Service `queueStateSnapshot()` supplier/helper hits remain `0`.
## NOTE 301 - Current-track replacement recovery no longer leaks a queue-manager wrapper type (2026-07-04)
- Current-track replacement slice: `PlaybackQueueManager.replaceCurrentTrackAndResume(...)` now returns only `restoredPositionMs` instead of a `CurrentTrackReplacementRecovery` wrapper. `PlaybackCurrentTrackReplacementOwner` keeps the same semantic command boundary but now emits `replacement + restoredPositionMs` directly to `PlaybackStreamingDiagnosticsRecorderOwner.recordRecovery(...)`, and the recovery scheduler is a plain `Runnable` that always schedules `PlaybackRecoveryScheduler.scheduleCurrentPlaybackRecovery(true)`.
- Source-of-truth status is unchanged: current-track replacement, queue mutation, and restored-position calculation still belong to `PlaybackQueueManager`; diagnostics formatting still belongs to `PlaybackStreamingDiagnosticsRecorderOwner`; playback-recovery scheduling still belongs to `PlaybackRecoveryScheduler`. This slice removes a transient result wrapper and one constant-boolean scheduler parameter; it does not add a facade, owner, state mirror, resolver path, cache policy path, or new Service strategy branch.
- Concrete wins: one queue-manager public result type is deleted, one `Consumer<Boolean>` recovery-scheduler chain collapses to `Runnable`, and streaming diagnostics recording no longer depends on a `PlaybackQueueManager.*` nested type just to receive a track and a position.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackReplacementOwnerTest --tests app.yukine.playback.PlaybackStreamingDiagnosticsRecorderOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/playback/src/main/java feature/playback/src/test/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 301: `EchoPlaybackService.java` remains `1383` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `36`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, and direct Service `queueStateSnapshot()` supplier/helper hits remain `0`.
## NOTE 302 - Mirrored transition result wrapper leaves PlaybackQueueManager public API (2026-07-04)
- Mirrored-transition slice: `PlaybackQueueManager.applyMirroredTransitionIndex(...)` no longer returns `MirroredTransitionResult`. The queue manager now returns only `Boolean?` for `stopAfterAutomaticAdvance`, while `PlaybackQueueMirroredTransitionOwner` captures the pre-transition `completedIndex` from `queueStateSnapshot()` and keeps assembling the service-facing `Transition` object at the owner boundary.
- Source-of-truth status is unchanged: queue index mutation, repeat-off stop behavior, and mirrored-transition playback-state preparation still belong to `PlaybackQueueManager`; `PlaybackQueueMirroredTransitionOwner` still owns Media3 transition-reason mapping plus the service-facing `Transition` contract; `EchoPlaybackService` still only reacts to the owner result at the lifecycle boundary.
- Concrete wins: one more queue-manager public result type is deleted, mirrored-transition queue semantics stay inside the queue manager, and the service boundary continues to receive a stable owner-level `Transition` object instead of learning more about queue internals.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMirroredTransitionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/playback/src/main/java feature/playback/src/test/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 302: `EchoPlaybackService.java` remains `1383` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `36`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, and direct Service `queueStateSnapshot()` supplier/helper hits remain `0`.
## NOTE 303 - playFirstQueuedTrack is not mechanically equivalent to playQueue(queueSnapshot(), 0, ...) (2026-07-04)
- Navigation API audit slice: `PlaybackQueueManager.playFirstQueuedTrack()` is intentionally retained for now. The method prepares playback from index `0` but does **not** overwrite the persisted `resumeRequested` flag, so it is not behaviorally equivalent to `playQueue(queueSnapshot(), 0, ...)`, which always saves `resumeRequested=true`.
- Source-of-truth status is unchanged: the persisted resume flag still belongs to `PlaybackQueueStore`, queue index/current track still belong to `PlaybackQueueManager`, and `PlaybackQueueNavigationOwner` remains the only production caller of `playFirstQueuedTrack()`. This slice adds behavior evidence only; it does not add a facade, owner, state mirror, or new runtime path.
- Concrete win: a focused regression test now protects a migration trap that would silently change queue-restore semantics if `playFirstQueuedTrack()` were replaced by a broader queue command.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest --console=plain`, and the existing UTF-8/mojibake scan remains `TOTAL=0`.
## NOTE 304 - Feature modules are contract-blocked from importing app playback implementations (2026-07-05)
- Dependency-direction contract slice: `MainActivityArchitectureContractTest` now rejects `feature` main-source imports of `app.yukine.playback.EchoPlaybackService` and `app.yukine.playback.Playback*Owner`. This closes a gap in the previous string whitelist, which already blocked specific app implementation names but did not explicitly catch future playback-owner imports via a wildcard pattern.
- Source-of-truth status is unchanged: this is a contract-only slice. No playback state owner, queue owner, or app-shell owner changed, and no new facade, state mirror, or runtime behavior was introduced.
- Concrete win: the architecture test now directly protects the intended dependency direction for future playback extraction work, so feature code cannot quietly reattach to app-layer playback implementation classes while the package names remain similar.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, and the UTF-8/mojibake scan remains `TOTAL=0`.
## NOTE 305 - Queue stop preparation is back behind PlaybackQueueCompletionOwner (2026-07-05)
- Completion-boundary slice: `PlaybackQueueCompletionOwner` now owns `stopAndClearPlayback()` and `stopAtEndOfQueue()` queue preparation, and `EchoPlaybackService` now contributes only Android/Media3 boundary actions through `stopAndClearBoundary()` / `stopAtEndOfQueueBoundary()`. `playAfterCompletion()` and `stopAfterAutomaticAdvance(...)` stay in the same focused owner, so this does not add a new facade, owner, or state mirror.
- Source-of-truth status is unchanged: queue clearing, current-index retention, persisted `resumeRequested`, and runtime `preparing` reset still belong to `PlaybackQueueManager`; `EchoPlaybackService` only handles player release, foreground shutdown, player pause/seek, and state publication at the boundary.
- Concrete wins: two direct `EchoPlaybackService -> PlaybackQueueManager` queue-preparation calls are removed, queue completion/stop behavior is re-centered on the existing completion owner instead of the service lifecycle class, and `PlaybackQueueCompletionOwnerTest` now directly protects `stopAndClearPlayback()` plus `stopAtEndOfQueue()`.
- App-shell/UTF-8 re-audit status: the current worktree still initializes `initializeRouteStoresAndStatus()` before `initializeNowPlayingGateways()`, `initializeLibraryGateway()`, and `initializePlaybackLifecycleControllers()`, and the focused mojibake scan over `app/src/main/java`, `app/src/test/java`, and `feature/ui-common/src/main/java` remains `TOTAL=0`.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCompletionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 305: `EchoPlaybackService.java` is `1381` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `36`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, and direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`.
## NOTE 306 - Now-playing queue visibility is reduced to a semantic hasQueue boundary (2026-07-05)
- Now-playing boundary slice: `NowPlayingPlaybackGateway` and `NowPlayingPlaybackServicePort` no longer expose raw `queueSnapshot(): List<Track>` just so `NowPlayingViewModel.hasQueue()` can answer a boolean. That path now uses `hasQueue()` instead. `PlaybackServiceHostPort` still keeps `queueSnapshot()` because `MainActivityBase` and `MainStreamingPlaybackListener` still need the full queue snapshot for the existing app-shell/streaming path.
- Source-of-truth status is unchanged: queue membership, current index, and current track still belong to `PlaybackQueueManager`; `EchoPlaybackService` remains a service boundary and now answers `hasQueue()` by delegating to its existing queue boundary; `NowPlayingViewModel` no longer reads a raw queue list from the service boundary for this case.
- Concrete wins: one raw queue-read method is removed from the now-playing service/gateway contract, `NowPlayingViewModel.hasQueue()` becomes semantic instead of list-based, and the remaining raw queue exposure is narrowed to the host path that still genuinely needs queue contents.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.NowPlayingPlaybackGatewayAdapterTest --tests app.yukine.NowPlayingViewModelTest --tests app.yukine.QueueActionControllerTest --tests app.yukine.PlaybackActionControllerTest --tests app.yukine.MainPlaybackServiceHostTest --tests app.yukine.MainNetworkActionsListenerTest --tests app.yukine.MainNowPlayingGatewayTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 306: `EchoPlaybackService.java` is `1385` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `36`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, and current Service `queueSnapshot()` literal hits are `3`.
## NOTE 307 - Streaming queue reads now come from MainPlaybackStore, not the service boundary (2026-07-05)
- Streaming pre-resolve wiring slice: `MainPlaybackStore` now exposes `queueSnapshot()`, and `MainActivityBase` wires `StreamingPlaybackController` through `() -> playbackStore.queueSnapshot()` instead of `this::playbackQueueSnapshot`. `PlaybackStateEventController` still reads queue from the service boundary because it is the path that refreshes `MainPlaybackStore` with the latest service queue.
- Source-of-truth status is unchanged: queue authority still belongs to `PlaybackQueueManager`, `PlaybackStateEventController` still republishes that queue into `PlaybackViewModel`, and `StreamingPlaybackController` now reads the already-published `PlaybackViewState.queue` instead of doing another `Activity -> service` round-trip.
- Concrete wins: one `MainActivityBase -> EchoPlaybackService.queueSnapshot()` usage is removed, the streaming pre-resolve path now consumes the app-shell playback store that already owns UI-visible playback state, and the contract test now distinguishes the service-fed queue refresh path from the store-fed streaming consumer path.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainStreamingPlaybackListenerTest --tests app.yukine.StreamingPlaybackControllerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 307: `EchoPlaybackService.java` remains `1385` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `36`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, and current Service `queueSnapshot()` literal hits remain `3`.
## NOTE 308 - Current-track preparation queue owner no longer forwards replaceCurrentQueueTrack (2026-07-05)
- Preparation callback slice: `PlaybackCurrentTrackPreparationQueueOwner.replaceCurrentQueueTrack(...)` is deleted. `PlaybackCurrentTrackPreparationOwner` still receives a current-queue replacement callback, but `EchoPlaybackService` now supplies that callback directly with the existing `PlaybackQueueManager.replaceCurrentQueueTrack(...)` null-guard, while `PlaybackCurrentTrackPreparationQueueOwner` keeps only mirrored-queue preparation behavior.
- Source-of-truth status is unchanged: current-slot replacement still belongs to `PlaybackQueueManager`, restored-track preparation still belongs to `PlaybackCurrentTrackPreparationOwner`, and mirrored queue/media-source preparation still belongs to `PlaybackCurrentTrackPreparationQueueOwner`. No new state mirror or facade is introduced.
- Concrete wins: one pure forwarding owner method is removed, one `service -> queue-preparation owner -> queue manager` callback hop disappears, and the equivalent behavior coverage now sits where it belongs: `PlaybackCurrentTrackPreparationOwnerTest` verifies the replacement callback is invoked for restored tracks, while `PlaybackQueueManagerTest` keeps the current-slot persistence assertions.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationOwnerTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationQueueOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/playback/src/main/java feature/playback/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 308: `EchoPlaybackService.java` is `1389` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `36`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, and current Service `queueSnapshot()` literal hits remain `3`.
## NOTE 309 - Queue screen UI now reads MainPlaybackStore queue instead of the service boundary (2026-07-05)
- App-shell queue-consumer slice: `bindQueueViewModelInputs()` and `renderQueue()` in `MainActivityBase` now read `playbackStore.queueSnapshot()` instead of `playbackQueueSnapshot()`. This leaves `playbackQueueSnapshot()` only for store refresh and the unresolved-streaming resolve path, while pure UI consumers read the already-published app-shell playback state.
- Source-of-truth status is unchanged: queue authority still belongs to `PlaybackQueueManager`; `PlaybackStateEventController` still refreshes `PlaybackViewModel` from the service boundary; `MainPlaybackStore.queueSnapshot()` now serves as the stable app-shell read model for queue UI consumers.
- Concrete wins: two more pure UI `MainActivityBase -> playbackService.queueSnapshot()` uses are removed, queue UI reads are now aligned with the existing `PlaybackViewModel/MainPlaybackStore` source of truth, and the remaining `playbackQueueSnapshot()` helper uses are narrowed to store refresh plus the unresolved-streaming resolve path.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 309: `EchoPlaybackService.java` remains `1389` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `36`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, current Service `queueSnapshot()` literal hits remain `3`, and `MainActivityBase.playbackQueueSnapshot()` call sites are now `3`.
## NOTE 310 - Service hasQueue now uses playback snapshot state, not a raw queue list (2026-07-05)
- Service queue-visibility slice: `EchoPlaybackService.hasQueue()` now returns `snapshot().queueSize > 0` instead of reading `queueSnapshot().isEmpty()`. The host port still exposes `queueSnapshot()` for the separate host/store refresh path that genuinely needs queue contents, but the semantic `hasQueue()` path now stays on the existing playback snapshot contract all the way through `NowPlayingPlaybackGatewayAdapter` and `NowPlayingViewModel`.
- Source-of-truth status is unchanged: queue membership, current index, and queue size still belong to `PlaybackQueueManager`; `PlaybackStateSnapshotOwner` remains the service boundary that maps that queue state into UI/service-readable playback state; no new queue mirror or facade is introduced.
- Concrete wins: one more raw `EchoPlaybackService -> queueSnapshot()` read is removed, the service-side `hasQueue()` query now consumes the same semantic playback snapshot contract already used by now-playing UI state, and `MainActivityArchitectureContractTest` now explicitly blocks regression to the old list-based check.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --tests app.yukine.NowPlayingPlaybackGatewayAdapterTest --tests app.yukine.NowPlayingViewModelTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 310: `EchoPlaybackService.java` remains `1389` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `36`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, current Service `queueSnapshot()` literal hits drop to `2`, and `MainActivityBase.playbackQueueSnapshot()` call sites remain `3`.
## NOTE 311 - Current streaming queue resolve now reads MainPlaybackStore, not the service queue helper (2026-07-05)
- App-shell unresolved-streaming slice: `resolveCurrentStreamingQueueTrackIfNeeded()` in `MainActivityBase` now passes `playbackStore.queueSnapshot()` into `StreamingViewModel.prepareCurrentStreamingQueueResolveTarget(...)` instead of `playbackQueueSnapshot()`. The helper itself stays only for the two genuine service-queue refresh paths: `PlaybackStateEventController` store republish and `publishPlaybackStore()`.
- Source-of-truth status is unchanged: queue authority still belongs to `PlaybackQueueManager`; `PlaybackStateEventController` still republishes the live service queue into `PlaybackViewModel/MainPlaybackStore`; unresolved streaming resolution now consumes that already-published app-shell queue state instead of doing another host -> service queue read.
- Concrete wins: one more `MainActivityBase -> playbackService.queueSnapshot()` use is removed from a user action path, the unresolved streaming retry path now aligns with the same app-shell playback store consumed by queue UI and streaming pre-resolve, and the architecture contract now blocks regression to `playbackQueueSnapshot()` in `resolveCurrentStreamingQueueTrackIfNeeded()`.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --tests app.yukine.StreamingViewModelTest --tests app.yukine.MainPlaybackActionListenerTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 311: `EchoPlaybackService.java` remains `1389` lines, `MainActivityBase.java` remains `2693` lines, current Service `queueSnapshot()` literal hits remain `2`, `MainActivityBase.playbackQueueSnapshot()` call sites drop to `2`, and direct `MainActivityBase` `playbackService.queueSnapshot()` occurrences remain `1`.
## NOTE 312 - Queue replacement no longer returns a stop-and-clear flag (2026-07-05)
- Queue API narrowing slice: `PlaybackQueueManager.replaceQueuedTrackById(...)` no longer returns `Boolean`, and `PlaybackQueueMutationOwner.replaceQueuedTrackById(...)` no longer branches on that result to run `stopAndClear()`. Replacement/collapse behavior stays inside `PlaybackQueueManager`; `PlaybackQueueMutationOwner` now simply delegates the semantic replace command.
- Source-of-truth status is unchanged: queue contents and current-index authority still belong to `PlaybackQueueManager`; `PlaybackQueueMutationOwner` remains only the service/UI command owner for queue mutations; no new state mirror or facade is introduced.
- Concrete wins: one public queue-manager command loses a dead return-value contract, one `PlaybackQueueMutationOwner` stop-and-clear branch is deleted, and a focused regression test now proves collapsing a replacement onto an already-queued track keeps a non-empty queue instead of triggering `stopAndClear()`.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMutationOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/playback/src/main/java feature/playback/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 312: `EchoPlaybackService.java` remains `1389` lines, `MainActivityBase.java` remains `2693` lines, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, `MainActivityBase.playbackQueueSnapshot()` call sites remain `2`, and direct `MainActivityBase` `playbackService.queueSnapshot()` occurrences remain `1`.
## NOTE 313 - Queue removal no longer returns a stop-and-clear flag (2026-07-05)
- Queue API narrowing slice: `PlaybackQueueManager.removeTracksById(...)` no longer returns `Boolean`. `PlaybackQueueMutationOwner.removeTracksById(...)` and `retainTracksById(...)` now derive the stop-and-clear case locally by comparing `queueStateSnapshot().isQueueEmpty()` before and after the mutation, instead of interpreting a queue-manager return flag.
- Source-of-truth status is unchanged: queue contents and current-index authority remain with `PlaybackQueueManager`; `PlaybackQueueMutationOwner` still owns the semantic mutation command path and the service-boundary `stopAndClear()` trigger, but it no longer depends on a special return contract from the queue manager.
- Concrete wins: one more public queue-manager command loses a migration-era return-value contract, two owner call sites stop branching on `PlaybackQueueManager.removeTracksById(...)`, and the focused regression coverage still proves stop-and-clear only happens when the queue actually becomes empty.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMutationOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/playback/src/main/java feature/playback/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 313: `EchoPlaybackService.java` remains `1389` lines, `MainActivityBase.java` remains `2693` lines, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, `MainActivityBase.playbackQueueSnapshot()` call sites remain `2`, and direct `MainActivityBase` `playbackService.queueSnapshot()` occurrences remain `1`.
## NOTE 314 - Current-track queue manager mutations now require non-null Track (2026-07-05)
- Queue nullability slice: `PlaybackQueueManager.replaceCurrentTrackAndResume(...)` and `replaceCurrentQueueTrack(...)` now take `Track` instead of `Track?`. UI/ViewModel entry points still keep their own nullable guards, but the feature queue owner no longer accepts null placeholders on these current-track mutation APIs.
- Source-of-truth status is unchanged: current-slot queue mutation and replacement recovery semantics still belong to `PlaybackQueueManager`; `PlaybackCurrentTrackReplacementOwner` and the current-track preparation callback remain the only semantic command owners on top of it.
- Concrete wins: two queue-manager APIs lose nullable parameters, one Java/Kotlin boundary gets narrower for current-track replacement/preparation, and focused tests still prove current-slot persistence and replacement recovery behavior without any null-based manager contract.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackReplacementOwnerTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationQueueOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/playback/src/main/java feature/playback/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 314: `EchoPlaybackService.java` remains `1389` lines, `MainActivityBase.java` remains `2693` lines, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, `MainActivityBase.playbackQueueSnapshot()` call sites remain `2`, and direct `MainActivityBase` `playbackService.queueSnapshot()` occurrences remain `1`.
## NOTE 315 - Mirrored transition validation now lives in the transition owner (2026-07-05)
- Mirrored-transition API slice: `PlaybackQueueManager.applyMirroredTransitionIndex(...)` now returns plain `Boolean` for stop/not-stop. `PlaybackQueueMirroredTransitionOwner.applyMirroredTransitionReason(...)` now owns the invalid/no-op checks (`nextIndex` bounds and same-index short circuit) before calling the queue manager, instead of depending on a tri-state `Boolean?` contract from the manager.
- Source-of-truth status is unchanged: queue index/current-track authority still belongs to `PlaybackQueueManager`; `PlaybackQueueMirroredTransitionOwner` remains the semantic owner that interprets Media3 transition reasons and decides whether a transition should be ignored before applying queue mutation.
- Concrete wins: one more queue-manager API loses a tri-state return contract, the invalid/no-op transition semantics move to the owner that already has `completedIndex` and `queueSize`, and focused regression coverage now proves those ignored transitions return `null` from the owner without mutating queue state or applying volume.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMirroredTransitionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/playback/src/main/java feature/playback/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 315: `EchoPlaybackService.java` remains `1389` lines, `MainActivityBase.java` remains `2693` lines, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, `MainActivityBase.playbackQueueSnapshot()` call sites remain `2`, and direct `MainActivityBase` `playbackService.queueSnapshot()` occurrences remain `1`.
## NOTE 316 - Queue completion action no longer uses nullable null-for-repeat semantics (2026-07-05)
- Completion API narrowing slice: `PlaybackQueueManager.preparePlaybackCompletionAction()` now always returns a `PlaybackCompletionAction`, with explicit `REPEAT_CURRENT` instead of `null`. `PlaybackQueueCompletionOwner.playAfterCompletion()` now treats that enum case as the no-boundary path, instead of relying on a nullable completion action contract.
- Source-of-truth status is unchanged: queue completion decision logic still belongs to `PlaybackQueueManager`; `PlaybackQueueCompletionOwner` remains the semantic owner that maps those decisions onto `stopAndClear`, `stopAtEnd`, or `skipNext` service-boundary actions.
- Concrete wins: one more queue-manager API loses a nullable return contract, one owner path stops branching on `null`, and focused tests still prove repeat-one completion prepares current playback without triggering any boundary action.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCompletionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/playback/src/main/java feature/playback/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 316: `EchoPlaybackService.java` remains `1389` lines, `MainActivityBase.java` remains `2693` lines, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, `MainActivityBase.playbackQueueSnapshot()` call sites remain `2`, and direct `MainActivityBase` `playbackService.queueSnapshot()` occurrences remain `1`.
## NOTE 317 - Notification and lyrics state owners no longer capture a null queue manager during service startup (2026-07-05)
- Service wiring slice: `EchoPlaybackService` now constructs `PlaybackQueueManager` before creating `PlaybackNotificationStateOwner` and `PlaybackLyricsStateOwner`, so those owners no longer capture the still-null field by value during startup. The two owners now also require a non-null `PlaybackQueueManager` and read queue state directly from it instead of carrying local null-to-empty fallback helpers.
- Source-of-truth status is unchanged: queue authority still belongs to `PlaybackQueueManager`; notification and lyrics state owners remain read-only adapters that project `currentTrack` / `isQueueEmpty` into `PlaybackNotificationManager` and `PlaybackLyricsManager`.
- Concrete wins: two startup-time by-value null captures are removed from `EchoPlaybackService`, two owner-level null-fallback helper branches are deleted, and `MainActivityArchitectureContractTest` now blocks regression by asserting `playbackQueueManager` is constructed before both state owners.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackLyricsStateOwnerTest --tests app.yukine.playback.PlaybackNotificationStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`, and MuMu smoke on `127.0.0.1:16480` via `.\\gradlew.bat :app:assembleDebug --console=plain`, `adb -s 127.0.0.1:16480 install -r app\\build\\outputs\\apk\\debug\\app-debug.apk`, `adb -s 127.0.0.1:16480 shell monkey -p app.yukine 1`, plus a post-launch `logcat` filter with no `FATAL EXCEPTION` / `AndroidRuntime` hits and running PID `2773`.
- Audit checkpoint after NOTE 317: `EchoPlaybackService.java` is `1390` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines are `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and `playbackQueueManager` construction now appears before both notification/lyrics state-owner constructions in `EchoPlaybackService`.
## NOTE 318 - Error-recovery command owner now requires a real queue manager (2026-07-05)
- Queue nullability slice: `PlaybackErrorRecoveryCommandOwner` now requires a non-null `PlaybackQueueManager` and reads queue state directly from it, instead of carrying a null-to-empty `QueueStateSnapshot` fallback. `EchoPlaybackService` was already constructing this owner only after `playbackQueueManager`, so this change removes migration residue rather than altering lifecycle shape.
- Source-of-truth status is unchanged: failed-track skip eligibility and current-track debug state still come from `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackErrorRecoveryCommandOwner` remains the semantic boundary that maps recovery logic onto `prepareCurrent`, `skipToNext`, `setErrorMessage`, and `publishState`.
- Concrete wins: one more Java owner loses a nullable queue-manager contract, one owner-local empty-snapshot fallback helper is deleted, and focused tests now lock the constructor contract instead of treating missing queue state as a valid runtime mode.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackErrorRecoveryCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 318: `EchoPlaybackService.java` remains `1390` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 319 - Session command owner no longer treats a missing queue manager as valid state (2026-07-05)
- Queue nullability slice: `PlaybackSessionCommandOwner` now requires a non-null `PlaybackQueueManager` and reads `queueStateSnapshot()` directly instead of converting a missing manager into `QueueStateSnapshot.empty()`. The session command owner is created as a local `onCreate()` value after `playbackQueueManager`, so this deletes a migration-era null contract without changing MediaSession wiring shape.
- Source-of-truth status is unchanged: current-track state for MediaSession commands still belongs to `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackSessionCommandOwner` remains only the delegate that maps MediaSession callbacks onto playback commands, repeat/seek controls, controller media items, and metadata lookup.
- Concrete wins: one more local service owner loses a nullable queue-manager contract, one owner-level empty-snapshot fallback disappears, and focused tests now lock constructor non-nullability while keeping the empty-queue `currentTrack() == null` behavior intact.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackSessionCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 319: `EchoPlaybackService.java` remains `1390` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 320 - Queue restore owner now requires a real queue manager (2026-07-05)
- Queue nullability slice: `PlaybackQueueRestoreOwner` now requires a non-null `PlaybackQueueManager`, calls `restorePlaybackQueue()` directly, and reads `queueStateSnapshot()` directly instead of converting a missing manager into `QueueStateSnapshot.empty()`. The service still creates this owner lazily through `playbackQueueRestoreOwner()`, but every production call site already passes the real `playbackQueueManager`.
- Source-of-truth status is unchanged: restore queue contents/current index still belong to `PlaybackQueueManager`, persisted restore-enabled and resume-requested flags still belong to `PlaybackQueueStore`, and `PlaybackQueueRestoreOwner` remains only the service-boundary restore coordinator that decides whether to create the player, publish state, or prepare current playback.
- Concrete wins: one more queue restore owner loses a nullable queue-manager contract, the fake “missing manager means publish empty state” fallback path is deleted, and focused tests now lock constructor non-nullability instead of encoding a migration-only runtime mode.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueRestoreOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 320: `EchoPlaybackService.java` remains `1390` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 321 - Playback snapshot owner no longer treats a missing queue manager as a valid empty queue (2026-07-05)
- Queue nullability slice: `PlaybackStateSnapshotOwner` now requires a non-null `PlaybackQueueManager` and reads `queueStateSnapshot()` directly instead of converting a missing manager into `QueueStateSnapshot.empty()`. The service already constructs this owner only after `playbackQueueManager`, so the slice removes a fake empty-queue mode without changing the service-level `snapshot()` fallback that still guards a missing `playbackStateSnapshotOwner` field.
- Source-of-truth status is unchanged: `PlaybackQueueManager.QueueStateSnapshot` remains the single source for `currentTrack`, `currentIndex`, and `queueSize`; `PlaybackStateSnapshotOwner` still only merges that queue state with playback position, runtime state, visualization, sleep timer, and realtime beat.
- Concrete wins: one more direct-construction service owner loses a nullable queue-manager contract, one owner-local empty-snapshot fallback is deleted, and focused tests now require a real empty queue manager when verifying default snapshot pieces.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackStateSnapshotOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 321: `EchoPlaybackService.java` remains `1390` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 322 - Current-track preparation queue owner no longer treats a missing queue manager as an empty queue (2026-07-05)
- Queue nullability slice: `PlaybackCurrentTrackPreparationQueueOwner` now requires a non-null `PlaybackQueueManager` and reads `queueSnapshot()` / `queueStateSnapshot()` directly instead of converting a missing manager into empty queue state. The service already constructs this owner after `playbackQueueManager`, so the slice removes another migration-only fake state path without changing media-source resolution ownership.
- Source-of-truth status is unchanged: current queue contents/current index still belong to `PlaybackQueueManager`; `PlaybackCurrentTrackPreparationQueueOwner` still only maps that queue through `PlaybackMediaSourceProvider` restore rules and mirrored queue media-source preparation.
- Concrete wins: one more direct-construction owner loses a nullable queue-manager contract, two owner-local empty fallbacks (`Collections.emptyList()` and `QueueStateSnapshot.empty()`) are deleted, and focused tests now encode constructor non-nullability instead of accepting a missing manager as a valid preparation mode.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationQueueOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 322: `EchoPlaybackService.java` remains `1390` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 323 - Crossfade state owner now requires a real queue manager (2026-07-05)
- Queue nullability slice: `PlaybackCrossfadeStateOwner` now requires a non-null `PlaybackQueueManager` and reads `queueStateSnapshot()` directly instead of converting a missing manager into `QueueStateSnapshot.empty()`. The service already constructs this owner after `playbackQueueManager`, so this is another direct-construction residue cleanup rather than a lifecycle reshaping.
- Source-of-truth status is unchanged: queue size/current-index authority for crossfade-advance eligibility still belongs to `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackCrossfadeStateOwner` still only combines that queue state with repeat mode, player availability, playback state, and base volume.
- Concrete wins: one more direct-construction owner loses a nullable queue-manager contract, one owner-local empty-snapshot fallback is deleted, and focused tests now lock constructor non-nullability while preserving the real single-track and repeat-off-at-end crossfade rules.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCrossfadeStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 323: `EchoPlaybackService.java` remains `1390` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 324 - Play-history action now requires a real queue manager when recorder wiring exists (2026-07-05)
- Queue nullability slice: `PlaybackPlayHistoryRecorder.recordIfPlaybackStartedAction(...)` now still returns a no-op when the recorder itself is absent, but once a recorder is present it requires a non-null `PlaybackQueueManager` and reads `queueStateSnapshot()` directly instead of converting a missing manager into a null current track. The service already wires this action only after `playbackQueueManager`, so this removes another fake missing-manager runtime mode without widening the event path.
- Source-of-truth status is unchanged: current-track authority for play-history recording still belongs to `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackPlayHistoryRecorder` still only decides whether a started track should be marked played and deduped against `PlaybackTransitionStateManager.lastMarkedTrack()`.
- Concrete wins: one more direct queue-state consumer loses a nullable queue-manager contract, one static helper no longer treats “missing manager” as a normal no-op case, and focused tests now lock that contract while preserving the explicit `recorder == null` no-op.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPlayHistoryRecorderTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 324: `EchoPlaybackService.java` remains `1390` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 325 - Favorite command owner now requires a real queue manager (2026-07-05)
- Queue nullability slice: `PlaybackFavoriteCommandOwner.toggleCurrentFavorite(...)` now requires a non-null `PlaybackQueueManager` and reads `queueStateSnapshot()` directly through `Objects.requireNonNull(...)` instead of treating a missing manager as a valid “no current track” runtime mode. The service already calls this owner only after `playbackQueueManager` initialization, so the slice removes migration residue without changing favorite policy or wiring shape.
- Source-of-truth status is unchanged: current-track authority for favorite toggling still belongs to `PlaybackQueueManager.QueueStateSnapshot`; `ToggleFavoriteUseCase` still owns persistence and favorite-state mutation semantics.
- Concrete wins: one more direct queue-state consumer loses a nullable queue-manager contract, one favorite-command helper no longer converts missing queue state into a silent no-op path, and focused tests now lock the non-null boundary while preserving the real empty-queue `currentTrack() == null` behavior.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackFavoriteCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 325: `EchoPlaybackService.java` remains `1390` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 326 - Queue command owner no longer treats a missing queue manager as an empty queue (2026-07-05)
- Queue nullability slice: `PlaybackQueueCommandOwner.bindPlaybackQueueManager(...)` now requires a non-null `PlaybackQueueManager`, and `queueStateSnapshot()` now reads directly through `Objects.requireNonNull(...)` instead of converting a missing binding into `QueueStateSnapshot.empty()`. Production service wiring already binds the owner immediately after queue-manager construction, so this removes a fake runtime mode rather than changing queue-command behavior.
- Source-of-truth status is unchanged: current-track authority for `prepareCurrent(...)` and `runIfCurrentTrackMissing(...)` still belongs to `PlaybackQueueManager.QueueStateSnapshot`; `PlaybackQueueCommandOwner` still only maps that queue state onto `playbackPreparer`, `statePublisher`, and the fallback callback.
- Concrete wins: one more bindable queue consumer loses a nullable queue-manager contract, one owner-local empty-snapshot fallback is deleted, and focused tests now distinguish the real empty-queue case from the invalid “owner used before binding” case.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 326: `EchoPlaybackService.java` remains `1390` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 327 - Mirrored-player owner no longer treats a missing queue manager as empty queue state (2026-07-05)
- Queue nullability slice: `PlaybackQueueMirroredPlayerOwner.bindPlaybackQueueManager(...)` now requires a non-null `PlaybackQueueManager`, and both `queueSnapshot()` and `queueStateSnapshot()` now read directly through `Objects.requireNonNull(...)` instead of converting a missing binding into `Collections.emptyList()` or `QueueStateSnapshot.empty()`. Production service wiring already binds the owner immediately after queue-manager construction, so this removes another migration-only fake runtime mode without changing mirrored-queue reuse policy.
- Source-of-truth status is unchanged: mirrored queue contents, current track, and current index still belong to `PlaybackQueueManager`; `PlaybackQueueMirroredPlayerOwner` still only decides whether the existing player queue matches and whether a mirrored `seekTo(...)` can reuse it.
- Concrete wins: one more bindable queue consumer loses both fake empty-list and fake empty-snapshot fallbacks, one more cross-Java/Kotlin non-null boundary is locked by focused tests, and the mirrored-player tests now distinguish a real empty queue from an invalid unbound owner.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMirroredPlayerOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 327: `EchoPlaybackService.java` remains `1390` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 328 - Mirrored-transition owner now requires a real queue manager (2026-07-05)
- Queue nullability slice: `PlaybackQueueMirroredTransitionOwner` now requires a non-null `PlaybackQueueManager` in its constructor and reads `queueStateSnapshot()` directly instead of converting a missing manager into `QueueStateSnapshot.empty()` or treating `null` as a valid early-return runtime mode. Production service wiring already constructs this owner with the real queue manager inside the Media3 transition callback path, so this removes another fake migration state without changing transition interpretation.
- Source-of-truth status is unchanged: mirrored transition queue contents/current index/current track still belong to `PlaybackQueueManager`; `PlaybackQueueMirroredTransitionOwner` still only decides whether the transition is ignorable, whether it should stop after automatic advance, and whether current-track volume should be re-applied.
- Concrete wins: one more direct-construction queue consumer loses a nullable queue-manager contract, one more owner-local empty-snapshot fallback disappears, and focused tests now distinguish the real empty-queue `canApplyMirroredTransition() == false` case from the invalid `null` constructor case.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMirroredTransitionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 328: `EchoPlaybackService.java` remains `1390` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 329 - Queue mutation owner no longer treats a missing queue manager as empty queue state (2026-07-05)
- Queue nullability slice: `PlaybackQueueMutationOwner` now requires a non-null `PlaybackQueueManager` in its constructor and reads `queueStateSnapshot()` directly instead of converting a missing manager into `QueueStateSnapshot.empty()`. Production service wiring already creates this owner through `EchoPlaybackService.playbackQueueMutationOwner()` with the real queue manager, so this removes another migration-only fake runtime mode without changing queue mutation semantics.
- Source-of-truth status is unchanged: queue contents/current index/current track still belong to `PlaybackQueueManager`; `PlaybackQueueMutationOwner` still only maps semantic queue mutations onto queue-manager commands plus the existing `stopAndClear()` service boundary when a mutation empties the queue.
- Concrete wins: one more direct-construction queue consumer loses a nullable queue-manager contract, one owner-local empty-snapshot fallback disappears, and focused tests now distinguish invalid `null` construction from the real empty-input no-op cases.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMutationOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 329: `EchoPlaybackService.java` remains `1390` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 330 - Current-track replacement owner now requires a real queue manager (2026-07-05)
- Queue nullability slice: `PlaybackCurrentTrackReplacementOwner` now requires a non-null `PlaybackQueueManager` in its constructor instead of treating a missing manager as a silent no-op in `replaceCurrentTrackAndResume(...)`. Production service wiring already constructs this owner inline in `EchoPlaybackService.replaceCurrentTrackAndResume(...)` with the real queue manager, so this removes another fake migration runtime mode without changing replacement or recovery semantics.
- Source-of-truth status is unchanged: current-track replacement and restored-position decisions still belong to `PlaybackQueueManager.replaceCurrentTrackAndResume(...)`; `PlaybackCurrentTrackReplacementOwner` still only maps the result onto diagnostics recording and recovery scheduling.
- Concrete wins: one more direct-construction queue consumer loses a nullable queue-manager contract, one more silent no-op path disappears at the Java boundary, and focused tests now distinguish invalid `null` construction from the real “replacement does not need recovery” no-op case.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackReplacementOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 330: `EchoPlaybackService.java` remains `1390` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 331 - Queue completion owner now requires a real queue manager (2026-07-05)
- Queue nullability slice: `PlaybackQueueCompletionOwner` now requires a non-null `PlaybackQueueManager` in its constructor instead of treating a missing manager as implicit `STOP_AND_CLEAR` / boundary-only behavior. Production service wiring already creates this owner through `EchoPlaybackService.playbackQueueCompletionOwner()` with the real queue manager, so this removes another migration-only fake runtime mode without changing completion semantics.
- Source-of-truth status is unchanged: queue completion decisions and queue preparation still belong to `PlaybackQueueManager`; `PlaybackQueueCompletionOwner` still only maps those decisions onto `stopAndClearBoundary`, `stopAtEndOfQueueBoundary`, and `skipToNext`.
- Concrete wins: one more direct-construction queue consumer loses a nullable queue-manager contract, three owner-level `null` guards disappear (`playAfterCompletion`, `stopAndClearPlayback`, `stopAtEndOfQueue`, `stopAfterAutomaticAdvance`), and focused tests now distinguish invalid `null` construction from the real repeat-current / stop-at-end / advance cases.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCompletionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 331: `EchoPlaybackService.java` remains `1390` lines, `MainActivityBase.java` remains `2693` lines, strict `private Playback*` Service field lines remain `40`, app playback `Playback*Owner.java` files remain `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` supplier/helper hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 332 - App-shell queue refresh now keeps one service read path and one disconnect-reset path (2026-07-05)
- App-shell wiring slice: `MainActivityBase.playbackQueueSnapshot()` now uses `playbackStore.queueSnapshot()` as the fallback when `playbackService` is null, and `publishPlaybackStore()` now publishes only when both `playbackStore` and `playbackService` exist. `PlaybackStateEventController` and `HeartbeatRecommendationSeedBinder` still reuse the same helper, so the host path keeps exactly one direct `playbackService.queueSnapshot()` occurrence instead of duplicating that read in multiple lambdas.
- Source-of-truth status is clearer: queue contents still come from `EchoPlaybackService -> PlaybackQueueManager` when the service is connected, but app-shell disconnect clearing now belongs only to the existing `playbackStore.reset()` path in `PlaybackServiceHostController`. The helper no longer introduces a second implicit “service missing means publish empty queue” reset path.
- Concrete wins: one more app-shell state-reset path is deleted, one direct service queue read path stays centralized in a single helper, and the queue fallback visible to `PlaybackStateEventController` / `HeartbeatRecommendationSeedBinder` now reuses `MainPlaybackStore` instead of manufacturing a fresh empty list when the service reference is absent.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 332: `EchoPlaybackService.java` remains `1390` lines, `MainActivityBase.java` is `2695` lines, direct `MainActivityBase` `playbackService.queueSnapshot()` occurrences remain `1`, `MainActivityBase.playbackQueueSnapshot()` call sites are `2`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 333 - MainActivity shell wiring contract now locks initialization order before gateway binding (2026-07-05)
- App-shell contract slice: `MainActivityArchitectureContractTest.mainActivityCreatesRouteStoresAndStatusBeforeGatewayBinding()` now explicitly guards the `onCreate()` order edge from `initializeRouteStoresAndStatus()` to `initializePlatformControllers()`, `initializeNavigationRendering()`, and `initializePlaybackControllers()`. Inside `initializeRouteStoresAndStatus()`, the contract also locks `uiShellController -> routeController -> playbackStore -> statusMessageController -> trackShareLauncher` creation order so host callbacks do not capture uninitialized fields by value.
- Source-of-truth status is unchanged: `routeController`, `playbackStore`, `statusMessageController`, and `uiShellController` remain the only app-shell host instances for route/status/playback wiring. This slice only hardens when they must exist before downstream gateway/controller construction.
- Concrete wins: one more initialization-order implicit dependency is made explicit, one more cross Java/Kotlin non-null host-boundary risk is protected by contract, and `initializePlaybackLifecycleControllers()` is now guarded to keep service disconnect clearing on the existing `() -> playbackStore.reset()` path instead of recreating or nulling the store through a hidden lambda.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 333: `MainActivityBase.java` remains `2695` lines, direct `MainActivityBase` `playbackService.queueSnapshot()` occurrences remain `1`, `MainActivityBase.playbackQueueSnapshot()` call sites remain `2`, and `EchoPlaybackService.java` remains `1390` lines.
## NOTE 334 - Queue manager public API contract now blocks derived-read regressions (2026-07-05)
- Queue/interface audit slice: `MainActivityArchitectureContractTest.playbackQueueManagerPublicApiStaysNarrowAndCommandFocused()` now snapshots the full public `PlaybackQueueManager` surface and requires it to stay at the current 18 method set: semantic queue commands plus the two snapshot reads `queueSnapshot()` / `queueStateSnapshot()`. This turns the earlier ad hoc “removed method” checks into one explicit interface boundary for future queue work.
- Source-of-truth status is unchanged: queue contents, current track, and current index still belong to `PlaybackQueueManager`; the new contract only states that callers must keep using `QueueStateSnapshot` / `queueSnapshot()` rather than reintroducing separate public derived reads.
- Concrete wins: one more interface-expansion risk is guarded centrally, `PlaybackQueueManager` cannot silently grow methods like `currentTrack`, `currentIndex`, `hasQueue`, `persistQueueState`, `restoreLastPlayback`, or `queuePreparationForNewPlayer`, and the next queue slice can focus on shrinking real command clusters instead of rediscovering surface drift.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 334: `PlaybackQueueManager` public methods remain `18`, `MainActivityBase.java` remains `2695` lines, `EchoPlaybackService.java` remains `1390` lines, and `QueueProvider` production hits remain `0`.
## NOTE 335 - Stop-at-end completion prep is now internal to PlaybackQueueManager (2026-07-05)
- Queue/completion slice: `PlaybackQueueManager.prepareStopAtEndOfQueue()` is no longer part of the public manager API. Stop-at-end preparation now happens inside `preparePlaybackCompletionAction()` when the manager decides `PlaybackCompletionAction.STOP_AT_END`, and `PlaybackQueueCompletionOwner` now only maps that semantic action to `stopAtEndOfQueueAction` instead of making a second explicit prep call.
- Source-of-truth status is unchanged: completion decision, queue current index, restored-position clearing, preparing/error/last-marked cleanup, and resume-request persistence still belong to `PlaybackQueueManager`. `PlaybackQueueCompletionOwner` remains only the boundary mapper from queue completion semantics to service stop/next actions.
- Concrete wins: one `PlaybackQueueManager` public method is deleted, one completion-owner forwarding hop disappears, and stop-at-end state cleanup is now verified on the real `preparePlaybackCompletionAction()` path rather than through a separate public prep helper.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCompletionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 335: `PlaybackQueueManager.kt` is `748` lines, `EchoPlaybackService.java` remains `1390` lines, `PlaybackQueueManager` public methods are `18`, and production `playbackQueueManager.prepareStopAtEndOfQueue()` hits are `0`.
## NOTE 336 - Automatic-advance stop prep is now internal to mirrored transition handling (2026-07-05)
- Queue/completion slice: `PlaybackQueueManager.prepareStopAfterAutomaticAdvance(...)` is no longer part of the public manager API. The automatic-advance stop-at-end preparation now happens inside `PlaybackQueueManager.applyMirroredTransitionIndex(nextIndex, automaticAdvance)` when the manager decides to stop at the queue end, so `PlaybackQueueMirroredTransitionOwner` no longer exports `completedIndex`, `EchoPlaybackService` no longer forwards that value, and `PlaybackQueueCompletionOwner.stopAfterAutomaticAdvance()` is now boundary-only.
- Source-of-truth status is unchanged: automatic-advance stop semantics, persisted position handling, and current-index clamping still belong to `PlaybackQueueManager`; `PlaybackQueueMirroredTransitionOwner` still only interprets Media3 transition reasons and decides whether mirrored transition reuse should stop immediately.
- Concrete wins: one more `PlaybackQueueManager` public method is deleted, one more `completedIndex` parameter chain disappears (`mirrored transition owner -> service -> completion owner`), and the automatic-advance stop preparation is now exercised through the real mirrored-transition path rather than a standalone public prep helper.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCompletionOwnerTest --tests app.yukine.playback.PlaybackQueueMirroredTransitionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 336: `PlaybackQueueManager.kt` is `750` lines, `EchoPlaybackService.java` remains `1390` lines, `PlaybackQueueManager` public methods are `17`, production `playbackQueueManager.prepareStopAfterAutomaticAdvance(...)` hits are `0`, and service `stopAfterAutomaticAdvance(transition.completedIndex())` hits are `0`.
## NOTE 337 - Automatic-advance boundary no longer detours through PlaybackQueueCompletionOwner (2026-07-05)
- Completion/service wiring slice: after NOTE 336 made automatic-advance preparation internal to `PlaybackQueueManager`, `PlaybackQueueCompletionOwner.stopAfterAutomaticAdvance()` had become a pure `run(stopAtEndOfQueueAction)` wrapper. That forwarding-only method is now removed, and `EchoPlaybackService` calls `stopAtEndOfQueueBoundary()` directly once `PlaybackQueueMirroredTransitionOwner.Transition.stopAfterAutomaticAdvance()` says the queue should stop.
- Source-of-truth status is unchanged: `PlaybackQueueMirroredTransitionOwner` still owns Media3 transition interpretation, and `PlaybackQueueManager` still owns automatic-advance stop preparation. The service only performs the Android/player boundary action after those owners have already decided the semantic outcome.
- Concrete wins: one forwarding-only completion-owner method is deleted, one `service -> completion owner -> boundary` chain becomes a direct `service -> boundary` call, and the completion owner surface is tighter without moving queue or transition strategy back into the service.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCompletionOwnerTest --tests app.yukine.playback.PlaybackQueueMirroredTransitionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 337: `EchoPlaybackService.java` remains `1390` lines, `PlaybackQueueManager.kt` remains `750` lines, production `playbackQueueCompletionOwner().stopAfterAutomaticAdvance()` hits are `0`, and `PlaybackQueueCompletionOwner` no longer declares `void stopAfterAutomaticAdvance()`.
## NOTE 338 - Completion cluster audit now shifts priority from queue API cuts to service wiring cleanup (2026-07-05)
- Audit-only checkpoint after NOTE 335-337: completion-related queue API is now down to `17` public `PlaybackQueueManager` methods total, with `prepareStopAtEndOfQueue()` and `prepareStopAfterAutomaticAdvance(...)` both gone from the public surface. Production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` / `playbackQueueStateOwner` supplier hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
- App-shell wiring remains on the guarded order `initializeRouteStoresAndStatus()` -> `initializeNowPlayingGateways()` -> `initializeLibraryGateway()` -> `initializePlatformControllers()` -> `initializePlaybackLifecycleControllers()` -> `initializeNavigationRendering()` -> `initializePlaybackControllers()`. `MainActivityBase.java` remains `2695` lines, direct `playbackService.queueSnapshot()` occurrences remain `1`, and `playbackQueueSnapshot()` call sites remain `2`.
- Current owner/file metrics after the completion batch: `EchoPlaybackService.java` remains `1390` lines, strict `private Playback*` Service field lines remain `36`, app playback `Playback*Owner.java` files remain `39`, and app playback `Playback*OwnerTest.java` files remain `39`.
- Direction update: do not force another completion API cut just because one prep method remains. The next highest-signal slice is service wiring/supplier reduction or field audit, because pushing `prepareStopAndClearPlaybackState()` out of `PlaybackQueueManager` next would risk moving queue strategy back into `EchoPlaybackService` instead of shrinking a real boundary.
## NOTE 339 - Current-track preparation owners no longer live as long-lived service fields (2026-07-05)
- Service-wiring slice: `EchoPlaybackService` no longer stores `PlaybackCurrentTrackPreparationOwner` and `PlaybackCurrentTrackPreparationQueueOwner` as mutable fields. Their only two runtime call sites now build them through private helper methods `playbackCurrentTrackPreparationOwner()` and `playbackCurrentTrackPreparationQueueOwner()` when `prepareCurrent(...)` / `prepareMirroredQueue(...)` actually need them.
- Source-of-truth status is unchanged: current-track queue replacement still belongs to `PlaybackQueueManager`, restored-position reads still belong to `PlaybackPositionManager`, and URI / MediaItem preparation still belongs to `PlaybackMediaSourceProvider`. This slice only removes two long-lived wiring holders from the service.
- Concrete wins: strict `private Playback*` Service field lines drop from `36` to `34`, two low-usage service fields disappear, and the metadata provider boundary no longer relies on an initialization-time by-value method reference because the helpers now use call-time-safe lambdas around `playbackNotificationManager`.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationOwnerTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationQueueOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 339: `EchoPlaybackService.java` is `1398` lines, strict `private Playback*` Service field lines are `34`, app playback `Playback*Owner.java` files remain `39`, direct `EchoPlaybackService` `queueStateSnapshot()` / `playbackQueueStateOwner` supplier hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 340 - Snapshot and buffering-diagnostics owners now build on demand instead of sitting as service fields (2026-07-05)
- Service-wiring slice: `EchoPlaybackService` no longer stores `PlaybackStateSnapshotOwner` or `PlaybackStreamingDiagnosticsRecorderOwner` as fields. `snapshot()`, buffering publication, and replacement recovery now call private helpers `playbackStateSnapshotOwner()` / `playbackStreamingDiagnosticsRecorderOwner()` that build these wrappers on demand from the existing queue/runtime/visualization/diagnostics owners.
- Source-of-truth status is unchanged: snapshot state still comes from `PlaybackQueueManager`, `PlaybackPlayerStateOwner`, `PlaybackRuntimeStateManager`, `PlaybackVisualizationAnalyzer`, and `PlaybackRealtimeVisualizationOwner`; buffering/recovery diagnostics still write through `PlaybackStreamingDiagnostics` with `PlaybackMediaSourceProvider` quality lookup. This slice only removes two long-lived wrapper fields from the service.
- Concrete wins: strict `private Playback*` Service field lines drop from `34` to `32`, two more low-usage service fields disappear, and the helper implementations now use call-time-safe lambdas instead of long-lived method references to runtime owners.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackStateSnapshotOwnerTest --tests app.yukine.playback.PlaybackCurrentTrackReplacementOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 340: `EchoPlaybackService.java` is `1402` lines, strict `private Playback*` Service field lines are `32`, app playback `Playback*Owner.java` files remain `39`, direct `EchoPlaybackService` `queueStateSnapshot()` / `playbackQueueStateOwner` supplier hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 341 - Realtime visualization owner no longer lives as a service field (2026-07-05)
- Service-wiring slice: `EchoPlaybackService` no longer stores `PlaybackRealtimeVisualizationOwner` as a field. `realtimeBeat()`, `realtimeBands()`, and `playbackStateSnapshotOwner()` now all read through a private helper `playbackRealtimeVisualizationOwner()` that rebuilds the lightweight wrapper from `playbackPlayerStateOwner` and `realtimeBassDetector` when needed.
- Source-of-truth status is unchanged: realtime beat/bands still belong to `RealtimeBassDetector` gated by `PlaybackPlayerStateOwner.isPlaying()`. This slice only removes one more long-lived wrapper field from the service.
- Concrete wins: strict `private Playback*` Service field lines drop from `32` to `31`, one more low-usage service field disappears, and the snapshot helper now reads realtime beat through the same on-demand helper path used by the public realtime accessors.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackRealtimeVisualizationOwnerTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 341: `EchoPlaybackService.java` is `1403` lines, strict `private Playback*` Service field lines are `31`, app playback `Playback*Owner.java` files remain `39`, direct `EchoPlaybackService` `queueStateSnapshot()` / `playbackQueueStateOwner` supplier hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 342 - Streaming diagnostics recorder owner is removed in favor of direct diagnostics lambdas (2026-07-05)
- Service-wiring slice: `PlaybackStreamingDiagnosticsRecorderOwner` is deleted. `EchoPlaybackService` now passes direct diagnostics lambdas to `PlaybackCurrentTrackReplacementOwner` for recovery logging, and `publishBufferingState()` now records buffering directly through `PlaybackStreamingDiagnostics` both for publisher fan-out and the no-publisher fallback path.
- Source-of-truth status is unchanged: buffering and recovery diagnostics still belong to `PlaybackStreamingDiagnostics`, and streaming quality still belongs to `PlaybackMediaSourceProvider`. This slice only removes a pure owner adapter layer between existing owners.
- Concrete wins: app playback `Playback*Owner.java` files drop from `39` to `38`, one forwarding-only owner file and its dedicated test disappear, and the service no longer constructs a diagnostics recorder helper just to adapt two existing callbacks.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackStatePublisherTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackReplacementOwnerTest --tests app.yukine.playback.PlaybackStreamingDiagnosticsTest --tests app.yukine.MainActivityArchitectureContractTest :app:compileDebugKotlin :app:compileDebugJavaWithJavac --console=plain`, plus `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py app/src/main/java app/src/test/java feature/ui-common/src/main/java` with `TOTAL=0`.
- Audit checkpoint after NOTE 342: `EchoPlaybackService.java` is `1408` lines, strict `private Playback*` Service field lines remain `31`, app playback `Playback*Owner.java` files are `38`, direct `EchoPlaybackService` `queueStateSnapshot()` / `playbackQueueStateOwner` supplier hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 343 - Queue navigation owner no longer accepts a missing queue manager (2026-07-05)
- Queue/nullability slice: `PlaybackQueueNavigationOwner` now requires a non-null `PlaybackQueueManager` in its constructor and calls `playFirstQueuedTrack()`, `skipToNextImmediately()`, `skipToPrevious()`, and `reuseMirroredQueueIfAvailable(...)` directly instead of treating a missing manager as a valid no-op runtime mode. `EchoPlaybackService` already creates this short-lived owner after `playbackQueueManager` exists.
- Source-of-truth status is unchanged: queue membership/current index/current track and mirrored-queue reuse remain owned by `PlaybackQueueManager`; `PlaybackQueueNavigationOwner` remains only the service-boundary mapper that notifies `onMirroredQueueReused(...)` when the manager actually reuses the mirrored player queue.
- Concrete wins: one more migration-era fake state path is deleted, four owner-local `playbackQueueManager != null` branches disappear, and the focused owner test now locks constructor non-nullability instead of preserving a silent missing-manager path.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueNavigationOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also ran app Kotlin/Java compile tasks.
- Audit checkpoint after NOTE 343: `EchoPlaybackService.java` remains `1408` lines, strict `private Playback*` Service field lines remain `31`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` / `playbackQueueStateOwner` supplier hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and `PlaybackQueueNavigationOwner` missing-manager null-check hits are `0`.
## NOTE 344 - Precache manager no longer accepts a missing queue manager (2026-07-05)
- Cache/nullability slice: `PlaybackPrecacheManager` now requires a non-null `PlaybackQueueManager` and reads `queueStateSnapshot()` / `queueSnapshot()` directly for current-track and upcoming-track precache decisions. Test helpers that previously used `null` as an irrelevant queue placeholder now pass an empty `PlaybackQueueManager`.
- Source-of-truth status is unchanged: queue membership/current index/current track still belong to `PlaybackQueueManager`; cache key, leading-range, segmented precache, and upcoming-track cache policy remain inside `PlaybackPrecacheManager`; URI / MediaItem matching still uses `PlaybackMediaSourceProvider` through `PlaybackMediaCacheOperations`.
- Concrete wins: two cache-owner fake missing-manager branches disappear, one `snapshot == null` derived fallback disappears, and focused tests now lock constructor non-nullability while preserving the null media-source-provider no-op behavior.
- Verification target passed after one expected test-input correction: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also ran app Kotlin/Java compile tasks. Focused mojibake scan remained `TOTAL=0`.
- Audit checkpoint after NOTE 344: `EchoPlaybackService.java` remains `1408` lines, strict `private Playback*` Service field lines remain `31`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` / `playbackQueueStateOwner` supplier hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and `PlaybackPrecacheManager` missing-manager fallback hits are `0`.
## NOTE 345 - Playback feature state providers no longer accept nullable queue-manager binding (2026-07-05)
- Feature playback/nullability slice: `PlaybackPositionManager.PlaybackStateProvider.bindPlaybackQueueManager(...)` and `PlaybackRuntimeStateManager.PlaybackStateProvider.bindPlaybackQueueManager(...)` now require a non-null `PlaybackQueueManager`. The providers still keep their internal queue-manager reference nullable so the service can create them before queue-manager construction and bind them immediately after `PlaybackQueueManager` exists.
- Source-of-truth status is unchanged: current track and queue state still come from `PlaybackQueueManager.queueStateSnapshot()`. This slice does not add a state mirror, facade, or service owner; it only removes the ability to explicitly bind a missing queue manager after the provider exists.
- Concrete wins: two cross Java/Kotlin nullable binding entry points disappear, the focused tests now preserve the real unbound-startup behavior without passing `null`, and `MainActivityArchitectureContractTest` now guards both feature providers against reintroducing `PlaybackQueueManager?` binding parameters.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackPositionManagerTest --tests app.yukine.playback.PlaybackRuntimeStateManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also ran feature playback and app Kotlin/Java compile tasks.
- Audit checkpoint after NOTE 345: `EchoPlaybackService.java` remains `1408` lines, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` / `playbackQueueStateOwner` supplier hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and nullable `bindPlaybackQueueManager(playbackQueueManager: PlaybackQueueManager?)` production/test owner signatures are guarded at `0`.
## NOTE 346 - Precache manager now requires an explicit playback runtime state source (2026-07-05)
- Cache/interface slice: `PlaybackPrecacheManager` no longer has constructor or `fromMediaSourceProvider(...)` overloads that omit `PlaybackRuntimeStateManager`. Upcoming-track precache now reads `playbackRuntimeStateManager.repeatMode()` directly instead of using the previous `playbackRuntimeStateManager != null && ...` fallback.
- Source-of-truth status is unchanged: queue membership/current index/current track still belong to `PlaybackQueueManager`, repeat mode still belongs to `PlaybackRuntimeStateManager`, cache policy remains inside `PlaybackPrecacheManager`, and URI / MediaItem / cache-key resolution still flows through `PlaybackMediaSourceProvider` via `PlaybackMediaCacheOperations`.
- Concrete wins: one nullable cache-policy state source disappears, two no-runtime constructor paths and one no-runtime factory path are deleted, and tests now pass an explicit default runtime manager for repeat-all behavior while preserving the `null` media-source-provider safe no-op case.
- Verification target passed after fixing one old factory test call: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also kept app Kotlin/Java compile tasks green.
- Audit checkpoint after NOTE 346: `EchoPlaybackService.java` remains `1408` lines, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` / `playbackQueueStateOwner` supplier hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and `PlaybackPrecacheManager` runtime-null fallback / no-runtime constructor path hits are `0`.
## NOTE 347 - Service no longer guards initialized queue-manager restore/preparation paths (2026-07-05)
- Service-wiring slice: `EchoPlaybackService.onCreate()` now calls `playbackQueueManager.restorePlaybackQueue()` directly after constructing `PlaybackQueueManager`, and the current-track preparation metadata helper now replaces the current queue track through `track -> playbackQueueManager.replaceCurrentQueueTrack(track)` without an inner `playbackQueueManager != null` guard.
- Source-of-truth status is unchanged: queue restore and current-track replacement remain owned by `PlaybackQueueManager`, while `EchoPlaybackService` still only wires the Media3/service boundary. The two remaining service queue-manager null checks are the public `queueSnapshot()` and `snapshot()` defensive read paths used by app-shell callers.
- Concrete wins: two initialization-after-construction null branches disappear from the service, reducing lifecycle ambiguity without adding an owner, supplier, facade, or state mirror.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueRestoreOwnerTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, which also kept app Kotlin/Java compile tasks green after updating one contract assertion to the direct lambda form.
- Audit checkpoint after NOTE 347: `EchoPlaybackService.java` is `1402` lines, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct `EchoPlaybackService` `queueStateSnapshot()` / `playbackQueueStateOwner` supplier hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and direct service `playbackQueueManager == null` checks are down to the two public snapshot/queueSnapshot guards.
## NOTE 348 - App shell gateway binding order is now guarded as a contract (2026-07-05)
- App-shell contract slice: `MainActivityArchitectureContractTest` now locks the full `onCreate()` shell binding phase from `initializeRouteStoresAndStatus()` through now-playing/download/library gateway binding and then platform/playback/navigation controllers. It also guards now-playing/library gateway construction against passing late host playback/service/store/controller fields by value.
- Source-of-truth status is unchanged: route/navigation remain owned by `MainRouteController` / `NavigationViewModel`, playback snapshots remain owned by `MainPlaybackStore` / service snapshot reads, and settings still flow through provider methods around `settingsStore`.
- Concrete wins: one implicit initialization-order dependency is now explicit, and future regressions that pass nullable host fields into Kotlin non-null gateway boundaries by value should fail the architecture contract instead of surfacing as Activity startup crashes.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
## NOTE 349 - Media cache operations now have an explicit interface audit (2026-07-05)
- Resolver/cache boundary slice: `MainActivityArchitectureContractTest` now extracts Java interface method names and locks `PlaybackMediaCacheOperations` to the current cache/reuse surface: cache key, cached length/range probing, cache data source, audio-cache release, and resolved-URI reuse comparison.
- Source-of-truth status is unchanged: URI / MediaItem / playback preparation remain owned by `PlaybackMediaSourceProvider`; cache policy remains inside `PlaybackPrecacheManager`; `PlaybackMediaCacheOperations` remains only the adapter that lets cache owners call the provider without turning the service or a resolution owner into a cache facade.
- Concrete wins: one interface-expansion risk is now covered by a method-level contract, so adding media-item resolution, playback preparation, headers, or queue policy to the cache adapter should fail the architecture test.
- Verification target passed after tightening the helper to ignore static factory method bodies: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
- Audit checkpoint after NOTE 349: `EchoPlaybackService.java` is `1304` lines, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` / `playbackQueueStateOwner` supplier hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 350 - Playback service ports now have method-level facade guards (2026-07-05)
- App/playback boundary contract slice: `MainActivityArchitectureContractTest` now locks the Kotlin method sets for `PlaybackServiceHostPort`, `NowPlayingPlaybackServicePort`, and `SettingsPlaybackServicePort`. The host port remains limited to listener registration, app visibility, realtime visualization reads, and queue snapshot reads; command/settings surfaces cannot quietly gain extra service-facade methods without failing the contract.
- Source-of-truth status is unchanged: playback snapshots and queue state still come from `EchoPlaybackService` / `PlaybackQueueManager` at the Android boundary, now-playing semantic commands still flow through `NowPlayingPlaybackGatewayAdapter`, and settings runtime writes still flow through `SettingsPlaybackServiceControlsAdapter`.
- Concrete wins: one more app-shell/playback interface expansion risk is covered by a method-level test, and the same contract blocks these service ports from referencing concrete `EchoPlaybackService` or Android `Context`.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
- Audit checkpoint after NOTE 350: `EchoPlaybackService.java` remains `1304` lines, strict `private Playback*` Service field lines remain `31`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` / `playbackQueueStateOwner` supplier hits remain `0`, and current Service `queueSnapshot()` literal hits remain `2`.
## NOTE 351 - Now-playing queue presence is derived from playback snapshot (2026-07-05)
- App/playback interface slice: `NowPlayingPlaybackServicePort.hasQueue()` and `EchoPlaybackService.hasQueue()` are removed. `NowPlayingViewModel.hasQueue()` keeps its existing service-connected guard but now derives queue presence from `player.snapshot()?.queueSize`, which was already populated by `PlaybackStateSnapshotOwner` from `PlaybackQueueManager.QueueStateSnapshot`.
- Source-of-truth status is unchanged: queue membership/current index/current track remain owned by `PlaybackQueueManager`, and the app-shell/ViewModel read path uses the existing `PlaybackStateSnapshot` instead of adding a queue mirror or exposing `queueSnapshot()` to the now-playing gateway.
- Concrete wins: one service-port method, one ViewModel gateway method, one concrete Service public method, and one service-to-service-port forwarding chain are deleted; the remaining `NowPlayingViewModel.hasQueue()` is a UI semantic helper over snapshot state.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.NowPlayingViewModelTest --tests app.yukine.NowPlayingPlaybackGatewayAdapterTest --tests app.yukine.QueueActionControllerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
- Audit checkpoint after NOTE 351: `EchoPlaybackService.java` is `1301` lines, strict `private Playback*` Service field lines remain `31`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` / `playbackQueueStateOwner` supplier hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and `EchoPlaybackService` public `hasQueue()` hits are `0`.
## NOTE 352 - Now-playing connection state is derived from playback snapshot presence (2026-07-05)
- App/playback interface slice: `NowPlayingPlaybackGateway.serviceConnected()` is removed. `NowPlayingViewModel` now treats `player.snapshot() != null` as the connection boundary, matching `NowPlayingPlaybackGatewayAdapter.snapshot()` returning `null` when no service is available and a `PlaybackStateSnapshot` when the service boundary exists.
- Source-of-truth status is unchanged: playback/queue state still comes from `PlaybackStateSnapshot`, backed by `EchoPlaybackService` and `PlaybackQueueManager`; this slice only deletes a separate boolean connection method from the ViewModel gateway.
- Concrete wins: one ViewModel gateway method and one adapter override are deleted, fake playback gateways no longer carry a second connection method, and architecture contracts now guard against reintroducing `serviceConnected()` or `player.serviceConnected()` calls.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.NowPlayingViewModelTest --tests app.yukine.NowPlayingPlaybackGatewayAdapterTest --tests app.yukine.QueueActionControllerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
- Audit checkpoint after NOTE 352: `EchoPlaybackService.java` remains `1301` lines, strict `private Playback*` Service field lines remain `31`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` / `playbackQueueStateOwner` supplier hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and `serviceConnected()` production hits are `0`.
## NOTE 353 - Now-playing ViewModel gateway method set is locked (2026-07-05)
- App/playback contract slice: `MainActivityArchitectureContractTest` now locks the full `NowPlayingPlaybackGateway` method set in `NowPlayingViewModel.kt`, matching the current semantic command plus snapshot surface after deleting `hasQueue()` and `serviceConnected()`.
- Source-of-truth status is unchanged: the gateway still exposes only commands and `PlaybackStateSnapshot`; queue membership and connection state remain derived from snapshot data rather than separate ViewModel gateway booleans.
- Concrete wins: one stronger boundary test now blocks reintroducing broad service state reads such as `hasQueue`, `serviceConnected`, or `queueSnapshot` into the ViewModel playback gateway without an explicit reviewed contract change.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
## NOTE 354 - Precache runtime state source is now covered by a constructor guard test (2026-07-05)
- Cache/source-of-truth test slice: `PlaybackPrecacheManagerTest` now has a focused constructor guard proving `PlaybackPrecacheManager` requires a non-null `PlaybackRuntimeStateManager`, matching the existing non-null queue-manager guard.
- Source-of-truth status is unchanged: queue membership/current index/current track remain owned by `PlaybackQueueManager`, repeat mode remains owned by `PlaybackRuntimeStateManager`, and cache policy remains in `PlaybackPrecacheManager`.
- Concrete win: the runtime-state fallback removal from the precache owner is now protected by behavior-level evidence instead of relying only on implementation shape; future attempts to treat a missing runtime manager as implicit repeat-all should fail the focused test.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
## NOTE 355 - MuMu startup smoke covers the current app-shell/service wiring batch (2026-07-05)
- Runtime verification slice: the current dirty app was assembled, installed on MuMu at `127.0.0.1:7555`, and launched through `adb shell monkey -p app.yukine 1`.
- Evidence: `:app:assembleDebug` passed, streamed install returned `Success`, launcher start produced `app.yukine/.MainActivity`, and filtered logcat had no `FATAL EXCEPTION`, no `Process app.yukine has died`, and no force-finish line for `app.yukine`.
- Scope: this is a startup smoke only. It validates the app-shell initialization order and service/app startup wiring enough to catch immediate Activity startup crashes, but it does not replace background playback, notification action, lyrics, queue restore, or streaming playback smoke.
## NOTE 356 - Playback service host applies settings through one connection snapshot (2026-07-05)
- App-shell wiring slice: `MainPlaybackServiceHostFactory` and `PlaybackServiceHostController.Host` now take one `PlaybackServiceConnectionSettings` source instead of six separate playback-speed, app-volume, concurrent-playback, status-bar-lyrics, restore, and replay-gain suppliers.
- Source-of-truth status is unchanged: these values still come from `MainActivityBase.settingsStore` at service-connection time, and `EchoPlaybackService` remains the Android/Media3 boundary that receives the applied values.
- Concrete wins: five host-interface methods and five Activity-to-host supplier chains are removed, `MainActivityBase` keeps one provider method with the previous null-safe defaults, and `MainPlaybackServiceHostTest` now verifies the controller applies the settings snapshot to the service.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainPlaybackServiceHostTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
## NOTE 357 - Playback service host method set is locked (2026-07-05)
- App-shell boundary test slice: `MainActivityArchitectureContractTest` now snapshots the `PlaybackServiceHostController.Host` method set after the settings snapshot contraction.
- Source-of-truth status is unchanged: settings still come from `MainActivityBase.settingsStore`, playback-store reset still clears the app-shell snapshot mirror on disconnect, and `EchoPlaybackService` remains the service boundary.
- Concrete win: a method-level contract now blocks re-expanding the host interface with separate setting reads or broad service facade methods unless the boundary is intentionally reviewed.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
## NOTE 358 - Playback service host chrome refresh is a single lifecycle callback (2026-07-05)
- App-shell wiring slice: `PlaybackServiceHostController.Host` now exposes one `renderPlaybackChrome()` callback instead of separate `renderSelectedTab()` and `renderNowBar()` callbacks. `MainPlaybackServiceHostFactory` likewise takes one `PlaybackChromeRenderer`, and `MainActivityBase` wires it through `renderPlaybackChromeIfReady()`.
- Source-of-truth status is unchanged: playback service connection/disconnection still only triggers app-shell chrome refresh; selected-tab and now-bar state still come from their existing render controllers/stores.
- Concrete wins: one Host method, one factory parameter, and one Activity-to-host callback chain are removed without adding an owner or moving feature strategy into `EchoPlaybackService`.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainPlaybackServiceHostTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
## NOTE 359 - Playback service host detach is a single lifecycle callback (2026-07-05)
- App-shell wiring slice: `PlaybackServiceHostController.Host` now exposes `detachPlaybackService()` instead of separate `clearPlaybackService()` and `resetPlaybackStore()` callbacks. `MainPlaybackServiceHostFactory` now takes one `PlaybackServiceDetacher`, and `MainActivityBase.detachPlaybackService()` performs the same `playbackService = null` plus `playbackStore.reset()` sequence.
- Source-of-truth status is unchanged: the bound service reference remains a MainActivityBase lifecycle reference, and `MainPlaybackStore` remains the app-shell playback snapshot mirror that is cleared on disconnect.
- Concrete wins: one Host method, one factory parameter, and one Activity-to-host callback chain are removed. `MainPlaybackServiceHostTest` now has focused disconnect coverage proving detach runs before chrome refresh.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainPlaybackServiceHostTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
## NOTE 360 - Playback service host connect setup is one lifecycle callback (2026-07-05)
- App-shell wiring slice: `PlaybackServiceHostController.Host` now exposes only `attachPlaybackService(...)`, `detachPlaybackService()`, and `renderPlaybackChrome()`. Applying the `PlaybackServiceConnectionSettings` snapshot and replaying pending tracks moved into `MainPlaybackServiceHost.attachPlaybackService(...)`, so the controller no longer pulls settings or pending playback through separate Host methods.
- Source-of-truth status is unchanged: playback settings still come from the existing `settingsStore` snapshot provider, pending playback still belongs to the playback-start owner, and `EchoPlaybackService` remains the Android/Media3 boundary that receives the applied runtime settings.
- Concrete wins: two Host interface methods and two controller-to-host calls are removed without adding an owner or facade. `MainPlaybackServiceHostTest` now verifies attach applies visibility, settings, and pending playback before the controller refreshes chrome, and `MainActivityArchitectureContractTest` locks the smaller Host method set.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainPlaybackServiceHostTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
## NOTE 361 - Playback queue manager public API is classified by responsibility (2026-07-05)
- Queue/interface audit slice: `MainActivityArchitectureContractTest` now requires the `PlaybackQueueManager` public method set to stay partitioned into mutation/navigation commands, restore/completion preparation, mirrored-queue coordination, and the two snapshot reads.
- Source-of-truth status is unchanged: queue membership/current index/current track remain owned by `PlaybackQueueManager`, and derived booleans stay on `QueueStateSnapshot` rather than becoming new public manager methods.
- Concrete win: the contract now guards against `QueueProvider` returning, against derived public reads such as `currentTrack`, `currentIndex`, `hasQueue`, `hasMultipleTracks`, and against historical residual APIs such as `retainTracksById`, `persistQueueState`, `restoreLastPlayback`, `queuePreparationForNewPlayer`, and `upcomingTracksForPrecache`.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
## NOTE 362 - Resolver/cache boundary blocks precache facade drift (2026-07-05)
- Resolver/cache audit slice: `MainActivityArchitectureContractTest` now requires `PlaybackPrecacheManager.fromMediaSourceProvider(...)` to be the only place where precache wiring touches `PlaybackMediaSourceProvider`, and the only allowed direct provider method there is `mediaItemMatchesTrackForReuse(...)` for current-player reuse matching.
- Source-of-truth status is unchanged: playable URI, MediaItem, cache-key, headers, and data-source resolution remain owned by `PlaybackMediaSourceProvider` / `PlaybackMediaCacheOperations`, while queue/current/upcoming cache policy remains in `PlaybackPrecacheManager` using `PlaybackQueueManager` and `PlaybackRuntimeStateManager`.
- Concrete win: the contract now blocks `PlaybackPrecacheManager` from directly calling media source, media item, cache key, cache data source, content length, headers, or audio cache APIs, and blocks `PlaybackMediaCacheOperations` from growing queue/precache policy dependencies.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
## NOTE 363 - Network render wiring stays behind initialized app-shell owners (2026-07-05)
- App-shell initialization contract slice: `MainActivityArchitectureContractTest` now locks `initializeNetworkRendering(...)` behind `initializeNetworkOwners(...)` after `NetworkRequestController`, `SettingsContextProvider`, `NetworkDialogController`, and `ConfirmationDialogController` are created. The test also keeps network event-controller construction ahead of `NetworkRenderCoordinator` consumption and blocks moving `initializeNetworkRendering(...)` directly into `onCreate()`.
- Source-of-truth status is unchanged: route/navigation remain owned by `MainRouteController` / `NavigationViewModel`, network actions remain owned by `NetworkRequestController` and `NetworkActionsViewModel`, and app-shell render state still flows through the existing stores/controllers. This adds no owner, state mirror, facade, or runtime behavior.
- Concrete win: one more initialization-order dependency is explicit and test-protected, reducing the risk of passing nullable host fields by value into network render/event owners before their Activity fields are ready.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
## NOTE 364 - Playback mode settings require the runtime state owner (2026-07-05)
- Runtime-state source slice: `PlaybackModeSettingsStore` now requires a non-null `PlaybackRuntimeStateManager` for restore, shuffle/repeat mutations, mode application, and repeat-mode reads. The previous null fallback that silently saved default shuffle/repeat values is removed.
- Source-of-truth status is unchanged and tighter: shuffle/repeat runtime state remains owned by `PlaybackRuntimeStateManager`, while repository persistence stays behind `PlaybackModeSettingsStore.ModeSettings`. This adds no owner, facade, or state mirror.
- Concrete win: one migration-era fake state path is removed, and `PlaybackModeSettingsStoreTest` now guards the non-null runtime owner contract while `MainActivityArchitectureContractTest` blocks `runtimeStateManager == null` and `normalizedRepeatFallback` from returning in the mode store.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackModeSettingsStoreTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
## NOTE 365 - Playback runtime settings require the runtime state owner (2026-07-05)
- Runtime-state source slice: `PlaybackRuntimeSettingsStore` now requires a non-null `PlaybackRuntimeStateManager` for restore, speed/volume/concurrent/replay-gain mutations, runtime reads, playback-parameter application, current-track volume application, and audio-focus handling. The previous null-to-default path (`1.0f`, `false`, or silent no-op) is removed from the store.
- Source-of-truth status is unchanged and tighter: playback speed, app volume, replay gain, concurrent playback, and current-track volume remain owned by `PlaybackRuntimeStateManager`; repository defaults still enter only through `PlaybackRuntimeSettingsStore.RuntimeSettings.restoreInto(...)`. `EchoPlaybackService` keeps its existing outer `playbackRuntimeSettingsStore == null` service-startup fallback, but the store itself no longer invents runtime state.
- Concrete win: another migration-era fake state path is removed without adding an owner, facade, state mirror, or Service strategy branch. `PlaybackRuntimeSettingsStoreTest` now guards the non-null runtime owner contract, and `MainActivityArchitectureContractTest` blocks `runtimeStateManager == null` / default runtime-setting fallbacks from returning in the runtime store.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackRuntimeSettingsStoreTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
- Runtime-state mini-audit after NOTE 364-365: `EchoPlaybackService.java` is `1398` lines, `MainActivityBase.java` is `2685` lines, strict `private Playback*` Service fields are `41`, app playback `Playback*Owner.java` files are `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and mode/runtime settings-store `runtimeStateManager == null` hits are both `0`.
## NOTE 366 - Service no longer carries settings-store null strategy branches (2026-07-05)
- Service wiring slice: `EchoPlaybackService` now calls `PlaybackModeSettingsStore` and `PlaybackRuntimeSettingsStore` directly after their `onCreate()` initialization instead of guarding every mode/runtime settings read, write, and apply call behind Service-level null checks. The crossfade state suppliers also read repeat mode and current-track volume from those stores directly.
- Source-of-truth status is unchanged: mode and runtime values remain owned by `PlaybackRuntimeStateManager`; repository persistence remains in the two settings stores; `EchoPlaybackService` only wires lifecycle and Media3-boundary calls. No owner, facade, state mirror, or high-risk notification/lyrics/shutdown behavior was added or changed.
- Concrete wins: five `PlaybackModeSettingsStore` Service null-guard branches and eleven `PlaybackRuntimeSettingsStore` Service null-guard branches are removed, including the crossfade `REPEAT_ALL` / `1.0f` Service fallback suppliers. `MainActivityArchitectureContractTest` now blocks those settings-store null branches from returning.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackModeSettingsStoreTest --tests app.yukine.playback.PlaybackRuntimeSettingsStoreTest --tests app.yukine.playback.PlaybackCrossfadeStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
- Current focused metrics: `EchoPlaybackService.java` is `1365` lines, strict `private Playback*` Service fields are `41`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and mode/runtime settings-store null-guard hits in `EchoPlaybackService` are all `0`.
## NOTE 367 - Late-bound queue state providers fail before binding (2026-07-05)
- Queue/runtime state source slice: `EchoPlaybackService.onCreate()` now creates the initial player only after `PlaybackQueueManager` is constructed and bound into `PlaybackRuntimeStateManager.PlaybackStateProvider` and `PlaybackPositionManager.PlaybackStateProvider`. Those two providers now fail explicitly when `currentTrack()` is read before queue-manager binding instead of treating an unbound provider as "no current track".
- Source-of-truth status is unchanged and tighter: current-track authority remains `PlaybackQueueManager.QueueStateSnapshot`; the slice removes a fake unbound state path rather than adding a state mirror. Empty queues still return `null` after binding, while missing binding is now a wiring error.
- Concrete win: one initialization-order dependency is made explicit, and production `playbackQueueManager?.queueStateSnapshot()` read paths are down to `0`. `MainActivityArchitectureContractTest` now keeps initial player creation after queue-manager provider binding and blocks the nullable snapshot path from returning.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackPositionManagerTest --tests app.yukine.playback.PlaybackRuntimeStateManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`.
- Current focused metrics: `EchoPlaybackService.java` is `1365` lines, strict `private Playback*` Service fields are `39`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and production nullable `playbackQueueManager?.queueStateSnapshot()` hits are `0`.
## NOTE 368 - Media cache operations require the media-source owner (2026-07-05)
- Resolver/cache boundary slice: `PlaybackMediaCacheOperations.fromMediaSourceProvider(...)` and its provider-backed adapter now require a non-null `PlaybackMediaSourceProvider` instead of manufacturing no-op cache behavior when the media-source owner is missing. The adapter still exposes only cache/reuse operations; URI, MediaItem, header restore, and playable MediaSource creation remain owned by `PlaybackMediaSourceProvider`.
- Source-of-truth status is unchanged and tighter: cache key and cache data-source decisions still delegate to `PlaybackMediaSourceProvider`; cache scheduling policy still lives in `PlaybackPrecacheManager`. No cache policy owner, resolution owner, facade, or state mirror was added.
- Concrete win: the media-cache boundary loses its fake missing-provider runtime mode, and provider-null strategy branches inside `PlaybackMediaSourceProviderCacheOperations` are down to `0`. `MainActivityArchitectureContractTest` now guards the non-null provider requirement and blocks the old `mediaSourceProvider == null` fallback from returning.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.manager.PlaybackMediaCacheOperationsTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`.
- Current focused metrics: `EchoPlaybackService.java` remains `1365` lines, strict `private Playback*` Service fields remain `39`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and media-cache provider-null branch hits are `0`.
## NOTE 369 - Playback precache requires cache/media-source owners (2026-07-05)
- Cache-owner boundary slice: `PlaybackPrecacheManager` now requires a non-null `PlaybackMediaCacheOperations`, and `PlaybackPrecacheManager.fromMediaSourceProvider(...)` requires a non-null `PlaybackMediaSourceProvider`. The old "missing cache owner means no precache work" mode is removed from constructor/factory tests.
- Source-of-truth status is unchanged and tighter: cache key and cache data-source behavior still belong to `PlaybackMediaSourceProvider` through `PlaybackMediaCacheOperations`; queue/current-track source still comes from `PlaybackQueueManager`; repeat-mode behavior still comes from `PlaybackRuntimeStateManager`; precache scheduling policy stays in `PlaybackPrecacheManager`.
- Concrete win: `PlaybackPrecacheManager` no longer carries fake missing-cache-owner or missing-media-source-owner fallback branches. `MainActivityArchitectureContractTest` guards `Objects.requireNonNull(mediaCacheOperations, "mediaCacheOperations")`, `Objects.requireNonNull(mediaSourceProvider, "mediaSourceProvider")`, and blocks `mediaCacheOperations == null`, `mediaCacheOperations != null`, and `mediaSourceProvider != null` branches from returning.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; the app Kotlin/Java compile tasks also ran in that Gradle invocation.
- Current focused metrics: `EchoPlaybackService.java` remains `1365` lines, strict `private Playback*` Service fields remain `39`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and precache cache/media-source null branch hits are `0`.
## NOTE 370 - Visualization cache requires cache/media-source owners (2026-07-05)
- Cache-owner boundary slice: `PlaybackVisualizationCacheManager` now requires a non-null `PlaybackMediaCacheOperations`, and `PlaybackVisualizationCacheManager.fromMediaSourceProvider(...)` requires a non-null `PlaybackMediaSourceProvider`. The old visualization-cache "missing cache owner means skip scheduling" path is removed.
- Source-of-truth status is unchanged and tighter: cache key, resolved URI reuse, and cache data-source decisions still belong to `PlaybackMediaSourceProvider` through `PlaybackMediaCacheOperations`; `PlaybackVisualizationCacheManager` only schedules and cancels the visualization cache window.
- Concrete win: two fake missing-owner branches are removed without adding an owner, facade, state mirror, or Service strategy branch. `PlaybackVisualizationCacheManagerTest` now guards both non-null contracts, and `MainActivityArchitectureContractTest` blocks `mediaCacheOperations == null`, `mediaCacheOperations != null`, and media-source null branches from returning in the visualization cache manager.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackVisualizationCacheManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`.
- Current focused metrics: `EchoPlaybackService.java` remains `1365` lines, strict `private Playback*` Service fields are `31`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and visualization cache cache/media-source null branch hits are `0`.
## NOTE 371 - Current-track preparation requires the media-source owner (2026-07-05)
- Resolver boundary slice: `PlaybackCurrentTrackPreparationOwner.fromMediaSourceProvider(...)` now requires a non-null `PlaybackMediaSourceProvider` and delegates preparation/media-source creation through one checked local owner reference. The old "missing provider returns null preparation/source" lambda path is removed.
- Source-of-truth status is unchanged and tighter: playable-track preparation, restored streaming headers, unplayable messages, and MediaSource creation still belong to `PlaybackMediaSourceProvider`; current-queue replacement stays in `PlaybackCurrentTrackPreparationQueueOwner`; preparation runtime state stays behind `PlaybackCurrentTrackPreparationRuntimeOwner`.
- Concrete win: two provider-null lambda branches and one cross Java/Kotlin nullable resolver boundary are removed without adding an owner, facade, state mirror, or Service strategy branch. `PlaybackCurrentTrackPreparationOwnerTest` now guards the non-null factory contract, and `MainActivityArchitectureContractTest` blocks the provider-null preparation path from returning.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; the app Kotlin/Java compile tasks also ran in that Gradle invocation. Focused mojibake scan remained `TOTAL=0`.
- Current focused metrics after NOTE 370-371: `EchoPlaybackService.java` remains `1365` lines, strict `private Playback*` Service fields are `31`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, visualization cache null branch hits are `0`, and current-track preparation provider-null branch hits are `0`.
## NOTE 372 - Current-track queue preparation requires the media-source owner (2026-07-05)
- Resolver boundary slice: `PlaybackCurrentTrackPreparationQueueOwner` now requires a non-null `PlaybackMediaSourceProvider` on its media-source constructor path. The direct function-injection constructors remain for focused tests, but the production media-source path no longer installs null-returning restore/media-source lambdas.
- Source-of-truth status is unchanged and tighter: queue membership/current index/current track remain owned by `PlaybackQueueManager`; restored queue tracks and mirrored MediaSource creation remain owned by `PlaybackMediaSourceProvider`; this owner only maps the queue snapshot into prepared queue input for player creation.
- Concrete win: two provider-null queue-preparation lambdas and one nullable resolver boundary are removed without adding an owner, facade, state mirror, or Service strategy branch. `PlaybackCurrentTrackPreparationQueueOwnerTest` now guards the non-null media-source constructor contract, and `MainActivityArchitectureContractTest` blocks the provider-null queue-preparation path from returning.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationQueueOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; the app Kotlin/Java compile tasks also ran in that Gradle invocation.
## NOTE 373 - Queue streaming restore requires the media-source owner (2026-07-05)
- Resolver boundary slice: `PlaybackQueueStreamingRestoreOwner` now requires a non-null `PlaybackMediaSourceProvider` on its production constructor path. The direct function-injection constructor remains for focused tests, but the service/queue restore path no longer installs null-returning restore/header actions.
- Source-of-truth status is unchanged and tighter: queue restore still runs through `PlaybackQueueManager.StreamingRestoreProvider`; restorable-track checks, restored streaming tracks, and header restore stay in `PlaybackMediaSourceProvider`.
- Concrete win: two provider-null restore/header branches and one nullable resolver boundary are removed without adding an owner, facade, state mirror, or Service strategy branch. `PlaybackQueueStreamingRestoreOwnerTest` now guards the non-null media-source constructor contract, and `MainActivityArchitectureContractTest` blocks the provider-null streaming-restore path from returning.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueStreamingRestoreOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; the app Kotlin/Java compile tasks also ran in that Gradle invocation.
- Current focused metrics after NOTE 372-373: `EchoPlaybackService.java` remains `1365` lines, strict `private Playback*` Service fields are `31`, all private `Playback*` Service fields are `49`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, current-track queue-preparation provider-null branch hits are `0`, and queue streaming-restore provider-null branch hits are `0`.
## NOTE 374 - Mirrored queue matcher requires the media-source owner (2026-07-05)
- Resolver boundary slice: `PlaybackMirroredQueueTrackMatcherOwner` now requires a non-null `PlaybackMediaSourceProvider` on its production constructor path. The direct matcher-injection constructor remains for focused tests, and player/index failures still return `false` at runtime.
- Source-of-truth status is unchanged and tighter: MediaItem/track identity matching still belongs to `PlaybackMediaSourceProvider`; mirrored queue state still flows through `PlaybackQueueManager` and its mirrored player owner.
- Concrete win: one provider-null matcher branch and one nullable resolver boundary are removed without adding an owner, facade, state mirror, or Service strategy branch. `PlaybackMirroredQueueTrackMatcherOwnerTest` now guards the non-null media-source constructor contract, and `MainActivityArchitectureContractTest` blocks the provider-null mirrored-matcher path from returning.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackMirroredQueueTrackMatcherOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; the app Kotlin/Java compile tasks also ran in that Gradle invocation.
## NOTE 375 - Current-track preparation runtime requires the runtime-state owner (2026-07-05)
- Runtime-state boundary slice: `PlaybackCurrentTrackPreparationRuntimeOwner.fromRuntimeStateManager(...)` now requires a non-null `PlaybackRuntimeStateManager` instead of installing null-safe no-op setters and a false preparing supplier. The direct function-injection constructor remains null-safe for focused test injection.
- Source-of-truth status is unchanged and tighter: preparing/error-message state stays owned by `PlaybackRuntimeStateManager`; `PlaybackCurrentTrackPreparationRuntimeOwner` only maps current-track preparation events into that runtime state.
- Concrete win: three runtime-state null fallback branches and one cross Java/Kotlin nullable boundary are removed without adding an owner, facade, state mirror, or Service strategy branch. `PlaybackCurrentTrackPreparationRuntimeOwnerTest` now guards the non-null factory contract, and `MainActivityArchitectureContractTest` blocks `runtimeStateManager == null` / `runtimeStateManager != null` from returning in the production factory.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationRuntimeOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; the app Kotlin/Java compile tasks also ran in that Gradle invocation. Focused mojibake scan remained `TOTAL=0`. Current focused metrics: `EchoPlaybackService.java` remains `1365` lines, strict `private Playback*` Service fields remain `31`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and current-track preparation runtime null branch hits are `0`.
## NOTE 376 - Playback snapshot runtime provider requires the runtime-state owner (2026-07-05)
- Runtime-state boundary slice: `PlaybackStateSnapshotOwner.fromRuntimeStateManager(...)` now requires a non-null `PlaybackRuntimeStateManager` instead of translating a missing runtime owner into default snapshot values for preparing, error message, shuffle, repeat, playback speed, and app volume. The `PlaybackStateSnapshotOwner` constructor still retains its direct `runtimeStateProvider == null` defaults for the separate constructor-injection path.
- Source-of-truth status is unchanged and tighter: playback runtime state in snapshots remains owned by `PlaybackRuntimeStateManager`, while `PlaybackStateSnapshotOwner` only maps queue, playback position, runtime state, sleep timer, visualization, and realtime beat into `PlaybackStateSnapshot`.
- Concrete win: six runtime-state null fallback branches and one nullable runtime owner factory boundary are removed without adding an owner, facade, state mirror, or Service strategy branch. `PlaybackStateSnapshotOwnerTest` now guards the non-null factory contract, and `MainActivityArchitectureContractTest` blocks `runtimeStateManager == null` / `runtimeStateManager != null` from returning in the snapshot runtime provider factory.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackStateSnapshotOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; the app Kotlin/Java compile tasks also ran in that Gradle invocation. Focused mojibake scan remained `TOTAL=0`. Current focused metrics: `EchoPlaybackService.java` remains `1365` lines, strict `private Playback*` Service fields remain `31`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, snapshot runtime-manager null branch hits are `0`, and direct constructor runtime-provider null branch hits remain `6` by design.
## NOTE 377 - Playback snapshot owner requires runtime-state provider injection (2026-07-05)
- Runtime-state boundary slice: `PlaybackStateSnapshotOwner` now requires a non-null `RuntimeStateProvider` in its constructor and no longer accepts a `defaultRepeatMode` constructor argument. The Service still passes `PlaybackStateSnapshotOwner.fromRuntimeStateManager(playbackRuntimeStateManager)`, while tests pass focused fake runtime providers when they need isolated snapshot behavior.
- Source-of-truth status is unchanged and tighter: snapshot runtime fields still come from `PlaybackRuntimeStateManager` through the `RuntimeStateProvider`; queue state remains in `PlaybackQueueManager`, position state in `PlaybackPositionProvider`, and visualization state in `PlaybackVisualizationAnalyzer`.
- Concrete win: six direct-constructor `runtimeStateProvider` null fallback branches and one constructor parameter are removed without adding an owner, facade, state mirror, or Service strategy branch. `PlaybackStateSnapshotOwnerTest` now guards the constructor non-null contract, and `MainActivityArchitectureContractTest` blocks `runtimeStateProvider == null` / `runtimeStateProvider != null` and `defaultRepeatMode` from returning in the snapshot owner.
- Verification target passed after updating an old string-contract assertion for the now-final realtime beat constructor argument: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackStateSnapshotOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`. Current focused metrics: `EchoPlaybackService.java` is `1364` lines, strict `private Playback*` Service fields remain `31`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, snapshot runtime-manager null branch hits are `0`, snapshot runtime-provider null branch hits are `0`, and snapshot `defaultRepeatMode` hits are `0`.
## NOTE 378 - Playback snapshot owner requires visualization provider injection (2026-07-05)
- Visualization boundary slice: `PlaybackStateSnapshotOwner` now requires a non-null `VisualizationProvider`, and `fromVisualizationAnalyzer(...)` requires a non-null `PlaybackVisualizationAnalyzer` instead of converting a missing analyzer/provider into empty waveform and spectrum snapshots. Empty visualization snapshots in tests are now represented by an explicit fake provider.
- Source-of-truth status is unchanged and tighter: waveform/spectrum snapshot behavior stays owned by `PlaybackVisualizationAnalyzer`; `PlaybackStateSnapshotOwner` only reads the provider while assembling `PlaybackStateSnapshot`. Queue, runtime, position, sleep timer, and realtime beat ownership are unchanged.
- Concrete win: six visualization null fallback branches are removed without adding an owner, facade, state mirror, or Service strategy branch. `PlaybackStateSnapshotOwnerTest` now guards both non-null contracts, and `MainActivityArchitectureContractTest` blocks `playbackVisualizationAnalyzer == null`, `visualizationProvider == null`, and their inverse null checks from returning in the snapshot owner.
- Verification target passed after updating an old nullable-provider contract assertion: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackStateSnapshotOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`. Current focused metrics: `EchoPlaybackService.java` remains `1364` lines, strict `private Playback*` Service fields remain `31`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, snapshot visualization-analyzer null branch hits are `0`, snapshot visualization-provider null branch hits are `0`, and snapshot runtime-provider null branch hits remain `0`.
## NOTE 379 - Queue state snapshot drops current-track derived API (2026-07-05)
- Queue interface slice: `PlaybackQueueManager.QueueStateSnapshot` no longer exposes `hasCurrentTrack`. The restore path now derives the same condition from `currentTrack == null`, so the snapshot public surface keeps `currentTrack`, `currentIndex`, and `queueSize` as source fields plus the remaining derived queue-shape reads.
- Source-of-truth status is unchanged and tighter: current track, current index, and queue size still come from `PlaybackQueueManager.queueStateSnapshot()`. No Service state mirror, queue provider, facade, or owner was added.
- Concrete win: one derivable snapshot API and its Java getter are removed from the cross-module queue boundary. `MainActivityArchitectureContractTest` now blocks `val hasCurrentTrack` and the `currentTrack != null` derived getter from returning.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueRestoreOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`. Current focused metrics: `EchoPlaybackService.java` remains `1364` lines, `MainActivityBase.java` is `2685` lines, strict `private Playback*` Service fields are `39`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and `QueueStateSnapshot.hasCurrentTrack` is absent.
## NOTE 380 - Queue state snapshot drops multi-track derived API (2026-07-05)
- Queue interface slice: `PlaybackQueueManager.QueueStateSnapshot` no longer exposes `hasMultipleTracks`. `PlaybackCrossfadeStateOwner` and `PlaybackErrorRecoveryCommandOwner` now derive the same policy condition from `queueSize`, while queue/current-track authority stays in `PlaybackQueueManager`.
- Source-of-truth status is unchanged and tighter: `currentTrack`, `currentIndex`, and `queueSize` remain the queue snapshot source fields. This slice adds no owner, facade, state mirror, Service strategy branch, or queue provider method.
- Concrete win: one more derivable snapshot API and Java getter are removed from the cross-module queue boundary. The architecture contract now blocks `val hasMultipleTracks`, `get() = queueSize >= 2`, and the old Java `getHasMultipleTracks()` production calls from returning.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCrossfadeStateOwnerTest --tests app.yukine.playback.PlaybackErrorRecoveryCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Current focused metrics: `EchoPlaybackService.java` remains `1364` lines, `MainActivityBase.java` is `2685` lines, strict `private Playback*` Service fields are `39`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and both `QueueStateSnapshot.hasCurrentTrack` and `QueueStateSnapshot.hasMultipleTracks` are absent.
## NOTE 381 - Queue and resolver/cache boundary audit (2026-07-05)
- Audit slice after the two queue snapshot API cuts: `PlaybackQueueManager.QueueProvider` is still absent, `QueueStateSnapshot.hasCurrentTrack` and `QueueStateSnapshot.hasMultipleTracks` are still absent, and the remaining snapshot source fields are `currentTrack`, `currentIndex`, and `queueSize`. The remaining `isQueueEmpty` and `isAtEndOfQueue` derived reads are intentionally not removed in this pass because they touch notification/restore/completion semantics and should be handled only with focused runtime evidence.
- Resolver/cache boundary audit: `PlaybackPrecacheManager` still holds `PlaybackMediaCacheOperations` instead of `PlaybackMediaSourceProvider`, has no media-source/cache-operation null fallback checks, and uses `PlaybackMediaCacheOperations.fromMediaSourceProvider(...)` only at the production factory boundary. `PlaybackMediaCacheOperations` has no `MediaItem` or `createMediaSource` methods, so URI/MediaItem resolution remains owned by `PlaybackMediaSourceProvider` and cache scheduling remains owned by `PlaybackPrecacheManager`.
- Source-of-truth status is unchanged: queue state remains in `PlaybackQueueManager`; cache keys/ranges remain delegated through `PlaybackMediaCacheOperations`; media item/source resolution remains in `PlaybackMediaSourceProvider`; no state mirror, owner, facade, or Service branch was added.
- Audit evidence: `EchoPlaybackService.java` remains `1364` lines, `MainActivityBase.java` remains `2685` lines, strict `private Playback*` Service fields are `39`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, `Precache.mediaSourceProviderNullChecks=0`, `Precache.cacheOperationsNullChecks=0`, `MediaCacheOperations.createMediaSourceHits=0`, and `MediaCacheOperations.mediaItemHits=0`.
## NOTE 382 - Current-track preparation runtime owner is no longer a Service field (2026-07-05)
- Service wiring slice: `EchoPlaybackService` no longer stores `PlaybackCurrentTrackPreparationRuntimeOwner` as a `private final` field. The Service now builds the lightweight runtime adapter through `playbackCurrentTrackPreparationRuntimeOwner()` at call sites that already share the same `PlaybackRuntimeStateManager`.
- Source-of-truth status is unchanged and tighter: preparing/error-message state remains owned by `PlaybackRuntimeStateManager`; `PlaybackCurrentTrackPreparationRuntimeOwner` still maps current-track preparation events into that runtime state. No state mirror, facade, new owner, or Service strategy branch was added.
- Concrete win: one Service-held playback owner field is removed while preserving the runtime-owner boundary and keeping direct `playbackRuntimeStateManager.setPreparing(...)` / `playbackRuntimeStateManager.preparing()` calls blocked by `MainActivityArchitectureContractTest`.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationRuntimeOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`. Current focused metrics: `EchoPlaybackService.java` is `1366` lines, `MainActivityBase.java` remains `2685` lines, broad `private Playback*` Service fields are `35`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and `private final PlaybackCurrentTrackPreparationRuntimeOwner playbackCurrentTrackPreparationRuntimeOwner` hits are `0`.
## NOTE 383 - Queue state snapshot drops end-of-queue derived API (2026-07-05)
- Queue interface slice: `PlaybackQueueManager.QueueStateSnapshot` no longer exposes `isAtEndOfQueue`. Completion and crossfade policy now derive end-of-queue semantics from `currentIndex` and `queueSize` at the owning decision sites.
- Source-of-truth status is unchanged and tighter: `currentTrack`, `currentIndex`, and `queueSize` remain the queue snapshot source fields; `isQueueEmpty` remains for notification/restore/mutation paths and was not touched in this slice. No queue provider, state mirror, facade, or Service strategy branch was added.
- Concrete win: one more derivable cross-module snapshot API and Java getter are removed. `MainActivityArchitectureContractTest` now blocks `val isAtEndOfQueue`, its getter, and old `queueStateSnapshot().isAtEndOfQueue` production usage from returning.
- Verification target passed after adjusting one source-field test that previously asserted the removed getter: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCrossfadeStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`. Current focused metrics: `EchoPlaybackService.java` remains `1366` lines, `MainActivityBase.java` remains `2685` lines, broad `private Playback*` Service fields are `35`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, current Service `queueSnapshot()` literal hits remain `2`, and production `QueueStateSnapshot.isAtEndOfQueue` hits are `0`.
## NOTE 384 - Queue state snapshot drops empty derived API (2026-07-05)
- Queue interface slice: `PlaybackQueueManager.QueueStateSnapshot` no longer exposes `isQueueEmpty`. Notification state, queue restore, queue mutation, mirrored transition, and completion tests now derive empty/non-empty semantics from the source `queueSize` field at their owning decision sites.
- Source-of-truth status is unchanged and tighter: queue authority remains `PlaybackQueueManager`; the snapshot source fields are still only `currentTrack`, `currentIndex`, and `queueSize`. No queue provider, state mirror, facade, owner, Service strategy branch, resolver/cache boundary change, notification behavior change, lyrics behavior change, shutdown behavior change, background playback behavior change, or package move was added.
- Concrete win: the last derived boolean on `QueueStateSnapshot` and its Java getter were removed. `MainActivityArchitectureContractTest` now blocks `val isQueueEmpty`, `get() = queueSize <= 0`, and old production `queueStateSnapshot().isQueueEmpty()` calls from returning, while leaving `PlaybackNotificationManager.StateProvider.isQueueEmpty()` as the notification semantic API.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMutationOwnerTest --tests app.yukine.playback.PlaybackQueueRestoreOwnerTest --tests app.yukine.playback.PlaybackNotificationStateOwnerTest --tests app.yukine.playback.PlaybackQueueCompletionOwnerTest --tests app.yukine.playback.PlaybackQueueMirroredTransitionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scan remained `TOTAL=0`. Current focused metrics: `EchoPlaybackService.java` remains `1366` lines, broad `private Playback*` Service fields are `35`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, production `QueueStateSnapshot.isQueueEmpty` usage hits are `0`, and production `QueueStateSnapshot.isAtEndOfQueue` hits remain `0`.
## NOTE 385 - Startup queue restore routes through restore owner (2026-07-05)
- Service boundary slice: `EchoPlaybackService.onCreate()` no longer calls `playbackQueueManager.restorePlaybackQueue()` directly. Startup queue restore now goes through `PlaybackQueueRestoreOwner.restoreQueueForStartup()`, while user/requested restore-and-prepare continues to use `restoreLastPlayback(...)`.
- Source-of-truth status is unchanged: queue contents, current index, and restored position still belong to `PlaybackQueueManager`; persisted restore settings and saved queue still come from `PlaybackQueueStore`. No new owner, facade, state mirror, resolver/cache boundary change, notification behavior change, lyrics behavior change, shutdown behavior change, background playback behavior change, or package move was added.
- Concrete win: one direct Service queue-manager command is removed from lifecycle wiring, and `MainActivityArchitectureContractTest` now keeps `playbackQueueManager.restorePlaybackQueue()` confined to `PlaybackQueueRestoreOwner`.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueRestoreOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. This run also recompiled app Kotlin/Java. The new owner test proves startup restore loads the queue without invoking create/prepare/publish playback boundaries.
## NOTE 386 - Core module dependency boundary contract (2026-07-05)
- Architecture contract slice: `MainActivityArchitectureContractTest` now scans `core:model` and `core:common` main source plus their Gradle files to prevent core modules from depending on app shell, feature implementations, playback manager implementations, concrete playback service classes, or app/feature Gradle modules.
- Source-of-truth status is unchanged: this is a dependency-direction guard only. It does not add or move owner classes, create state mirrors, change playback/cache/resolve/lyrics/notification behavior, or reorganize packages.
- Concrete win: future cross-module API extraction has a guardrail that keeps contracts flowing down into core without pulling app implementation back into core. Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`.
## NOTE 387 - Playback owner/interface audit after queue mutation and precache slices (2026-07-05)
- Audit scope: this follows the queue mutation stop-boundary slice and the precache queue-snapshot access slice. `PlaybackQueueMutationOwner` now requires the stop/clear boundary action instead of allowing a silent no-op, and `PlaybackPrecacheManager` now funnels queue-state reads through one private `queueStateSnapshot()` helper.
- Source-of-truth status is unchanged: queue state remains `PlaybackQueueManager` (`QueueStateSnapshot.currentTrack`, `currentIndex`, `queueSize` plus `queueSnapshot()`); cache scheduling remains `PlaybackPrecacheManager`; cache keys and data-source behavior remain delegated through `PlaybackMediaCacheOperations`; URI/MediaItem resolution remains `PlaybackMediaSourceProvider`.
- Audit evidence: `EchoPlaybackService.java` is `1366` lines, `MainActivityBase.java` is `2685` lines, broad `private Playback*` Service fields are `33`, Service-held `Playback*Owner` fields are `8`, app playback `Playback*Owner.java` files are `38`, `PlaybackQueueManager` public method count remains `17`, production `fromPlaybackQueueManager(...)` hits are `0`, direct Service `queueStateSnapshot()` hits are `0`, current Service `queueSnapshot()` literal hits remain `2`, `PlaybackPrecacheManager` direct `playbackQueueManager.queueStateSnapshot();` hits are `1`, and chained `playbackQueueManager.queueStateSnapshot().getCurrentTrack()` hits are `0`.
- App-shell wiring evidence: `MainActivityBase.onCreate()` still initializes `initializeRouteStoresAndStatus()` before now-playing, download, library, platform, playback lifecycle, navigation, and playback controller binding. This preserves the guard against passing uninitialized route/status/store host fields into non-null Kotlin gateway/owner constructors.
- Verification covering the two preceding slices passed with focused targets: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMutationOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain` and `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scans remained `TOTAL=0`.
- Next candidates should avoid adding owners: prefer reducing remaining `EchoPlaybackService` wiring fields only when a field is low-use and reconstructible from stable owners, or tightening resolver/cache contracts only when it removes a real nullable boundary or interface surface.
## NOTE 388 - Current-track supplier wiring audit after visualization and Wi-Fi lock slices (2026-07-05)
- Service wiring slices: `PlaybackVisualizationCacheStateOwner` and `PlaybackWifiLockManager` now read the current track directly from `PlaybackQueueManager.queueStateSnapshot()` instead of receiving `EchoPlaybackService`'s shared `currentTrackSupplier`. The Service still keeps `currentTrackSupplier` only for `PlaybackNotificationArtworkManager`, which is intentionally deferred with the broader notification P4 work.
- Source-of-truth status is unchanged and tighter: current track authority remains `PlaybackQueueManager`; visualization cache scheduling remains owned by `PlaybackVisualizationCacheManager`; Wi-Fi lock acquire/release policy remains owned by `PlaybackWifiLockManager`; HTTP-track classification remains delegated to `PlaybackMediaSourceProvider.isHttpTrack`. No state mirror, facade, new owner, notification behavior change, lyrics behavior change, shutdown behavior change, or package move was added.
- Concrete wins: two Service supplier/lambda forwarding paths are removed, and the focused owner tests now prove both owners read the latest queue-manager current track rather than a captured track. `MainActivityArchitectureContractTest` also registers both owners in the explicit queue-snapshot usage audit.
- Audit evidence after NOTE 388: `EchoPlaybackService.java` is `1366` lines, `MainActivityBase.java` is `2685` lines, broad `private Playback*` Service fields are `31`, Service-held `Playback*Owner` fields are `8`, app playback `Playback*Owner.java` files remain `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, Service `currentTrackSupplier` hits are `2`, and current Service `queueSnapshot()` literal hits remain `2`.
- Verification targets passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackVisualizationCacheStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain` and `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackWifiLockManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`. Focused mojibake scans remained `TOTAL=0`, and `git diff --check` reported only CRLF warnings.
## NOTE 389 - App-shell gateway binding is guarded behind route/status/store creation (2026-07-05)
- App-shell contract slice: `MainActivityArchitectureContractTest` now explicitly checks the `onCreate()` prefix before `initializeRouteStoresAndStatus()` and rejects binding now-playing gateways, download request wiring, library gateway, or playback lifecycle wiring before `uiShellController`, `routeController`, `playbackStore`, and `statusMessageController` are created.
- Source-of-truth status is unchanged: route/navigation still belongs to `MainRouteController` / `NavigationViewModel`, playback shell snapshots still belong to `MainPlaybackStore`, and status text still flows through `StatusMessageController`. No owner, facade, state mirror, runtime behavior, package move, playback behavior, notification behavior, lyrics behavior, or shutdown behavior changed.
- Concrete win: the previous `MainLibraryGatewayFactory` null-route regression now has a direct guard against moving host gateway binding above route/status/store initialization, including the Kotlin non-null `LibraryRouteActions` boundary that receives `routeController` by value.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`.
## NOTE 390 - Replacement recovery diagnostics require the media-source owner (2026-07-05)
- Resolver boundary slice: `EchoPlaybackService.replaceCurrentTrackAndResume(...)` no longer treats a missing `PlaybackMediaSourceProvider` as a valid recovery-diagnostics mode. The recovery quality string now comes directly from `mediaSourceProvider.streamingQualityForTrack(track)` after the provider is initialized in `onCreate()`.
- Source-of-truth status is unchanged and tighter: streaming quality remains owned by `PlaybackMediaSourceProvider`; queue replacement remains owned by `PlaybackCurrentTrackReplacementOwner` / `PlaybackQueueManager`; diagnostics remain in `PlaybackStreamingDiagnostics`. No owner, facade, cache policy, state mirror, notification behavior, lyrics behavior, shutdown behavior, or package move was added.
- Concrete win: one Service-level missing-owner strategy branch is removed from the resolver/diagnostics boundary, and `MainActivityArchitectureContractTest` now blocks `String quality = mediaSourceProvider == null` from returning.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackReplacementOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; this run also recompiled app Kotlin and Java.
## NOTE 391 - Service snapshots require the queue manager after initialization (2026-07-05)
- Service boundary slice: `EchoPlaybackService.queueSnapshot()` and `EchoPlaybackService.snapshot()` no longer convert a missing `PlaybackQueueManager` into `Collections.emptyList()` or `PlaybackStateSnapshot.empty()`. After `onCreate()` constructs the queue manager, these service-port reads delegate directly to `PlaybackQueueManager.queueSnapshot()` and `PlaybackStateSnapshotOwner.snapshot()`.
- Source-of-truth status is unchanged and tighter: queue membership/current index/current track remain owned by `PlaybackQueueManager`; the full playback snapshot remains assembled by `PlaybackStateSnapshotOwner`; `PlaybackViewModel` and `MainPlaybackStore` still receive published snapshots through the existing service/listener path. No state mirror, owner, facade, notification behavior, lyrics behavior, shutdown behavior, or package move was added.
- Concrete wins: two Service-level missing-queue-manager fake state branches are removed, and `MainActivityArchitectureContractTest` now blocks `return playbackQueueManager == null` and the old empty-list queue fallback while allowing exactly one direct `return playbackQueueManager.queueSnapshot();` at the service-port boundary.
- Mini-audit after NOTE 390-391: `EchoPlaybackService.java` is `1263` lines, `MainActivityBase.java` is `2479` lines, broad `private Playback*` Service fields are `50`, Service-held `Playback*Owner` fields are `19`, app/feature playback `Playback*Owner` files are `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, Service `playbackQueueManager == null` hits are `0`, Service `currentTrackSupplier` hits remain `2` and are still deferred to P4 notification artwork, and current Service `queueSnapshot()` literal hits remain `2`.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackStateSnapshotOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; this run also recompiled app Kotlin and Java.
## NOTE 392 - Recovery diagnostics no longer guards a final diagnostics owner (2026-07-05)
- Service boundary slice: `EchoPlaybackService.replaceCurrentTrackAndResume(...)` no longer checks `streamingDiagnostics == null` before recording replacement recovery. `streamingDiagnostics` is a final Service-owned diagnostics sink and the public `streamingDiagnostics()` method already reads it directly.
- Source-of-truth status is unchanged: replacement remains owned by `PlaybackCurrentTrackReplacementOwner` / `PlaybackQueueManager`, streaming quality remains owned by `PlaybackMediaSourceProvider`, and diagnostics remain owned by `PlaybackStreamingDiagnostics`. The existing `track == null` guard is unchanged. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, or package move was added.
- Concrete win: one more Service-level fake missing-owner strategy branch is removed, and `MainActivityArchitectureContractTest` now blocks `streamingDiagnostics == null` from returning in `EchoPlaybackService`.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackReplacementOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; this run also recompiled app Kotlin and Java.
## NOTE 393 - Warmup coordinator is required after Service initialization (2026-07-05)
- Service boundary slice: `EchoPlaybackService.warmPlaybackTrack(...)`, mirrored queue preparation, and single-track preparation now call `playbackWarmupCoordinator.warmup(track)` directly instead of treating the coordinator as optional. `PlaybackWarmupCoordinator.release()` already owns the post-shutdown no-op behavior, so Service-level null guards were migration residue rather than lifecycle protection.
- Source-of-truth status is unchanged: queue/current track state remains in `PlaybackQueueManager`, cache scheduling remains in `PlaybackPrecacheManager`, visualization cache scheduling remains in `PlaybackVisualizationCacheManager`, and warmup fanout/release state remains in `PlaybackWarmupCoordinator`. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete win: three Service-level fake missing-owner checks are removed, and `MainActivityArchitectureContractTest` now blocks `playbackWarmupCoordinator != null` from returning in the Service while keeping `PlaybackWarmupCoordinator::release` as the shutdown boundary.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackWarmupCoordinatorTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; this run also recompiled app Kotlin and Java.
## NOTE 394 - Warmup fanout uses required cache owners (2026-07-05)
- Service wiring slice: `EchoPlaybackService` now wires `PlaybackWarmupCoordinator` with direct `PlaybackPrecacheManager.precacheTrack(...)` and `PlaybackVisualizationCacheManager.scheduleVisualizationCache(...)` callbacks instead of wrapping each callback in a Service-level null check. Both cache owners are created before the coordinator and already own their post-release no-op/cancel behavior.
- Source-of-truth status is unchanged: queue/current track state remains in `PlaybackQueueManager`, cache scheduling and cache release remain in `PlaybackPrecacheManager`, visualization cache scheduling and writer cancellation remain in `PlaybackVisualizationCacheManager`, and warmup release gating remains in `PlaybackWarmupCoordinator`. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete win: two more Service-level missing-cache-owner strategy checks are removed, and `MainActivityArchitectureContractTest` now blocks `playbackPrecacheManager != null` and `playbackVisualizationCacheManager != null` from returning in the Service. Current focused metrics: `EchoPlaybackService.java` is `1347` lines, `MainActivityBase.java` remains `2685` lines, strict `private Playback*` Service fields are `50`, app/feature `Playback*Owner` files remain `39`, production `fromPlaybackQueueManager(...)` hits are `0`, direct Service `queueStateSnapshot()` hits are `0`, and warmup/precache/visualization nullable guard hits in production Service are `0`.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackWarmupCoordinatorTest --tests app.yukine.playback.PlaybackVisualizationCacheManagerTest :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; app Kotlin/Java compile tasks were also up to date.
## NOTE 395 - Position/progress managers are required Service wiring (2026-07-05)
- Service wiring slice after the two warmup/cache slices: `EchoPlaybackService.persistCurrentPlaybackPosition(...)`, `startProgressUpdates()`, and `stopProgressUpdates()` now call the already-created `PlaybackPositionManager` / `PlaybackProgressUpdateManager` directly instead of treating them as optional. `PlaybackProgressUpdateManager.release()` remains the owner of callback cancellation; `PlaybackPositionManager` remains the owner of saved playback-position throttling and clamping.
- Source-of-truth status is unchanged: current track still comes from `PlaybackQueueManager` through the late-bound `PlaybackPositionManager.PlaybackStateProvider`, current player position still comes from `PlaybackPlayerStateOwner`, persisted resume position still goes through `PlaybackQueueStore`, and progress ticks remain owned by `PlaybackProgressUpdateManager`. No state mirror, owner, facade, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete wins: three Service-level missing-owner checks are removed, and `MainActivityArchitectureContractTest` now blocks `playbackPositionManager != null` and `playbackProgressUpdateManager != null` helper guards from returning. Mini-audit after NOTE 393-395: `EchoPlaybackService.java` is `1341` lines, `MainActivityBase.java` remains `2685` lines, strict `private Playback*` Service fields are `50`, app/feature `Playback*Owner` files remain `39`, production `fromPlaybackQueueManager(...)` hits are `0`, direct Service `queueStateSnapshot()` hits are `0`, position/progress nullable guard hits in production Service are `0`, and `PlaybackMediaSourceResolutionOwner` production hits remain `0`.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackPositionManagerTest --tests app.yukine.playback.PlaybackProgressUpdateManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; this run also recompiled app Kotlin and Java.
## NOTE 396 - Audio effect settings store is required after Service initialization (2026-07-05)
- Service wiring slice: `EchoPlaybackService.audioEffectSettings()` and `applyAudioEffectSettings(...)` now call `PlaybackAudioEffectSettingsStore` directly instead of converting a missing store into `AudioEffectSettings.DEFAULT`. The store is created and restored near the start of `onCreate()` and remains the owner of null normalization through `restore()` / `apply(...)`.
- Source-of-truth status is unchanged: audio-effect settings current value remains in `PlaybackAudioEffectSettingsStore`, persistence still flows through `MusicLibraryRepository` via the store, and runtime binding remains in `PlaybackAudioEffectManager`. No state mirror, owner, facade, resolver/cache boundary change, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, or package move was added.
- Concrete win: two Service-level missing-store strategy branches are removed, and `MainActivityArchitectureContractTest` now blocks `playbackAudioEffectSettingsStore == null` from returning.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackAudioEffectSettingsStoreTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; this run also recompiled app Kotlin and Java.
## NOTE 397 - Sleep timer command owner requires its manager binding (2026-07-05)
- Service wiring slice: `PlaybackSleepTimerCommandOwner` no longer treats a missing `PlaybackSleepTimerManager` binding as a valid no-op mode for start, cancel, or remaining-time reads. `EchoPlaybackService.onCreate()` already creates the command owner, creates `PlaybackSleepTimerManager`, and immediately calls `bindPlaybackSleepTimerManager(...)` before service commands are exposed.
- Source-of-truth status is unchanged: sleep timer end time, remaining-time calculation, callback scheduling, cancel policy, and release behavior remain in `PlaybackSleepTimerManager`; `PlaybackSleepTimerCommandOwner` only owns the command/action bridge to pause and publish state. No state mirror, owner, facade, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete win: three unbound-manager no-op/fallback paths are removed from the command owner, and `MainActivityArchitectureContractTest` now blocks `if (manager != null)` / `manager == null ? 0L` from returning.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackSleepTimerCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; this run also recompiled app Kotlin and Java.
## NOTE 398 - Playback owner/interface mini-audit after required-store/binding slices (2026-07-05)
- Audit scope: this follows NOTE 396 audio-effect settings and NOTE 397 sleep-timer command-owner binding. Both slices removed fake missing-owner states without adding owners, facades, state mirrors, resolver/cache behavior, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, or package moves.
- Current metrics: `EchoPlaybackService.java` is `1239` lines, `MainActivityBase.java` is `2479` lines, strict `private Playback*` Service fields are `40`, Service-held `Playback*Owner` fields are `17`, app/feature `Playback*Owner` files are `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, Service `queueSnapshot()` literal hits remain `2`, Service `currentTrackSupplier` hits remain `2`, and `PlaybackMediaSourceResolutionOwner` production hits remain `0`.
- Queue/interface evidence: `PlaybackQueueManager.QueueProvider` remains absent; `QueueStateSnapshot` still exposes only source fields `currentTrack`, `currentIndex`, and `queueSize`; derived booleans such as `hasCurrentTrack`, `hasMultipleTracks`, `isQueueEmpty`, and `isAtEndOfQueue` remain absent from the snapshot.
- Resolver/cache boundary evidence: `PlaybackPrecacheManager` still does not hold a `PlaybackMediaSourceProvider` field; it uses `PlaybackMediaCacheOperations.fromMediaSourceProvider(...)` at construction and keeps queue reads behind its private `queueStateSnapshot()` helper. `PlaybackMediaCacheOperations` still has no MediaItem/source creation surface, so URI/MediaItem resolution remains in `PlaybackMediaSourceProvider` and cache scheduling remains in `PlaybackPrecacheManager`.
- App-shell wiring evidence: `MainActivityBase.onCreate()` still creates route/status/store state at `initializeRouteStoresAndStatus()` before now-playing gateways, download request wiring, library gateway, platform controllers, playback lifecycle controllers, navigation rendering, and playback controllers. This preserves the guard against passing uninitialized route/status/store host fields by value into Kotlin non-null gateway/owner constructors.
- Next safe candidates: prefer low-risk required-owner or queue-interface cuts outside notification/lyrics/shutdown, such as auditing `PlaybackSessionCommandOwner` / `PlaybackErrorRecoveryCommandOwner` queue-state reads, or keeping the next round as resolver/cache contract reinforcement. Defer `PlaybackNotificationStateOwner`, `PlaybackLyricsStateOwner`, artwork suppliers, and shutdown resource ordering until the smoke table is stable.
## NOTE 399 - Session command owner requires its delegate dependencies (2026-07-05)
- Service/session boundary slice: `PlaybackSessionCommandOwner` now requires its playback command delegate, seek delegate, repeat-mode delegate, controller-media-items delegate, queue manager, and metadata provider at construction time. `EchoPlaybackService.onCreate()` already builds the owner after queue manager, controller media items, and notification metadata owners exist.
- Source-of-truth status is unchanged: queue/current-track state remains in `PlaybackQueueManager`, controller media item mapping remains in `PlaybackControllerMediaItemsOwner`, metadata remains behind `PlaybackNotificationManager.mediaMetadataForTrack(...)`, and Media3 session/player lifecycle remains in `PlaybackSessionManager` / `PlaybackSessionPlayer`. No state mirror, owner, facade, notification behavior change, lyrics behavior change, shutdown behavior change, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete win: five session command delegate nullable boundary risks now fail during owner construction instead of later inside Media3 command callbacks, and `MainActivityArchitectureContractTest` locks the constructor `Objects.requireNonNull(...)` assignments.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackSessionCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; this run also recompiled app Kotlin and Java.
## NOTE 400 - Error recovery command owner requires its action delegates (2026-07-05)
- Error-recovery boundary slice: `PlaybackErrorRecoveryCommandOwner` now requires its playback preparer, skip-next command, error-message store, state publisher, warning logger, and queue manager at construction time. `EchoPlaybackService.onCreate()` already builds the owner after `PlaybackQueueManager`, `PlaybackQueueCommandOwner`, and the preparation runtime owner exist.
- Source-of-truth status is unchanged: failed/current-track state remains in `PlaybackQueueManager`, retry/skip policy remains in `PlaybackErrorRecoveryManager`, prepare-current remains in `PlaybackQueueCommandOwner`, and error-message state remains behind `PlaybackCurrentTrackPreparationRuntimeOwner`. No state mirror, owner, facade, notification behavior change, lyrics behavior change, shutdown behavior change, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete win: five error-recovery action delegate nullable boundary risks now fail during owner construction instead of later during player-error recovery callbacks, and `MainActivityArchitectureContractTest` locks the constructor `Objects.requireNonNull(...)` assignments.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackErrorRecoveryCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; this run also recompiled app Kotlin and Java.
## NOTE 401 - MuMu app-shell startup smoke checkpoint (2026-07-05)
- App-shell verification slice: current `MainActivityBase.onCreate()` creates route/status/store state through `initializeRouteStoresAndStatus()` before now-playing gateway binding, download request wiring, library gateway binding, platform controllers, playback lifecycle wiring, navigation rendering, and playback controllers. `MainActivityArchitectureContractTest` already rejects moving now-playing, download, library, or playback lifecycle wiring before this state creation step.
- UI text status: focused UTF-8/mojibake scan for `feature/ui-common/src/main/java/app/yukine/ui/HomeDashboardScreen.kt`, `LibraryGroupsScreen.kt`, `TrackListScreen.kt`, and `MainActivityArchitectureContractTest.java` returned `TOTAL=0`. A MuMu startup screenshot showed readable Chinese home text such as "搜索本地和网络音乐", "继续播放", "今日回声", "最近播放", and "查看全部"; no source or visible mojibake was observed in this smoke scope.
- Runtime smoke evidence: MuMu was connected as `127.0.0.1:7555 device` (`HUAWEI ALN-AL00`, Android 12). Verification passed with `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`, `python C:\\Users\\31283\\.codex\\skills\\chinese-utf8-development\\scripts\\scan_mojibake.py feature\\ui-common\\src\\main\\java\\app\\yukine\\ui\\HomeDashboardScreen.kt feature\\ui-common\\src\\main\\java\\app\\yukine\\ui\\LibraryGroupsScreen.kt feature\\ui-common\\src\\main\\java\\app\\yukine\\ui\\TrackListScreen.kt app\\src\\test\\java\\app\\yukine\\MainActivityArchitectureContractTest.java`, `.\\gradlew.bat :app:assembleDebug --console=plain`, `adb -s 127.0.0.1:7555 install -r app\\build\\outputs\\apk\\debug\\app-debug.apk`, and `adb -s 127.0.0.1:7555 shell monkey -p app.yukine 1`.
- Source-of-truth status is unchanged: this was a verification/documentation checkpoint only. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, or package move was added. Logcat showed the Activity displayed successfully and no `FATAL EXCEPTION`; observed MuMu/SystemUI resource warnings and notification-artwork StrictMode debug stack remain outside this low-risk app-shell smoke slice.
## NOTE 402 - Queue snapshot companion API contract tightened (2026-07-05)
- Queue/interface contract slice: `MainActivityArchitectureContractTest.playbackQueueManagerPublicApiStaysExplicitlyAudited()` now also locks `PlaybackQueueManager.QueueStateSnapshot` companion public functions to only `empty()`. This prevents derived queue reads from returning as companion helpers after the instance snapshot was narrowed to source fields.
- Source-of-truth status is unchanged: queue membership/current index/current track remain in `PlaybackQueueManager`; `QueueStateSnapshot` still exposes only `currentTrack`, `currentIndex`, and `queueSize`; `QueueProvider` remains absent; URI/MediaItem resolution remains in `PlaybackMediaSourceProvider`; cache scheduling remains in `PlaybackPrecacheManager` through `PlaybackMediaCacheOperations`. No owner, facade, state mirror, Service strategy branch, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete win: one more queue-interface regression path is now covered by the existing architecture contract without adding a new layer or widening `PlaybackQueueManager`.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused test-file mojibake scan returned `TOTAL=0`, and added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`.
## NOTE 403 - Queue completion owner requires Service boundary actions (2026-07-05)
- Queue completion boundary slice: `PlaybackQueueCompletionOwner` now requires the three Service boundary actions (`stopAndClearAction`, `stopAtEndOfQueueAction`, and `skipToNextAction`) at construction time instead of treating missing delegates as valid no-ops. `EchoPlaybackService.playbackQueueCompletionOwner()` already wires concrete method references for all three actions.
- Source-of-truth status is unchanged: queue membership/current index/current track and stop/clear queue preparation remain in `PlaybackQueueManager`; completion routing remains in `PlaybackQueueCompletionOwner`; Media3/Service shutdown and player boundary behavior remain in `EchoPlaybackService`. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete win: three late nullable completion-boundary no-op risks now fail during owner construction, and `MainActivityArchitectureContractTest` locks the required delegate assignments plus removal of `if (action != null)`.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCompletionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, and added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`.
## NOTE 404 - Precache manager requires scheduling and matching delegates (2026-07-05)
- Cache-owner boundary slice: `PlaybackPrecacheManager` now requires `currentPlayerMediaItemSupplier`, `mediaItemTrackMatcher`, and `callbackScheduler` at construction time. `currentPlayerLoadsTrack(...)` now calls those required delegates directly instead of keeping missing-supplier or missing-matcher fallback branches.
- Source-of-truth status is unchanged: queue/current-track state remains in `PlaybackQueueManager`, cache scheduling and release remain in `PlaybackPrecacheManager`, cache key/range operations remain in `PlaybackMediaCacheOperations`, and URI/MediaItem resolution remains in `PlaybackMediaSourceProvider`. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete win: three more late nullable precache delegate risks now fail during owner construction, and `MainActivityArchitectureContractTest` locks the required delegate assignments plus removal of the supplier/matcher null fallback. Current mini-audit metrics: `EchoPlaybackService.java` is `1337` lines, `MainActivityBase.java` is `2685` lines, strict `private Playback*` Service fields are `40`, Service-held `Playback*Owner` fields are `17`, app/feature `Playback*Owner` files are `38`, production `fromPlaybackQueueManager(...)` hits are `0`, direct Service `queueStateSnapshot()` hits are `0`, Service `currentTrackSupplier` hits remain `2`, Service `queueSnapshot()` literal hits remain `2`, `QueueProvider` and derived `QueueStateSnapshot` helpers remain absent, and precache media-source/delegate null branch hits are `0`.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, and added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`.
## NOTE 405 - Feature Gradle modules cannot depend on app implementation (2026-07-05)
- Dependency-direction contract slice: `MainActivityArchitectureContractTest` now checks every feature `build.gradle` file and rejects `project(":app")` / `project(':app')`. This complements the existing source-level feature guards against app shell and concrete playback-service implementation classes.
- Source-of-truth status is unchanged: this is a structure guard only. Playback queue state remains in `PlaybackQueueManager`, cache policy remains in `PlaybackPrecacheManager`, URI/MediaItem resolution remains in `PlaybackMediaSourceProvider`, app-shell host wiring remains in `MainActivityBase` / app-level owners, and feature modules must not reach back into app implementation to bypass contracts.
- Concrete win: one cross-module implementation-dependency regression path is now covered without adding an owner, facade, bridge, state mirror, package move, or runtime behavior change. The remaining Service `currentTrackSupplier` hits are intentionally deferred because they belong to notification artwork wiring and should wait for fuller notification smoke coverage.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused test-file mojibake scan returned `TOTAL=0`, and added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`.
## NOTE 406 - Playback state snapshot owner requires snapshot providers (2026-07-05)
- Snapshot boundary slice: `PlaybackStateSnapshotOwner` now requires playback-position, sleep-timer, and realtime-beat providers at construction time. Snapshot generation calls these required providers directly instead of treating missing owner wiring as a valid zero-position, zero-sleep, or zero-beat state.
- Source-of-truth status is unchanged: queue/current-track/current-index state remains in `PlaybackQueueManager`, player position and playing state remain in `PlaybackPlayerStateOwner`, runtime state remains in `PlaybackRuntimeStateManager`, sleep timer state remains in `PlaybackSleepTimerCommandOwner` / `PlaybackSleepTimerManager`, visualization remains in `PlaybackVisualizationAnalyzer`, and realtime beat remains in `PlaybackRealtimeVisualizationOwner`. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete win: three late nullable snapshot-provider fallback paths now fail during owner construction, and `MainActivityArchitectureContractTest` locks the required assignments plus removal of the null fallback branches.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackStateSnapshotOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, and added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`.
## NOTE 407 - Buffered progress owner requires position delegates (2026-07-05)
- Visualization boundary slice: `PlaybackBufferedProgressOwner` now requires playback-position and buffered-position suppliers at construction time. It still returns `0` for invalid duration and for `IllegalStateException` while reading a released player, but missing owner wiring no longer becomes a silent zero-progress state.
- Source-of-truth status is unchanged: player position and buffered position remain in `PlaybackPlayerStateOwner`, buffered-progress calculation remains in `PlaybackBufferedProgressOwner`, and visualization state remains in `PlaybackVisualizationStateOwner` / `PlaybackVisualizationAnalyzer`. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete win: two nullable buffered-progress delegate fallback paths now fail during owner construction, and `MainActivityArchitectureContractTest` locks the required assignments plus removal of the null fallback branches.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackBufferedProgressOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, and added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`.
## NOTE 408 - Playback owner/interface mini-audit after required-delegate slices (2026-07-05)
- Audit scope: this is the recurring check after the queue-completion, precache, snapshot, and buffered-progress required-delegate slices. It records current owner/interface/wiring/state metrics only; no runtime behavior, owner, facade, state mirror, notification, lyrics, shutdown, background playback, resolver/cache boundary, or package move was changed.
- Current metrics: `EchoPlaybackService.java` is `1239` lines and `MainActivityBase.java` is `2479` lines. Service `private Playback*` field declarations are `40`, Service-held `Playback*Owner` field declarations are `10`, app/feature `Playback*Owner` files are `39`, production `fromPlaybackQueueManager(...)` hits are `0`, direct Service `queueStateSnapshot()` hits are `0`, Service `queueSnapshot()` literal hits remain `2`, Service `currentTrackSupplier` hits remain `2`, production `PlaybackMediaSourceResolutionOwner` hits remain `0`, feature Gradle `project(":app")` dependency hits are `0`, root-package `*Bindings*` files remain `0`, and root-package `*Controller*` files remain `44`.
- Queue/interface evidence: exact production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` hits are `0`; the remaining exact hits are architecture-test guards. `PlaybackQueueManager.QueueStateSnapshot` still exposes only source fields `currentTrack`, `currentIndex`, and `queueSize`; its companion public API is still only `empty()`, and derived helpers such as `hasCurrentTrack`, `hasMultipleTracks`, `isQueueEmpty`, and `isAtEndOfQueue` remain absent. Current public queue commands/reads are `appendToQueue`, `applyMirroredTransitionIndex`, `moveQueueTrack`, `playFirstQueuedTrack`, `playQueue`, `preparePlaybackCompletionAction`, `prepareStopAndClearPlaybackState`, `queueSnapshot`, `queueStateSnapshot`, `removeTracksById`, `replaceCurrentQueueTrack`, `replaceCurrentTrackAndResume`, `replaceQueuedTrackById`, `restorePlaybackQueue`, `reuseMirroredQueueIfAvailable`, `skipToNextImmediately`, and `skipToPrevious`.
- Resolver/cache boundary evidence: `PlaybackPrecacheManager.fromMediaSourceProvider(...)` still converts the resolver into `PlaybackMediaCacheOperations.fromMediaSourceProvider(...)` plus `mediaSourceProvider::mediaItemMatchesTrackForReuse`; it does not own URI/MediaItem creation. URI/MediaItem resolution remains in `PlaybackMediaSourceProvider`, while cache scheduling, partial range planning, callback cancellation, executor release, and cache cleanup remain in `PlaybackPrecacheManager`.
- App-shell wiring evidence: `MainActivityBase.onCreate()` still runs `initializeViewModels(...)`, creates the streaming action gateway, binds the streaming playback coordinator, then creates route/status/store state through `initializeRouteStoresAndStatus()` before `initializeNowPlayingGateways()`, `initializeDownloadRequests()`, `initializeLibraryGateway()`, platform controllers, playback lifecycle controllers, navigation rendering, playback controllers, settings effects, streaming owners, stores/data gateways, network owners, and onboarding/startup. The route/status/store host fields are therefore initialized before library gateway construction passes `routeController` by value or callbacks capture `statusMessageController` / `playbackStore`.
- Source-of-truth status is unchanged for this batch: queue membership/current index/current track remain in `PlaybackQueueManager`; player position/playing/buffered position remain in `PlaybackPlayerStateOwner`; runtime playback settings remain in `PlaybackRuntimeStateManager` plus the settings stores; cache policy remains in `PlaybackPrecacheManager`; URI/MediaItem resolution remains in `PlaybackMediaSourceProvider`; app-shell route state remains in `MainRouteController` / `NavigationViewModel`. No new mirrored state was added.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; `MainActivityArchitectureContractTest.java` mojibake scan returned `TOTAL=0`, and added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`. Full-document scan still reports historical handoff mojibake outside this added NOTE.
## NOTE 409 - Queue command owner requires prepare/publish delegates (2026-07-05)
- Queue command boundary slice: `PlaybackQueueCommandOwner` now requires its playback preparer and state publisher at construction time. `EchoPlaybackService` already wires concrete method references for both delegates before binding the owner to `PlaybackQueueManager`, so missing prepare/publish wiring is no longer treated as a valid no-op or delayed crash mode.
- Source-of-truth status is unchanged: queue membership/current index/current track remain in `PlaybackQueueManager`; current-track preparation remains behind the Service/Media3 boundary method reference; state publishing remains in `EchoPlaybackService.publishState()`. The fallback callbacks in `prepareCurrentOrRunFallback(...)` and `runIfCurrentTrackMissing(...)` remain nullable by caller semantics and were not changed. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete win: two late nullable queue-command delegate paths now fail during owner construction, and `MainActivityArchitectureContractTest` locks the required assignments plus removal of the `playbackPreparer == null` prepare fallback.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`, and `git diff --check` reported only CRLF normalization warnings.
## NOTE 410 - Play history recorder requires history and queue delegates (2026-07-05)
- Play-history boundary slice: `PlaybackPlayHistoryRecorder` now requires its history sink, transition-state manager, repository factory input, recorder action input, play-when-ready supplier, and queue manager at construction/action creation time. Missing owner wiring no longer becomes a no-op recorder or a false `playWhenReady` state.
- Source-of-truth status is unchanged: current track still comes from `PlaybackQueueManager.queueStateSnapshot()`, last-marked-track de-duplication remains in `PlaybackTransitionStateManager`, and persisted play history still flows through `MusicLibraryRepository.markPlayed(...)`. The business skips for paused playback, missing current track, and repeated same-track marks are unchanged. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete win: five late nullable play-history wiring paths now fail at owner/action construction, and `MainActivityArchitectureContractTest` locks removal of the recorder null no-op, nullable `playWhenReady` false fallback, and nullable transition-state skip.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPlayHistoryRecorderTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`, and `git diff --check` reported only CRLF normalization warnings.
## NOTE 411 - Playback owner/interface mini-audit after queue-command and history required-delegate slices (2026-07-05)
- Audit scope: this follows NOTE 409 and NOTE 410. Both slices removed nullable delegate fallback paths from existing owners/actions without adding owners, facades, state mirrors, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary changes, or package moves.
- Current metrics: `EchoPlaybackService.java` is `1338` lines and `MainActivityBase.java` is `2686` lines. Service `private Playback*` field declarations are `34`, Service-held `Playback*Owner` field declarations are `8`, app/feature `Playback*Owner` files are `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, Service `queueSnapshot()` literal hits remain `2`, Service `currentTrackSupplier` hits remain `2`, production `PlaybackMediaSourceResolutionOwner` hits remain `0`, exact production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` hits remain `0`, feature Gradle `project(":app")` dependency hits remain `0`, root-package `*Bindings*` files remain `0`, and root-package `*Controller*` files remain `44`.
- Source-of-truth status is unchanged for this mini-batch: queue membership/current index/current track remain in `PlaybackQueueManager`; current-track preparation and state publishing remain at the Service/Media3 boundary; play-history de-duplication remains in `PlaybackTransitionStateManager`; persisted history writes remain in `MusicLibraryRepository`. No new mirrored state was added.
## NOTE 412 - Mirrored transition owner requires runtime delegates (2026-07-05)
- Queue mirrored-transition boundary slice: `PlaybackQueueMirroredTransitionOwner` now requires the current-track volume applier and player-mirrors-queue supplier at construction time. Missing runtime wiring no longer defaults to "mirrors queue" or silently skips volume application after a mirrored transition.
- Source-of-truth status is unchanged: queue membership/current index/current track remain in `PlaybackQueueManager`; player-mirrors-queue state remains in `PlaybackQueueRuntimeStateManager`; current-track volume application remains in the Service/Media3 runtime boundary through `EchoPlaybackService.applyCurrentTrackVolumeToPlayer()`. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete win: two nullable mirrored-transition delegate fallback paths now fail during owner construction, and `MainActivityArchitectureContractTest` locks removal of the null-as-true mirror fallback and nullable current-track-volume guard.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMirroredTransitionOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`, and `git diff --check` reported only CRLF normalization warnings.
## NOTE 413 - Favorite command owner requires toggle/publish delegates (2026-07-05)
- Favorite command boundary slice: `PlaybackFavoriteCommandOwner.toggleCurrentFavorite(...)` now requires `ToggleFavoriteUseCase` and the state publisher at command entry. Missing favorite command wiring no longer becomes a silent no-op or skipped state publish.
- Source-of-truth status is unchanged: current track still comes from `PlaybackQueueManager.queueStateSnapshot()`, favorite mutation still belongs to `ToggleFavoriteUseCase`, and state publishing remains at the Service boundary through `EchoPlaybackService.publishState()`. The no-current-track case still flows through `ToggleFavoriteUseCase.toggle(null) == false`. Notification favorite display state was not changed in this slice.
- Concrete win: two nullable favorite-command delegate fallback paths now fail at command entry, and `MainActivityArchitectureContractTest` locks removal of `toggleFavoriteUseCase != null` / `statePublisher != null` from the command owner.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackFavoriteCommandOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`, and `git diff --check` reported only CRLF normalization warnings.
## NOTE 414 - Playback owner/interface mini-audit after mirrored-transition and favorite required-delegate slices (2026-07-05)
- Audit scope: this follows NOTE 412 and NOTE 413. Both slices removed nullable delegate fallback paths from existing owners/commands without adding owners, facades, state mirrors, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary changes, or package moves.
- Current metrics: `EchoPlaybackService.java` is `1338` lines and `MainActivityBase.java` is `2686` lines. Service `private Playback*` field declarations are `34`, Service-held `Playback*Owner` field declarations are `8`, app/feature `Playback*Owner` files are `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, Service `queueSnapshot()` literal hits remain `2`, Service `currentTrackSupplier` hits remain `2`, production `PlaybackMediaSourceResolutionOwner` hits remain `0`, exact production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` hits remain `0`, feature Gradle `project(":app")` / `project(':app')` dependency hits remain `0`, root-package `*Bindings*` files remain `0`, and root-package `*Controller*` files remain `44`.
- Source-of-truth status is unchanged for this mini-batch: queue membership/current index/current track remain in `PlaybackQueueManager`; player-mirrors-queue state remains in `PlaybackQueueRuntimeStateManager`; favorite mutation remains in `ToggleFavoriteUseCase`; playback state publishing remains at the Service boundary. No new mirrored state was added.
## NOTE 415 - Visualization cache state owner requires scheduler wiring (2026-07-05)
- Visualization cache boundary slice: `PlaybackVisualizationCacheStateOwner` now requires its main handler and cache-task scheduler at construction time. Missing visualization-cache scheduler wiring no longer becomes a silent no-op.
- Source-of-truth status is unchanged: current track still comes from `PlaybackQueueManager.queueStateSnapshot()`, visualization cache scheduling remains in `PlaybackVisualizationCacheManager`, and background execution remains on the existing `PlaybackTaskScheduler` wired by `EchoPlaybackService`. Cache keys/ranges still belong to `PlaybackMediaCacheOperations`; URI/MediaItem resolution still belongs to `PlaybackMediaSourceProvider`. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete win: two nullable visualization-cache state dependencies now fail during owner construction, and `MainActivityArchitectureContractTest` locks removal of the nullable cache scheduler no-op.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackVisualizationCacheStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`, and `git diff --check` reported only CRLF normalization warnings.
## NOTE 416 - Mirrored player owner requires queue mirror delegates (2026-07-05)
- Queue mirrored-player boundary slice: `PlaybackQueueMirroredPlayerOwner` now requires its queue mirror matcher, player availability, preparing state, waveform reset, playback parameter, seek, play-when-ready, player start, mirror-state, and failure logger delegates at construction time. `mirroredQueueMatcher(...)` also requires its mirror-state, player-count, queue-snapshot, and queue-track matcher inputs instead of accepting missing inputs as a false queue match.
- Source-of-truth status is unchanged: queue membership/current index/current track remain in `PlaybackQueueManager`, player-mirrors-queue state remains in `PlaybackQueueRuntimeStateManager`, and Media3 player operations remain behind `EchoPlaybackService` method references/lambdas. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete win: multiple nullable mirrored-player delegate fallback paths now fail at owner/matcher construction, and `MainActivityArchitectureContractTest` locks removal of the nullable matcher/player-availability guards. The app-shell initialization order and migrated UI Chinese strings were rechecked first; current source already has `initializeRouteStoresAndStatus()` before gateway binding and the UI mojibake guard remains in `MainActivityArchitectureContractTest`.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueMirroredPlayerOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`, and `git diff --check` reported only CRLF normalization warnings.
## NOTE 417 - Playback owner/interface mini-audit after visualization-cache and mirrored-player slices (2026-07-05)
- Audit scope: this follows NOTE 415 and NOTE 416. Both slices removed nullable delegate fallback paths from existing owners without adding owners, facades, state mirrors, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary changes, or package moves.
- Current metrics: `EchoPlaybackService.java` is `1338` lines and `MainActivityBase.java` is `2686` lines. Service `private Playback*` field declarations are `34`, Service-held `Playback*Owner` field declarations are `8`, app/feature `Playback*Owner` files are `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, Service `queueSnapshot()` literal hits remain `2`, Service `currentTrackSupplier` hits remain `2`, production `PlaybackMediaSourceResolutionOwner` hits remain `0`, exact production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` hits remain `0`, feature Gradle `project(":app")` / `project(':app')` dependency hits remain `0`, root-package `*Bindings*` files remain `0`, and root-package `*Controller*` files remain `44`.
- Queue/API evidence: `PlaybackQueueManager.QueueStateSnapshot` still exposes only source fields `currentTrack`, `currentIndex`, and `queueSize`; derived helpers such as `isQueueEmpty`, `hasCurrentTrack`, `hasMultipleTracks`, and `isAtEndOfQueue` remain absent. Current public queue functions are `applyMirroredTransitionIndex`, `playQueue`, `appendToQueue`, `playFirstQueuedTrack`, `skipToNextImmediately`, `preparePlaybackCompletionAction`, `prepareStopAndClearPlaybackState`, `skipToPrevious`, `moveQueueTrack`, `removeTracksById`, `replaceQueuedTrackById`, `replaceCurrentTrackAndResume`, `replaceCurrentQueueTrack`, `reuseMirroredQueueIfAvailable`, `restorePlaybackQueue`, `queueSnapshot`, and `queueStateSnapshot`, plus the three nested callback-contract methods `prepareCurrent`, `publishState`, and `restoreTrackForPlayback` and mirrored-player methods `matchesCurrentQueue` / `seekTo`.
- Resolver/cache boundary evidence: `PlaybackMediaSourceProvider` still owns URI/MediaItem/media-source resolution. `PlaybackPrecacheManager.fromMediaSourceProvider(...)` only adapts it into `PlaybackMediaCacheOperations.fromMediaSourceProvider(...)` plus `mediaSourceProvider::mediaItemMatchesTrackForReuse`; cache scheduling, queue lookahead, executor release, and callback cancellation remain in `PlaybackPrecacheManager`.
- App-shell wiring evidence: `MainActivityBase.onCreate()` still creates route/status/store state through `initializeRouteStoresAndStatus()` before `initializeNowPlayingGateways()`, `initializeDownloadRequests()`, and `initializeLibraryGateway()`. The remaining two `routeController,` constructor arguments are after route creation; they remain a wiring-size risk to watch, not an uninitialized-field risk in the current source.
- Source-of-truth status is unchanged for this mini-batch: queue membership/current index/current track remain in `PlaybackQueueManager`; visualization cache scheduling remains in `PlaybackVisualizationCacheManager` / `PlaybackTaskScheduler`; player-mirrors-queue remains in `PlaybackQueueRuntimeStateManager`; URI/MediaItem resolution remains in `PlaybackMediaSourceProvider`; cache policy remains in `PlaybackPrecacheManager`; app-shell route state remains in `MainRouteController` / `NavigationViewModel`. No new mirrored state was added.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`, `git diff --check` reported only CRLF normalization warnings, and full handoff scan still reports historical mojibake outside this added NOTE.
## NOTE 418 - Crossfade state owner requires runtime state delegates (2026-07-05)
- Crossfade state boundary slice: `PlaybackCrossfadeStateOwner` now requires transition-state, player-availability, playback-state, repeat-mode, queue-manager, and base-volume delegates at construction time. Missing base-volume wiring no longer defaults to `1.0f`, and missing state delegates no longer wait until the first crossfade callback to fail.
- Source-of-truth status is unchanged: queue size/current index still comes from `PlaybackQueueManager.queueStateSnapshot()`, repeat mode and base volume still come from playback runtime settings/state stores wired by `EchoPlaybackService`, and crossfade transition state still belongs to `PlaybackTransitionStateManager`. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete win: one nullable base-volume fallback path plus the remaining crossfade state delegate late-failure paths now fail during owner construction, and `MainActivityArchitectureContractTest` locks removal of the nullable `baseVolumeProvider` default branch.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCrossfadeStateOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`, and `git diff --check` reported only CRLF normalization warnings.
## NOTE 419 - Queue state snapshot no longer exposes empty fallback factory (2026-07-05)
- Queue API boundary slice: `PlaybackQueueManager.QueueStateSnapshot` now exposes only the source fields `currentTrack`, `currentIndex`, and `queueSize`. The unused public `QueueStateSnapshot.empty()` factory was removed so owners cannot reintroduce nullable queue-state fallback behavior through a shared static empty snapshot.
- Source-of-truth status is unchanged: queue membership, current index, and current track still come from `PlaybackQueueManager.queueStateSnapshot()`. Empty-queue state is produced by the queue manager itself, not by callers manufacturing a fallback snapshot. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, service behavior change, or package move was added.
- Concrete win: one public queue snapshot API method was removed, `PlaybackQueueManagerTest` now proves empty snapshot semantics through the manager, and `MainActivityArchitectureContractTest` locks the absence of the `QueueStateSnapshot.empty()` fallback factory.
- Verification target passed: `.\\gradlew.bat :feature:playback:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueManagerTest :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`, and `git diff --check` reported only CRLF normalization warnings.
## NOTE 420 - Playback owner/interface audit after crossfade and queue-snapshot slices (2026-07-05)
- Audit scope: this follows NOTE 418 and NOTE 419. Both slices reduced existing owner/interface fallback surface without adding owners, facades, state mirrors, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary changes, service behavior changes, or package moves.
- Current metrics: `EchoPlaybackService.java` is `1338` lines and `MainActivityBase.java` is `2686` lines. Service `private Playback*` field declarations are `33`, Service-held `Playback*Owner` field declarations are `8`, app/feature `Playback*Owner` files are `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, direct Service `playbackQueueManager.queueStateSnapshot()` hits remain `0`, Service `queueSnapshot()` literal hits remain `2`, Service `currentTrackSupplier` hits remain `2`, production `PlaybackMediaSourceResolutionOwner` hits remain `0`, exact production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` hits remain `0`, production `QueueStateSnapshot.empty()` / `fun empty(): QueueStateSnapshot` hits are now `0`, and feature/core Gradle `project(":app")` / `project(':app')` dependency hits remain `0`.
- Queue/API evidence: `PlaybackQueueManager.QueueStateSnapshot` exposes only source fields `currentTrack`, `currentIndex`, and `queueSize`; derived helpers and the previous empty fallback factory are absent. Current public queue functions are command/state-change methods `applyMirroredTransitionIndex`, `playQueue`, `appendToQueue`, `playFirstQueuedTrack`, `skipToNextImmediately`, `preparePlaybackCompletionAction`, `prepareStopAndClearPlaybackState`, `skipToPrevious`, `moveQueueTrack`, `removeTracksById`, `replaceQueuedTrackById`, `replaceCurrentTrackAndResume`, `replaceCurrentQueueTrack`, and `reuseMirroredQueueIfAvailable`; restore/persistence entry `restorePlaybackQueue`; read entries `queueSnapshot` and `queueStateSnapshot`; plus nested callback contracts `prepareCurrent`, `publishState`, `restoreTrackForPlayback`, `matchesCurrentQueue`, and `seekTo`.
- Resolver/cache boundary evidence: `PlaybackMediaSourceProvider` still owns URI/MediaItem/media-source resolution, and production `PlaybackMediaSourceResolutionOwner` remains absent. `PlaybackPrecacheManager` still owns precache scheduling, generation checks, executor release, callback cancellation, segmented precache planning, and cache key decisions through `PlaybackMediaCacheOperations`; it takes `PlaybackQueueManager` directly for current/upcoming queue reads.
- App-shell wiring evidence: `MainActivityBase.onCreate()` still orders `initializeRouteStoresAndStatus()` before `initializeNowPlayingGateways()`, `initializeDownloadRequests()`, `initializeLibraryGateway()`, platform controllers, playback lifecycle controllers, navigation rendering, and playback controllers. `initializeLibraryGateway()` still passes `routeController,` by value exactly once, but it is after route-controller creation; this remains a wiring-size risk to reduce later, not a current uninitialized-host-field risk.
- Source-of-truth status is unchanged for this mini-batch: queue membership/current index/current track remain in `PlaybackQueueManager`; crossfade transition state remains in `PlaybackTransitionStateManager`; repeat/base-volume state remains in playback runtime settings/state stores; URI/MediaItem resolution remains in `PlaybackMediaSourceProvider`; cache policy remains in `PlaybackPrecacheManager`; app-shell route state remains in `MainRouteController` / `NavigationViewModel`. No new mirrored state was added.
- Concrete wins in this mini-batch: `PlaybackCrossfadeStateOwner` now fails missing runtime delegates at construction time, and `PlaybackQueueManager.QueueStateSnapshot` has one fewer public API method. Next safest candidates are a focused URI/MediaItem/cache boundary test audit, or a narrow reduction of the two remaining Service `queueSnapshot()` literal reads if it shortens a real call path without adding a snapshot facade.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; full handoff mojibake scan still reports historical `TOTAL=234` outside this added NOTE, added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`, and `git diff --check` reported only CRLF normalization warnings.
## NOTE 421 - Queue streaming restore owner requires media-source delegates (2026-07-05)
- Queue restore / resolver boundary slice: `PlaybackQueueStreamingRestoreOwner` now requires both `restoredTrackForPreparation` and `restoreHeadersForDataPath` delegates at construction time. It still allows `restoredTrackForPreparation.apply(track)` to return `null`, in which case queue playback falls back to the original restorable track and still restores headers for the selected playback track.
- Source-of-truth status is unchanged: restorable queue-track rules and streaming header restoration remain owned by `PlaybackMediaSourceProvider`, queue restore still calls through `PlaybackQueueManager.StreamingRestoreProvider`, and `EchoPlaybackService` still wires `new PlaybackQueueStreamingRestoreOwner(mediaSourceProvider)`. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, cache policy change, service behavior change, or package move was added.
- Concrete win: two nullable delegate fallback paths were removed from `PlaybackQueueStreamingRestoreOwner`, and `PlaybackQueueStreamingRestoreOwnerTest` now covers constructor-required delegates plus the intended resolver-null fallback behavior.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackQueueStreamingRestoreOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`, and `git diff --check` reported only CRLF normalization warnings.
## NOTE 422 - Precache owner requires diagnostics and executor delegates (2026-07-05)
- Cache/precache boundary slice: `PlaybackPrecacheManager` now requires both `PlaybackStreamingDiagnostics` and the explicit playback cache executor when the test-visible constructor is used. Missing diagnostics wiring no longer creates a hidden diagnostics instance, and missing executor wiring no longer creates a hidden cache executor from the explicit constructor path.
- Source-of-truth status is unchanged: URI and MediaItem resolution remain owned by `PlaybackMediaSourceProvider`; cache operations still flow through `PlaybackMediaCacheOperations`; cache scheduling, queue lookahead, generation checks, callback cancellation, and executor release remain in `PlaybackPrecacheManager`; queue membership/current index/current track remain in `PlaybackQueueManager`. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, service behavior change, or package move was added.
- Concrete win: two nullable precache wiring fallback paths now fail during owner construction, and `PlaybackPrecacheManagerTest` plus `MainActivityArchitectureContractTest` lock the required diagnostics/executor delegates.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackPrecacheManagerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`, and `git diff --check` reported only CRLF normalization warnings.
## NOTE 423 - Playback owner/interface audit after queue-restore and precache delegate slices (2026-07-05)
- Audit scope: this follows NOTE 421 and NOTE 422. Both slices removed nullable delegate fallback paths from existing owners without adding owners, facades, state mirrors, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, service behavior changes, or package moves.
- Current metrics: `EchoPlaybackService.java` is `1337` lines and `MainActivityBase.java` is `2685` lines. Service `private Playback*` field declarations are `31`, Service-held `Playback*Owner` field declarations are `8`, app/feature `Playback*Owner.java` files are `38`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` and `playbackQueueManager.queueStateSnapshot()` hits remain `0`, Service `queueSnapshot()` literal hits remain `2`, Service `currentTrackSupplier` hits remain `2`, production `PlaybackMediaSourceResolutionOwner` hits remain `0`, exact production `PlaybackQueueManager.QueueProvider` / `interface QueueProvider` hits remain `0`, production `QueueStateSnapshot.empty()` / `fun empty(): QueueStateSnapshot` hits remain `0`, feature/core Gradle `project(":app")` / `project(':app')` dependency hits remain `0`, root-package `*Bindings*` files remain `0`, and root-package `*Controller*` files remain `44`.
- Queue/interface evidence: `PlaybackQueueManager.QueueStateSnapshot` still exposes only source fields `currentTrack`, `currentIndex`, and `queueSize`; derived snapshot helpers and fallback factories remain absent. The remaining public queue surface is command/state-change methods `applyMirroredTransitionIndex`, `playQueue`, `appendToQueue`, `playFirstQueuedTrack`, `skipToNextImmediately`, `preparePlaybackCompletionAction`, `prepareStopAndClearPlaybackState`, `skipToPrevious`, `moveQueueTrack`, `removeTracksById`, `replaceQueuedTrackById`, `replaceCurrentTrackAndResume`, `replaceCurrentQueueTrack`, and `reuseMirroredQueueIfAvailable`; restore/persistence entry `restorePlaybackQueue`; read entries `queueSnapshot` and `queueStateSnapshot`; plus nested callback contracts `prepareCurrent`, `publishState`, `restoreTrackForPlayback`, `matchesCurrentQueue`, and `seekTo`.
- Resolver/cache boundary evidence: `PlaybackMediaSourceProvider` still owns URI, MediaItem, and MediaSource resolution. `PlaybackPrecacheManager.fromMediaSourceProvider(...)` still only adapts it into `PlaybackMediaCacheOperations.fromMediaSourceProvider(...)` plus `mediaSourceProvider::mediaItemMatchesTrackForReuse`; `PlaybackPrecacheManager` does not hold a `PlaybackMediaSourceProvider` field and no precache diagnostics/executor/cache/media-source null fallback branches remain.
- App-shell wiring evidence: `MainActivityBase.onCreate()` still runs `initializeRouteStoresAndStatus()` before `initializeNowPlayingGateways()`, `initializeDownloadRequests()`, `initializeLibraryGateway()`, platform controllers, playback lifecycle controllers, navigation rendering, and playback controllers. The remaining `routeController,` constructor arguments occur after route-controller creation; this remains a wiring-size risk to reduce later, not a current uninitialized-host-field risk.
- Source-of-truth status is unchanged for this mini-batch: queue membership/current index/current track remain in `PlaybackQueueManager`; restorable streaming-track rules and header restoration remain in `PlaybackMediaSourceProvider`; cache scheduling and cache policy remain in `PlaybackPrecacheManager`; cache operations remain in `PlaybackMediaCacheOperations`; app-shell route state remains in `MainRouteController` / `NavigationViewModel`. No new mirrored state was added.
- Concrete wins in this mini-batch: `PlaybackQueueStreamingRestoreOwner` now fails missing resolver delegates at construction while preserving the intentional nullable resolved-track result, and `PlaybackPrecacheManager` now fails missing diagnostics/executor delegates at construction. Next safest candidates are either a narrow reduction of the two remaining Service `queueSnapshot()` literal reads if it shortens a real path without adding a snapshot facade, or a focused resolver/cache boundary test audit that keeps resolution in `PlaybackMediaSourceProvider` and cache policy in `PlaybackPrecacheManager`.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`, and `git diff --check` reported only CRLF normalization warnings.
## NOTE 424 - App shell route owner is provider-bound for library/network gateways (2026-07-05)
- App-shell wiring slice: `MainActivityBase` no longer passes the `routeController` host field by value into `MainLibraryGateway` or `NetworkSourcesEventController`. Both call sites now pass `() -> routeController`, and those owners resolve the current route controller at action time instead of capturing the field during construction.
- Source-of-truth status is unchanged: route state remains owned by `MainRouteController` / `NavigationViewModel`; library routing policy remains in `MainLibraryGateway`; network source routing policy remains in `NetworkSourcesEventController`; Activity remains the platform assembly boundary. No owner, facade, bridge, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, resolver/cache boundary change, or package move was added.
- Concrete win: two by-value host-field constructor captures were removed from `MainActivityBase`, and focused tests now prove both route users read their route owner from the provider at action time. `MainActivityArchitectureContractTest` locks the absence of `routeController,` constructor arguments and the presence of two `() -> routeController` providers.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.MainLibraryGatewayTest --tests app.yukine.NetworkSourcesEventControllerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`, and `git diff --check` reported only CRLF normalization warnings.
## NOTE 425 - Current-track queue preparation requires media-source boundary (2026-07-05)
- Queue preparation / resolver boundary slice: `PlaybackCurrentTrackPreparationQueueOwner` now requires the test-visible `mediaSourcesForTracks` boundary at construction time. Missing media-source ownership no longer silently preserves the current track while skipping mirrored queue media-source creation.
- Source-of-truth status is unchanged: queue membership/current index/current track remain in `PlaybackQueueManager`; mirrored queue track preparation still reads the queue manager snapshot; URI/MediaSource creation remains in `PlaybackMediaSourceProvider` through the `mediaSourcesForTracks` boundary. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, cache policy change, service behavior change, or package move was added.
- Concrete win: one nullable resolver/media-source fallback branch was removed from an existing owner, and `PlaybackCurrentTrackPreparationQueueOwnerTest` now proves the constructor requirement while preserving the intentional `mediaSourcesForTracks.apply(...) == null` result fallback.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationQueueOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`, and `git diff --check` reported only CRLF normalization warnings.
## NOTE 426 - Queue preparation requires restored-track delegate (2026-07-05)
- Queue preparation / resolver boundary slice: `PlaybackCurrentTrackPreparationQueueOwner` now requires `queueTrackForPreparation` at construction time. The two-argument test-visible constructor installs an explicit identity delegate instead of using `null` as a hidden "use original track" mode, and the three-argument constructor now fails immediately when the restored-track delegate is missing.
- Source-of-truth status is unchanged: queue membership/current index/current track remain in `PlaybackQueueManager`; restored-track rules in production still come from `PlaybackMediaSourceProvider.restoredTrackForPreparation`; MediaSource creation still flows through the explicit `mediaSourcesForTracks` boundary. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, cache policy change, service behavior change, or package move was added.
- Concrete win: one nullable resolver delegate fallback branch was removed from an existing owner, and `PlaybackCurrentTrackPreparationQueueOwnerTest` now proves both required queue-preparation delegates while preserving the intentional `queueTrackForPreparation.apply(...) == null` result fallback.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationQueueOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`, and `git diff --check` reported only CRLF normalization warnings.
## NOTE 427 - Playback owner/interface mini-audit after queue-preparation delegate slices (2026-07-05)
- Audit scope: this follows NOTE 425 and NOTE 426. Both slices reduced nullable resolver delegate fallback behavior inside the existing `PlaybackCurrentTrackPreparationQueueOwner` without adding owners, facades, state mirrors, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, cache-policy changes, service behavior changes, or package moves.
- Current metrics: `EchoPlaybackService.java` is `1337` lines and `MainActivityBase.java` is `2685` lines. Broad Service `private Playback*` field declarations are `40`, Service-held `Playback*Owner` field declarations are `17`, app/feature `Playback*Owner` files are `39`, production `fromPlaybackQueueManager(...)` hits remain `0`, direct Service `queueStateSnapshot()` hits remain `0`, Service `queueSnapshot()` literal hits remain `2`, and production `PlaybackMediaSourceResolutionOwner` / `QueueProvider` / `queueTrackForPreparation == null` / `mediaSourcesForTracks == null` hits remain absent.
- Source-of-truth status is unchanged for this mini-batch: queue membership/current index/current track remain in `PlaybackQueueManager`; URI/MediaSource and restored-track resolution remain in `PlaybackMediaSourceProvider`; cache scheduling remains in `PlaybackPrecacheManager`; app-shell route state remains in `MainRouteController` / `NavigationViewModel`. No new mirrored state was added.
- Next safest candidates remain low-risk queue/resolver/cache contract work or a narrow Service wiring reduction that does not touch notification, lyrics, shutdown, or background playback.
## NOTE 428 - Current-track preparation requires resolver delegates (2026-07-05)
- Current-track preparation / resolver boundary slice: `PlaybackCurrentTrackPreparationOwner` now requires both `playbackPreparationProvider` and `mediaSourceResolver` at construction time. A missing preparation provider no longer silently falls back to the original track, and a playable prepared track can no longer carry a missing MediaSource resolver.
- Source-of-truth status is unchanged: playable-track preparation, restored-track resolution, and MediaSource creation still come from `PlaybackMediaSourceProvider`; queue current-track replacement still goes through the existing queue replacement callback; restored playback position still comes from the existing position provider. No owner, facade, state mirror, notification behavior, lyrics behavior, shutdown behavior, background playback behavior, cache policy change, service behavior change, or package move was added.
- Concrete win: two nullable resolver delegate fallback paths were removed from an existing owner, and `PlaybackCurrentTrackPreparationOwnerTest` now proves both constructor requirements while preserving the intentional null-result fallback from `prepareTrackForPlayback(...)`.
- Verification target passed: `.\\gradlew.bat :app:testDebugUnitTest --tests app.yukine.playback.PlaybackCurrentTrackPreparationOwnerTest --tests app.yukine.MainActivityArchitectureContractTest --console=plain`; focused source/test mojibake scan returned `TOTAL=0`, added-line mojibake scan returned `ADDED_LINE_MOJIBAKE=0`, and `git diff --check` reported only CRLF normalization warnings.
## 2026-06-27 DIRECTION PIVOT: stabilization before more extraction

- New controlling doc: `docs/ARCHITECTURE_STABILIZATION_PIVOT_2026-06-27.md`.
- This pivot supersedes the previous default of continuing fast owner/manager extraction when the two conflict.
- Freeze broad architecture expansion first: do not add new `Manager`, `Coordinator`, `Controller`, `Bindings`, or `Gateway` layers by default.
- Stabilize the dirty migration surface, record current counts, and prefer reviewable commits/checkpoints before more migration.
- Reduce existing over-abstraction: merge/delete forwarding-only owners, shrink oversized provider/listener interfaces, and shorten UI -> service/data call chains.
- Do not expand `PlaybackQueueManager.QueueProvider` or similar large interfaces without a prior split/merge/inline plan.
- String-based architecture contracts are not enough for fragile flows; pair them with behavior tests, dependency-direction checks, integration smoke, or device evidence.
- For Windows/KSP verification, run Gradle invocations serially with project defaults first; use `--no-daemon` or `--max-workers=1` only after a reproducible daemon, KSP, or lock issue.
- Continue P1/P2 only after a slice demonstrably reduces net files, methods, state sources, dependencies, or call-chain length.
## 2026-06-27 ????

- ???????????/????????????? owner ????
- `EchoPlaybackService` ???? `MainActivity` ???????????/???????? launcher intent?
- `PlaybackQueueManager.QueueProvider` ??????????????????????????????
- ?????????????????????????????
- ???????? `docs/ARCHITECTURE_STABILIZATION_PIVOT_2026-06-27.md`???? P1/P2 ????????????????
